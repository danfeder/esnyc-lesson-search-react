{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR\"/scripts/install_pkgs.sh",
            "timeout": 600,
            "statusMessage": "Preparing cloud session: npm dependencies + Docker daemon"
          }
        ]
      }
    ]
  }
}
