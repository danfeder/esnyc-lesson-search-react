# C02 — Cooking Skills & Main Ingredients Re-tag — Execution Status

**Last updated:** 2026-06-25 by Session 15 — **tasks (c) re-framed §8 greenlight + (d) canary scope are DONE** (doc-only commit `8389d01`). **(c):** authored design §8 "Greenlight criterion (re-framed)", replacing the retired "4 gates pass" with two USER-gated checkpoints on the SHIPPED per-field output — ① pilot greenlight (P2′.7) + ② pre-apply gate (P3.1). A **Codex cross-family + Claude-lens audit** (parallel, adversarial) caught **three overclaims in MY first draft**, all folded: (1) "+0.065 clears the +0.05 bar" re-introduced a retired gate (the +0.065 is all-69 cooking F1, not the judgment-row Gate② bar) → reframed as diagnostic evidence + surfaced the precision cost (floor-only P0.900/R0.732 → floor-retention P0.833/R0.915); (2) "no regression BY CONSTRUCTION" → *no loss RELATIVE TO THE FLOOR* (a floor mis-fold is uncatchable, both fields inherit it → the P3.1 floored-row spot-check confirms at scale); (3) sentinels=1.0 as sufficiency → **necessary-only** (the keep-only lock pins Tasting/KFS high, KFS below support; the all-700 prevalence review is the real cooking-precision-at-scale guard). Added a cooking-precision tripwire (~0.833), an over-tag remedy (re-pilot or deterministic post-filter, never a silent relax), and a both-consistency-AND-correctness "much cleaner" definition. **(d): D-P12** — the fresh-25 canary labels + scores `cooking_skills` ONLY (ingredients ship the floor, subsumed by the all-700 review). Also fixed impl P3.1's floored-row selector (normalizationCount → ship-policy provenance bucket). Full suite **1815**, `npm run check` clean. **Next = P2′.7 (cooking-scoped fresh-25 run + final greenlight, USER-GATED) → then P3.**

## Current State

> ✅ **P2′.7 GREENLIT — PILOT PASSES (Session 16, 2026-06-25).** The fresh-25 cooking canary ran (Opus-4.8, $0.69) and the USER greenlit → **next = open the P2′ PR (scripts-only), then BUILD P3 (apply).** Headline read (apples-to-apples, gold v2): **SHIPPED cooking P 0.819 / R 0.800 / F1 0.810** vs pilot 0.833/0.915/0.872; floor-retention beats floor-only **+0.207 F1** on fresh data. **User directive carried into P3: the apply touches ONLY `cooking_skills` + `main_ingredients` (columns + their JSONB keys), NONE of the other 15 metadata fields** (= D-P10 C02-only scope; verify via GATE 2 Codex + TEST-DB diff).

**Phase: P2′.7 GREENLIT (Session 16) → next = open the P2′ scripts PR (GATE 3 Codex + reviewer) → merge → BUILD P3 (apply migration, fresh `feat/c02-apply`).** Branch `feat/c02-pilot` (scripts-only, no DB, no PR yet). Full suite **1815** green, `npm run check` clean. **Authoritative spec = design §3·PIVOT (D-P1..D-P12) + the new §8 "Greenlight criterion (re-framed)" subsection** — supersedes the "4 gates pass" ship test; the 4 gates remain a diagnostic instrument over the SHIPPED per-field output.

**P2′.7 DONE — fresh-25 cooking canary + greenlight (Session 16, committed this session).** Froze the harness (reconcile policy `c02-anchored-verify-and-diff-v4`, prompt sha `3eec2390…`, gates, vocab), built a blind body-only labeling input for the 25 held-out ids, and labeled `cooking_skills` (D-P12 cooking-scoped) via the Q6 protocol: 2 independent blind cross-family derivations (Claude + Codex) → 15/25 agreed → tie-break → **USER adjudicated the 4 disputed sentinels (all EXCLUDE)** → gold key v1. Ran the frozen harness over the 25 (Opus-4.8 via proxy, 24 Zod-pass/1 harmless no-cook Zod-fail, $0.69). New scorer `c02-heldout-score.ts` (reuses `materializeC02Ship`; **self-validated** — reproduces the 69-key re-score gate 0.807/0.836/0.872 exactly). **First read (gold v1) = SHIPPED cooking P 0.422/R 0.854** — far below pilot 0.833. **Diagnosis (not over-fitting): a labeling-standard mismatch I introduced** — the 69-key credits generic `Reading & following recipes` (34/54 cooking lessons) + `Measuring` (37/54) broadly; my v1 brief credited them on 2 lessons each; the model tags them broadly (matches the 69-key). **USER chose Option A → re-label the held-out to the 69-key standard + re-score (free, no re-run).** Re-derived gold **v2** independently: 2 blind re-labelers calibrated to 8 REAL 69-key examples (`c02-heldout-calibration-69key.json`) + a tie-break for the 13 technique disputes, **with the 4 user-locked Tasting/KFS sentinel decisions forced back in** (the re-label only fixes process/technique, never re-opens sentinels). **Re-score vs gold v2 = SHIPPED cooking P 0.819 / R 0.800 / F1 0.810** (≈ pilot 0.833; floor-retention beats floor-only **+0.207 F1**). Sentinels: 0 over-predictions (clean precision guard); 3 keep-only recall misses by design. Error tail tiny: 1 model FP (Seeds→`Pickling`); the Dumplings `Dough making`/`Sautéing`/`Steaming` FPs are the **FLOOR retaining wrong legacy tags** (existing `Kneading`→`Dough making`) the model correctly dropped — a P3.1 floored-row-spot-check data point, NOT model over-tagging. **Codex adversarial cross-exam (gpt-5.5): verdict MIXED — legitimate correction, NOT gaming** (gold v2 not model-copied — real FPs/FNs survive; 69-key premise verified; arithmetic re-run + reproduced exactly), with three fair caveats folded honestly: (1) post-hoc → the fresh-25 is now a **calibration check, not a blind confirmation**; (2) I overstated "essentially every" (true: 63%/69%; gold v2 ~80–86%, so true precision ~0.79–0.82); (3) additive-only revision. **USER GREENLIT** ("greenlight and build, only cooking + ingredients metadata, none other"). **Binding precision-at-scale guard remains §8 ② (P3.1 all-700 prevalence review + floored-row spot-check).** Committed: `c02-heldout-score.ts` + gold keys v1/v2 (+ provenance) + scorecards v1/v2 + all label/tiebreak provenance (no bodies; force-added). Bodies/run/calibration/alt-keys stay gitignored.

**P2′.8 DONE (`c4139ae`) — per-field ship policy + field-isolated validation.** New `ship-policy.ts` `materializeC02Ship(record, existingTags, floorInput, cookingValues)`: `main_ingredients` = floor-only; `cooking_skills` = floor ∪ the LLM cooking final (read from `finalC02`-or-raw `keep∪add`, off-vocab-filtered). Committed **re-score gate** (`ship-policy.rescore.test.ts`) runs it over the stored r4 run (all 69, 4 reconstructed) and asserts D-P11 reproduces with ZERO drift: floor-only 0.807 / LLM-as-is 0.836 / **floor-retention 0.872** (+0.065), ingredients==floor 69/69 — supersedes the throwaway `c02-cooking-floor-vs-llm.ts`. **Part 2 (run-time field isolation, `processC02Decision`):** the executor's initial STOP-judgment was refuted (by its own Codex + verified against source — the reconcile core is field-modular + lenient, NOT partition-rejecting), so it was done contained: on a whole-object parse failure, each field validates independently — a valid field reconciles from the LLM, an off-vocab field falls back to its floor (empty decision → reconcile implicit-KEEPs the anchor); mixed-case only; reconcile internals + `finalC02` shape UNCHANGED. Supervisor-verified: re-read the full materializer + re-score gate + Part 2 diff + tests, re-ran check + suite (1815). ⚠️ **Carry-forward for GATE 3 (P2′ pre-push) + P3:** Part 2 changed the LIVE run path — a future run's partial-decision rows now get a valid `finalC02` (and so won't re-run on `--resume`). Benign + desired, but flag it at the P2′ PR review and re-confirm before the P3 full run.

**Decision (a) — per-field ship method — LOCKED Session 14 as D-P11 (user-verdict + double Codex cross-exam).** The full run ships a DIFFERENT source per field: **`main_ingredients` = deterministic floor ONLY** (no LLM judgment; coherent with accept-ceiling; floor 0.918 > LLM 0.831 clean-core, and the LLM leaks group-FPs the gate can't catch); **`cooking_skills` = LLM + floor-retention ("option 3")** = floor ∪ `finalC02.cooking` (LLM KEEPs/ADDs but never drops a floored skill). Honest all-69 (Session-14 `c02-cooking-floor-vs-llm.ts`): floor-only 0.807 / LLM-as-is 0.836 / **floor-retention 0.872 (+0.065 over floor, P0.833/R0.915)** — recovers ~50 real body-derived cooking skills the floor can't add (replacing `Basic Skills`/`Cooking Techniques` junk) + restores the ~28 high-confidence skills the LLM otherwise wrongly drops; **zero clean-core data loss** (floor fully retained) → satisfies the re-framed greenlight. **Floor-retention is a NEW reconcile/materialization policy** (NOT the existing add-suppressing keep-only lock — opposite direction; Codex-confirmed), append-only for cooking only (re-opens D-P6 for cooking, sanctioned by user-verdict). **P3 MUST field-isolate run-time validation** (the pilot lost 4 valid-cooking lessons to an off-vocab ingredient — whole-record coupling). Build = **P2′.8** (authored in the impl plan, runs BEFORE the P2′.7 freeze).

**Round 4 (Session 13, `46b3ddf` code + `53794bc` scorecard) — SPECIFICS KEEP-ONLY.** Extended the D-P1 keep-only lock to ALL 46 main_ingredient specifics (`reconcile.ts` ingredient lock = `C02_INGREDIENT_KEEP_ONLY_LOCK ∪ input.floor.specificValues`; new `specificValues` on `C02FloorInput`; `C02_RECONCILE_POLICY_ID`→v4). The LLM may KEEP/DROP an anchored specific but never ADD a new one. **Tested for FREE + deterministically** via a new `rereconcile-run.ts` that re-runs ONLY the floor+reconcile stage over the STORED round-3 decisions (no LLM call) — golden-checked **69/69** against the unchanged code (faithful reconstruction), then 38 records change with the lock. **Result: pooled added-specific precision 0.621→0.773 (fp 72→27) — EXACTLY the supervisor projection** (independently recomputed off the run output, not just the scorer). But **all 4 gates still FAIL:** per-specific floor 9→4 violators (Bell peppers 0.400 / Ginger 0.500 / Parsley 0.500 / Tomatoes 0.500, all now over-KEEP); Sweeteners 0.500→0.750 (<0.80); Gate ② ingredients +0.058→+0.012 (recall traded — lost 26 correct adds); Gate ① ingredients 0.831 < rules-floor 0.918.

**The decisive insight (verified):** the 27 residual FP specifics are ALL incidental specifics already in the existing corpus tags (the model's add/drop/keep split on specifics: r3 add 71 / drop 9 / keep 119 — it rubber-stamps the anchor). The deterministic floor carries them forward too, so **NO mechanical method prunes them** (pruning needs the subtle centrality call the case review showed is hard). And with adds locked, **the LLM no longer beats a pure deterministic floor on ingredients** (floor 0.918 vs LLM 0.831 clean-core; +0.012 judgment, CI includes 0). This is a STRUCTURAL ceiling (tight curation vs mechanical re-tag of loosely-tagged legacy data), not a tuning miss. **cooking_skills + ingredient GROUPS are solid; only the ingredient-SPECIFICS tier hit the ceiling.**

**TWO USER DECISIONS (Session 13, both via AskUserQuestion → recommended option):**
1. **Specifics keep-only** (extends D-P1 to all 46 specifics) — built + verified as round 4 above. Amends the two-level "AI adds 1–3 central specifics" design goal: the LLM no longer adds specifics.
2. **ACCEPT THE CEILING + SHIP CLEAN** — greenlight C02's big wins (vocab canonicalization + groups + cooking-skills + keep-only specifics); accept specific foods at cleaned-EXISTING-tag quality (~0.77 vs the tight key), NOT perfect centrality. **Re-frame the greenlight bar from "4 gates pass" → "much cleaner than today + no harmful regression"** (no mechanical method can hit the tight gates; the re-tag is still a large net improvement). AMENDS the locked Q5/§8 "4 gates" greenlight — recorded as a Session-13 design §8 amendment.

**P2′.6 prep (Session 12, `b814ad1`):** the preflight (`preflight-token-mass`) was measuring the STALE monolithic prefix, not the live C02 anchored one (D-P9 changed it). Added `buildC02TokenCountRequest` + a `--c02` flag (TDD). Empirically confirmed via proxy: the real C02 cached prefix is ~3,584 tok and caches cleanly (`cache_read>0`), ~$0.030/lesson. ⚠️ **Codex (minor, DEFERRED):** the preflight has no `--model` flag → always uses opus-4-7 for count_tokens + cache-floor; immaterial here (opus-4-7/4-8 same family/tokenizer/floor, and the empirical probe used 4-8 directly), but worth a 1-line fix before any non-opus preflight.

**The 3 tuning rounds (all USER-approved at the gate):**
- **Round 1** (`e7349b6` scorecard) — anchored verify-and-diff as-built. ALL 4 gates FAIL. Combined **P76.3/R79.3** (up from the blind re-read's P66.5 — the pivot works directionally) but still over-tags: Tasting 0.214 (28 preds), Kitchen-safety 0.400, Sweeteners 0.364, pooled added-specific 0.617; 6 Zod-fails (partition-contract violations).
- **Round 2** (`f46fce0` lock+prompt, `2627b5a` scorecard) — **D-P1 keep-only lock** on Tasting + Kitchen & food safety (reconcile filters their ADDs; policy id→v2) + prompt tightening (keep-only framing, aromatics rule, bucket-contract, 5th few-shot). Result: **Tasting 0.214→1.000, Kitchen-safety 0.400→1.000** (sentinels PASS). Gate ② cooking −0.069→−0.010. But ingredient over-adds UNMOVED by prompt (Garlic 0.286→0.167); 8 Zod-fails (prompt bucket-contract didn't help).
- **Round 3** (`cc0a5ef` lenient+Sweeteners, `9bb4d68` Codex fix, `1949ebf` scorecard) — **lenient reconcile** (recovers a malformed partition instead of rejecting; policy id→v3) + **Sweeteners keep-only lock**. Result: Zod-fails **8→4** (the 4 left are off-vocab adds, correctly not masked), **Gate ② ingredients PASS (+0.058)**, Gate ② cooking −0.006. Codex caught a real DROP-wins-for-non-anchor-overlap bug (`9bb4d68`, fixed + tested; verified the round-3 run never hit it). **STILL FAILING: Gates ①, ③, ④** — all on ingredient-specific over-adds (pooled added-specific 0.621 vs 0.70; Garlic 0.313/16 preds, Carrots/Lemon/Honey/etc. under the per-specific floor; Sweeteners 0.500 even locked, via over-KEEP from the anchor).

**The decisive case review (Session 12, agent `ac4c11a4…`, worksheet `artifacts/c02-specifics-disagreement-cases.md`, gitignored — has body excerpts):** 13 real Garlic/Lemon/Honey/Carrots/etc. over-adds, lesson-by-lesson. **Verdict: the tight key is MOSTLY RIGHT — the model genuinely over-tags** (single cloves, dressing acids, sauce parsley, interchangeable "example" produce; lessons that name their own mains never include the disputed food). A real MINORITY are borderline (corn-salad signature flavors; honey named in a dressing TITLE; **"Bee Habitat" — honey is the literal subject but never cooked → exposes "is the tag *what the lesson is about* or *what students cook with*?"**). This RULES OUT loosening the key/thresholds (would be gaming).

**↑ The Session-12 "OPEN design decision" above is RESOLVED (Session 13):** the user picked **specifics-keep-only** (option 1) and then, after round 4 showed the keep-only ceiling, **accept-ceiling-ship-clean**. The Bee-Habitat "what is the tag for" question is moot under accept-ceiling (specifics = cleaned existing tags, not curated-to-centrality). Lines 9–18 above are Session-12 history.

**OPEN — next session (P2′.7 is USER-GATED to RUN, but no gate to START prep):**
- **✅ (a) DONE — method LOCKED as D-P11 (Session 14).** Floor-only `main_ingredients` + floor-retention `cooking_skills` (option 3).
- **✅ (b) DONE — P2′.8 BUILT + verified (`c4139ae`, Session 14).** `materializeC02Ship` + re-score gate + run-time field isolation. Full suite 1815.
- **✅ (c) DONE — re-framed §8 greenlight authored + audited (`8389d01`, Session 15).** Design §8 "Greenlight criterion (re-framed)": two USER-gated checkpoints on the SHIPPED per-field output (① pilot greenlight P2′.7 / ② pre-apply gate P3.1). Codex+Claude audit corrected three of my first-draft overclaims (see the Session-15 log + recent-decisions). The 4 gates are now a diagnostic instrument, NOT the pass/fail ship test.
- **✅ (d) DONE — D-P12: fresh-25 canary is cooking-scoped (`8389d01`, Session 15).** Labels + scores `cooking_skills` only; ingredients (floor-only) are covered by the P3.1 all-700 prevalence review.
- **✅ (DONE Session 16) P2′.7 — cooking-scoped fresh-25 run + greenlight (USER-GATED).** GREENLIT (see the Current State P2′.7 block + the Session-16 recent-decision/log). Apples-to-apples canary = SHIPPED cooking P 0.819/R 0.800/F1 0.810; Codex MIXED (legitimate, calibration-not-blind); user said "greenlight and build, cooking+ingredients ONLY."
- **➡️ (NEXT) open the P2′ scripts PR** (`feat/c02-pilot`, scripts-only — the whole P2′.1–.8 + .7 accumulated diff): pre-push code-reviewer + **GATE 3 Codex** in parallel (call out the P2′.8 part-2 live-run-path change + the new `c02-heldout-score.ts`), four-surface triage, merge. THEN P3 branches fresh.
- **(e) THEN P3** — the full apply (DB phase, highest-risk), fresh `feat/c02-apply`: snapshot + dual-write (column + JSONB), **C02-only scope = `cooking_skills` + `main_ingredients` ONLY, none of the other 15 metadata fields (user directive Session 16 = D-P10)** — verify with the TEST-DB diff + GATE 2 Codex, concurrency guard, targeted rollback. P3.1/P3.2 consume the **per-field ship output** (P2′.8 / D-P11), NOT raw `finalC02`; lift the `selectComparedLessons` fail-closed guard; **add the ship-policy provenance bucket** for the floored-row spot-check (NOT normalizationCount — Session-15 carry-forward; the Dumplings `Kneading`→`Dough making` floor mis-fold is a concrete spot-check target found Session 16); re-confirm P2′.8 part-2 field-isolation on the full run. **GATE 2 Codex on the migration before TEST.**

**Session 10 progress (per-task supervisor-verified + Codex-cross-checked):**
- **✅ P2′.1 DONE — one canonical floor** (`b9f6a8d` + Codex fix-up `462987c`). New `scripts/stage2-retag/c02-floor.ts` `applyC02Floor()`: executes the (previously inert) `drops` list, folds aliases, drops unmapped junk, removes the `Herbs & Aromatics` literal (split candidate), enforces specific→group, carries per-tag **provenance** (`exact-canonical`/`alias-fold`/`parent-derived`; `ambiguous` reserved for P2′.2 anchor). The 3 divergent floors (`predictMembership` in sample-answer-key, `floorAnchor` in build-c02-answer-key, the baseline's `computeRulesBaseline` transitively) now all DELEGATE to it (old `foldField`/`dedupe`/`foldAndKeepCanonical` deleted = real import-identity). 25 floor tests; full suite 1656; `npm run check` clean. Codex (gpt-5.5, `abf7eb036`) found 4: **#2 (fixed)** order-dependent provenance → exact-canonical now wins deterministically; **#1/#4 deferred by design** (see carry-forwards); #3 folded into #2.
- **✅ P2′.2 DONE — anchored verify-and-diff** (`5910473` + Codex fix-up `2fd1c33`). Widened `corpusLineSchema`/`loadCorpus` to RETAIN both C02 fields + a freshness preflight (`assertCorpusHasC02Tags`, now requires both keys present per row); dedicated 2-field **KEEP/DROP/ADD** tool in `schema.ts` (`buildC02SubmitTagsTool` JSON + `buildC02DecisionSchema` Zod, closed reason-code enums, **tool name REUSED as `submit_tags`** for proxy detection-clearing); new `c02-anchor.ts` (provenance-annotated anchor + D-P9 `computeEffectiveInputHash` over body+raw-tags+anchor+manifest-version+policy-id); new verify-and-diff prompt `prompts/c02-retag.md` (strict Tasting/Kitchen-safety ADD criteria, Herbs split, garnish + B-lite pantry, 4 negative few-shots); `computeBodyHash` rewired at all 3 consumers (main / resume / repair — repair map widened to `lessonRecordById`). 1703 suite green; check clean. Codex (gpt-5.5, `acc01f1b2`) confirmed hash-consistency across the 3 sites + tool↔Zod parity + D-P9 independence + transitional coherence; 3 findings all FIXED (#1 partial-stale preflight, #2 tasting-only prompt contradiction, #3 floored-anchor prompt alignment). **TRANSITIONAL STATE (sanctioned):** the live `callForcedTool` STILL sends the OLD monolithic tool + body-only prompt — the new tool/anchor/prompt are built+tested units NOT yet wired live (the 2-field KEEP/DROP/ADD output can't be consumed until reconcile exists). **P2′.3 wires the live call.**
- **✅ P2′.3 DONE — `reconcile.ts` + run-record `finalC02` contract + LIVE wiring** (`34e185c` + Codex fix-up `8d523a3`). New `scripts/stage2-retag/reconcile.ts` `reconcileC02Tags()`: floor-anchored core + LLM KEEP/DROP/ADD → `finalC02`; KEEP∪DROP must **exactly partition** the floored anchor (reject overlap/omission/outside-anchor); ADD disjoint; **subtractive** (a dropped floor tag is removed — never append-only); specific→group invariant + drop-parent/keep-child → keep parent; canonical/unique/deterministic + per-value provenance. **Live call WIRED** in `runMainPass`: `loadC02SystemPrompt` + `buildC02SubmitTagsTool` + the **anchored body** (current tags appended) replace the monolithic tool + body-only prompt; new pure `processC02Decision` = decision-schema (`buildC02DecisionSchema`) → floor+reconcile → `finalC02` canonical (`buildC02FinalSchema`); fallback path mirrors via a `processResult` override; `promptSchemaHash` now over the C02 tool/prompt. **Run-record contract (D-P6):** NEW optional `llmDecisions` (raw decision) + `finalC02` fields; `rawInput` preserved (holds the raw decision); line schema parses both; `score-answer-key.loadRunContestant` reads `finalC02` for the two C02 fields (rawInput fallback for legacy). **Codex #1 CLOSED:** `normalizeRecordInput({skipC02:true})` bypasses R7/R8/R9 for the two C02 fields on the run path (floor/reconcile supersede them); guard tests prove no double-apply + the other 12 fields still normalize. Suite 1731. **Codex (gpt-5.5, agent `a485c820…`) 🟢 on reconcile/fallback/D-P9/skipC02; 2 findings FIXED (`8d523a3`):** (1) failed C02 records now RE-RUN on `--resume` (new `requireFinalC02` identity flag — a matching record lacking `finalC02` is a validation failure with `error===null` → re-run, not resume-skip; `--repair` safely stale-skips C02, resume is the recovery path); (2) `selectComparedLessons` (the shared diff+apply chokepoint) now **fails closed** on C02 anchored records (`finalC02`/`llmDecisions` present) instead of silently emitting empty C02 arrays. Suite 1738; check clean.
- **✅ P2′.4 DONE — per-value gates + baseline recompute** (`7a5845a`). In `c02-gates.ts`: `evaluateGate3` AUGMENTED — pooled added-specific precision ≥0.70 (unchanged) **+** a per-specific precision floor ≥0.60 gated only where `truthCount≥3 AND predictionCount≥3` (below-support specifics stay informational but their FPs still feed the pooled denominator — accumulation runs before the guard) **+** named sentinels (`Tasting`/`Kitchen & food safety` from cooking_skills ≥0.70, `Sweeteners` reuses the Gate④ ≥0.80), each gated only at predictionCount≥3 (informational below). Gate③ now passes iff pooled-OK **and** no per-specific violation **and** sentinelsPassed (null precision still fails closed). New `SentinelResult[]`/`perSpecificViolations` on `Gate3Result` + scorecard rows. **D-P7:** no code change needed — P2′.1 already routed `computeRulesBaseline→predictMembership→applyC02Floor`, so Gate①/② read the CLEANED baseline; proof tests added (Basic Skills/Herbs & Aromatics/Salt absent from the baseline). Scorer reads `finalC02` end-to-end (gate-level conflicting-fixture test added). Suite 1752; check clean. **Codex (gpt-5.5, agent `ab36d5eb…`, inline) verdict: SOUND, no blockers** — 5/6 🟢 (pooled order, no vacuous pass, null-fails-closed, baseline cleaned, finalC02 consumed, no new metric math); 1 🟡 follow-up captured below.
**Session 11 progress (supervisor-verified + Codex-cross-checked):**
- **✅ P2′.5 DONE — held-out sampler (D-P2)** (`f4075b1` + Codex fix-up `ca981b7`). `buildC02Sample` gained `excludeIds:Set<string>` (filters the eligible pool ONCE at entry → all 3 layers inherit it) + `coverageMode:'strict'|'relaxed'`. **Relaxed mode = a plain representative stratified draw** (the existing proportional activity_type × body-length-quartile `stratifiedSample` over the full eligible pool — NO hard-case oversampling, NO ≥2× set-cover, `warnings:[]`, all rows `clean-core`) — the anti-overfit-canary design call (supervisor-level, within locked scope): the fresh-25 must reflect the natural corpus distribution, so oversampling rare/hard cases (strict mode's goal) is dropped; per-value ≥2× coverage stays on the 69-key only. `runC02` writes **held-out filenames** (`c02-heldout-*`) in relaxed mode so the locked 69-key artifacts are never overwritten; manifest records `coverageMode`/`excludedCount`/`excludeKey`/seed/size/version. CLI: `--relaxed-coverage`, `--exclude-key <path>` (Zod-validated `loadExcludeKey`), `--size`; relaxed defaults seed **20260624**/size **25**. `main()` gates the under-coverage hard-fail on `strict && warningCount>0`. **`SAMPLER_VERSION` b1-v1→b1-v2** + the committed 69-key manifest surgically refreshed (only `sampler_version` + `coverage` under the cleaned floor; `postSampleRemovals`/`coverageNote`/`size:69`/`selected`/`layerSizes` preserved). **Emitted + committed `c02-heldout-manifest.json` (25 ids, verified disjoint from the 69; the held-out sample jsonl stays gitignored like the 69-key one).** Supervisor-verified independently: regenerated both samples in a scratch dir (held-out byte-identical determinism; 69-key strict replay under the cleaned floor produces a coverage block IDENTICAL to the committed refresh + 70-id selection = committed 69 ∪ the 1 documented removal — proves a real recompute, not a hand-edit). Suite 1774; check clean. **Codex (gpt-5.5, agent `a99cf7dd…`, inline) verdict: 2 findings, both FIXED (`ca981b7`)** — operator-misuse footguns around the LOCKED 69-key artifacts (the committed output was already correct + disjoint): (1) `--relaxed-coverage` didn't require `--exclude-key` (un-excluded "held-out" draw → canary contamination); (2) `--exclude-key`/`--size` accepted in strict mode → a "forgot --relaxed-coverage" typo would clobber the locked 69-key artifacts before the gate fired. Fix = a `parseArgs` biconditional guard (`--relaxed-coverage` ⟺ `--exclude-key`; `--size` held-out-only), validation-only (throws before any write); 3 new negative tests. Rebutted the BLOCKER severity (committed artifact sound; these are robustness guards for the P2′.7 re-run), accepted the cheap fixes.

- **✅ P2′.6 DONE (Sessions 12–13) — 4 tuning rounds, ceiling reached, USER accepted-ceiling-ship-clean (see header).** [Historical task spec below.] **NEXT: P2′.6 — Re-pilot on the 69 (Opus 4.8) — USER-GATED.** `preflight-token-mass` FIRST (D-P9 changed the prefix; confirm < budget). Run the anchored+reconciled harness over the 69 via the proxy/Opus-4.8 (runbook below); score the 4 per-value gates. Iterate prompt/few-shots **against the 69** until the gates pass. If the universal catch-alls (`Tasting`/`Kitchen & food safety`) still over-predict, **STOP and present the D-P1 keep-only-lock recommendation — do NOT implement the lock without user approval.** Commit the scorecard summary (bulk run gitignored). **Stop for the user at the gate result.** Then **P2′.7 fresh-25** (freeze prompt+thresholds, label the committed 25 independently, final greenlight). **Proxy runbook (P2′.6):** CLIProxyAPI v7.2.33 Anthropic base URL = `http://127.0.0.1:8317` (old `…/api/provider/anthropic` is 404); start `~/.local/bin/cli-proxy-api -config ~/.cli-proxy-api/config.yaml &` → verify `lsof -nP -iTCP:8317 -sTCP:LISTEN` → re-run preflight via `--base-url http://127.0.0.1:8317` → run `run-retag.ts --model claude-opus-4-8 --base-url http://127.0.0.1:8317 --ids …/c02-answer-key.final.jsonl --concurrency 1` (confirm cache_read>0 on call ≥2); ~$0.055/lesson (~$3.80 for the 69). `claude-opus-4-7` fallback if 4.8 misbehaves.

**Codex carry-forwards (must close in later tasks):**
- **✅ CLOSED in P2′.3 (`34e185c`):** the R7/R8/R9-on-LLM-output double-apply (Codex #1) — the two C02 fields now bypass normalize via `{skipC02:true}`; reconcile is the sole canonicalizer.
- **→ P3.1 / P3.2 (NEW, from P2′.3 Codex):** `generate-diff-report.ts` + `prepare-apply.ts` still read the two C02 fields from `rawInput` (which on a C02 record is the raw KEEP/DROP/ADD decision OBJECT, not arrays) — the reconciled arrays live on `record.finalC02`. P2′.3 made `selectComparedLessons` (the shared chokepoint) **fail closed** on C02 anchored records so nothing silently emits empty C02 / stages an all-C02-emptying apply. **P3.1 must thread `finalC02` into the diff** and **P3.2 must switch the apply emitter to `finalC02` + C02-only scope** (D-P10a), which also REMOVES the fail-closed guard. The P3.1/P3.2 task text already specifies the `finalC02` read; this carry-forward just records that the guard is the interim safety + must be lifted when P3 threads it.
- **✅ CLOSED in P2′.5 (`f4075b1`):** the stale `c02-answer-key-manifest.json` coverage payload + `sampler_version:"b1-v1"` (Codex #4). Selection-stability re-verified (strict seed-20260612 replay under the cleaned floor = the committed 69 ∪ the 1 documented removal, identical ids); surgically refreshed `sampler_version`→`b1-v2` + the `coverage` block under the cleaned floor; all hand-annotations (`postSampleRemovals`/`coverageNote`/`size:69`/`selected`/`layerSizes`) preserved.
- **→ P2′.6 tuning (NEW, from P2′.4 Codex, NON-blocking):** the cooking sentinels (`Tasting`/`Kitchen & food safety`) are informational below 3 predictions, AND cooking-skill FPs do NOT feed Gate③'s pool (which iterates main-ingredient specifics only) — so on a *very small* key (<~10 lessons) 2 spurious catch-all predictions could escape the sentinel. **Harmless for both the 69-key (P2′.6: Tasting ~54 preds ≫3) and the fresh-25 (P2′.7); the full run (P3) is gated by the corpus-wide prevalence review + user spot-check, not these per-value gates.** Decision: NOT coded (lowering the guard to predictionCount≥1 makes the gate brittle to single-prediction noise; thresholds are pilot-tunable). At P2′.6, if a catch-all over-predicts but the sentinel doesn't bite, consider tuning the sentinel support floor. Optional tidy (Codex Finding 4 nit): strengthen the Gate② Herbs test fixture to assert the cleaned baseline scores *higher* F1 (not just F1=0 either way) — low value, the cleaned-floor drop is already proven by sibling tests.
- **→ P3.1 (NEW, Session-15 audit carry-forward):** the diff/spot-check floored-row selector must be an explicit **ship-policy provenance bucket** (a row whose SHIPPED C02 output differs from its existing tags via the floor / floor-retention), NOT `normalizationCount` — the two C02 fields bypass `normalize` (`{skipC02:true}`, P2′.3) and the floor is applied later in `materializeC02Ship`, so `normalizationCount` is 0 for floored C02 rows. Recorded in impl P3.1 + design §8 ②. Also at P3: re-confirm P2′.8 part-2's field-isolation holds on the full run (no row drops a valid field to an off-vocab sibling).

**The re-locked method (one line each):** anchored **verify-and-diff** (LLM sees the current provenance-annotated tags + proposes KEEP/DROP/ADD, not re-tag-from-scratch) · **one canonical floor** (executes drops, drops junk, splits Herbs & Aromatics; unified across reconcile/scorer/gold-key/baseline) · code-level **`reconcile.ts`** (floor-anchored core + LLM keep/drop/add → `finalC02`; never append-only) · **C02-only apply scope** + concurrency guard + targeted rollback (D-P10) · **per-value gates** with a **recomputed baseline** · a **3-layer test strategy** (69-key per-value workhorse / fresh-25 anti-overfit canary / corpus-wide prevalence review).

**Why (decisive forensics, on the failed run vs the tight 69-key):** raw-LLM P66.5/R89.7 · pure-floor P80/R79 · LLM∩floor P91/R72 · floor∪LLM (additive) **P63 — worse than raw**. 84% of FPs are pure-LLM adds (anchoring suppresses); 67% of misses were already in existing tags (anchoring recovers); BUT 27%/17% of gold tags are genuinely body-derived (floor alone caps recall — the LLM is still needed for the new tail). The over-tags are body-PLAUSIBLE (Tasting on 78% of lessons), so the LLM's *bar* is too low → anchor + negative few-shots + per-value gates raise it. **BOTH halves required; neither alone clears the gates.**

**Two user-verdicts (Session 9):** **D-P1** add-leash = "gentle fixes only, then measure" — anchoring + per-label ADD criteria + negative few-shots + per-value gates, with NO deterministic keep-only lock on Tasting/Kitchen-safety in pilot-1; add the lock later ONLY if the re-pilot still shows them over-predicted (P2′.6 stops for the user before adding it). **D-P2** held-out = fresh ~25 newly-labeled, the anti-overfit CANARY (not the sole gate; per-value 46-specific gating stays on the 69; + a corpus-wide prevalence review at full-run).

**Validation:** the pivot direction + 5 grounding subsystems were Codex-cross-examined (agent `ab877ed343f682131`); the re-authored P2′/P3 tasks passed **GATE 1B** (agent `a843c00e40545408c`) with ~8 authoring gaps folded in (finalC02 consumed by diff/apply, run-record schema contract, reconcile validation flow, bodyHash 3-path update, D-P10b capture/report mechanism, single-floor import-identity test, user-stop-before-lock, fresh-25 acceptance contract).

**Reusable (carries forward, NOT thrown away):** the whole harness, the 93-value vocab manifest, the 201-key alias floor (gets the D-P3 hygiene top-up: execute drops + drop `Basic Skills`/`Cooking Techniques`/`Forming patties` junk + split `Herbs & Aromatics`), the **69-lesson gold key** (`c02-answer-key.final.jsonl`, `1a434c6`), the 764-row PROD corpus, both pilot run outputs + scorecards (`artifacts/c02-run.opus-4-{8,7}.jsonl` + `c02-pilot-scorecard.*`, on disk).

**Proxy/runbook correction (carry forward):** CLIProxyAPI **v7.2.33** Anthropic base URL = `http://127.0.0.1:8317` (the old `…/api/provider/anthropic` path is GONE → 404); the harness `--base-url` takes the bare host. The preflight 12K-budget breach (measured 13.7K) was **proxy-cloak inflation only** (real prompt ~6.9K static) — not a real cost/size problem. Proxy binary backed up at `~/.local/bin/cli-proxy-api.v7.1.68.bak`; OAuth auto-refreshes while running.

---

_The P2.3 runbook + "current task = P2.3" content below is **SUPERSEDED** by the pilot outcome above — kept for historical reference only._

**Phase (historical — pre-pilot orientation):** **P1 COMPLETE & MERGED** — PR #542 squashed to `a5ff8a9` on `main` (rounds 1–5 bot triage + a user-requested Codex merge-review all folded; round-4 caught + fixed a real 🔴 gold-key data-loss bug). The full P1 PR cycle (per-task as-built + Sessions 0–5 log) is in the **archive** (`…execution-status-archive.md`) — read on demand via `grep -n`, not at session start. Baseline now: harness suite **631/631**, full suite ~**75 files / 1583**, `npm run check` clean.

**NOW: P2 (Pilot) on a new `feat/c02-pilot` branch.** P1's `feat/c02-harness` is merged, so P2 branches fresh. **Model = Opus 4.8 ONLY** — the §4 Q11 Opus-vs-Sonnet bake-off was **SUPERSEDED 2026-06-23** by user decision (`project_c02_p2_opus_only`): one Opus run, scored alone against the same 4 gates, `claude-opus-4-7` fallback only if it fails. Session 6 formalized this supersede across the design / impl / kickoff / status docs (the explicit P2-start bookkeeping item from that memory).

**Current task = P2.3 (single Opus-4.8 run — NEEDS CONSOLE CREDITS; gated on the user loading credits).** P2.1 complete (`eb0c304`/`16880b5`/`30f0a0b`). **P2.2 COMPLETE this session (all supervisor-verified):**
- **Build tooling** `f1d0f78` + `33ffbdb`: `scripts/stage2-retag/build-c02-answer-key.ts` (+ test, 30 cases) — `scaffold` / `worksheet` (per-tag provenance) / `assemble` (validates closed-vocab + parent invariant). Scripts committed; bodies-bearing artifacts gitignored.
- **Two-lens AI draft (no Console credits — session billing):** 7 Sonnet agents drafted all 70; 3 Codex/GPT-5.5 agents independently re-derived the 51 judgment rows (Sonnet draft withheld from Codex for genuine independence, Q6). Both supervisor-validated 0 errors.
- **Interactive user adjudication of ALL 70** (user chose this over editing the worksheet file): supervisor walked the user lesson-by-lesson — blurb + locked (agreed) tags + a pick-list of just the uncertain tags — across ~16 AskUserQuestion rounds. Decisions recorded to gitignored `artifacts/c02-adjudication.jsonl` (+ `_record.py` helper + `c02-adjudication-input.json`, all gitignored). **Gold key force-committed `53aec72` = `artifacts/c02-answer-key.final.jsonl`** (70 rows, id+tags only, no bodies; 0 vocab/parent errors).
- **Gold-key shape:** 15 empty (garden/science), 274 cooking tags / 19 of 23 skills, 373 ingredient tags / 65 of 70 ingredients; **84/93 canonical values present, 78/93 with ≥2 support** (the gates' SUPPORT_FLOOR). 9 values dropped to zero in adjudication (Beets, Cocoa & chocolate, Fennel, Fermenting, Grilling, Melons, Plating & garnishing, Steaming, Stone fruits) — those won't be scored, expected (key = ground truth). **User ran a TIGHT/precise key:** keeps ingredient GROUPS, adds SPECIFICS only when the food is genuinely central; drops garnish-level/incidental items + most pantry sweeteners (B-lite).

**Preflight DONE (Session 7):** `preflight-token-mass` PASSED on the offline static basis — prefix **~6,947 tokens** (budget 12,000; Opus 4096 floor cleared). The `count_tokens` call 400'd with **"credit balance too low"** — the **Console API key is empty**, which is exactly why P2.3 bills via the **CLIProxyAPI proxy (OAuth → Claude Max credits)**, NOT a Console key. User does P2.3/P2.4 in a **fresh post-/clear session**.

**P2.3 RUNBOOK (proxy/OAuth billing — `reference_cliproxyapi_max_billing`):** the harness already supports this via `--base-url`; when set, the API-key source switches to `ANTHROPIC_API_KEY` (the PROXY-side key in `.env.local`, = contents of `~/.cli-proxy-api/.api-key`, NOT `sk-ant-…`). Binary + OAuth creds present (`~/.local/bin/cli-proxy-api`, `~/.cli-proxy-api/claude-mail@danfeder.org.json`); proxy was NOT running at Session-7 end.
1. **Refresh + start proxy** (version-tracking: pull latest release before a fresh run — it's in a detection cat-and-mouse): start `~/.local/bin/cli-proxy-api -config ~/.cli-proxy-api/config.yaml &` → verify `lsof -nP -iTCP:8317 -sTCP:LISTEN` → smoke-test the curl in the memory. Re-login only if refresh token expired (`-claude-login`).
2. **Re-run preflight via proxy** (precise count): `npx tsx scripts/stage2-retag/preflight-token-mass.ts --base-url http://127.0.0.1:8317/api/provider/anthropic` (expect ~8.4K measured = 6,947 static + ~1.4K cloak; still < 12K).
3. **P2.3 run** (target the 70 via `--ids` = the committed gold key; corpus.jsonl is on disk, gitignored): first confirm caching with `--limit 3 --concurrency 1`, then the full 70 at LOW concurrency (proxy favors low concurrency / custom tool names):
   `npx tsx scripts/stage2-retag/run-retag.ts --model claude-opus-4-8 --base-url http://127.0.0.1:8317/api/provider/anthropic --ids scripts/stage2-retag/artifacts/c02-answer-key.final.jsonl --output scripts/stage2-retag/artifacts/c02-run.opus-4-8.jsonl --concurrency 1`
   Confirm `cache_read>0` on call ≥2; capture per-lesson `totalCostUsd`; `claude-opus-4-7` is the fallback model if 4.8 misbehaves. (fable suspended.)
4. **P2.4 score** Opus ALONE vs the gold key on the 4 gates: `npx tsx scripts/stage2-retag/score-answer-key.ts --key scripts/stage2-retag/artifacts/c02-answer-key.final.jsonl --run opus-4-8=scripts/stage2-retag/artifacts/c02-run.opus-4-8.jsonl` (+ `--v3-from-corpus artifacts/corpus.jsonl` for the rules/current baseline) → reads `c02-gates.ts` → commit `artifacts/c02-pilot-scorecard.md` → **USER-GATED greenlight** (all 4 gates must pass; Opus must earn it). 70-lesson identity locked by `c02-answer-key-manifest.json`.

**Hibiscus note (resolved in adjudication):** appears in ≥2 lessons (Temperature & Tea Making; Red Drinks for Juneteenth) with NO canonical mapping — user left both untagged (no manifest addition). Closed.

**P2 — key carry-forwards:**
- (a) **P2.1 MUST regenerate `artifacts/corpus.jsonl`** — the on-disk one (765 lines, pre-2026-06-12) LACKS `cooking_skills`/`main_ingredients`; the C02 sampler + rules-baseline read current tags from corpus record fields. The `CorpusRecordForSampling` type carries the two fields.
- (b) P2.2 gold key is USER-GATED (AI-drafts → user-adjudicates + the Q6 independent judgment-row 2nd pass).
- (c) **P2.3 = a SINGLE `--model claude-opus-4-8` run** over the 70-key (run `preflight-token-mass` first; needs Console credits). **NO Sonnet bake-off** — the §4 Q11 bake-off was **SUPERSEDED 2026-06-23** by user decision (`project_c02_p2_opus_only`); one model run, not two.
- (d) P2.4 greenlight is USER-GATED (Opus 4.8 scored **alone** on the 4 gates, no model tie-break; Opus must earn it; `claude-opus-4-7` fallback only if it fails).
- (e) the C02 sampler covers all **93** values (both fields), not just the 70 ingredient values — per-value scoring needs ≥2 support on both fields.
- (f) **Gate ③ zero-specifics — RESOLVED in P1** (`96c0ab1`, Codex merge-review + user sign-off): gate ③ now FAILS when a contestant predicts zero added specifics (requires `addedSpecificPrecision !== null`), so a model emitting only group tags can't clear ③ vacuously. Also hardened in the same commit: gate ① fails on an empty clean-core; `assertCorpusHasC02Tags` aborts on a stale corpus. No longer a P2 decision.
- (g) **P2 efficiency cleanups (round-2 bot; negligible at 70 rows, do when P2 exercises the harness at scale):** add `dropKeys: Set<string>` to `C02Floor` so `buildC02SamplerContext` stops re-reading the alias-map file (#3); memoize `loadC02Manifest` in `vocab.ts` like `loadC02Floor` (#4); pass a precomputed `judgmentRow` into `bootstrapGate2Delta` instead of recomputing `partitionKey` (#5).

**The locks (carry forward):** Q2 flat `string[]` + parent-map superRefine · Q3 alias-floor + parent-reconcile R-rules in `normalize.ts` · Q4 3-layer strata + size **70** · Q5 4 gates over the **existing** `evalMetrics` precision/fp (NOT new metric math) · Q6 independent hard-case 2nd-pass gold key · Q7 harness R-rule + Zod superRefine, **no DB trigger** · Q8 Title-Case `value===label` · Q9 P3→P4a→P4b expand/contract (never bundle) · Q10 P-branch map · **Q11 SUPERSEDED 2026-06-23 → Opus-4.8 only, NO bake-off** (`project_c02_p2_opus_only`; original lock was Opus-vs-Sonnet, Sonnet wins ties — now skipped). **Q1 vocab = 93 canonical:** 23 cooking_skills + 70 main_ingredients (24 groups + 46 specifics, 4 null-parents: Celery/Fennel/Seaweed (nori)/Cocoa & chocolate; Melons parented under "Squash, cucumbers & melons"); **B-lite pantry** (Sugar→Sweeteners; drop Salt/Oil/Soy sauce); **freeze the manifest at end of P3**.

**Live census (CORRECTED Session 6, 2026-06-23):** PROD `retired_at IS NULL` = **764 live rows**; **122 distinct cooking_skills** (1,758 appearances, 435 lessons) / **230 distinct main_ingredients** (1,847 appearances, 430 lessons). ⚠️ The earlier "121/202 re-census" was a **TEST-DB query mislabeled as PROD** (forensics: `121` = TEST cooking_skills distinct exactly, the census table's named-appearance sum `1,709` = TEST exactly; PROD is reproducibly 122/230 per the 2026-06-11 `oq2` verbatim-SQL census + a fresh live query; the q1 census table even self-sums to 123/212 ≠ its 121/202 headline). 122/230 (the original draft figure) is ground truth; 121/202 retracted. Floor coverage re-measured against live PROD = **92.4% cooking / 94.3% ingredients** appearances (designed band), so the TEST-sourcing did NOT materially damage the alias map — but it left ~10–15 clean folds the TEST list missed (Beans/Squash/Parmesan cheese/Sour Cream/Peas/Lettuce/Various seeds/Beyond Sausage (pea protein)/… → their groups); P2.1 augments the alias map with these.

**Pre-next-PR verification (if any):** none (no DB until P3). P1 is scripts-only, git-revert reversible.

## Recent decisions worth carrying forward

- **✅ P2′.7 PILOT GREENLIT (Session 16, 2026-06-25, user "greenlight and build").** Fresh-25 cooking canary, scored apples-to-apples vs the 69-key standard, = **SHIPPED cooking P 0.819/R 0.800/F1 0.810** (≈ pilot 0.833; floor-retention +0.207 over floor-only). **The canary surfaced a real methodology lesson:** the FIRST read was P 0.422 — but the cause was a **labeling-standard mismatch I introduced** (my held-out brief credited generic `Reading & following recipes`/`Measuring` strictly; the 69-key credits them broadly — 34/54, 37/54 — and the model matches the 69-key), NOT model over-fitting. Fix (user Option A) = re-label the held-out to the 69-key standard (2 blind re-derivations calibrated to real 69-key examples + tie-break; **the 4 user-locked Tasting/KFS sentinel decisions were force-held, never re-opened**) + re-score the existing run (free). **Codex cross-exam verdict = MIXED (legitimate, NOT gaming)** — gold v2 isn't model-copied (real FPs/FNs survive; Seeds→`Pickling` stays a model FP; arithmetic re-reproduced exactly) — with three honest caveats folded: post-hoc → the fresh-25 is now a **calibration check, not a blind confirmation**; my "essentially every" framing overstated (true 63%/69% → real precision ~0.79–0.82); additive-only revision. **Binding precision-at-scale guard stays §8 ② (P3.1 all-700 prevalence review + floored-row spot-check).** Notable: some shipped FPs (Dumplings `Dough making`/`Sautéing`/`Steaming`) are the FLOOR retaining wrong legacy tags the model correctly dropped — a floored-row-spot-check target, not over-tagging. *Lessons:* (1) a fresh-eval gold MUST be labeled to the SAME standard the model was tuned to, or precision is confounded — pin the eval standard before testing; (2) cross-examine a result that FLIPS on your own re-label (Codex confirmed it wasn't gaming but correctly demoted it to calibration). Reinforces `feedback_codex_over_crossexamine` + `feedback_census_provenance_committed`.
- **✅ TASK (c) — RE-FRAMED §8 GREENLIGHT CRITERION authored + audited (Session 15, 2026-06-25, `8389d01`).** Replaced the retired "4 gates pass" with design §8 "Greenlight criterion (re-framed)": two USER-gated checkpoints on the SHIPPED per-field output — ① pilot greenlight (P2′.7) + ② pre-apply gate (P3.1). A **Codex cross-family + Claude-lens audit** (parallel, both adversarial, both verified every number against the committed code/scorecards) caught **three overclaims in MY first draft**, all folded: (1) "+0.065 clears the +0.05 bar" RE-INTRODUCED a retired gate — the +0.065 is all-69 cooking F1, not the judgment-row Gate② bar → reframed as diagnostic evidence floor-retention beats floor-only, + surfaced the precision COST (floor-only P0.900/R0.732 → floor-retention P0.833/R0.915); (2) "no clean-core regression BY CONSTRUCTION" overclaimed — it guarantees no loss only RELATIVE TO THE FLOOR; a floor mis-fold is uncatchable + both fields inherit it → the P3.1 floored-row spot-check confirms fold-correctness at scale; (3) "sentinels = 1.0" presented as sufficiency — the keep-only LOCK pins Tasting/KFS high (KFS below support on the 69-key), so other skills can collapse on fresh data while sentinels read clean → the all-700 prevalence review is the real cooking-precision-at-scale guard, sentinels are necessary-not-sufficient. Also added: a cooking-precision tripwire (~0.833), an over-tag remedy (abort+re-pilot or deterministic post-filter, NEVER a silent threshold relax = gaming), a both-consistency-AND-correctness "much cleaner" definition, cohort-qualified ingredient numbers, and the impl P3.1 floored-row-selector fix (normalizationCount → ship-policy provenance bucket). *Lesson:* even a faithful transcription of locked decisions deserves an adversarial cross-exam for a load-bearing ship gate — the overclaims were MINE (framing the locked numbers too strongly), not in the locked decisions. Reinforces + extends `feedback_codex_over_crossexamine`.
- **✅ TASK (d) — D-P12: FRESH-25 CANARY = COOKING-SCOPED (Session 15, 2026-06-25, user-verdict via AskUserQuestion → recommended, `8389d01`).** Under D-P11's per-field ship the canary labels + scores `cooking_skills` ONLY. The audit's finding C3 gave the basis: the 25 has unique teeth for COOKING (the one fresh-LABELED cooking-precision check; the all-700 prevalence review is unlabeled firing-rate) and is REDUNDANT for ingredients (floor-only is deterministic → the 700-row review strictly dominates). Halves the labeling burden, keeps the unique signal. P2′.7 freezes everything, then labels cooking on the 25 (Q6 protocol) + scores cooking on the SHIPPED floor-retention output.
- **✅ DECISION (a) — PER-FIELD SHIP METHOD LOCKED as D-P11 (Session 14, 2026-06-25, user "go for it" + double Codex cross-exam).** `main_ingredients` ships the **deterministic floor only**; `cooking_skills` ships **LLM + floor-retention (option 3)** = floor ∪ `finalC02.cooking` (KEEP/ADD allowed, DROP of a floored skill forbidden). Honest all-69 re-score (`c02-cooking-floor-vs-llm.ts`): floor-only 0.807 / LLM-as-is 0.836 / floor-retention **0.872 (+0.065)**, no clean-core data loss. **Process note (a real save):** my first reconciliation claimed the LLM "wins on cooking" via an r2-backfill of 4 missing records; Codex (`a1be3c2d…`) REFUTED it — cooking changed r2→r3 (lenient reconcile is field-generic), so r2 is a bad proxy, and the 4 records' true root cause was **whole-record validation coupling** (an off-vocab *ingredient* discarded their valid *cooking*). I re-ran honestly (reconstructed the 4 from their raw r4 cooking decisions, scored all 69) → the corrected numbers STILL land on option 3, for a better reason. Codex also corrected: floor-retention is a NEW mechanism (not an extension of the add-suppressing keep-only lock), and P3 must field-isolate validation. Authored build task **P2′.8**. *Lesson reinforced:* the Session-13 "trace concrete records before trusting an aggregate" rule applies to cross-round mixing too — and cross-examining my OWN overturning-claim with Codex caught the flaw.
- **✅ USER DECISION 2 — ACCEPT THE CEILING + SHIP CLEAN (Session 13, 2026-06-24, AskUserQuestion → recommended).** After round 4 showed specifics-keep-only clears the pooled bar but not the 4 gates (structural ceiling: incidental existing-tag specifics no mechanical method prunes), the user chose to **greenlight C02's big wins and re-frame the greenlight from "4 gates pass" → "much cleaner than today + no harmful regression."** Specifics ship at cleaned-existing-tag quality (~0.77 vs the tight key), not perfect centrality. This AMENDS the locked Q5/§8 "4 gates" greenlight (recorded as a design §8 amendment) and the two-level "AI curates central specifics" goal. **Rationale:** the tight key is right but encodes a precision standard the loosely-tagged legacy data + any mechanical re-tagger cannot reach; the re-tag is still a large net improvement, so the user judges "good enough to ship" directly rather than via an unreachable proxy gate.
- **✅ USER DECISION 1 — SPECIFICS KEEP-ONLY (Session 13, 2026-06-24, AskUserQuestion → recommended).** Extend the D-P1 keep-only lock to ALL 46 main_ingredient specifics — the LLM may KEEP/DROP an anchored specific but never ADD a new one. Built + verified as round 4 (`46b3ddf`): pooled added-specific precision 0.621→0.773. **Supervisor caught + corrected a real analysis bug mid-decision** (the LLM `add`/`drop` lists are `{value,reason}` OBJECTS, `keep` is strings — an early aggregate that treated all as strings wrongly read "0 adds"; a concrete per-lesson trace exposed it). The corrected split: 72 FP specifics = 45 via ADD (lock removes) + 27 via KEEP (lock can't touch). Amends the two-level "AI adds 1–3 central specifics" goal.
- **✅ CASE REVIEW SETTLED "whose bar is right" (Session 12, 2026-06-24): the tight key is MOSTLY RIGHT — the model genuinely over-tags ingredient specifics.** 13 real Garlic/Lemon/Honey/etc. disagreements adjudicated (agent `ac4c11a4…`, worksheet `artifacts/c02-specifics-disagreement-cases.md`). The disputed foods are overwhelmingly background aromatics, dressing/sauce parts, "example" produce, sprig/pinch add-ins; lessons that name their own mains never include them. → **Loosening the key OR the gate thresholds to pass would be GAMING — off the table.** Real MINORITY of borderlines: signature-flavor side dishes, honey named in a dressing TITLE, and **"Bee Habitat" honey = the lesson's subject but never cooked** → raises the open question *"is the ingredient tag what the lesson is ABOUT or what students COOK WITH?"*. The next move is a USER design decision (specifics-keep-only vs specifics-advisory vs settle-tag-purpose-first), which re-opens the two-level "add 1–3 central specifics" design goal → needs explicit sign-off (don't pick unilaterally).
- **✅ P2′.6 ran 3 tuning rounds, USER-gated each (Session 12).** R1 all-fail (P76.3) → R2 D-P1 keep-only lock fixed the catch-all SKILLS (Tasting/KFS → 1.0) → R3 lenient reconcile + Sweeteners lock flipped Gate ② ingredients. **Two locked decisions were AMENDED with user approval:** (a) reconcile is now LENIENT (recovers a malformed KEEP/DROP/ADD partition instead of rejecting — D-P6's "reject" rule relaxed; the strict reject cost ~12% of lessons their tags and the prompt couldn't fix it); (b) the keep-only lock concept (D-P1) was extended to the pantry GROUP `Sweeteners`. `C02_RECONCILE_POLICY_ID` now `…-v3`.
- **✅ PIVOT RE-LOCKED (Session 9, 2026-06-23/24).** The Session-8 pivot direction was worked into a full design + re-locked as **design §3·PIVOT (D-P1..D-P10)**; impl **P2′/P3 re-authored**; **GATE-1B-validated**. Headline: anchored verify-and-diff + one canonical floor + `reconcile.ts` (never append-only) + C02-only apply (concurrency guard + targeted rollback) + per-value gates with a recomputed baseline + 3-layer test strategy. User-verdicts: **D-P1** gentle-leash-then-measure (no add-lock in pilot-1) + **D-P2** fresh-25 canary. Codex-validated twice (method `ab877ed343f682131`, GATE-1B `a843c00e40545408c`). The Session-8 bullet below is the now-realized pivot intent; the "hybrid-floor full LLM re-read" bullet two below is dead.
- **⚠️ PIVOT (Session 8, 2026-06-23): the as-built method failed the pilot and is being replaced.** The "hybrid-floor full LLM re-read" was implemented as a **blind** re-read (floor R7/R8 inert on canonical LLM output, apply stores raw LLM, LLM never sees existing tags) → both Opus models over-tagged and failed all 4 gates. New direction (re-lock next session): **floor-first reconciliation in code + anchored prompt + negative few-shots + C02-only apply scope + per-value gates + held-out slice.** Codex-confirmed. The bullet below is the ORIGINAL (now-superseded) method decision, kept for history.
- **Method = hybrid-floor full LLM re-read** (decided in the 2026-06-22 scoping discussion + a Codex cross-exam): LLM reads every lesson; deterministic alias-map floor anchors the ~94% clean core; LLM does the judgment work. NOT rules-only (can't add specifics), NOT blind full-LLM (regresses the core). **[SUPERSEDED by the pivot above — the implementation was, in practice, the blind full-LLM it claimed to avoid.]**
- **Prior $121 fable run produced ZERO output for these two fields** (verified) — no reusable work; fable-5 is suspended. The pilot was originally going to re-run an Opus-vs-Sonnet bake-off, but that was **superseded 2026-06-23 → Opus 4.8 only** (`project_c02_p2_opus_only`).
- **Vocab amendments (user, 2026-06-22):** +Seaweed (nori), +Cocoa & chocolate (group-less specifics), +Sunflower butter/Tahini/Peanut butter (under Nuts & seeds); Hummus→Chickpeas remap; Frying→Sautéing & stir-frying. Solo sign-off (no curriculum-team round).
- **Pilot gold key = AI-drafts-user-adjudicates** + an independent hard-case protocol; greenlit on 4 separate gates, not a macro score.
- **Enforcement = expand/contract** (P3 data → P4a frontend deploy → P4b CHECK), mirroring `garden_skills`.
- **Floor matching is now case-insensitive + trim** (Session 4, user-asked, supervisor-verified safe = 0 collisions): `matchKey = NFC.trim().toLowerCase()` + a canonical-case rule + a build-time collision guard + 3 machine-checked invariants. *Strengthens* the Q3 idempotency lock ("no canonical is an alias key" → "no canonical matchKey-collides with an alias key pointing elsewhere") rather than changing scope. **Lowercase+trim only, NOT diacritic/punctuation folding** — that adds collision surface for little gain, and P4a closes both fields to dropdowns so future free-form case/accent drift becomes impossible; the floor's robustness matters mainly for the historical corpus (P3) + the P1.6 rules-baseline. This was the fix for the `Mixing` (180-lesson) alias-map gap.
- **Gate ④ never-stored scan is case-folded** (Session 5): the literal scan compares on `matchKey`, so a casing/spacing variant (`salt`, ` Oil `) can't slip a never-stored literal past the greenlight gate. The ≥2× sampler coverage is a **hard CLI failure** (exit 1), not just a warning.

## Done

- **P2.2 (Session 7) — COMPLETE + supervisor-verified** on `feat/c02-pilot`: gold-key build tooling `f1d0f78` + per-tag-provenance worksheet `33ffbdb` (TDD, 30 tests) · two-lens AI draft (7 Sonnet draft agents all 70 + 3 Codex/GPT-5.5 independent re-derivation of the 51 judgment rows; both supervisor-validated 0 errors; Sonnet draft withheld from Codex for Q6 independence) · **interactive user adjudication of all 70 lessons** (~16 question rounds) → **gold key force-committed `53aec72`** (`c02-answer-key.final.jsonl`, 70 rows, 0 vocab/parent errors; 84/93 values present, 78/93 ≥2-support; tight/precise key). **Next = P2.3 single Opus-4.8 run (needs Console credits).**
- **P2.1 (Session 6) — COMPLETE + supervisor-verified** on `feat/c02-pilot`: census forensics (TEST-mislabeled "121/202" → corrected 122/230) · `export-corpus` guard fix + PROD corpus regen (764 rows, both fields) `eb0c304` · alias-floor top-up (+14 clean folds, 201 keys) `16880b5` · 70-lesson pilot sample (all 93 canon ≥2×, deterministic) `30f0a0b`. Not yet PR'd. **Next = P2.2 gold key (USER-GATED).**
- **P1 (Sessions 0–5) — MERGED** as PR #542 → `a5ff8a9` on `main`. Scaffold + GATE 1A → §4 design lock (11 Q's) + impl-plan P1.1–P4b.1 + GATE 1B → P1.1–P1.6 harness build (vocab manifest + alias-floor maps · both fields wired into schema/vocab/prompt · R7/R8/R9 normalize rules · export/diff/validate plumbing · 3-layer set-cover sampler · 4-gate scoring + rules baseline) → pre-push review + 5 bot-triage rounds + Codex merge-review folded. **All supervisor-verified; harness 631/631, full suite ~75/1583 green.** Full P1 PR cycle (per-task as-built + Sessions 0–5 log) → **archive file**.

## In flight

(**P2′.6 COMPLETE → toward SHIP** on `feat/c02-pilot` (Session 13): round 4 ran (`46b3ddf` code + `53794bc` scorecard) on top of Session-12's rounds 1–3 (`b814ad1`,`f46fce0`/`2627b5a`,`cc0a5ef`/`9bb4d68`/`1949ebf`,`e7349b6`). cooking_skills + ingredient GROUPS solid; ingredient SPECIFICS hit a structural ceiling (keep-only → pooled 0.773 but 4 gates still fail on existing-tag over-KEEP). **Two user decisions: keep-only specifics, then accept-ceiling-ship-clean** (see header + recent decisions). **No PR open yet** — P2′ is scripts-only; its PR opens once the ingredient method (floor-only vs LLM) is confirmed and the re-framed greenlight is written next session. Full suite **1793**, `npm run check` clean. Branch carries the 69-key, the held-out 25 manifest, all 4 round run-outputs + scorecards (`c02-run.opus-4-8.p2prime{,-r2,-r3,-r4}.jsonl` gitignored; scorecards committed), the disagreement-case worksheet + `rereconcile-run.ts` harness. P2 + P2′.6 pilot artifacts stay as provenance. **Next: confirm ingredient method → re-framed greenlight → (maybe P2′.7) → P3 apply (DB).**)

## Blocked

(none — user-approval gates for the pilot greenlight and PROD migrations are EXPECTED, not blockers)

## Decisions made during execution

**Session 1 (2026-06-23) — §4 locks.** User verdicts (AskUserQuestion, all came back on the recommended option): pantry **B-lite**; pilot size **70**; invariant mechanism **B** (R-rule + superRefine, no DB trigger); freeze-candidates **pre-add the 4 high-count** (Apples/Coconut/Oranges/Lime). Evidence-locked: Q2 flat string[]; Q3 normalize R-rules; Q5 gates ③④ are plumbing over existing `evalMetrics` (supervisor-verified — corrects the design's "new metric families" framing); Q8 Title-Case value===label; Q11 bake-off mechanism. Ratified recommendations (presented, no pushback): Q1 freeze-after-P3 + 70-value roster; Q5 thresholds (①strict ②+0.05 both-fields ③0.7/5% ④0.8, re-tunable at pilot); Q6 independent 2nd-pass; Q9 Option-A choreography; Q10 P-branch/artifact map. Full rationale in design §4.

## Out-of-scope follow-ups captured here

(for the project memory after the initiative ships — seed list from design §10)
- Embeddings regeneration (rebuild's C2.4) — own session; relates to `project_embedding_pipeline_mismatch`.
- Filter-UI surfacing of the two ingredient tiers (group vs. specific in the public menu).
- A hard "1–3 specifics" cap (deliberately left as guidance).
- A DB trigger for the specific→group invariant (leaning app-layer only).
- Drop the retained C02 snapshot table(s) in a future cleanup once PROD-stable.
- **(Optional, low-priority) Collapse the now-redundant case-twin alias keys** in `c02-alias-map.json` (e.g. `Chopping`+`chopping`, `Measuring`+`measuring`) — harmless under case-insensitive matching (the collision guard only throws on *different*-target collisions), left in place to minimize churn. Codex flagged ~20 same-target collisions (P1 GATE 3); a future tidy-up could dedupe them.
- **(Minor, DEFERRED — Session 12 Codex) `preflight-token-mass` has no `--model` flag** → it always uses `DEFAULT_MODEL` (opus-4-7) for the `count_tokens` call + `assessCacheFloor`. Immaterial for the C02 pilot (opus-4-7/4-8 share tokenizer + cache floor, and the empirical `cache_read` probe used 4-8 directly), but a 1-line `--model` add would make the guard honest before any non-opus / cross-family preflight.

## Pointers to durable context

- Kickoff prompt: `2026-06-22-c02-cooking-ingredients-retag-kickoff.md`
- Design doc: `2026-06-22-c02-cooking-ingredients-retag-design.md` (LOCKED decisions + §4 open questions)
- Implementation plan: `2026-06-22-c02-cooking-ingredients-retag-implementation.md` (P1.1–P4b.1 tasks authored)
- Worksheet (decided vocab): `2026-06-12-metadata-rebuild-stage1-cooking-skills-ingredients-worksheet-decided.md`
- **Provisional canonical manifest (Session 1):** `docs/plans/c02-session1-discovery/q1-vocab-census.md` (census + 70-value set + parent map — the byte-source for P1.1's `c02-vocab.json`; later lock-corrected to 46 specifics / Melons parented)
- Harness: `scripts/stage2-retag/`
- Archive: `2026-06-22-c02-cooking-ingredients-retag-execution-status-archive.md` (per-task P1 as-built detail + Sessions 0–4 log — read on demand via `grep -n`)

## Recent session log

> **PR cycle 1 (P1, Sessions 0–5) is fully ARCHIVED** — full per-session detail + learnings in `…execution-status-archive.md`. Only the current PR cycle's sessions stay here.

### Session 16 — 2026-06-25 — P2′.7 fresh-25 cooking canary → PILOT GREENLIT

Major events:
- Oriented (git HEAD `f665529` = Session-15; `npm run check` + suite 1815 clean; proxy already up on :8317). User confirmed → "proceed."
- **Froze** the harness (policy `c02-anchored-verify-and-diff-v4`, prompt sha `3eec2390…`, gates, vocab). Built a **blind body-only labeling input** for the 25 held-out ids (no tags).
- **Labeled `cooking_skills` (D-P12)** via the Q6 protocol: 2 independent blind cross-family derivations (Claude general-purpose + Codex gpt-5.5) → **15/25 exact agreement** → a grounded tie-break for the 10 disputes → **USER adjudicated the 4 disputed sentinels (all EXCLUDE)** → gold key v1.
- **Ran** the frozen harness over the 25 (Opus-4.8 via proxy, concurrency 1; 24 Zod-pass / 1 harmless no-cook Zod-fail; $0.69; cache held). Wrote `c02-heldout-score.ts` (reuses `materializeC02Ship`; **self-validated** — reproduces the 69-key re-score gate 0.807/0.836/0.872 exactly).
- **First read (gold v1) = SHIPPED cooking P 0.422/R 0.854** — far below pilot. **Diagnosed** (3 alt-gold re-scores) as a **labeling-standard mismatch**, not over-fitting: the 69-key credits `Reading & following recipes`/`Measuring` broadly (34/54, 37/54); my v1 credited them on 2 each; the model matches the 69-key. Surfaced to the user honestly (NOT a fake STOP, NOT a fake pass).
- **USER → Option A** (re-label to the 69-key standard, re-score the existing run). Built 8 disjoint 69-key calibration examples; ran **2 blind re-derivations** (Claude A2 [1st attempt died on an API connection-close, re-dispatched] + Codex B2) calibrated to them; reconciled (12/25 agree) + a tie-break for 13 technique disputes; **force-held the 4 locked Tasting/KFS sentinel decisions** (the re-label only fixes process/technique). Built gold **v2** → **re-score = SHIPPED cooking P 0.819/R 0.800/F1 0.810** (≈ pilot; floor-retention +0.207 over floor-only).
- **Codex adversarial cross-exam (gpt-5.5, inline) on the re-label = MIXED (legitimate, NOT gaming):** gold v2 not model-copied (real FPs/FNs survive; Seeds→`Pickling` stays a model FP; arithmetic re-reproduced exactly; 69-key premise verified) — but post-hoc → calibration-not-blind-confirmation; my "essentially every" overstated (true 63%/69%); additive-only. Folded all three caveats honestly into the presentation.
- **USER GREENLIT** ("greenlight and build, only cooking + ingredients metadata, none other"). Committed P2′.7 deliverables (scorer + gold keys v1/v2 + scorecards + label/tiebreak provenance; bodies/run/calibration gitignored). Checkpointed this doc. **No PR, no push, no DB.**

Dispatch pattern: 5 fresh-context subagents (2 blind labelers ×2 rounds + 2 tie-breaks + 1 Codex cross-exam), all returning inline/files; supervisor-verified every result (validated vocab, reconciled, self-validated the scorer, re-derived numbers). Operational run (proxy/LLM) + scoring in the main loop. 1 mid-session USER PAUSE (cleanly checkpointed to a resume note, since superseded by this doc).

Learnings (candidates to promote):
- **A fresh-eval gold MUST be labeled to the SAME standard the model was tuned to** — else precision is confounded. My held-out brief drifted stricter than the 69-key on generic process-skills, tanking the apparent precision 0.83→0.42. Pin (and ideally calibrate-by-example to) the eval standard BEFORE running the test, not after.
- **When a result flips on your own re-label, cross-examine it for gaming.** Codex confirmed the re-label was a legitimate correction (real FPs/FNs survive, not model-copied) but correctly DEMOTED it from blind-confirmation to calibration-check. A pass you manufactured deserves the same adversarial scrutiny as a finding — extends `feedback_codex_over_crossexamine`.
- **Separate "fix the test" from "re-open settled decisions."** The re-label could have silently re-opened the user-adjudicated Tasting/KFS sentinels (both re-labelers proposed changing them); force-holding the locked decisions kept the correction honest and bounded.

### Session 15 — 2026-06-25 — Tasks (c) re-framed §8 greenlight + (d) cooking-scoped canary (D-P12)

Major events:
- Oriented (git HEAD `98b2769` = Session-14 checkpoint, consistent; `npm run check` clean — the 3 lint hits are warnings on the throwaway `c02-cooking-floor-vs-llm.ts`). Confirmed orientation with the user → "proceed".
- **(c) Drafted the re-framed §8 greenlight criterion** (consolidating the Session-13 §6 amendment + D-P11 + impl P2′.7/P3.1 into one canonical §8 subsection), then ran a **two-lens parallel adversarial audit** — a Codex cross-family pass (`codex:codex-rescue`, gpt-5.5, inline-return, agent `ae3da94c…`) + a Claude lens (`claude`, agent `a3bb6c6f…`). Both converged on **three overclaims in my first draft**, all folded:
  1. **Retired-gate "+0.05" re-introduced.** "+0.065 clears the +0.05 bar" — but +0.065 is all-69 cooking F1, while the +0.05 Gate② bar was per-judgment-row + both-fields. Reframed as *diagnostic evidence floor-retention beats floor-only*; surfaced the precision COST (floor-only P0.900/R0.732 → floor-retention P0.833/R0.915).
  2. **"By construction" overclaimed.** "No clean-core regression by construction" only guarantees no loss *relative to the floor*; a floor mis-fold (wrong alias, mis-dropped clean tag, bad Herbs&Aromatics split) is uncatchable + both fields inherit it. Reframed + required the P3.1 floored-row spot-check to confirm fold-correctness at scale.
  3. **Sentinels = sufficiency.** Tasting/KFS = 1.0 is pinned by the keep-only LOCK, not by the LLM judging fresh data well (KFS is below the support floor on the 69-key); other skills can collapse on fresh data while sentinels read clean. Demoted to necessary-not-sufficient; the all-700 prevalence review is the real cooking-precision-at-scale guard.
  Also folded: a cooking-precision tripwire (~0.833), an over-tag remedy (abort+re-pilot or deterministic post-filter, NEVER a silent relax = gaming), a both-consistency-AND-correctness "much cleaner" definition, cohort-qualified ingredient numbers (0.831<0.918 clean-core; +0.012 judgment-row CI∋0), and the impl-P3.1 floored-row-selector fix (normalizationCount → ship-policy provenance bucket, since C02 bypasses normalize + the floor lands in `materializeC02Ship`).
- **(d) D-P12 — fresh-25 canary cooking-scoped** (user-verdict via AskUserQuestion → recommended). The audit's finding C3 gave the principled basis: the canary has unique teeth for COOKING only (the one fresh-LABELED cooking-precision check; the all-700 prevalence review is unlabeled), and is REDUNDANT for ingredients (floor-only, deterministic). So P2′.7 labels + scores `cooking_skills` only.
- Folded everything → committed doc-only `8389d01` (design §8 criterion + table + §6/D-P11 pointers + D-P12; impl P2′.7 cooking-only + P3.1 provenance-bucket note). Supervisor-verified the edits landed coherently (grep + diff-stat). No code, no DB, no push, no PR.

Dispatch pattern: 2 parallel adversarial reviewers (Codex + Claude), both read the locked decisions from disk + verified every number against the committed code/scorecards. No executor dispatch (scaffold-doc authoring is supervisor-owned).

Learnings (candidates to promote):
- **Adversarially cross-exam your OWN doc authoring, not just executors' code.** The three overclaims were MINE — a retired gate cited as live, a relative guarantee stated as absolute, a lock-pinned metric read as evidence of model quality. The locked decisions were fine; the *transcription* wasn't. Even "just writing down a locked decision" is substantive enough to deserve a cross-exam, especially for a load-bearing ship gate. The Claude lens caught the same class independently — perspective-diverse verification (two families), not just cross-family.

### Session 14 — 2026-06-25 — Decision (a) LOCKED as D-P11 (per-field ship method)

Major events:
- Oriented (git HEAD `70377e1` = Session-13 checkpoint, consistent; `npm run check` clean; the untracked `*-kickoff.md` are other initiatives). Confirmed orientation with the user, then dug into the OPEN decision (a) — the ingredient/cooking ship method.
- **Codex round 1 (agent `ae4414a5…`)** on decision (a): agreed floor-only ingredients but surfaced that **cooking_skills LLM is ALSO below floor on the scorecard's per-slice F1** (clean-core 0.787<0.808, judgment 0.801<0.807) → my "LLM earns its place on cooking" claim was not metric-proven. Flagged group-FP leakage past Gate ③ + the P3 per-field staging-object complexity.
- **Dug into the cooking sub-question.** Wrote `scripts/stage2-retag/c02-cooking-floor-vs-llm.ts` (floor via the real `applyC02Floor`; finalC02 vs gold; deviation buckets). First pass (65 lessons, excluding 4 with no `finalC02`) showed LLM > floor — but that **excluded** 4 records and I **backfilled** 3 from r2, claiming a "flip."
- **Codex round 2 (agent `a1be3c2d…`, user-requested) REFUTED the reconciliation.** Correct catches: (1) the r2 backfill is invalid — cooking output DID change r2→r3 (lenient reconcile is field-generic, not ingredient-only); (2) the "flip" was backfill-driven, not Simpson's; (3) the 4 "missing" records are **whole-record validation coupling** (an off-vocab *ingredient* discarded valid *cooking*) — I confirmed this directly from the raw r4 decisions; (4) option 3 (floor-retention) is a NEW mechanism, NOT an extension of the add-suppressing keep-only lock.
- **Re-ran HONESTLY** (v2 of the script: all 69 lessons; reconstruct the 4 records' cooking from their valid raw r4 decisions; no cross-round backfill): floor-only F1 **0.807** / LLM-as-is **0.836** / floor-retention (option 3) **0.872 (+0.065)**. The corrected numbers still land on option 3 — Codex's "LLM loses 0.795" had scored the 4 valid-cooking records as zero output (which its own CLAIM 2 contradicts). Verified Codex's key numbers myself before relaying.
- **USER → "go for it":** locked decision (a) as **D-P11** (design §3·PIVOT) — floor-only ingredients + floor-retention cooking; authored impl task **P2′.8** (per-field ship policy + field-isolated validation, runs before the P2′.7 freeze); amended P3.1; updated this status doc.

Dispatch pattern: 2 Codex cross-exams (decision-advisory + adversarial-audit, both inline-return, `gpt-5.5`) + supervisor analysis script, re-run honestly after the audit. No executor dispatch (analysis + doc authoring is supervisor-owned). No DB, no push, no PR.

Learnings (candidates to promote):
- **Cross-examine your OWN overturning-claim, not just executors'.** I had an aggregate that "overturned" the scorecard; Codex caught that it rested on an invalid cross-round backfill. The fix (reconstruct the actual r4 decisions) preserved the conclusion but on honest footing. Mixing rounds to fill gaps is the same class of error as the Session-13 string-vs-object aggregate bug.
- **"Whole-record validation coupling" is a real harness anti-pattern** — one field's off-vocab value silently discarded another field's valid output. Field-isolate validation when fields are independent.
- **Don't relay a cross-examiner's numbers on faith either.** Codex's "LLM 0.795 loses" used a record-treatment (zero-output) its own finding contradicted; verifying its number myself surfaced that and produced the honest 0.836/0.872.

- **Then BUILT P2′.8** (dispatched fresh-context Opus executor `a639ff33…`; supervisor-verified). `materializeC02Ship` (floor-only ingredients + floor∪cooking, finalC02-or-raw read) + `ship-policy.rescore.test.ts` (re-score gate reproducing D-P11 with ZERO drift through real code) + run-time field isolation in `processC02Decision` (executor's STOP-judgment refuted by its own Codex + source-verified → done contained, reconcile core untouched). 22 new tests; full suite **1815**; `npm run check` clean. Commit `c4139ae`. I independently re-read the materializer + re-score gate + Part 2 diff/tests and re-ran the suite before accepting. ⚠️ Part 2 touched the live run path — flagged as a GATE-3/P3 carry-forward (header).

Next: write the re-framed §8 greenlight (c) → P2′.7 fresh-25 fate (d) → P3 apply (e). P2′ PR opens after the greenlight is written (then GATE 3 Codex on the accumulated P2′ diff, incl. the P2′.8 run-path change).

### Session 13 — 2026-06-24 — P2′.6 round 4 (specifics keep-only) → 2 user decisions → C02 toward SHIP

Major events:
- Oriented (git HEAD `b30f4a0` = Session-12 checkpoint, consistent; `npm run check` clean; the untracked `docs/plans/*-kickoff.md` are OTHER initiatives, left). Read the design's two-level + greenlight sections + the disagreement-case worksheet.
- **Presented the Session-12 OPEN decision** (specifics-keep-only vs advisory vs settle-tag-purpose). **USER → specifics keep-only.**
- **Supervisor verification caught a real bug in my own analysis, mid-decision.** Before promising keep-only would help, I checked whether the specific FPs were LLM-ADDs (lock removes) or KEEPs (lock can't). A first aggregate read "0 adds" → would've meant keep-only is a no-op. A concrete per-lesson **trace** exposed the bug: the decision `add`/`drop` lists are `{value,reason}` OBJECTS, `keep` is plain strings; my script treated all as strings. Corrected: 72 FP specifics = **45 via ADD (lock removes) + 27 via KEEP (lock can't touch)**; keep-only projected to lift pooled precision 0.621→0.773 at a 26-TP cost. (Lesson: trace concrete records before trusting an aggregate.)
- **Round 4 built (dispatched executor `a09569828dd93820c`, Opus) + supervisor-verified.** Extended the keep-only lock to all 46 specifics; built `rereconcile-run.ts` to re-run ONLY floor+reconcile over the STORED round-3 decisions (no LLM call = free, deterministic A/B). **Golden check 69/69** (unchanged code reproduces round 3 → faithful), 38 records change with the lock. **Independently recomputed** pooled precision off the r4 output = **0.773 (tp 92/fp 27)** — matched the scorecard + my projection exactly. Verified the executor's 5 modified existing tests were legitimate add→anchor-keep reroutes (intent preserved). Full suite 1793; check clean. Commits `46b3ddf` (code+tests+harness) + `53794bc` (scorecard).
- **Round-4 result: all 4 gates still FAIL** on the over-KEEP / existing-tags ceiling (Bell peppers/Ginger/Parsley/Tomatoes per-specific; Sweeteners 0.750; Gate ① ingredients 0.831 < floor 0.918; Gate ② +0.012). Key insight: with adds locked the LLM no longer beats a pure deterministic floor on ingredients → structural ceiling, not a tuning miss.
- **Presented the ceiling + 4 options (accept / narrow-tier / drop-lever / groups-only). USER → ACCEPT THE CEILING + ship clean** (re-frame greenlight; specifics = cleaned-existing quality).
- Checkpointed the status header + recent decisions + this log. Did NOT decide the ingredient method (floor-only vs LLM) — teed up as the first next-session confirm.

Dispatch pattern: 1 fresh-context executor (round-4 build, Opus) → supervisor-verify (golden check + independent recompute + diff/test inspection) → 2 user-gated decisions. No Codex this round (the change was small, golden-checked, and independently recomputed 3 ways; reserve Codex for the P3 migration GATE 2). No DB, no push, no PR.

Learnings (candidates to promote):
- **Trace concrete records before trusting an aggregate over heterogeneous JSON.** A mixed string|{value,reason} shape silently zeroed a count and nearly flipped a recommendation to "keep-only is a no-op"; one 2-record trace caught it. The supervisor-verify gate worked exactly as intended — on my OWN analysis, not just an executor's.
- **Re-reconcile stored decisions is the clean way to A/B a deterministic post-LLM lever** — free, no LLM variance, isolates exactly the code change; gate it with a golden check (reproduce the prior run with unchanged code) so the reconstruction is proven faithful before flipping the lever.
- **"Mechanical ceiling" is a legitimate, shippable conclusion.** When neither the LLM nor a deterministic floor can hit a tight gold-key precision because the noise is in the *existing data*, the honest move is to re-frame the ship bar to "net improvement + no harmful regression," not to keep tuning or to game the key.

Next: confirm the ingredient method (floor-only vs LLM+keep-only — floor scores higher) → write the re-framed §8 greenlight → decide P2′.7's fate → P3 apply (DB phase, GATE 2 Codex on the migration).

### Session 12 — 2026-06-24 — P2′.6 RE-PILOT: 3 tuning rounds → mechanical plateau → OPEN design decision

Major events:
- Oriented (git/design/impl/status consistent; `npm run check` clean; proxy started, base URL `http://127.0.0.1:8317`, OAuth ok). User confirmed → "go for it" on P2′.6.
- **Preflight fix (`b814ad1`, TDD):** caught that `preflight-token-mass` measured the STALE monolithic prefix, not the live C02 anchored one — load-bearing because the 2-field tool nearly HALVES the prefix (~6,947→~4,469 char/4) and risked the Opus cache floor. Added `buildC02TokenCountRequest` + `--c02`. Proxy preflight + a 3-lesson probe confirmed the real ~3,584-tok prefix caches cleanly (`cache_read=7168`), ~$0.030/lesson. Codex (agent `ad84333…`) found 1 minor (no `--model` flag → immaterial for opus↔opus; DEFERRED).
- **Round 1** (run `c02-run.opus-4-8.p2prime.jsonl`, scorecard `e7349b6`): ALL 4 gates FAIL. P76.3/R79.3 (up from blind re-read P66.5). Diagnosed: Tasting over-add (KEEP 3/3 perfect, ADD 3/25) → D-P1 trigger. **USER chose: lock + tune + fix (combined).**
- **Round 2** (`f46fce0` + scorecard `2627b5a`): D-P1 keep-only lock on Tasting/Kitchen-safety (reconcile filters ADDs; policy→v2) + prompt tightening. **Tasting/KFS → 1.000** (sentinels PASS); Gate ② cooking −0.069→−0.010. Ingredient over-adds unmoved by prompt; 8 Zod-fails (partition). Codex (agent `a245a71…`) clean. **USER chose: mechanical fixes + re-run** (lenient reconcile + Sweeteners lock).
- **Round 3** (`cc0a5ef`+`9bb4d68` + scorecard `1949ebf`): lenient reconcile (recover, don't reject; policy→v3) + Sweeteners keep-only lock. Zod-fails **8→4**, **Gate ② ingredients PASS (+0.058)**. Codex (agent `ab6389a7…`) caught a REAL major (DROP-wins didn't fire for non-anchor overlaps) → fixed+tested `9bb4d68` (verified the run never hit it, so round-3 scorecard stands). **Gates ①③④ still FAIL** on ingredient-specific over-adds.
- **Case review (USER chose: look at real cases first; agent `ac4c11a4…`):** 13 real disagreements written up (`artifacts/c02-specifics-disagreement-cases.md`, gitignored). **Verdict: tight key MOSTLY RIGHT, model genuinely over-tags** (aromatics, dressing acids, sauce items, "example" produce). Minority borderline (signature flavors; honey in a dressing title; **Bee-Habitat** honey = the subject but not cooked → "what is the tag FOR?"). Rules out loosening the key/thresholds.
- Checkpointed; OPEN decision teed up (specifics-keep-only vs specifics-advisory vs settle-tag-purpose-first).

Dispatch pattern: operational session in the main loop (proxy ops + 3 LLM runs + deterministic scoring/diagnostics + the preflight & lock & lenient-reconcile builds done inline with TDD — proportionate to method-critical tuning the supervisor owns). 3 Codex cross-exams (inline contract honored on all 3, `--model gpt-5.5`) + 1 case-extraction subagent. Every round: build → supervisor-verify (check+suite) → run → score → diagnose → commit scorecard → USER gate. No DB, no push, no PR.

Learnings (candidates to promote):
- **A keep-only lock is the right lever for a value the model KEEPS well but ADDS badly.** Tasting (KEEP 3/3, ADD 3/25) → locking ADD took it 0.214→1.000 at a 3-TP cost. Diagnose the keep-vs-add split BEFORE reaching for a prompt fix; prompts didn't move it, the lock did instantly.
- **Look at real disagreement cases before "loosening to pass."** The case review converted an abstract "the key may be too tight" into a grounded "the key is right, the model over-tags" — which flipped the honest path from threshold-tuning (gaming) to a method-ceiling decision. The single most decision-shaping artifact of the session.
- **Lenient recovery > strict-reject for an LLM bookkeeping contract it can't reliably satisfy.** The partition-reject cost ~12% of lessons their tags and the prompt could not fix it; recovering the model's intent (8→4 fails) flipped a gate. But verify the recovery is uniform — Codex caught DROP-wins leaking for the non-anchor overlap edge case.
- **Re-run the preflight when the cached prefix changes shape, not just size.** The 2-field tool halved the prefix toward the cache floor; measuring the old shape would have masked a possible no-cache cost blow-up. The empirical `cache_read>0` probe is the authority, not the (cloak-inflated) count.

Next: **USER design decision** (settle "what is the ingredient tag for" → specifics-keep-only OR specifics-advisory). Then implement the chosen lever, re-run round 4, re-score. Proxy is still up. The cooking_skills side is essentially done; only main_ingredients specifics remain.

### Session 11 — 2026-06-24 — P2′ BUILD: P2′.5 held-out sampler shipped → P2′ BUILD COMPLETE

Major events:
- Oriented (git/design/impl/status consistent; the kickoff "RIGHT NOW" line was stale at P2′.1 — trusted git+status: P2′.1–P2′.4 done, next = P2′.5; `npm run check` clean). Read design §3·PIVOT D-P2 + the impl P2′.5/.6/.7 tasks + re-confirmed the sampler anchors (line drift: the CLI hard-fail is ~L1452-1463, not L1432-1443). User confirmed orientation → "go ahead."
- **Pre-dispatch grounding (supervisor):** read `buildC02Sample`/`runC02`/`parseArgs`/the CLI hard-fail + verified the committed artifacts — found the **69-key manifest is hand-annotated** (`postSampleRemovals`/`coverageNote`/`size:69`) so a naive `runC02` regen would clobber it (→ surgical-edit + STOP-guard brief), and the **sample jsonl is gitignored while the manifest is the committed ID record** (→ "commit the disjoint ID manifest" = commit the held-out manifest). **Settled the one genuine design call** (supervisor-level, within locked scope): relaxed/held-out mode = a representative stratified draw (skip hard-case oversampling + set-cover), faithful to D-P2's anti-overfit-canary intent.
- **✅ P2′.5 (held-out sampler)** — executor `f4075b1` (TDD): `excludeIds` + `coverageMode` on `buildC02Sample`; relaxed = representative stratified draw over the eligible pool; `runC02` writes held-out filenames (never the 69-key); `--relaxed-coverage`/`--exclude-key`/`--size` CLI + `loadExcludeKey`; `SAMPLER_VERSION`→b1-v2; surgical 69-key manifest coverage refresh (selection-stability verified first, annotations preserved); committed the 25-id held-out manifest. **Supervisor-verified independently** (re-ran check + the 77-test sampler file; regenerated BOTH samples in a scratch dir — held-out byte-identical determinism, 69-key coverage matches a real cleaned-floor replay, 25 ∩ 69 = ∅; read the full code diff confirming the strict path is untouched + the hard-fail gating).
- **Codex (gpt-5.5, agent `a99cf7dd…`, inline) → 2 findings, both real, both FIXED** (`ca981b7`): operator-misuse footguns around the LOCKED 69-key artifacts — `--relaxed-coverage` didn't require `--exclude-key` (canary-contamination risk), and `--exclude-key`/`--size` in strict mode could clobber the locked 69-key artifacts on a "forgot --relaxed-coverage" typo. Fix = `parseArgs` biconditional guard (validation-only, throws before any write) + 3 negative tests. Rebutted the BLOCKER severity (committed output was already correct + verified-disjoint; these are robustness guards for the P2′.7 re-run), accepted the cheap fixes. Suite 1774; check clean; regenerating the held-out is zero-churn.
- **Checkpointed** the status Current State header + carry-forwards (P2′.5 carry-forward CLOSED) + this log; **P2′ BUILD is now COMPLETE** (P2′.6/.7 are user-gated runs/labeling, not build).

Dispatch pattern: 1 fresh-context executor (P2′.5, Agent/general-purpose, inherit-Opus) → supervisor-verify (independent scratch-dir regeneration was the strongest check — caught nothing wrong but proved determinism + the cleaned-floor coverage refresh) → Codex cross-exam (inline, `--model gpt-5.5`) → supervisor-implemented the small guard fix-up directly (TDD, RED→GREEN; proportionate to a 2-finding validation-only fix). No DB, no push, no PR.

Learnings (candidates to promote):
- **Independent regeneration > reading the diff** for a deterministic-artifact task: re-running the sampler in a scratch dir proved both the held-out determinism AND that the surgical 69-key coverage refresh was a real cleaned-floor recompute (not a hand-edit guess) — a stronger guarantee than inspecting the manifest diff.
- **Codex's "BLOCKER" was again the right fix at the wrong severity** (3rd time this initiative): the committed artifact was correct; the findings were future-operator footguns. Write the rebuttal, accept the cheap guard, reject the urgency (`feedback_bot_review_investigation`).
- **Guard locked-artifact-adjacent CLI flags as a biconditional.** When held-out semantics span several flags (exclude + relaxed + distinct filenames), making them travel together in `parseArgs` (validation before any write) is the cheap way to stop a typo from clobbering a locked artifact — cheaper + safer than relying on the operator to remember the full flag set.

Next: **P2′.6 — re-pilot on the 69 (Opus 4.8), USER-GATED** (proxy runbook in the Current State header). Then P2′.7 fresh-25 + the P2′ PR.

### Session 10 — 2026-06-24 — P2′ BUILD: P2′.1 + P2′.2 + P2′.3 shipped (executor→verify→Codex each)

Major events:
- Oriented (git/design/impl/status consistent; `npm run check` clean; suite 1631→ baseline). User confirmed + asked to **lean hard on Codex cross-checks throughout P2′**. Ran the per-task loop = **executor → supervisor-verify → Codex (gpt-5.5, inline) → triage/fix-up → checkpoint**.
- **✅ P2′.1 (one canonical floor)** — executor `b9f6a8d`: new `c02-floor.ts` `applyC02Floor()` (executes the inert `drops`, folds, drops junk, removes `Herbs & Aromatics`, parent-reconciles, per-tag provenance); the 3 divergent floors (`predictMembership`/`floorAnchor`/baseline) now DELEGATE (old `foldField`/`dedupe`/`foldAndKeepCanonical` deleted = real import-identity). Codex (`abf7eb036`) FAIL→4 findings: **#2 fixed** (`462987c`) — provenance was order-dependent (alias-fold vs exact-canonical depended on input order); now exact-canonical wins deterministically. **#1 (normalize R7/R8/R9 still on LLM output) deferred to P2′.3 by design**; #4 (stale manifest coverage) deferred to P2′.5; #3 folded into #2.
- **✅ P2′.2 (anchored verify-and-diff)** — executor `5910473`: corpus reader retains both fields + freshness preflight; dedicated 2-field KEEP/DROP/ADD tool (name reused `submit_tags`); `c02-anchor.ts` provenance anchor + D-P9 effective-input hash; verify-and-diff prompt; `computeBodyHash` rewired at all 3 sites. Codex (`acc01f1b2`) confirmed the high-risk checks (3 hash sites identical, tool↔Zod parity, D-P9 independence, transitional coherence) + 3 findings all fixed (`2fd1c33`): #1 preflight now requires both keys per row (partial-stale catch), #2 prompt "tasting-only" contradiction, #3 prompt aligned to the floored anchor.
- **Checkpointed at a clean 2-task boundary** (suite 1703, check clean) before the heavy, linchpin P2′.3 — supervisor-verify is load-bearing and P2′.3 is where the method comes alive + the Codex #1 bypass must close.
- **✅ P2′.3 (reconcile.ts + run-record finalC02 contract + LIVE wiring)** — executor `34e185c` (12 files, TDD throughout): new `reconcile.ts` (subtractive floor∪LLM-keep/drop/add, partition-enforced, parent-conflict-resolved); `runMainPass` swapped to the 2-field anchored call (`loadC02SystemPrompt` + `buildC02SubmitTagsTool` + anchored body) with a new pure `processC02Decision` flow (decision-schema → reconcile → `finalC02` canonical); run-record carries NEW `llmDecisions` + `finalC02` (rawInput preserved); fallback mirrors via `processResult`; scorer reads `finalC02`; **Codex #1 CLOSED** via `normalizeRecordInput({skipC02:true})` + guard tests. Supervisor-verified (re-ran check + 1731 suite, read reconcile.ts/.test.ts + the full run-retag wiring diff + the normalize bypass + the scorer/diff/apply changes). **Codex (gpt-5.5, agent `a485c820…`, inline) 🟢** on reconcile correctness, fallback parity, D-P9 hashing, skipC02 — and raised 2 findings (1 🔴-downgraded-🟡, 1 🟡), both **FIXED** by a supervisor-dispatched fix-up `8d523a3`: (1) failed C02 records RE-RUN on `--resume` (`requireFinalC02` identity flag; `--repair` safely stale-skips C02, resume is the recovery path); (2) `selectComparedLessons` **fails closed** on C02 anchored records so diff/apply can't silently emit empty C02 (the proper `finalC02` threading is P3.1/P3.2). Suite 1738; check clean. Triage discipline: rebutted Codex's 🔴 framing (a failed record scoring empty is the intended harsh signal; no good-record corruption) before accepting the cheap defensive fixes.

- **✅ P2′.4 (per-value gates + baseline recompute)** — executor `7a5845a`: `evaluateGate3` AUGMENTED with a support-guarded per-specific floor (≥0.60 @ truth/pred ≥3; below-support FPs still feed the pool) + named sentinels (Tasting/Kitchen-safety ≥0.70 from cooking_skills, Sweeteners reuses Gate④ ≥0.80, each gated @ pred≥3) → Gate③ passes iff pooled-OK ∧ no per-specific violation ∧ sentinelsPassed (null-precision still fails closed). D-P7 needed no code (P2′.1 already routed the baseline through the cleaned floor) — proof tests added; finalC02 consumed end-to-end (gate-level conflict fixture). Suite 1752. Supervisor-verified (re-ran check+suite, read the full `evaluateGate3`/`evaluateSentinel` diff + confirmed the pooled-accumulation-before-guard order + the test names cover both sides of the support guard). **Codex (gpt-5.5, agent `ab36d5eb…`, inline) verdict SOUND/no-blockers**, 5/6 🟢; the 1 🟡 (cooking-sentinel sub-3-prediction gap) is a documented NON-blocking P2′.6-tuning carry-forward (the 69/25 keys are safe; full run gated by the prevalence review) — rebutted + captured, not coded.

Dispatch pattern: 4 fresh-context executors total across Session 10 (P2′.1–P2′.4, Agent/general-purpose, inherit-Opus) + 1 supervisor-dispatched fix-up executor (P2′.3 Codex), each supervisor-verified in the main loop (re-ran check/suite, inspected `git show`, read the actual artifacts + delegation call-sites + test names, not just the executor prose) + a per-task Codex cross-exam (inline contract honored on all 4, `--model gpt-5.5`). Fix-ups small + TDD (RED before GREEN). No DB, no push, no PR. Checkpointed the status header + committed it after each verified task.

Learnings (candidates to promote):
- **Per-task Codex cross-check (not just per-PR) earned its keep twice.** #2 (order-dependent provenance) and #1/#2 on P2′.2 (preflight gap + a locked-criterion prompt contradiction) were real and would have shipped into the pilot otherwise. Cheap insurance on method-critical harness code (`feedback_codex_over_crossexamine`).
- **Triage discipline mattered:** of 7 Codex findings, 3 were genuinely deferred-by-design or out-of-scope (rebut, don't reflex-fix) and 4 were real. Writing the rebuttal for each kept scope tight (`feedback_bot_review_investigation`).
- **A unifying refactor (P2′.1) creates subtle order-dependence bugs** the import-identity tests (equal value-projection) don't catch — provenance is a per-tag annotation, so dedup order leaks into it. Test the annotation, not just the values.
- **Codex's 🔴 framing is sometimes the right *fix* with the wrong *severity*.** On P2′.3, Codex called the resume/repair gap a blocker "before P2′.6"; the rebuttal (a failed record scoring empty is the intended harsh signal; passed⟺finalC02, so no good-record corruption) downgraded it to a cheap defensive fix-up, not a pilot blocker. On P2′.4, Codex's own verdict was "no blocker." Write the rebuttal before sizing the fix — accept the cheap guard, reject the urgency (`feedback_bot_review_investigation`).
- **"Fail closed" beats a code-comment marker for a deferred-but-dangerous seam.** P2′.3 deferred the diff/apply `finalC02` read to P3 (per the plan), but the interim state silently emitted empty C02 / an all-emptying apply draft. A throw at the shared chokepoint (`selectComparedLessons`) converts a latent footgun into a loud, P3-actionable error — the marker alone wasn't enough (Codex caught this).

Next: **P2′.5 — held-out sampler** (`excludeIds` + `--exclude-key` + relaxed-coverage mode, disjoint ~25 over the remaining ~695, commit the ID manifest before labeling). Then USER-GATED **P2′.6 re-pilot on the 69** (proxy/Opus-4.8 per the runbook; score the 4 gates; stop for the user at the gate result — and present the D-P1 keep-only-lock recommendation only if the catch-alls still over-predict) and **P2′.7 fresh-25** (freeze prompt+thresholds, independent label, final greenlight). P2′ remains scripts-only (its own PR when the build completes through P2′.5).

### Session 9 — 2026-06-23/24 — METHOD-PIVOT DESIGN: re-locked + GATE-1B-validated (NO code)

Major events:
- Oriented (git/design/impl/status consistent; `npm run check` clean; both pilot scorecards + filter-usefulness read). User confirmed orientation → ran the design pivot via `superpowers:brainstorming`.
- **Grounding workflow** (`wd2lywf4v`, 5 read-only subsystems in parallel): floor/normalize · apply blast-radius · prompt path · gate plumbing · **run-data forensics**. Confirmed: R7/R8 fire **0×** (measured); apply writes **ALL ~14 fields** (would stomp the completed rebuild); existing tags are **already on disk** (cheap to thread). **Forensics counterfactuals** (the decisive evidence, replicated in Python over the failed run vs the 69-key): raw-LLM P66.5/R89.7 · pure-floor P80/R79 · LLM∩floor P91/R72 · floor∪LLM **P63 (worse than raw)**; 84% of FPs are pure-LLM adds, 67% of misses were in existing tags, 27%/17% of gold is body-derived-new, over-tags are body-plausible → **both halves required**.
- **2 user-verdicts** (AskUserQuestion; plain-language re-ask on request): D-P1 add-leash = gentle-fixes-then-measure (no add-lock in pilot-1); D-P2 held-out = fresh ~25.
- **Codex method cross-exam** (agent `ab877ed343f682131`, gpt-5.5, inline) — validated the direction + caught ~8 folded into the design: recompute the baseline after floor hygiene; the floor is two divergent impls + `drops` is inert (unify); confidence-typed anchors (78–97% by provenance); apply concurrency guard + targeted rollback; bodyHash excludes anchors; per-label ADD criteria; parent/child conflict.
- **Re-locked** design §3·PIVOT (D-P1..D-P10; banner flipped; §3 / §4-Q3/Q5/Q6 marked SUPERSEDED). **Re-authored** impl P2′ (P2′.1–P2′.7) + P3.2 (D-P10 apply safety) + P3.1 (corpus prevalence review). **GATE 1B** (agent `a843c00e40545408c`, gpt-5.5, inline) on the re-authored tasks → folded ~8 authoring gaps (finalC02 consumed by diff/apply [D-P6], run-record schema contract, reconcile validation flow vs the all-field result schema, bodyHash 3-path update, D-P10b capture/report mechanism, single-floor import-identity test, user-stop-before-lock, fresh-25 acceptance contract) + anchor NIT fixes.

Dispatch pattern: design session in the main loop (orientation + user checkpoints + all four-doc authoring supervisor-owned); one grounding workflow (5 parallel read agents, structured returns, context-light); two Codex cross-exams (method + GATE 1B), both inline-contract honored. **No build executors — zero code this session** (design-only, per the kickoff "no code before re-lock").

Learnings (candidates to promote):
- **Run the counterfactuals on the failed output before redesigning.** Replicating pure-floor / intersection / union policies in Python over the failed run turned "anchoring should help" into a quantified "both halves required; additive is *worse* than raw" — the most decision-shaping artifact of the session.
- **GATE 1B on a re-authored plan catches authoring gaps the method pass cannot** (it reviews a plan that didn't exist during the method pass) — ~8 real gaps (finalC02 never threaded to apply, run-record contract undefined). The two Codex passes are genuinely different lenses (`feedback_codex_over_crossexamine`).
- **A pivot re-lock = banner + one authoritative new section + superseded-markers**, not a section rewrite — preserves history and keeps a single source of truth.

Next: **P2′ BUILD** — orient, confirm, dispatch the first executor at **P2′.1 (unify the canonical floor)**, then P2′.2/.3 (anchored 2-field tool + `reconcile.ts`), P2′.4 (per-value gates + baseline recompute), P2′.5 (held-out sampler) → P2′.6 re-pilot (USER-GATED).

### Session 8 — 2026-06-23 — P2.3/P2.4 RAN → pilot FAILED → method pivot (Codex-confirmed)

Major events:
- Oriented (git/design/impl/status consistent; `npm run check` clean). User chose to **refresh the proxy first**: pulled **CLIProxyAPI v7.2.33** (was v7.1.68, backed up `.v7.1.68.bak`), verified checksum, installed, started, OAuth auto-refreshed, smoke-tested. **Found the endpoint moved** in v7.2.33 → base URL is now `http://127.0.0.1:8317` (old `…/api/provider/anthropic` 404s).
- **Preflight via proxy measured 13.7K (> 12K budget)** → diagnosed as **proxy-cloak inflation** (v7.2.33's cloak ~6.7K vs v7.1.68's ~1.4K); real prompt unchanged ~6.9K. 3-lesson probe confirmed: ~$0.055/lesson, cache working, detection cleared.
- **Caught + fixed a gold-key data bug:** the P2.1c sampler had drawn a deletion-slated stub (`1jfFP2nKtAti3…`, "Equivalent Ratios", on `corpus-exclusions`) into the 70-key → unrunnable, would score as a phantom all-miss. Dropped it → **clean 69-lesson key**, committed `1a434c6` (verified 0 support-floor damage). Root cause: `export-corpus` exports all live rows but the 12-id deletion slate is a `run-retag` *runtime* filter not applied at sampling.
- **P2.3 ran both models** (69 lessons each, concurrency 3, via proxy, 0 errored): `claude-opus-4-8` $3.69 (5 Zod-fail), `claude-opus-4-7` $3.87 (12 Zod-fail).
- **P2.4 scored both alone on the 4 gates (corrected the runbook — `--c02` flag is REQUIRED).** BOTH **FAIL ALL 4 GATES**. 4.8 > 4.7 (Gate② ingredients +0.098 vs +0.005; higher recall; fewer Zod-fails). Scorecards preserved as `c02-pilot-scorecard.opus-4-{8,7}.md`.
- **Root-cause diagnosis (supervisor):** over-tagging (high recall, low precision) driven by a **blind full-LLM re-read** — LLM gets body only, floor R7/R8 fire 0× on canonical output (only R9 fires, adds tags), apply stores raw rawInput. Built `c02-filter-usefulness.md` quantifying the over-tag (`Tasting` filter would match 78% of lessons).
- **User chose: run 4.7 fallback first** (done, worse), then **Pause & discuss** the methodology, then asked to **cross-examine with Codex**.
- **Codex (gpt-5.5, read-only, inline) confirmed the diagnosis** and added 5 findings (apply blast-radius rewrites ALL fields; fields are review-metadata/FTS-ranking not facets; allow evidence-backed drops; held-out slice needed; per-value gates). Recommended **B (floor-first reconciliation in code) > A (anchored prompt + negative few-shots) > C (gates last)**.
- **User chose: Checkpoint + fresh session.** Captured the pivot in design banner + kickoff RIGHT-NOW + this status; committed pilot artifacts as provenance.

Dispatch pattern: operational session (proxy ops + harness runs + deterministic scoring/diagnostics in the main loop), one Codex cross-exam subagent (inline contract honored). Supervisor re-verified every artifact (committed-object row counts, FP/FN recompute reproducing the scorecard's 93-specific-FP figure, floor-firing grep on the run JSONL).

Learnings (candidates to promote):
- **A pilot's job is to expose method gaps before the expensive run — this one did.** The "hybrid-floor" anchoring (alias-overwrite R-rules) was built correctly but is *inert on LLM output* (the LLM emits canonical, not aliases), so the method silently degraded to the blind re-read the design rejected. Verify a guardrail actually *fires* on the real input, not just that it exists.
- **Run-then-score caught a runbook gap twice:** `--c02` is required for the C02 gates (omitting it silently runs the old 57-lesson mode), and the scorer reads `rawInput` (raw model output) so Zod-failures and off-vocab count as real FPs/misses — score the harshest honest signal first.
- **Cross-examine pivotal verdicts.** Codex independently confirmed the diagnosis AND caught the apply-blast-radius safety issue (apply rewrites ALL fields) the supervisor missed — cheap insurance on a method-level decision (`feedback_codex_over_crossexamine`).

Next: **method-pivot DESIGN session** — re-lock floor-first reconciliation + anchored prompt + C02-only apply scope + per-value gates + held-out slice (§3/§4-Q3/Q5/Q6 + apply scope), THEN build. The 69-key + harness + vocab + alias floor + both run outputs all carry forward.

### Session 7 — 2026-06-23 — P2.2 gold-key build + two-lens AI draft → paused at user-adjudication gate

Major events:
- Oriented (git/design/impl/status all consistent; `npm run check` clean). User answered the two P2.2 forks via AskUserQuestion: **adjudicate ALL 70 rows** + **Codex/GPT-5.5** as the independent lens for judgment rows.
- **Design subtlety caught + honored:** drafting the gold key with Opus-4.8 (the P2.3 contestant) would make Gate ② near-circular. Plan keeps the gold key's judgment-row answers independent of Opus: Sonnet drafts (different family, session billing), Codex independently re-derives, user is final authority. No Console credits spent (reserved for the P2.3 Opus run).
- **Built the gold-key tooling** (executor `f1d0f78`, TDD): `build-c02-answer-key.ts` `scaffold`/`worksheet`/`assemble`. Executor caught a real self-inconsistency — a fold-only floor anchor emits orphan specifics that `assemble`'s own parent invariant rejects; fixed by applying R9 parent-reconcile to the anchor (matches the actual harness floor R7/R8+R9). Supervisor re-verified: 70 scaffold rows, layers 28/22/20, 30 judgment rows.
- **Draft fan-out:** prep a full-body 70-lesson pilot corpus (median body 3910 chars → the 2000-char scaffold excerpt would truncate ingredient lists, so full bodies used) → 7 Sonnet agents drafted 10 each → **70 rows, 0 vocab/parent/dup errors** (supervisor-revalidated against `c02-vocab.json`).
- **Codex independent pass:** computed the 51-row judgment set (hardCaseJudgment ∪ draft-changed-vs-floor ∪ added-specific) → 3 `codex:codex-rescue` agents (gpt-5.5, file-first + inline contract) re-derived → **51 rows, 0 errors, full coverage**; all returned inline (no backgrounding). Sonnet draft withheld from Codex for genuine independence.
- Draft-vs-Codex exact-set agreement is low (3/51 both-fields) — expected for ~10-tag arrays across two families — so the worksheet shows **per-tag** provenance instead. **Worksheet enhancement** (executor `33ffbdb`, TDD, 30 tests): AGREED = floor ∪ (draft∩codex) pre-filled into FINAL (parent-reconciled, assemble-valid); CONTESTED tags listed `[sonnet]`/`[codex]`. Fixed the original codex-wholesale pre-fill that dropped correct Sonnet additions (e.g. `Boiling & simmering` on the rice lesson).
- Emitted the worksheet; user chose an **interactive walk-through** over editing the file. Supervisor recomputed the agreed/contested split with a better model (AGREED = sonnet∩codex, so floor tags BOTH lenses dropped surface as "keep?" removal decisions — caught the Health-Discussion incidental-fruit case the first split hid). Walked all 70 lesson-by-lesson (~16 AskUserQuestion rounds: blurb + locked tags + a ≤4-option pick-list of the uncertain tags + my read on each), recording to gitignored `c02-adjudication.jsonl` via a `_record.py` helper that parent-reconciles. **Gold key force-committed `53aec72`** (70 rows, 0 errors). User ran a deliberately tight/precise key (groups in, specifics only when central, B-lite pantry).

Dispatch pattern: each code task = one fresh-context executor → supervisor re-verified the artifact (commit stat, test run, regen + assemble exit, the rendered rice block). Draft/Codex = parallel Agent fan-outs writing gitignored part-files (context-light); supervisor re-validated every part against the vocab+parent invariant, not just the agents' self-reports. Adjudication itself ran in the main loop (supervisor read each lesson's extract + the two-lens proposals to frame each question).

Learnings (candidates to promote):
- **Don't let the contestant model draft its own grading key.** For an LLM-judged pilot, the gold key's judgment-row answers must come from a *different* lens than the model under test, else the "beats baseline" gate is circular. Withholding the first lens's draft from the second keeps the two-lens agreement signal honest.
- **Exact-set agreement is the wrong metric for multi-tag arrays** — two good independent lenses rarely match exactly; per-TAG provenance (agreed / single-lens-contested) is what makes human adjudication fast and the disagreement signal usable.
- **Part-files on disk > structured returns** for fan-outs that produce bulk data — agents write gitignored part-files + return a 1-line confirmation, keeping supervisor context light while the supervisor re-validates the files directly.

Next: **user adjudicates the worksheet** → supervisor runs `assemble` → force-commits `c02-answer-key.final.jsonl` → P2.3 (single Opus-4.8 run, needs Console credits) → P2.4 (4-gate greenlight, USER-GATED).

### Session 6 — 2026-06-23 — P1 merge reconciled + Opus-only supersede + census forensics + P2.1 COMPLETE

Major events:
- Oriented: git showed **P1 PR #542 already MERGED** (`a5ff8a9`) while the status header still read "P1 PR being opened" → trusted git, reconciled the header to P2 start.
- User flagged the **Opus-only decision** (`project_c02_p2_opus_only`, skip the Q11 bake-off). **Formalized the supersede** across all four scaffold docs (design §4 Q11 banner + §6 + §8 table; impl Tech-Stack + P2 table + P2 header + P2.3/P2.4; kickoff phased-delivery + LOCKED + RIGHT-NOW; status carry-forwards (c)/(d) + locks line + header).
- **PR-cycle archival:** moved the Session 5 (P1 PR open→merge) entry to the archive; refreshed the Current State header to P2.
- **Deep census forensics** (user-requested): the recorded "121/202 live PROD census" was a **TEST-DB query mislabeled as PROD** (2 read-only agents + live PROD/TEST queries: `121`=TEST cs distinct, the q1 table's named-appearance sum `1,709`=TEST exactly; table self-sums 123/212≠its 121/202 headline). Truth = 122/230. Corrected design §1 / status / impl / `q1-vocab-census.md` (banner). Floor re-measured on live PROD = 92.4% cs / 94.3% mi coverage → TEST-sourcing didn't damage the alias map; ~14 clean folds it missed queued for P2.1b. Memory: `feedback_census_provenance_committed`. (Doc commit `9939289`, impl-plan P2.1 expand `59736f7`.)
- **P2.1a done + verified** (`eb0c304`): export-corpus guard 767→764 + empty ghost set; corpus regenerated = 764 rows, both fields, 122/230.
- **P2.1b done + verified** (`16880b5`): floor top-up — 14 clean folds (Beans/Peas→Beans & legumes, Squash→Squash cucumbers & melons, Parmesan/Mozzarella cheese→Cheese, Sour Cream/Buttermilk/Condensed milk→Dairy, Various seeds→Nuts & seeds, Beyond Sausage (pea protein)→Tofu & plant proteins, Lettuce→Leafy greens, Cereal-grains/Whole wheat wraps→Grains & starches, Beet juice→Beets) → 201 alias keys; all 14 now floor-covered; invariants green; harness 649/649.
- **P2.1c done + verified** (`30f0a0b`): ran `--c02` sampler → 70-lesson key (20 hard-case/28 coverage/22 clean-core; ALL 93 canonical values ≥2×, 0 under; deterministic seed 20260612; manifest force-committed = 70 ids + coverage, no bodies; bulk sample gitignored). **P2.1 COMPLETE.**

Dispatch pattern this session: each P2.1 sub-task = one fresh-context executor (Agent) → supervisor re-verified independently (git stat + the actual artifact: corpus line/field/distinct counts, alias-map keys+coverage recompute, manifest 70-count + ≥2× over c02-vocab.json). All three passed clean on first dispatch.

Learnings (candidates to promote):
- **The wrong-DB census was caught only by re-deriving it** — a prose "PROD census" that nobody reproduced silently overwrote the correct figure in a locked doc (→ `feedback_census_provenance_committed`). Re-derive any census that overturns a prior number before trusting it.
- **Stale hardcoded census guards are a landmine after data cleanups** — `export-corpus.ts`'s `EXPECTED_LIVE_ROWS=767` would have hard-failed the regen because Wave-4 deleted 3 rows; always re-census PROD before a guarded export and treat the guard as a maintenance knob, not a constant.
- Supervisor-verifying executor results against the ACTUAL artifact (not just the executor's prose) stayed cheap and load-bearing — the corpus distinct-count check doubled as confirmation of the census correction.

Next: **P2.3 — single Opus-4.8 run over the 70 (NEEDS Console credits).** `preflight-token-mass` first → `--model claude-opus-4-8` (first 3 `--concurrency 1` to confirm cache_read>0) → P2.4 score Opus alone vs the gold key on the 4 gates (USER-GATED greenlight). Gold key is committed (`53aec72`), so P2.3/P2.4 just need credits + the run.
