# C02 — Cooking Skills & Main Ingredients Re-tag — Execution Status

**Last updated:** 2026-06-26 by Session 23 — **🎉 C02 COMPLETE — P4b SHIPPED TO PROD + MCP-VERIFIED. PR #549 MERGED (`5d44bbe`); `migrate-production.yml` user-approved → migration applied to PROD → PROD MCP verify ALL GREEN:** both `valid_cooking_skills`/`valid_main_ingredients` present + `convalidated=false` (NOT VALID) + `contype='c'`; live def byte-identical to canonical (cooking 23 / ingredients 70, both `=true`); **18 cooking + 11 ingredient grandfathered rows tolerated** (= the census → NOT VALID confirmed; a plain CHECK would've failed the apply); behavior probe (zero-commit) off-vocab-cooking + off-vocab-ingredient **REJECTED**, canonical **PASS**. **The two C02 fields are now closed end-to-end on PROD: app-layer (P4a) + DB-layer (P4b).** Remaining = pure follow-ups (out-of-scope list): future `VALIDATE CONSTRAINT` after the corpus-exclusion deletion track clears the 18 grandfathered rows; drop `c02_retag_*` snapshot tables; optional live-DB enums↔CHECK sync test; collapse dup `setsEqual`/`mulberry32`. <!--PRIOR Session 23 pre-PROD: P4b built + TEST-verified; PR #549 open, awaiting PROD approval.--> First merged the Session 22 docs PR #548 (`433ad43`) so `main` is authoritative. P4b = the DB-layer enforcement: **enums.json registration** (10→12 keys; cooking_skills 23 + main_ingredients 70 wired into `generate-enums-json.ts`, freshness test bumped; `b8f8090`) + **`20260626000000_c02_enforce_check.sql`** (+ `.sql.rollback`; `d297f64`) — two table-wide CHECKs `valid_cooking_skills` / `valid_main_ingredients`, **NOT VALID**. **Why NOT VALID (USER VERDICT 2026-06-26 — §4 Q9 re-opened on new census evidence):** a read-only PROD drift census found **18 pre-existing rows P3 never touched** (11 live corpus-exclusions slated for deletion + 7 retired lessons never in P3's live-only scope) that a plain validating CHECK would reject. NOT VALID enforces on every future INSERT/UPDATE (block re-pollution) while grandfathering those 18; a future `VALIDATE CONSTRAINT` promotes to fully VALID after the deletion cleanup track. Pure additive DDL, **zero data mutation**; arrays byte-identical to enums.json / the frozen manifest (23/70). Local: `db reset` clean, off-vocab rejected / canonical+empty+null pass, `test:rls` baseline. **GATE 2 Codex** (safe, no blocker, 1 LOW operational note folded into the header) + **GATE 3** (Claude reviewer + Codex, both PASS; converged LOW = no static enums→SQL sync test → **default-rejected** as a pre-existing repo-wide pattern + fragile if built; closed empirically via the live constraint-def verify). **MANDATORY TEST-DB MCP verify ALL GREEN:** both constraints `NOT VALID`; live def byte-identical to canonical (23/70 `=true`); 18 cooking + 11 ingredient grandfathered rows tolerated on TEST (= a plain CHECK would've failed → NOT VALID confirmed working); behavior off-vocab-reject / canonical+empty+null-pass; pure DDL. `npm run check` clean; `npm run test:run` **1964**. 3/4 review bots clean (no posts); E2E + all CI green; **`claude-review` still pending** (the slow ~13min bot). **NEXT: claude-review triage → PROD approval (USER-GATED) → PROD MCP verify → merge → initiative-close retrospective.** <!--PRIOR Session 22: P4a SHIPPED TO PROD (#547 `8513b18`) — frontend + 11 edge functions deployed, enforcement LIVE; manifest FROZEN (`4165ebc`). Full P4a detail → archive at next PR-cycle boundary.-->

## Current State

> ✅ **P3 SHIPPED (Sessions 18–21, #544); P4a SHIPPED (Session 22, #547); P4b BUILT + TEST-VERIFIED (Session 23, PR #549 OPEN, awaiting `claude-review` triage + PROD approval).** The C02 data is canonical on PROD; the app-layer write surface is closed + deployed (P4a); the DB-layer CHECK is built + TEST-verified, pending merge + PROD approval (P4b). **Authoritative spec = design §7 (enforcement) + §4 Q9 (expand/contract — the NOT VALID variant LOCKED Session 23 on new census evidence). Single source of truth for every literal = `scripts/stage2-retag/data/c02-vocab.json` (FROZEN; 23 cooking + 70 ingredient = 24 groups + 46 specifics) = enums.json keys `cooking_skills`/`main_ingredients`.**

**Phase: 🎉 C02 COMPLETE — P4b SHIPPED TO PROD + MCP-VERIFIED (Session 23, PR #549 `5d44bbe`).** All four phases shipped: P1 harness (#542) → P2′ pivot/pilot (#543) → P3 data apply (#544) → P4a app-layer enforcement (#547) → P4b DB CHECK (#549). The two C02 fields are closed end-to-end on PROD. **Do NOT re-run the LLM re-tag, re-apply any migration, re-open the frozen `c02-vocab.json`/enums.json keys, or edit the applied `20260625000000_c02_retag_apply.sql` / `20260626000000_c02_enforce_check.sql`.** Remaining items are pure follow-ups (out-of-scope list below) — none blocking.

**Built so far on `feat/c02-check` (each supervisor-verified):**
- **enums.json registration (`b8f8090`, TDD red→green)** — `cooking_skills` (23) + `main_ingredients` (70) wired into `generate-enums-json.ts` from the existing P4a Zod `*_VALUES` consts; `enums.json` 10→12 keys; freshness test +2 field assertions + key-count guard 10→12. Byte-identical to the frozen manifest (supervisor-verified `12 23 70` + both arrays `=true`). Full suite 1964.
- **CHECK migration (`d297f64`, supervisor-direct — data-safety-critical)** — `20260626000000_c02_enforce_check.sql` + `.sql.rollback`: `valid_cooking_skills` (23) + `valid_main_ingredients` (70), `pg_constraint`-guarded, `col IS NULL OR col <@ ARRAY[...]::text[]` **NOT VALID**, `-- SOURCE: enums.json[...]` markers. Rollback = DROP both (zero data to restore). Header documents the 18 grandfathered rows (11 live corpus-exclusions + 7 retired) + the future `VALIDATE CONSTRAINT` follow-up + the GATE-2 LOW operational note (NOT VALID still enforces on UPDATE → a partial future write to a grandfathered row must canonicalize-or-delete).
- **(local-only, NOT pushed — bundle with the claude-review triage push)** this Session-23 status-doc update + the design §4 Q9 NOT-VALID note + the kickoff RIGHT-NOW banner.

**NEXT — P4b PR cycle (PR #549 OPEN; steps 1–4 DONE):**
1. ✅ Merged the Session 22 docs PR #548 (`433ad43`) → branched `feat/c02-check` off updated `main`. Built enums registration (executor, supervisor byte-identity verify) + CHECK migration (supervisor-direct). Local `db reset` clean + constraint-behavior verified (off-vocab reject / canonical+empty+null pass); `test:rls` baseline (2 unrelated `archive_duplicate_lesson` fails).
2. ✅ **GATE 2 Codex** (migration) safe, no blocker (1 LOW folded into header). **GATE 3** (Claude reviewer + Codex, whole diff) both PASS; converged LOW (no static enums→SQL sync test) **default-rejected** w/ rationale (pre-existing repo-wide pattern; a static migration↔registry test would be fragile) → closed empirically via the live constraint-def verify; logged a robust live-DB sync test as an optional follow-up.
3. ✅ Pushed `feat/c02-check` → **PR #549**. CI applied the migration to TEST (E2E success; dry-run applied only `…c02_enforce_check.sql`, correctly skipped the `.sql.rollback`).
4. ✅ **MANDATORY TEST-DB MCP verify — ALL GREEN** (both `NOT VALID`; live def byte-identical to canonical 23/70 `=true`; 18 cooking + 11 ingredient grandfathered rows tolerated on TEST = a plain CHECK would've failed → NOT VALID confirmed working; behavior off-vocab-reject / canonical+empty+null-pass; pure DDL). Verification + gate-disposition comment posted on #549. Four-surface triage so far: 3/4 review bots clean (no posts), E2E + all CI green.
5. ⏳ **`claude-review` triage** (the slow ~13min bot — still pending at session end) → then **PROD approval (USER-GATED) → PROD MCP verify → merge.** ← stop for the user.
6. **THEN initiative-close retrospective** (final C02 session): lift out-of-scope follow-ups to project memory, promote process learnings, MEMORY.md hygiene, archive the P4 PR cycle.

**Live census:** PRE-apply baseline (Session 6) = **764 live rows**; 122 distinct cooking_skills / 230 distinct main_ingredients. **POST-apply (P3 shipped) the live in-scope corpus is canonical = 23 cooking / 70 ingredient distinct, 0 off-vocab** (the only legacy residue is the 12 corpus-exclusion non-lessons, out of scope). Vocab target (now FROZEN) = **93 canonical** (23 cooking_skills + 70 main_ingredients: 24 groups + 46 specifics; B-lite pantry). Byte-source = `scripts/stage2-retag/data/c02-vocab.json`; forensics in design §1/§5 + `q1-vocab-census.md`.

> _P2′-era working detail (the decisions/tuning-round/method narrative + Sessions 6–17 logs) is now in the **archive** (`…execution-status-archive.md`, PR cycle 2) — read on demand via `grep -n`. The locked method lives in design §3·PIVOT; the greenlight criterion in design §8._

## Done

- **P1 (Sessions 0–5) — MERGED** PR #542 → `a5ff8a9` on `main`. Scaffold + GATE 1A → §4 design lock (11 Q's) + GATE 1B → P1.1–P1.6 harness build (vocab manifest + alias-floor maps · both fields wired · R7/R8/R9 normalize rules · 3-layer set-cover sampler · 4-gate scoring + rules baseline). Full per-task detail + Sessions 0–5 → archive (PR cycle 1).
- **P2 pilot (Session 8) — RAN → FAILED** (both Opus models over-tagged via a blind re-read; all 4 gates). Triggered the method pivot (design §3·PIVOT). Artifacts retained as provenance.
- **P2′ (Sessions 6–17) — MERGED** PR #543 → `3ce37a4` on `main`. Scripts-only pivot rebuild: one canonical floor + anchored 2-field verify-and-diff tool/prompt + `reconcile.ts` + per-value gates + held-out sampler + **D-P11 per-field ship policy** (floor-only ingredients / floor-retention cooking) + **P2′.7 fresh-25 cooking canary GREENLIT** (shipped cooking P 0.819 / R 0.800 / F1 0.810). GATE 3/4 + 4 external bot rounds folded. Full per-session detail + the P2′ narrative → archive (PR cycle 2).
- **P3 scripts-side tooling (Session 18) + carry-forward hardening (Session 19)** — see the Current State "Built so far" list. No DB, not pushed.
- **P3 full run + P3.2 apply migration (Session 20)** — Opus-4.8 over all 752 in-scope live lessons (746 valid + 6 empty-anchor no-ops; ~$22.3); user-greenlit (§8② gate); migration `20260625000000_c02_retag_apply.sql` (+`.sql.rollback`) authored, GATE-2-passed (LOCK fix folded), local-validated, committed `73bfc4a`. NOT pushed.

## In flight

(**⏳ P4b — PR #549 OPEN on `feat/c02-check` (`b8f8090` enums + `d297f64` CHECK migration), TEST-verified ALL GREEN.** Awaiting (a) `claude-review` triage — the last of 4 review bots, still pending at session end (3/4 clean) — and (b) PROD approval (USER-GATED). On merge: `migrate-production.yml` → user approval → PROD apply → mandatory PROD MCP verify (constraints present + NOT VALID + live def byte-identical + behavior). A local-only session-end docs commit is pending; bundle its push with the claude-review triage outcome to avoid a wasted docs-only CI cycle. **Do NOT bundle anything else into #549** (it is the P4b CHECK PR; P4a already shipped separately per expand/contract §4 Q9).)

## Blocked

(none — the user-approval gates for the full LLM run, the pilot greenlight, and PROD migrations are EXPECTED, not blockers)

## Recent decisions worth carrying forward

- **✅ P2′.7 PILOT GREENLIT (Session 16, user "greenlight and build, only cooking + ingredients metadata, none other").** Fresh-25 cooking canary = SHIPPED cooking **P 0.819 / R 0.800 / F1 0.810** (≈ pilot 0.833; floor-retention +0.207 over floor-only). Codex cross-exam = MIXED (legitimate, calibration-not-blind). The binding precision-at-scale guard is **§8 ②** (P3.1 all-700 prevalence review + floored-row spot-check + the ~0.833 calibration tripwire).
- **D-P11 (per-field ship method, LOCKED Session 14):** `main_ingredients` = deterministic floor ONLY; `cooking_skills` = LLM + floor-retention (floor ∪ `finalC02.cooking`, KEEP/ADD allowed, never DROP a floored skill). Built as `materializeC02Ship` (`ship-policy.ts`); re-score gate reproduces 0.807/0.836/**0.872** with zero drift.
- **D-P12 (Session 15):** the fresh-25 canary is **cooking-scoped** (ingredients are floor-only deterministic → covered by the all-700 prevalence review).
- **Re-framed §8 greenlight (Sessions 13–15):** the binary "4 gates pass" is RETIRED as the ship test (gate ③ is structurally unreachable on ingredient specifics — the residual over-tags are incidental existing-tag specifics no mechanical method prunes); the 4 gates stay on as a DIAGNOSTIC. Ship = two USER-gated checkpoints on the SHIPPED per-field output (① pilot greenlight P2′.7 / ② pre-apply gate P3.1).
- **D-P10 (apply safety):** C02-only write scope (`C02_APPLY_FIELDS`) + optimistic concurrency guard + C02-targeted rollback + column↔JSONB lockstep. The current all-fields apply would STOMP the completed metadata rebuild — this is the highest-risk fix in P3.
- **Enforcement = expand/contract** (P3 data → P4a frontend deploy → P4b CHECK), mirroring `garden_skills`; never bundle P4a + P4b (§4 Q9).
- **P4b CHECK = NOT VALID, zero data mutation (USER VERDICT 2026-06-26, Session 23 — §4 Q9's "re-canonicalize drift before ADD CONSTRAINT" re-opened on new census evidence).** A read-only PROD drift census found **18 pre-existing rows P3 never re-tagged** (11 LIVE corpus-exclusion non-lessons slated for deletion + 7 RETIRED lessons never in P3's live-only scope) — so a plain *validating* `ADD CONSTRAINT` (the garden_skills pattern) would FAIL. The user chose **NOT VALID**: enforce on every future INSERT/UPDATE (block re-pollution) while grandfathering the 18; zero data mutation; a future `VALIDATE CONSTRAINT` promotes to fully VALID after the deletion cleanup track. The two alternatives presented — (A) snapshot+empty all 18 then plain CHECK, (C) exempt retired in the predicate + empty 11 live junk — were declined in favor of the zero-mutation option (data-safety first).
- **P4a closes 3 surfaces + a 4th-surface canonicalizer (Session 22):** the reviewer dropdown (filterDefinitions), the reviewer control (non-creatable Select), and the Zod enums + specific→group `superRefine` across all 4 schemas (incl. the edge mirror) — PLUS `canonicalizeReviewMetadata` extended for the two C02 fields so reopening a legacy `submission_reviews` draft (35 PROD rows w/ old kebab slugs) doesn't fail the closed schema. Filter options are now DERIVED from the enum consts (drift-impossible). The `superRefine` REJECTS an orphan specific on save (harness auto-adds; reviewer rejects — §4 Q7); the canonicalizer parent-completes legacy reopens so they pass.
- **`enums.json` registration belongs to P4b, not P4a (Session 22):** `enums.json` is the generated single-writer registry backing the SQL CHECKs (`-- SOURCE: enums.json[...]`). Its generator hard-lists 10 keys and doesn't auto-discover the new VALUES, so nothing drifts in P4a. P4b adds cooking_skills + main_ingredients to `generate-enums-json.ts`, regenerates, bumps `enums.json.test.ts` (10→12), and derives the CHECK from it.
- **Method = anchored verify-and-diff** (design §3·PIVOT): LLM sees the provenance-annotated current tags + proposes KEEP/DROP/ADD; one canonical floor + `reconcile.ts` (never append-only) merge into `finalC02`. The old "hybrid-floor full LLM re-read" was, as built, a blind re-read — DEAD.

## Decisions made during execution

**Session 1 (2026-06-23) — §4 locks.** User verdicts (AskUserQuestion, all on the recommended option): pantry **B-lite**; pilot size **70**; invariant mechanism **B** (R-rule + superRefine, no DB trigger); freeze-candidates **pre-add the 4 high-count** (Apples/Coconut/Oranges/Lime). Evidence-locked: Q2 flat string[]; Q3 normalize R-rules; Q5 gates ③④ are plumbing over existing `evalMetrics` (supervisor-verified); Q8 Title-Case value===label; Q11 → Opus-4.8-only (`project_c02_p2_opus_only`). Full rationale in design §4 + the archive (PR cycle 1, Session 1).

## Out-of-scope follow-ups captured here

(for the project memory after the initiative ships — seed list from design §10)
- Embeddings regeneration (rebuild's C2.4) — own session; relates to `project_embedding_pipeline_mismatch`.
- Filter-UI surfacing of the two ingredient tiers (group vs. specific in the public menu).
- A hard "1–3 specifics" cap (deliberately left as guidance).
- A DB trigger for the specific→group invariant (leaning app-layer only).
- Drop the retained C02 snapshot table(s) in a future cleanup once PROD-stable.
- **`VALIDATE CONSTRAINT valid_cooking_skills` + `valid_main_ingredients` (promote NOT VALID → VALID)** — run AFTER the corpus-exclusion deletion track removes the 11 live junk rows AND the 7 retired-row off-vocab values are handled (empty/null/delete). Until then both CHECKs stay NOT VALID (enforced on writes, existing 18 rows grandfathered). Cheap once the drift is gone; verify the drift count is 0 first.
- **(Optional, low-priority — GATE 3 converged LOW) A robust enums.json↔DB-CHECK sync test** that compares `pg_get_constraintdef` (the LIVE constraint) to `enums.json`, NOT the migration file (the static "parse this migration vs current enums.json" form is fragile — it couples an immutable migration to a mutable registry). No field currently has this; the `-- SOURCE:` marker + migration immutability is the existing repo convention. Defense-in-depth only.
- **(Optional, low-priority) Collapse the now-redundant case-twin alias keys** in `c02-alias-map.json` — harmless under case-insensitive matching, left in place to minimize churn.
- **(P4a GATE-3 LOW-C, DEFERRED — type hardening)** narrow `INGREDIENT_PARENT_MAP`'s type from `Record<string, string | null>` to `Partial<Record<(typeof MAIN_INGREDIENTS_VALUES)[number], string | null>>` so a future caller can't conflate `null` (parentless specific) with `undefined` (group/unknown). Deferred because it adds string-index friction at every lookup site across the byte-identical src + edge copies; the current truthy-guard is correct and test-guarded, and a doc comment now warns against `!== null`.
- **(P4a bot LOW-2, DEFERRED — reviewer UX)** the orphan-specific `superRefine` rejects on save with a generic banner + a misleading "Required." hint (the field IS filled). Options: auto-add the parent group in the `mainIngredients` `onChange` (mirrors the harness; demotes superRefine to a backstop — but that CHANGES the §4 Q7 lock, needs user sign-off), OR surface the descriptive Zod message instead of "Required." Near-zero frequency (0 in-scope orphans on PROD); by design.
- **(Minor, DEFERRED — Session 12 Codex) `preflight-token-mass` has no `--model` flag** → it always uses `DEFAULT_MODEL` (opus-4-7). Immaterial for the C02 pilot (opus-4-7/4-8 share tokenizer + cache floor); a 1-line add before any non-opus / cross-family preflight.
- **(Round-2 claude-review LOW, DEFERRED — scripts maintainability)** collapse the duplicated `setsEqual` (`prepare-apply.ts` + `generate-diff-report.ts`) and `mulberry32` (`prepare-apply.ts` + `score-answer-key.ts`) into single exports; optionally have `buildStagingRows`/`buildDiffReport` share one `selectComparedLessons` result instead of each re-scanning. Pure DRY; no correctness/safety impact (the isC02 seam is now assertion-guarded). Drop the retained C02 snapshot tables (`c02_retag_rollback`/`c02_retag_skipped`) in the same future cleanup once PROD-stable.
- **(Round-2 claude-review MED, NOT actionable — noted)** the in-migration lockstep assertion can't distinguish column=`[]` from an absent JSONB key (both coalesce to `{}`). Defense-in-depth gap only — `jsonb_set` always sets the key, so the apply can't produce that state; the migration is already applied + immutable. No follow-up needed unless a future apply changes the dual-write shape.
- **(Initiative-close retrospective) Promote two niche learnings to memory:** the `existsSync`-skip CI gotcha (a committed test reading gitignored data dumps ENOENTs in CI on first push); "label a fresh-eval gold to the SAME standard the model was tuned to, or precision is confounded." Both are preserved verbatim in the archive (PR cycle 2, Sessions 16/17).

## Pointers to durable context

- Kickoff prompt: `2026-06-22-c02-cooking-ingredients-retag-kickoff.md`
- Design doc: `2026-06-22-c02-cooking-ingredients-retag-design.md` (§3·PIVOT D-P1..D-P12 + §8 greenlight = AUTHORITATIVE)
- Implementation plan: `2026-06-22-c02-cooking-ingredients-retag-implementation.md` (P1.1–P4b.1 tasks)
- Worksheet (decided vocab): `2026-06-12-metadata-rebuild-stage1-cooking-skills-ingredients-worksheet-decided.md`
- **Provisional canonical manifest (Session 1):** `docs/plans/c02-session1-discovery/q1-vocab-census.md` (the byte-source for `c02-vocab.json`)
- Harness: `scripts/stage2-retag/`
- Archive: `2026-06-22-c02-cooking-ingredients-retag-execution-status-archive.md` — **PR cycle 1 (P1, Sessions 0–5)** + **PR cycle 2 (P2′, Sessions 6–17 + the P2′ narrative)** — read on demand via `grep -n`.

## Recent session log

> **PR cycles 1 (P1, Sessions 0–5), 2 (P2′, Sessions 6–17), and 3 (P3, Sessions 18–21)** are fully ARCHIVED (`…execution-status-archive.md`) — full per-session detail + learnings there. Only the current PR cycle (P4, Sessions 22+) stays here.

### Session 23 — 2026-06-26 — P4b built (enums.json + NOT VALID CHECK migration) + PR #549, TEST-verified

Major events:
- **Oriented + reconciled stale docs.** git HEAD `8513b18` (#547 P4a) but the status doc *on main* + the kickoff banner still said "build P4a" — STALE. Found **PR #548 OPEN** = the Session 22 status update (docs-only, all CI green, MERGEABLE). Read the Session 22 status from the branch → established the truth: P3+P4a both shipped to PROD, enforcement LIVE, manifest FROZEN, **next = P4b**. Presented orientation + 2 decisions via AskUserQuestion.
- **User verdicts:** (1) **merge #548 now** (done → `433ad43`; branched `feat/c02-check` off updated main); (2) **P4b CHECK = NOT VALID, zero data mutation** (the data-safety option; declined the snapshot+empty alternatives). Both recorded above.
- **Read-only PROD drift census (the decisive P4b finding):** a table-wide CHECK validates EVERY row, and P3 scoped to live+in-scope only → **18 pre-existing off-vocab rows** (11 live corpus-exclusions [all 12 confirmed in `corpus-exclusions.json`, 11 carry tags] + 7 retired never in P3 scope). This is why NOT VALID (not the §4-Q9 "re-canonicalize" plan, which assumed only small window drift).
- **enums.json registration (`b8f8090`, fresh-context executor, TDD red→green):** wired the existing P4a Zod `*_VALUES` consts into `generate-enums-json.ts` (10→12 keys), +2 freshness assertions + key-count guard. Supervisor-verified: `12 23 70`, both arrays `=true` byte-identical to the manifest, 3-file scope, generator change is imports+spread only. Full suite 1964.
- **CHECK migration (`d297f64`, supervisor-direct — data-safety-critical, mirrors Sessions 20–21):** invoked `database-migrations`; generated byte-identical SQL literals from enums.json; authored `20260626000000_c02_enforce_check.sql` (two `pg_constraint`-guarded `ADD CONSTRAINT … NOT VALID`, `-- SOURCE: enums.json[...]`) + `.sql.rollback` (DROP both). Local `supabase db reset` clean; both constraints present + `convalidated=false`; **behavior DO-block (self-rolling-back): off-vocab cooking+ing REJECTED, canonical+empty+null PASS**; `test:rls` baseline (2 unrelated `archive_duplicate_lesson` fails).
- **GATE 2 Codex** (gpt-5.5, migration): SAFE, no blocker; 1 LOW (NOT VALID still enforces on UPDATE → a partial future write to a grandfathered row must canonicalize-or-delete) → **folded into the header** (amended `d297f64`, comment-only).
- **GATE 3** (Claude code-reviewer + Codex, whole diff, parallel): both **PASS**; byte-identity 23/70 confirmed programmatically by both. **Converged LOW** = no static enums→SQL sync test. Rebuttal: **default-rejected** — pre-existing repo-wide pattern (no field has it) + a static migration↔registry test would be FRAGILE (immutable migration vs mutable registry) → closed the hop **empirically** via the live constraint-def verify; logged a robust live-DB sync test as optional follow-up.
- **Pushed → PR #549.** CI applied the migration to TEST (E2E success; dry-run applied only `…c02_enforce_check.sql`, correctly skipped the `.sql.rollback`). **MANDATORY TEST-DB MCP verify — ALL GREEN:** both `NOT VALID`; live def value-list extracted via regexp and asserted `=` to the canonical literals (cooking 23, ing 70); **18 cooking + 11 ingredient grandfathered rows on TEST tolerated** (a plain CHECK would've failed → NOT VALID confirmed); behavior off-vocab-reject/canonical-pass on a real TEST row (zero persisted mutation). Posted a verification + gate-disposition comment on #549.
- **Four-surface triage COMPLETE:** all 4 review bots clean. `claude-review` (round 1) posted 3 findings, **all previously identified + dispositioned, 0 actionable** (the enums→SQL sync gap = GATE-3 LOW, default-rejected + closed empirically; the NOT-VALID-traps-updates note = already in the header; the `contype='c'` idempotency-guard nit = pattern-consistent, migration already TEST-applied/immutable). Triage-response comment posted. **No fix-ups.**
- **MERGED + SHIPPED TO PROD (user "yes you can merge"):** pushed the bundled docs commit → squash-merged #549 (`5d44bbe`, branch deleted) → `migrate-production.yml` (Check Migration Changes ✓ / Dry Run ✓ — applied only `…c02_enforce_check.sql`, skipped the `.sql.rollback` / Verify Recent Backup ✓) → **user approved the `production` environment gate → Apply to Production success.**
- **PROD MCP verify (mandatory, `mcp__supabase-remote`) — ALL GREEN:** both constraints present + `convalidated=false` + `contype='c'`; live def value-lists extracted via regexp and asserted `=` to the canonical literals (cooking 23, ing 70); `total_rows=785`; **18 cooking + 11 ingredient grandfathered rows** (= the census → NOT VALID tolerated them); behavior probe (transaction-aborted, zero commit) off-vocab-reject / canonical-pass. **C02 COMPLETE.**
- **Initiative-close hygiene:** flipped `project_c02_p2_opus_only` memory to closed (+ carried-forward follow-ups), moved + compressed the MEMORY.md index line to the shipped/closed section. This close-out status finalize lands via its own small docs PR (pushing to main is user-gated; the Session-23 working detail already merged with #549).

Dispatch pattern: 1 fresh-context executor (enums registration, supervisor byte-identity verify) + 1 GATE-2 `codex:codex-rescue` (inline, gpt-5.5) + 2 parallel GATE-3 reviewers (feature-dev:code-reviewer + codex:codex-rescue inline). The migration authoring, all local + TEST MCP verification, the drift census, and the merge of #548 were supervisor-direct (data-safety criticality + user-gated).

Learnings (candidates to promote — audit at initiative close):
- **A read-only PROD census before authoring a table-wide CHECK is load-bearing.** It surfaced 18 grandfathering rows (incl. 7 RETIRED rows the locked plan never anticipated, since P3 was live-only), turning the §4-Q9 "re-canonicalize before ADD CONSTRAINT" plan into a NOT-VALID user-verdict. Reinforces: validate the *whole-table* reality, not just the in-scope corpus, before a table-wide constraint.
- **`NOT VALID` is the clean expand/contract tool when a table-wide constraint must coexist with deliberately-out-of-scope legacy rows** — enforce-on-write now, `VALIDATE` later after the cleanup track. Zero data mutation; most data-safe.
- **Default-reject a "add a sync test" finding when the obvious static form is fragile** — coupling an immutable migration file to a mutable registry breaks on the next legitimate change. The right defense-in-depth was the live-DB constraint-def verify, done empirically.

### Session 22 — 2026-06-26 — P4a built (freeze + close surfaces) + PR #547 + save-blocker fix

Major events:
- Oriented (git HEAD `d420ba2` = Session-21 checkpoint, consistent; `npm run check` clean; read design §7/§4Q7-9 + impl P4a/P4b + status). User said "proceed."
- **Freeze (`4165ebc`):** read-only PROD freeze census = **752 live non-excluded rows / 0 off-vocab** against the exact 23+70 set → flipped `c02-vocab.json` PROVISIONAL→FROZEN. Generated the canonical TS/SQL literals ONCE from the manifest (scratchpad) to feed all executors byte-identically.
- **P4a build — 3 sequential fresh-context executors, each supervisor-verified in the main loop** (re-ran `check`+`test:run`, runtime byte-identity, diff inspection): 4a.1 filterDefinitions (`6f69375`; confirmed the dead `category` field has 0 consumers before dropping), 4a.2 ReviewDetail CreatableSelect→Select (`e3c43a5`; kept culturalHeritage's), 4a.3 Zod enums + `superRefine` across all 4 schemas incl. edge mirror (`02ba33c`, TDD, +65 tests). **Before 4a.3 ran an orphan census on PROD: 0 in-scope orphans** (all 7 orphan rows are corpus-exclusions) → reject-on-save `superRefine` is safe.
- **GATE 3 (Claude code-reviewer + Codex gpt-5.5, parallel):** shippable, 0 HIGH/MED. LOW-1 (no manifest↔TS drift test) ACCEPTED → drift-lock test (`3457454`). LOW-2 (orphan-rejection UX is a late/generic error) DEFERRED — by design (§4 Q7); auto-add would change the lock. Pushed `feat/c02-enforce` → **PR #547**; round-1 CI green (E2E, edge TEST deploy ×11, CodeQL, Lighthouse, Coverage, semgrep, claude-database-review, performance-review); the 2 review bots ran slow (~13m) but PASSED (no malfunction).
- **Round-1 bot triage — caught a REAL save-blocker** (claude-component-review): closing the enums would block reviewer re-save of legacy `submission_reviews.tagged_metadata`. Censused PROD: **35 of 113** rows hold old kebab cooking/ingredient slugs (43 distinct, all old `filterDefinitions` dropdown values). Built slug→canonical maps (resolved via the C02 alias map + 7 manual judgment calls e.g. herbs-aromatics→Fresh herbs, cutting slugs→Knife skills), extended `canonicalizeReviewMetadata` (the PR-6e-E2c precedent) with the maps + parent-completion + dedupe (`13a9e5b`, executor + TDD). **PROVEN 35/35** by running all 35 real PROD rows through the *actual* `canonicalizeReviewMetadata` + *actual* `reviewFormPayloadSchema` (temporary vitest spec, then removed). Other findings: B (derive filter options from consts), MED (parent-complete mapper fixtures), C (document INGREDIENT_PARENT_MAP undefined-for-groups) → `1477b40`; E (4-line JSX comment) rejected (mirrors gardenSkills template).
- **GATE 4 Codex** on `13a9e5b`+`1477b40`: all 5 scrutinize items CLEAN, LGTM. Posted a per-finding triage comment on #547; pushed fix-ups for round 2.
- **enums.json investigation:** it's the generated single-writer registry backing SQL CHECKs (`-- SOURCE: enums.json[...]`). Its generator hard-lists 10 keys (doesn't auto-discover the new VALUES → no drift, freshness test still green) → registration of cooking_skills/main_ingredients DEFERRED to P4b alongside the CHECK.
- **Round 2 (after the fix-up push):** both bots "shippable / no blockers"; `claude-component-review` flipped CHANGES_REQUESTED→COMMENTED (save-blocker confirmed fixed). `claude-review`'s two "CLAUDE.md violation" findings (4-line JSX comment; "unreachable" typeof guard) cited rules NOT present in any repo CLAUDE.md (verified by grep) → REJECTED with rationale; type/edge-comment nits deferred. Round-cap reached.
- **User said "you can merge" → squash-merged PR #547 (`8513b18`), branch deleted.** Frontend "Deploy to Production" success (PROD HTTP 200). The edge-function PROD deploy gated on "Approve PROD Deploy" (a manual production-environment gate, like P3's migration); **user chose "approve it for me"** → waited for the frontend to go live first (expand/contract order), then approved via `gh` → **all 11 edge functions deployed to PROD (success, none skipped → no silent no-op)**, incl. `complete-review` + `process-submission`. `password-reset` flaked once on esm.sh 522 (the known CDN flake) → reran green. **P4a enforcement is LIVE on PROD.** PR cycle 3 (Sessions 18–21) archived; status rewritten to Session 22.

Dispatch pattern: 4 fresh-context executors (3 P4a tasks + 1 canonicalizer save-blocker), each supervisor-verified in the main loop; 2 parallel GATE-3 reviewers (superpowers:code-reviewer + codex:codex-rescue, inline, `--model gpt-5.5`); 1 GATE-4 Codex. All PROD censuses (freeze / orphan / submission_reviews / 35-row proof), the manifest freeze, the slug→canonical map construction, and all byte-identity verification were supervisor-direct (data-safety criticality). The drift-lock test, the derive-options transform, the parent-map comment, and the mapper-fixture fixes were supervisor-implemented directly (small, full context).

Learnings (candidates to promote — audit at initiative close):
- **Closing a column's enum must census EVERY write surface that feeds it, not just the column.** `lessons` was canonical (P3), but `submission_reviews.tagged_metadata` is a SEPARATE surface P3 never touched — 35 legacy rows there would have silently blocked reviewers on reopen. The bot flagged it; a PROD census + the PR-6e-E2c `canonicalizeReviewMetadata` precedent + a 35/35 end-to-end proof closed it. Generalize: enforcement-lockdown PRs need a census of the reviewer-draft/submission surfaces, not only the published table.
- **Prove a data-safety fix on the real affected rows before shipping it.** Pulling all 35 PROD rows and running them through the actual canonicalize + actual schema (35/35) is far stronger than representative fixtures — it turns "should work" into "does work on every live row."

