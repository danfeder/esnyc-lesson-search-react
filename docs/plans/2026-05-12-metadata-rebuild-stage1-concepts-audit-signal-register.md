# Stage 1 Concepts — Audit Signal Register

**Last updated:** 2026-05-12 — Session 79 initialization. 11 open signals (CON-01 through CON-11) extracted from Phase 1 upfront Opus batch and Phase 2 §11 high-impact tier per-value fills. Signals are evidence-or-action items for Stage 2 corpus cleanup / reviewer-validation.

> **Purpose.** Open audit signals surfaced during Stage 1 per-value worksheet fills, structured for Stage 2 corpus cleanup / reviewer-validation intake. Mirrors the heritage worksheet's audit register at `2026-05-10-metadata-rebuild-stage1-heritage-audit-signal-register.md`. Each entry is sourced from existing documentation (worksheet `<details>` block, claude_notes paragraph, or Phase 1 Opus batch scratch files). Do not invent new signals here; append from session work as it lands.
>
> **Status vocabulary.**
> - `Open` — flagged, not yet addressed
> - `Resolved` — closed by a Stage 2 corpus cleanup, re-tag, or reviewer-validation action; record the closing PR, backfill query, or reviewer decision in the row
> - `Deferred` — explicitly scoped out; record the reason in the row
>
> **Prefix.** `CON-NN` (distinguishes from heritage's `ASI` / `AME` / `AFR` / `EUR` / `ME` / `X` prefixes).
>
> **Scope.** Stage 1 surfaces audit signals as a byproduct of per-value worksheet fills + upfront Opus batches. Stage 2 (corpus cleanup + reviewer-validation flow — including re-tag PRs, body-content reviews, and reviewer judgment) is where signals close. This register is the bridge between the two stages.

## Open signals

| ID | Cluster / source | Signal | Evidence already in docs | Stage 2 action | Status |
|----|------------------|--------|---------------------------|----------------|--------|
| CON-01 | seasonality cluster (§11 `seasonality` + `seasonal_changes` entries) | "Seasons" template-trio lessons tagged inconsistently across the two seasonality canonicals. `Seasons: Spring` is tagged `seasonal changes` (phenomenology lens); `Seasons: Fall` + `Seasons: Winter` (same template) are tagged `seasonal eating` (food-availability lens). Same content template, opposite verdict-direction — direct evidence the legacy taxonomy could not reliably distinguish food-availability lens from phenomenology lens | Worksheet §11 `seasonality` entry `<details>` block (audit signal note at end); Wave 2 Opus batch (`/tmp/session-79-opus-seasonality.md`) | Stage 2 re-tag needs an explicit decision rule for food-availability lens (`seasonality`) vs phenomenology lens (`seasonal_changes`). Re-tag the 3 template-trio rows consistently per the rule | Open |
| CON-02 | Wave 3 garden-meta singletons | `Soul Food Sunday` template-stub lesson never filled — body is ESYNYC scaffolding ("Do something here. (time)"). Surfaced as the lone `general exploration` Science singleton. Likely belongs to the existing "14 stuck submissions" cleanup track | Wave 3 Opus batch (`/tmp/session-79-opus-singletons-garden-meta.md`) | Coordinate with the existing stuck-submissions cleanup track to either delete the template stub or recover / refill it | Open |
| CON-03 | Wave 3 garden-meta singletons | `Informational Writing - Topics/Subtopics` lesson carries BOTH `garden topics` AND `plant science` in `Science::academicConcepts` — defensive Science-slot-filling on a non-Science ELA lesson (lesson teaches informational-writing structure using garden brainstorming as vehicle, not botany). 2 of 3 garden-meta singletons collapse to this single row | Wave 3 Opus batch (`/tmp/session-79-opus-singletons-garden-meta.md`) | Stage 2 re-tag should drop both Science concepts on this row; the ELA tagging is correct (no Science axis to preserve) | Open |
| CON-04 | historical_figures cross-subject split (§11 `historical_figures` entry) | Same historical figure (George Washington Carver) gets different subject tags in two different lessons (`In the Garden with Dr. Carver` tagged Literacy/ELA; `Lotion & Agar Soap - K` tagged Social Studies). Direct evidence of v3 batch-tagging inconsistency — supports the entry's consolidate-to-Social-Studies recommendation | Worksheet §11 `historical_figures` entry `<details>` block + claude_notes; Wave 1 Opus batch (`/tmp/session-79-opus-historical-figures.md`) | Resolves when Stage 2 re-tag consolidates `historical_figures` to Social Studies per the entry's `recommended_primary_subject`. The Literacy/ELA-side Carver lesson's read-aloud-biography axis is preserved by its co-present `storytelling` tag | Open |
| CON-05 | community_systems cluster (§11 `community_systems` entry) | `community activism` (Social Studies, 1 — Guerilla Gardening for Birds) should merge into the existing `advocacy` (Social Studies, 3) canonical, NOT into its current cluster mate `community_systems`. Cross-cluster merge target outside the surface-label-implied cluster | Worksheet §11 `community_systems` entry claude_notes; Wave 2 Opus batch (`/tmp/session-79-opus-community-systems.md`) | When `advocacy` is filled in Session 80 (mid-tier, freq 3), add `community activism` as a merge_alias on that entry. Stage 2 re-tag rewrites the one `community_activism` row to the `advocacy` canonical | Open |
| CON-06 | community_systems boundary (§11 `community_systems` entry) | `community_systems` (15) currently double-duties as "food systems" + "environmental systems" + "community structures" in the corpus. Overlaps semantically with `food systems` (Social Studies, 1 — itself a case-normalized concept↔theme overlap per §6) and `environmental justice` (Social Studies, 3). Curriculum team may want to sharpen the boundary or split this canonical | Worksheet §11 `community_systems` entry claude_notes; Wave 2 Opus batch | Stage 2 reviewer-validation pass — sharpen the `community_systems` vs `food_systems` vs `environmental_justice` boundary, or confirm the current overlap is an intentional broad bucket | Open |
| CON-07 | storytelling cross-subject split (§11 `storytelling` entry) | `Mr. Anthony's Spring Trees Unit` is the ONLY lesson in the corpus tagging `storytelling` under Arts. The row redundantly carries `storytelling` under BOTH `Arts` AND `Literacy/ELA`; the lesson summary is one sentence and body fields (objectives / lessonContent / description) are all empty strings. Direct evidence supporting the entry's drop-Arts-tag recommendation. **Also surfaces this row as a cleanup-candidate** (empty-body parallel to CON-02 Soul Food Sunday) | Worksheet §11 `storytelling` entry `<details>` block + claude_notes; Wave 1 Opus batch (`/tmp/session-79-opus-storytelling.md`) | Resolves when Stage 2 re-tag drops the `Arts:storytelling` tag on this row per the entry's `recommended_primary_subject`. Separately, the empty-body cleanup-candidate aspect may coordinate with the stuck-submissions track flagged in CON-02 | Open |
| CON-08 | companion_planting cross-subject split (§11 `companion_planting` entry) | 13 Three-Sisters / Lenape / Squanto lessons split between Science (10) and Social Studies (3) for the SAME agronomic concept. Identical lesson templates land on opposite subjects: `Three Sisters Tacos` (Science) vs `3 Sisters Tacos` (Social Studies); `Three Sisters Soup` (Science) vs `3 Sisters Dip` (Social Studies). Smoking-gun reviewer-inconsistency pattern, not a genuine cognitive-lens distinction | Worksheet §11 `companion_planting` entry `<details>` block + claude_notes; Wave 1 Opus batch (`/tmp/session-79-opus-companion-planting.md`) | Resolves when Stage 2 re-tag consolidates `companion_planting` to Science per the entry's `recommended_primary_subject`. Indigenous-agriculture cultural context is preserved on the rows via co-present Social-Studies concepts (`cultural traditions`, `Native American history`, `historical figures`, `colonialism's impact`) plus `culturalHeritage` tags | Open |
| CON-09 | preservation cluster (Wave 1 spillover; affects Session 80 mid-tier) | `preservation` (Science, 3) and `food preservation` (Science, 1) are merge candidates — the Science cohort across both labels is indistinguishable in lens (jam-canning, vegetable pickling, rose tincture/infusion, cultured-butter fermentation are all food-or-herbal substance-stabilization techniques). 4-lesson Science split is pure naming inconsistency, not lens divergence. Note: the single Social-Studies-tagged `preservation` lesson (Three Sisters Succotash, "modern and past methods of preservation") is plausibly distinct — a cultural-heritage-preservation lens — but with N=1 the curriculum team may collapse it anyway | Wave 1 Opus batch (`/tmp/session-79-opus-preservation.md`); will land in Session 80 mid-tier worksheet entry for `preservation` (Science, 3) | Resolves when Session 80 mid-tier worksheet entry for `preservation` adopts `food preservation` as a merge_alias. Cultural-preservation lens (Social Studies, 1) handling is a Session 80 sub-decision | Open |
| CON-10 | nutrition_education cross-subject split (§11 `nutrition_education` entry) | The Science / Health split (7 / 100) tracks each lesson's `academicIntegration` array choice rather than its actual content. 6 of 7 Science-tagged rows are cultural-recipe lessons with no biological framing; the 1 genuinely-science-framed row (Glucose Regulation) is already double-tagged with Health. Direct evidence of how v3 batch-tagging populated `academicConcepts` subject keys via `academicIntegration` array proxies — broader pattern visible across CON-04 + CON-07 + CON-08 specific cases | Worksheet §11 `nutrition_education` entry `<details>` block + claude_notes; Wave 1 Opus batch (`/tmp/session-79-opus-nutrition-education.md`) | Resolves when Stage 2 re-tag consolidates `nutrition_education` to Health per the entry's `recommended_primary_subject`. **Broader pattern note for Stage 2 reviewer-validation:** wherever `academicConcepts` subject keys diverge from `academicIntegration` content framing, the `academicIntegration`-proxy convention is unreliable — Stage 2 may want a corpus-wide audit of this proxy pattern | Open |
| CON-11 | historical_context singleton (Wave 1 spillover; affects Session 81 long-tail) | `historical_context` (Social Studies, 1) is a candidate merge_alias into the `historical_figures` canonical (Session 81 long-tail review). Note also a possible standalone-keep verdict if the curriculum team distinguishes "historical context" as a different cognitive lens (era/setting backdrop) from "historical figures" (people-as-history-makers) | Worksheet §11 `historical_figures` entry claude_notes (forward reference) | Session 81 long-tail worksheet review will fill the `historical_context` per-value entry; verdict determines whether to merge into `historical_figures` or keep standalone | Open |

## Closed signals

(None yet — register initialized Session 79.)

---

*Initialized Session 79 (2026-05-12) during PR-Concepts-1 fill. Updates land at PR closeout backfill per the Stage 1 PR closeout checklist convention.*
