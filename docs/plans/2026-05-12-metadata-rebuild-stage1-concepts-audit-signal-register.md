# Stage 1 Concepts — Audit Signal Register

**Last updated:** 2026-05-12 — Session 81 PR-Concepts-3 long-tail per-value fills (pending PR). 24 open signals (CON-01 through CON-24); 9 new signals appended this session (CON-16 Indigenous-cluster cross-field overlap; CON-17 holidays cluster; CON-18 cardiovascular/circulatory form-drift pair; CON-19 phases_of_matter/states_of_matter form-drift pair; CON-20 climate/climate_change sub-topic boundary; CON-21 harvest/harvesting noun/gerund form-drift pair; CON-22 reading-cluster boundary; CON-23 measurement-cluster boundary; CON-24 figurative-language cluster boundary). 2 prior signals closed at Session 81 fill: CON-11 historical_context (forward-reference closed by `historical_context` 1 long-tail entry; verdict pending curriculum team between merge-into-historical_figures vs standalone-keep); the §11/§12 merge-alias forward-references for cultural_traditions / community_systems / visual_arts / adaptations / plant_identification all have alias-side §13 entries landed. Prior Session 80 closeout: 15 open signals (CON-01 through CON-15); 4 new this session (CON-12 writing-cluster canonical-shape question; CON-13 observation `drawing`-attached procedural artifact; CON-14 garden_exploration drop-or-keep boundary; CON-15 nutrition vs nutrition_education boundary). 2 prior signals (CON-05 advocacy + CON-09 preservation) have merge_aliases proposals now landed in Session 80 §12 worksheet entries; Stage 2 actions updated to reflect the proposal-landed state while preserving Open status (signals close at Stage 2 re-tag implementation, not at Stage 1 worksheet fill). PR #495 round-1 fix-up `c4c0aa5` enriched CON-12 Evidence column to reflect symmetric explicit-citation coverage (added `(see CON-12)` pointers to 3 §12 writing sub-type entries that previously referenced the cluster implicitly). Signals are evidence-or-action items for Stage 2 corpus cleanup / reviewer-validation. Prior Session 79 initialization + PR #494 round-1 fix-up: 11 signals (CON-01 through CON-11) extracted from Phase 1 upfront Opus batch and Phase 2 §11 high-impact tier per-value fills; round-1 review fix-up stripped ephemeral `/tmp` Opus-batch references from CON-01/04/05/06/07/08/10 (durable worksheet pointers retained); inlined lesson IDs + body excerpts for CON-02/03/09 (sole-evidence rows).

> **Purpose.** Open audit signals surfaced during Stage 1 per-value worksheet fills, structured for Stage 2 corpus cleanup / reviewer-validation intake. Mirrors the heritage worksheet's audit register at `2026-05-10-metadata-rebuild-stage1-heritage-audit-signal-register.md`. Each entry is sourced from existing documentation (worksheet `<details>` block, claude_notes paragraph, or Phase 1 Opus batch scratch files). Do not invent new signals here; append from session work as it lands.
>
> **Status vocabulary.**
> - `Open` — flagged, not yet addressed
> - `Resolved` — closed by a Stage 2 corpus cleanup, re-tag, or reviewer-validation action; record the closing PR, backfill query, or reviewer decision in the row
> - `Deferred` — explicitly scoped out; record the reason in the row
>
> **Prefix.** `CON-NN` (distinguishes from heritage's `ASI` / `AME` / `AFR` / `EUR` / `ME` / `X` prefixes).
>
> **Scope.** Stage 1 surfaces audit signals as a byproduct of per-value worksheet fills + upfront Opus batches. Stage 2 (corpus cleanup + reviewer-validation flow — including re-tag PRs, body-content reviews, and reviewer judgment) is where signals close. This register is the bridge between the two stages.

## Open signals

| ID | Cluster / source | Signal | Evidence already in docs | Stage 2 action | Status |
|----|------------------|--------|---------------------------|----------------|--------|
| CON-01 | seasonality cluster (§11 `seasonality` + `seasonal_changes` entries) | "Seasons" template-trio lessons tagged inconsistently across the two seasonality canonicals. `Seasons: Spring` is tagged `seasonal changes` (phenomenology lens); `Seasons: Fall` + `Seasons: Winter` (same template) are tagged `seasonal eating` (food-availability lens). Same content template, opposite verdict-direction — direct evidence the legacy taxonomy could not reliably distinguish food-availability lens from phenomenology lens | Worksheet §11 `seasonality` entry `<details>` block (audit signal note at end) | Stage 2 re-tag needs an explicit decision rule for food-availability lens (`seasonality`) vs phenomenology lens (`seasonal_changes`). Re-tag the 3 template-trio rows consistently per the rule | Open |
| CON-02 | Wave 3 garden-meta singletons | `Soul Food Sunday` template-stub lesson never filled — body is ESYNYC scaffolding ("Do something here. (time)"). Surfaced as the lone `general exploration` Science singleton. Likely belongs to the existing "14 stuck submissions" cleanup track | Lesson `1voTOBrqizCtSDbkVdDiEt3MUE51jtm1GTKtrM-4H18M` "Soul Food Sunday" — body is verbatim ESYNYC blank template ("Do something here. (time)"; empty Summary; placeholder "Lesson Title [DO NOT TYPE ON THIS: MAKE A COPY!!!!]"). Title suggests an African-American culinary-heritage cooking lesson; the document was never actually written | Coordinate with the existing stuck-submissions cleanup track to either delete the template stub or recover / refill it | Open |
| CON-03 | Wave 3 garden-meta singletons | `Informational Writing - Topics/Subtopics` lesson carries BOTH `garden topics` AND `plant science` in `Science::academicConcepts` — defensive Science-slot-filling on a non-Science ELA lesson (lesson teaches informational-writing structure using garden brainstorming as vehicle, not botany). 2 of 3 garden-meta singletons collapse to this single row | Lesson `1cH_8eRYyGYLfAMROmDowd8aPddx1tDMoUxTM0QBR42s` "Informational Writing - Topics/Subtopics" — body teaches the *literacy* concept of topics vs. subtopics using cooking/gardening as brainstorming fodder ("Students will use the items on their tables to help them come up with as many subtopics for their topic — either cooking or gardening"). Zero plant biology, zero growth processes, zero botany. Both `garden topics` and `plant science` are vague-Science stamps on an ELA-topic-structure lesson | Stage 2 re-tag should drop both Science concepts on this row; the ELA tagging is correct (no Science axis to preserve) | Open |
| CON-04 | historical_figures cross-subject split (§11 `historical_figures` entry) | Same historical figure (George Washington Carver) gets different subject tags in two different lessons (`In the Garden with Dr. Carver` tagged Literacy/ELA; `Lotion & Agar Soap - K` tagged Social Studies). Direct evidence of v3 batch-tagging inconsistency — supports the entry's consolidate-to-Social-Studies recommendation | Worksheet §11 `historical_figures` entry `<details>` block + claude_notes | Resolves when Stage 2 re-tag consolidates `historical_figures` to Social Studies per the entry's `recommended_primary_subject`. The Literacy/ELA-side Carver lesson's read-aloud-biography axis is preserved by its co-present `storytelling` tag | Open |
| CON-05 | community_systems cluster (§11 `community_systems` entry) | `community activism` (Social Studies, 1 — Guerilla Gardening for Birds) should merge into the existing `advocacy` (Social Studies, 3) canonical, NOT into its current cluster mate `community_systems`. Cross-cluster merge target outside the surface-label-implied cluster | Worksheet §11 `community_systems` entry claude_notes; Worksheet §12 `advocacy` entry claude_notes + merge_aliases | Session 80 worksheet entry for `advocacy` (§12) added `community activism` (1) as a merge_alias per recommendation; Stage 2 re-tag rewrites the one `community_activism` row to the `advocacy` canonical pending curriculum-team confirmation of the merge | Open |
| CON-06 | community_systems boundary (§11 `community_systems` entry) | `community_systems` (15) currently double-duties as "food systems" + "environmental systems" + "community structures" in the corpus. Overlaps semantically with `food systems` (Social Studies, 1 — itself a case-normalized concept↔theme overlap per §6) and `environmental justice` (Social Studies, 3). Curriculum team may want to sharpen the boundary or split this canonical | Worksheet §11 `community_systems` entry claude_notes | Stage 2 reviewer-validation pass — sharpen the `community_systems` vs `food_systems` vs `environmental_justice` boundary, or confirm the current overlap is an intentional broad bucket | Open |
| CON-07 | storytelling cross-subject split (§11 `storytelling` entry) | `Mr. Anthony's Spring Trees Unit` is the ONLY lesson in the corpus tagging `storytelling` under Arts. The row redundantly carries `storytelling` under BOTH `Arts` AND `Literacy/ELA`; the lesson summary is one sentence and body fields (objectives / lessonContent / description) are all empty strings. Direct evidence supporting the entry's drop-Arts-tag recommendation. **Also surfaces this row as a cleanup-candidate** (empty-body parallel to CON-02 Soul Food Sunday) | Worksheet §11 `storytelling` entry `<details>` block + claude_notes | Resolves when Stage 2 re-tag drops the `Arts:storytelling` tag on this row per the entry's `recommended_primary_subject`. Separately, the empty-body cleanup-candidate aspect may coordinate with the stuck-submissions track flagged in CON-02 | Open |
| CON-08 | companion_planting cross-subject split (§11 `companion_planting` entry) | 13 Three-Sisters / Lenape / Squanto lessons split between Science (10) and Social Studies (3) for the SAME agronomic concept. Identical lesson templates land on opposite subjects: `Three Sisters Tacos` (Science) vs `3 Sisters Tacos` (Social Studies); `Three Sisters Soup` (Science) vs `3 Sisters Dip` (Social Studies). Smoking-gun reviewer-inconsistency pattern, not a genuine cognitive-lens distinction | Worksheet §11 `companion_planting` entry `<details>` block + claude_notes | Resolves when Stage 2 re-tag consolidates `companion_planting` to Science per the entry's `recommended_primary_subject`. Indigenous-agriculture cultural context is preserved on the rows via co-present Social-Studies concepts (`cultural traditions`, `Native American history`, `historical figures`, `colonialism's impact`) plus `culturalHeritage` tags | Open |
| CON-09 | preservation cluster (Wave 1 spillover; landed in Session 80 mid-tier `preservation` entry) | `preservation` (Science, 3) and `food preservation` (Science, 1) are merge candidates — the Science cohort across both labels is indistinguishable in lens (jam-canning, vegetable pickling, rose tincture/infusion, cultured-butter fermentation are all food-or-herbal substance-stabilization techniques). 4-lesson Science split is pure naming inconsistency, not lens divergence. Note: the single Social-Studies-tagged `preservation` lesson (Three Sisters Succotash, "modern and past methods of preservation") is plausibly distinct — a cultural-heritage-preservation lens — but with N=1 the curriculum team may collapse it anyway | Science cohort (4 lessons indistinguishable in lens): `1jS-jjcZs0zF-WSwzkrgryefL2kiBepp2ahvqQzzQodI` Bud Not Buddy/Jam (canning); `1yUHWKsyYlQoHqmOuuX4IttgvcVY5uDOeJwsKAwjco9s` Pickle Lesson with Ms. Ingrit (pickling); `1130Pa-vFa5TJC8uK-sjgpOjsvD7TkJYAau5esNfsmY0` Healing with Roses (herbal infusion); `1w0c7tESqksEPlQL-Fj1tpTL3i2ofmN_Agq-UwwqErBE` Sandor Katz: Food Hero (fermentation, currently tagged `food preservation`). Social-Studies-tagged `preservation` is `1w_JBTJYmqF0qcXiOtV_nV32nphCjD8uEYRSHdHyROFE` Three Sisters Succotash ("modern and past methods of preservation"), plausibly a distinct cultural-heritage lens (N=1). Worksheet §12 `preservation` entry `<details>` block reproduces all 5 lesson excerpts per F2 hardening pattern | Session 80 worksheet entry for `preservation` (§12) added `food preservation` (1) as a merge_alias per Session 79 Opus read recommendation; `<details>` block reproduces the 5-lesson corpus evidence (Bud Not Buddy/Jam, Pickle Lesson with Ms. Ingrit, Healing with Roses, Three Sisters Succotash, Sandor Katz: Food Hero) per F2 hardening pattern. Cultural-preservation lens (Social Studies, 1 — Three Sisters Succotash) flagged as `recommended_secondary_subjects: Social Studies (conditional)` for curriculum-team decision between (a) consolidate-all-under-`food_preservation` vs (b) keep-`preservation`-as-Social-Studies-cultural-canonical. Stage 2 re-tag conforms to curriculum-team verdict | Open |
| CON-10 | nutrition_education cross-subject split (§11 `nutrition_education` entry) | The Science / Health split (7 / 100) tracks each lesson's `academicIntegration` array choice rather than its actual content. 6 of 7 Science-tagged rows are cultural-recipe lessons with no biological framing; the 1 genuinely-science-framed row (Glucose Regulation) is already double-tagged with Health. Direct evidence of how v3 batch-tagging populated `academicConcepts` subject keys via `academicIntegration` array proxies — broader pattern visible across CON-04 + CON-07 + CON-08 specific cases | Worksheet §11 `nutrition_education` entry `<details>` block + claude_notes | Resolves when Stage 2 re-tag consolidates `nutrition_education` to Health per the entry's `recommended_primary_subject`. **Broader pattern note for Stage 2 reviewer-validation:** wherever `academicConcepts` subject keys diverge from `academicIntegration` content framing, the `academicIntegration`-proxy convention is unreliable — Stage 2 may want a corpus-wide audit of this proxy pattern | Open |
| CON-11 | historical_context singleton (Wave 1 spillover; affects Session 81 long-tail) | `historical_context` (Social Studies, 1) is a candidate merge_alias into the `historical_figures` canonical (Session 81 long-tail review). Note also a possible standalone-keep verdict if the curriculum team distinguishes "historical context" as a different cognitive lens (era/setting backdrop) from "historical figures" (people-as-history-makers) | Worksheet §11 `historical_figures` entry claude_notes (forward reference) | Session 81 long-tail worksheet review will fill the `historical_context` per-value entry; verdict determines whether to merge into `historical_figures` or keep standalone | Open |
| CON-12 | writing-cluster canonical shape (§12 `writing` 8 entry) | Generic `writing` 8 (Literacy/ELA) sits alongside 5 mid-tier writing sub-types (`how_to_writing` 5, `narrative_writing` 3, `opinion_writing` 3, `descriptive_writing` 3, `persuasive_writing` 4) + 7 long-tail variants (`argumentative writing` 2, `informational writing` 2, `recipe writing` 2, `comparative writing` 1, `creative writing` 1 — corpus-tagged Arts not Lit/ELA, `letter writing` 1, `writing claims` 1). Three canonical-shape options exist: (a) keep `writing` 8 as residual catch-all for un-genred writing instruction; (b) drop `writing` 8 in favor of more-specific genre tags; (c) flip and treat `writing` as canonical with all genre sub-types as merge_aliases. Each option produces a structurally different vocabulary outcome (~36 lessons spread across cluster) | Worksheet §12 `writing` entry claude_notes (full cluster + 3-option framing); §12 `how_to_writing`/`narrative_writing`/`opinion_writing`/`descriptive_writing`/`persuasive_writing` entries each explicitly cross-reference CON-12 by name (round-1 fix-up cycle accepted bot-converged suggestion to add `(see CON-12)` pointers to the 3 sub-types that previously only referenced the cluster implicitly) | Curriculum-team verdict during Stage 1 fill — which of (a) / (b) / (c) to adopt. Stage 2 re-tag conforms to that verdict. Per D-C15 the writing-cluster decision is one of the "pay closer attention" mid-tier items flagged in §12's status banner | Open |
| CON-13 | observation cross-subject artifact (§12 `observation` 6 entry) | v3 tagger appears to attach `observation` to whichever subject already carried `drawing` on the same lesson row. Smoking-gun pair: `19Sjs7Fz7Z8ToT421a3VVT_j8_b-KUXAp` **Fall: Special Spot** has `Science: ["seasonal change","observation"]` + `Arts: ["drawing"]` while its curricular twin `1CSPiEww-2bIuRRyPnbZAqG8K_8D1kw7hr85RSRJACSk` **Spring Special Spot and Signs of Spring Scavenger Hunt** has `Arts: ["drawing","observation"]` + `Science: ["seasonal change","weather"]` — identical lesson series (Special Spot scavenger hunts), opposite subject placement for observation. Distinct procedural-artifact pattern (separate from the academicConcepts-tracks-academicIntegration pattern in CON-04/08/10/biodiversity-fourth-data-point) — affects whatever lesson cohort the v3 tagger applied this drawing-pairing heuristic to | Worksheet §12 `observation` entry `<details>` block + claude_notes (with smoking-gun evidence from Spring vs Fall Special Spot) | Corpus-wide audit before Stage 2 consolidation — count how many `Science::observation` rows + `Arts::observation` rows would be affected by the consolidate-to-Science recommendation. If the audit surfaces lessons where Arts::observation IS the right placement (e.g., still-life observational drawing as Arts pedagogy), curriculum team may want to keep the cross-subject placement on those specific rows. Stage 2 re-tag conforms | Open |
| CON-14 | garden_exploration drop-or-keep boundary (§12 `garden_exploration` 4 entry) | `garden exploration` 4 (Science) functions as a context tag ("this lesson takes place in/around the garden") rather than a distinct scientific concept being taught — unlike specific Science concepts like §11 `plant_parts` 239 or `pollinators` 28. The broader "garden as context" is already captured by the `location: garden` filter on lessons (see `src/utils/filterDefinitions.ts`). Adjacent long-tail vague-Science singletons `garden topics` 1, `general exploration` 1, `plant science` 1 are recommended drops per CON-02 + CON-03 + the Session 79 singletons-garden-meta scan — `garden_exploration` 4 may belong on the same drop list | Worksheet §12 `garden_exploration` entry claude_notes | Curriculum-team verdict — keep as canonical (4 lessons share the framing) or also drop as context-tag-not-concept. Stage 2 re-tag conforms. If drop, the 4 lessons need replacement Science tags (likely none — they're already weakly-Science by content; existing per-row Science tags would carry the natural-science framing) | Open |
| CON-15 | nutrition vs nutrition_education boundary (§12 `nutrition` 4 entry) | `nutrition` 4 (Science) overlaps semantically with §11 `nutrition_education` 107 (Health 100 + Science 7). May be: (a) Science-specific narrower biology-of-nutrition concept (macronutrients, energy from food, plant-vs-animal sources) distinct from Health-side `nutrition_education`, OR (b) pure label drift — shorter alias of `nutrition_education` that should merge into nutrition_education's Science-7 secondary. Not decidable from probe data alone; requires Opus corpus-inspection of the 4 `nutrition` lessons or curriculum-team direct-judgment. Related long-tail concepts `plant nutrition` 2, `macronutrients` 1, `macromolecules` 1 are distinguishably narrower biology concepts kept separate regardless of this verdict | Worksheet §12 `nutrition` entry claude_notes; full v3 baseline subject distribution in worksheet Appendix A.1 (Science 92 distinct concepts) | Curriculum-team verdict — targeted Opus read of the 4 `nutrition` rows OR direct judgment call. If (b) — merge `nutrition` into `nutrition_education` as a Science-side alias. If (a) — keep `nutrition` standalone and consider clearer surface label (e.g., `nutrition_biology` or `nutritional_science`) to distinguish from the broader Health-side `nutrition_education` | Open |
| CON-16 | Indigenous-cluster cross-field overlap (§13 `indigenous_knowledge` / `indigenous_stories` / `native_american_history` entries) | Three Social Studies singletons cover Indigenous / Native American identity at the concepts field: `Indigenous knowledge` 1, `Indigenous stories` 1, `Native American history` 1. The heritage worksheet's §9.1 cross-cluster `Indigenous and Diaspora` cluster (D1 — single combined root with `African American` + `Indigenous` as parallel direct children) covers Indigenous identity at the heritage `culturalHeritage` field; concepts-side coverage is fragmented across these 3 singletons. Cross-field overlap question: should the concepts side defer to heritage's cluster-rooted treatment, OR should concepts independently consolidate the 3 singletons under a single `indigenous_perspectives` canonical, OR should the 3 singletons merge into broader concept canonicals (cultural_traditions / storytelling / historical_figures + geography)? **First Stage 1 cross-field cross-reference between concepts and heritage worksheets** | Worksheet §13 `indigenous_knowledge` + `indigenous_stories` + `native_american_history` entries claude_notes; heritage worksheet §9.1 `Indigenous and Diaspora` cluster framing | Curriculum-team verdict during Stage 1 concepts fill (Session 82+) — pick from: (a) consolidate 3 singletons into single `indigenous_perspectives` canonical; (b) defer to heritage-side §9.1 cluster + drop concepts-side singletons (treat as already-covered by heritage `culturalHeritage` tagging); (c) merge each singleton into closest concept canonical (Indigenous_knowledge → cultural_traditions; Indigenous_stories → cultural_narratives or storytelling; Native_American_history → historical_figures + geography). Cross-field reconciliation may need a separate pass after both worksheets ship | Open |
| CON-17 | holidays cluster (§13 `holidays` + `national_and_religious_holidays` entries) | Two Social Studies singletons (`holidays` 1, `national and religious holidays` 1) both plausibly merge into §11 `cultural_traditions` 206 (holidays are typically cultural-tradition observations — Diwali, Thanksgiving, Día de los Muertos, Eid). The verbose surface label "national and religious holidays" overlaps semantically with the shorter `holidays` and with broader `cultural traditions`. 3-way merge candidate: holidays + national_and_religious_holidays → cultural_traditions | Worksheet §13 `holidays` + `national_and_religious_holidays` entries claude_notes; §11 `cultural_traditions` 206 entry | Stage 2 re-tag absorbs both singletons into §11 `cultural_traditions` 206. Curriculum team can keep separate if the holidays-specific framing is pedagogically distinct from broader cultural traditions, but the corpus signal at 1+1 is too thin to support standalone canonicals | Open |
| CON-18 | cardiovascular_system / circulatory_system form-drift pair (§13 `cardiovascular_system` + `circulatory_system` entries) | Two Science singletons (`cardiovascular system` 1, `circulatory system` 1) refer to the same anatomical system with different vocabulary registers (cardiovascular = medical/clinical; circulatory = K-12 pedagogically common). Same content, different surface labels — merge-candidate pair | Worksheet §13 `cardiovascular_system` + `circulatory_system` entries claude_notes | Stage 2 re-tag merges the two into a single canonical; surface label is a curriculum-team call (both labels in active K-12 use). Lean toward `circulatory_system` as the K-12-friendlier label | Open |
| CON-19 | phases_of_matter / states_of_matter form-drift pair (§13 `phases_of_matter` freq-2 + `states_of_matter` freq-1 entries) | Two Science entries (`phases of matter` 2, `states of matter` 1) refer to the same physical-chemistry concept with different vocabulary registers (phases-of-matter = more chemistry-formal; states-of-matter = more K-12 colloquial). Same content, different surface labels — merge-candidate pair. Higher-frequency wins per established convention (phases_of_matter as canonical, states_of_matter as merge_alias) | Worksheet §13 `phases_of_matter` (freq-2) + `states_of_matter` (freq-1) entries claude_notes | Stage 2 re-tag merges `states of matter` 1 into `phases of matter` 2 as a merge_alias; surface label is a curriculum-team call (both labels in active K-12 use). Lean toward `phases_of_matter` (higher-frequency canonical) | Open |
| CON-20 | climate / climate_change sub-topic boundary (§13 `climate` freq-2 + `climate_change` freq-1 entries) | Two Science entries (`climate` 2, `climate change` 1) — climate-change is a sub-topic of climate science (specific phenomenon within broader climate framing). Recommend curriculum team consider: (a) merge `climate change` 1 into `climate` 2 as a sub-concept (sub-topic absorption); OR (b) keep both standalone if curriculum team wants distinct climate-change tagging for environmental-justice / advocacy lessons. The merge direction is supported by climate being the broader canonical | Worksheet §13 `climate` + `climate_change` entries claude_notes | Stage 2 re-tag conforms to curriculum-team verdict. If (a), merge `climate change` 1 into `climate` 2 with merge_alias entry. If (b), keep both as parallel canonicals with cross-reference | Open |
| CON-21 | harvest / harvesting noun/gerund form-drift pair (§13 `harvest` + `harvesting` entries) | Two Science singletons (`harvest` 1 noun-form, `harvesting` 1 gerund-form) cover the same activity. Same content, different word-forms — merge-candidate pair. Surface-label choice (noun vs gerund) is curriculum-team preference. The combined-pair concept may also merge into a broader gardening / life-cycle canonical | Worksheet §13 `harvest` + `harvesting` entries claude_notes | Stage 2 re-tag merges the two into a single canonical (`harvest` or `harvesting` per curriculum-team verdict). May also fold into a broader canonical (life_cycles / plant_growth / seeds) depending on whether harvest-as-activity warrants its own canonical | Open |
| CON-22 | reading-cluster boundary (§13 `reading` + `reading_comprehension` ref + `narrative_reading` + `biography_reading` + `informational_text` + `biography` entries) | Six entries cover reading-side practices and genres with overlapping semantics: `reading` 2 (generic), `reading_comprehension` 6 (§12 — comprehension lens), `narrative_reading` 1 (narrative-genre lens), `biography_reading` 1 (biography-genre lens), `informational_text` 1 (nonfiction-genre lens), `biography` 1 (genre-as-noun). Boundary questions: (a) is `reading` 2 redundant with `reading_comprehension` 6? (b) are the genre-specific reading entries (narrative_reading + biography_reading + informational_text) sub-applications of reading_comprehension, or distinct genre-canonicals? (c) does the biography pair (`biography` + `biography_reading`) collapse into a single canonical? Cluster of 6 entries needs unified resolution | Worksheet §13 `reading` + `narrative_reading` + `biography_reading` + `informational_text` + `biography` entries; §12 `reading_comprehension` 6 entry | Curriculum-team verdict at Stage 1 fill — preferred approach: (a) merge `reading` 2 into `reading_comprehension` 6 as a label-shortening alias; (b) merge genre-specific reading singletons into `reading_comprehension` 6 as sub-application aliases (narrative_reading + biography_reading + informational_text → reading_comprehension); (c) merge `biography` 1 + `biography_reading` 1 into single canonical (likely fold into §11 `storytelling` 76 if biographies are read-aloud narratives OR into `cultural_narratives` 8). Stage 2 re-tag implements the chosen consolidation | Open |
| CON-23 | measurement-cluster boundary (§13 `volume` freq-2 + `area` + `weight` + `perimeter` freq-1 entries) | Four entries cover specific measurement dimensions (`volume` 2, `area` 1, `weight` 1, `perimeter` 1) alongside §11 `measurement` 66 (the established broad canonical). Boundary question: does `measurement` 66 absorb the sub-dimension singletons as merge_aliases (treat measurement as the canonical home for all measurement lessons), OR keep the sub-dimensions as parallel narrower-specific canonicals (volume / area / weight / perimeter as standalone Math concepts)? The 5-entry cluster needs unified resolution | Worksheet §13 `volume` (freq-2) + `area` + `weight` + `perimeter` (freq-1) entries; §11 `measurement` 66 entry | Curriculum-team verdict at Stage 1 fill — preferred approach: merge sub-dimensions into §11 `measurement` 66 as merge_aliases (measurement is the established broad canonical; the sub-dimensions are specific applications). Alternative: keep parallel canonicals if curriculum team wants explicit measurement-dimension tagging. Stage 2 re-tag implements the chosen consolidation | Open |
| CON-24 | figurative-language cluster boundary (§13 `figurative_language` + `similes` + `descriptive_language` + `sensory_details` entries) | Four Lit/ELA singletons cover figurative-language craft with overlapping semantics: `figurative_language` 1 (broad), `similes` 1 (specific device), `descriptive_language` 1 (sensory-language frame), `sensory_details` 1 (sensory-imagery frame). The §11 `vocabulary_development` 59 covers broader word-craft. Boundary question: does the cluster consolidate under `figurative_language` 1 (with similes + descriptive_language + sensory_details as sub-type aliases), OR fold the whole cluster into §11 `vocabulary_development` 59, OR keep separate canonicals? `literary_elements` 1 (also long-tail) is plausibly part of a broader literary-craft cluster including these | Worksheet §13 `figurative_language` + `similes` + `descriptive_language` + `sensory_details` + `literary_elements` entries; §11 `vocabulary_development` 59 entry | Curriculum-team verdict at Stage 1 fill — preferred approach: (a) consolidate 4-entry figurative-language cluster under `figurative_language` 1 as canonical; OR (b) fold cluster into §11 `vocabulary_development` 59 as broader word-craft canonical; OR (c) keep `literary_elements` 1 as a separate literary-craft canonical and consolidate figurative-language entries beneath it. Stage 2 re-tag implements the chosen consolidation | Open |

## Closed signals

(None yet — register initialized Session 79.)

---

*Initialized Session 79 (2026-05-12) during PR-Concepts-1 fill. Session 80 (2026-05-12) appended CON-12 through CON-15 during PR-Concepts-2 mid-tier fill. Session 81 (2026-05-12) appended CON-16 through CON-24 during PR-Concepts-3 long-tail fill (24 open signals total). Updates land at PR closeout backfill per the Stage 1 PR closeout checklist convention.*
