# PR 6 — Stage 2 Corpus Re-tag (PR 3b folds in) Execution Status

**Last updated:** 2026-06-16 by **Session 19 (PR C2 BUILD — C2.1 + C2.2 + U-1 resolution DONE + supervisor-verified; GATE 2 Codex review running).** PR C1 DONE + LIVE ON PROD (#509 → `f3f419c`). PR C2 (data apply) IN BUILD on `feat/pr6c-stage2-retag-apply`, **NOT pushed**. **Done this session:** C2.1 `f6308a2` (heritage corrections manifest + emitter wiring + grade guard, TDD); C2.2 `3b2ac6c` (apply migration `supabase/migrations/20260617000000_pr6c2_retag_apply.sql`, 8 load-bearing sections: rollback snapshot incl `content_text` → 753 grade-guarded dual-write UPDATEs → Who's-Who `content_text` repair → retired `15T4wU94…` Food Justice→Social Justice fold → 6 small-field CHECK constraints → U-1 assertion → ANALYZE; `supabase/seed.sql` canonicalized in 5 demo lessons so the new CHECKs pass `db reset` — no test depends on old values); **U-1 fix-up `b5bf020`** (below). **MAJOR FINDING — supervisor-verify gate caught what local testing structurally can't:** the migration's U-1 hard-fail assertion would have ABORTED the apply on REAL data; local `db reset` (5 demo rows) passes trivially. A read-only PROD probe found 41 legacy U-1 violators (North American + an Indigenous/AA identity label); the re-tag's specific-only output auto-fixes 33, leaving **8** = 7 staged rows where Fable's OWN output still paired North American+Indigenous, + 1 non-staged deletion-slated corpus-exclusion ("Soul Food Sunday" `1voTOBrqiz…`). **USER DECIDED 2026-06-16:** (a) drop `North American` from the 7 (manifest → 11 corrections); (b) scope the U-1 assertion past the 12 `corpus-exclusions.json` ids. Fix-up VERIFIED: PROD-simulation post-apply U-1 = **0**; the 12 embedded excluded ids EXACT-match corpus-exclusions.json; `db reset` clean (NOTICE "excluding 12…"); 68/68 runner tests; type-check+lint 0; 753 UPDATEs + grade guard (728/25) unchanged by the fix-up. **PR #510 OPEN** (https://github.com/danfeder/esnyc-lesson-search-react/pull/510, pushed end of Session 19). Both pre-push gates CLEAN: **GATE 2** Codex adversarial review (gpt-5.5/xhigh, `--base origin/main`) = *approve, no findings*; **GATE 3** independent Claude code-reviewer = *SHIP* (mechanized: all 753 dual-writes consistent, rollback snapshot set-equal to touched rows, U-1 `v_excluded` set-equal to corpus-exclusions.json, 6 CHECK vocabs byte-equal). **Pre-flight PROD read-only probes (both pass):** post-apply U-1 = 0; CHECK-install blockers = 0 (census re-confirmed current: 113 live non-canonical rows all in the staged-753 set + 1 retired straggler fold-fixed → all 6 CHECKs install cleanly). Unit suite 1112/1112, runner 68/68, type-check+lint 0. **CI ROUND 1 DONE (E2E fixed, bot triaged):** E2E first FAILED at "Apply migrations to TEST" — `valid_academic_integration` CHECK violated by **6 PROD-absent stale TEST rows** with legacy kebab-case values (`social-studies`, `kitchen-skills`, …) not in the staged-753 corpus → TEST↔PROD data divergence (my pre-flight census was PROD-only, correctly 0 there). Fixed by: (1) triggered `reset-test-db.yml --reset_type=full` (PROD→TEST sync, run `27662896603`) — it ran but the 6 survived (the data-only restore has NO truncate, so it can't delete TEST-only drift — the systemic cause); (2) confirmed the 6 are FK-isolated (0 inbound refs) + PROD-absent → **DELETE'd them on TEST** via MCP. Re-verified TEST: 0 non-staged blockers (100 staged + 1 fold-fix straggler). Re-ran E2E → **PASS** (4m13s). **TEST migration MCP-verified (verbatim ids):** 6 CHECKs installed, U-1 live-violators=0, Fattoush 5-tags, U-8/U-12 `[]`, Arroz 3-tags, NA-drop `1zrWJ0`→`Indigenous|Three Sisters traditions`, retired fold has-FoodJustice=false, Who's-Who len 3236, rollback rows 732 (TEST missing 22 of 754 = known divergence; PROD coverage full). **Bot review: `claude[bot]` *Approve*, 5 below-bar findings, 0 code changes** (triaged + reply posted `#issuecomment-4725570263`). Security Audit red = pre-existing `main` dep-CVE (unrelated). **NEXT = USER Protocol-B sign-off on `pr6-retag-grade-diff.md` + diff/spot-check (gates merge)** → merge → USER PROD approval → verbatim-id PROD verify (U-1=0, grade 728/25, 6 CHECKs, Who's-Who, fold, 11 heritage corrections, rollback count) → known SASL flake rerun pattern. C2.4 embeddings DEFERRED. Then PR D (synonyms), PR E (drop rollback tables). **All merges + PROD approvals USER-GATED. This session DB writes: 1 TEST DELETE (6 FK-isolated PROD-absent drift rows) + local `db reset`; PROD read-only only.** **Follow-up (separate small PR): fix `reset-test-db.yml` full mode to truncate-before-restore (or delete rows absent from the PROD dump) so TEST is a FAITHFUL mirror — the no-truncate gap is why TEST drifts.** Pending housekeeping: archive C1 Sessions 16-17 entries.

> **▶▶ DIRECTION PIVOT (2026-06-16, Session 12) — heritage storage + filter, decided interactively with the user. READ THIS FIRST.**
> A pre-build investigation of the apply tooling surfaced (and VERIFIED against PROD) that **applying the re-tag's heritage output as-is would silently break the hierarchical Cultural Heritage filter.** Root cause: the live `search_lessons` RPC expands a selected parent only ONE tier deep against a 6-row `cultural_heritage_hierarchy` table that has NO `Americas`/`North American`/`Caribbean` parent and skips middle tiers; the filter works today ONLY because every lesson redundantly stores its full ancestor chain (PROD-confirmed: Chinese = `["East Asian","Asian","Chinese"]`, Mexican = `["Mexican","Latin American","Americas"]`). The re-tag emits leaf-heavy/inconsistent heritage (Fattoush = `["Middle Eastern","Levantine","Lebanese",…]`), so storing it as-is drops the chains and narrows the filter.
> **USER DECISION (4 walkthrough rounds, plain-language):** do the cleaner long-term **Option B** — store **specific-only** heritage + rebuild the live filter to do **complete recursive expansion from one source of truth** (the §16 `cultural-heritage.vocab.json`, which is already a complete, correct multi-tier tree with the identity-anchor baked in: African American + Indigenous root under `Indigenous and Diaspora`, never under North American → **U-1 satisfied structurally**). NOT Option A (full-family storage, never touch filter). Site is in a **maintenance window (users told not to use it; open-ended runway)** → transient inconsistency is harmless, so we go straight for the clean end-state. **Build order = FILTER-FIRST** (rebuild + verify the smart filter against CURRENT prod data — it's backward-compatible, can only return more-correct results — THEN the data apply with specific-only heritage).
> **WHAT THIS CHANGES:** re-opens design §7 ("filter UI / heritage hierarchy out of scope" — now partially IN), OQ7 (heritage storage shape → specific-only + smart filter), OQ13 (PR breakdown gains a heritage-filter-rebuild PR before the apply). The $121 re-tag is NOT wasted — leaf tags are identical regardless of storage shape. The targeted drops (U-5/U-8/U-12) still apply as specific-tag removals; U-1 needs no special guard under Option B (the hierarchy can't route AA/Indigenous to North American). A post-apply integrity assertion still asserts no AA/Indigenous row stores `North American`.
> **NEXT = heritage-filter-rebuild DESIGN pass (brainstorming-first).** The substance is unifying THREE divergent hierarchy copies that disagree today (UI `filterDefinitions.ts` slugs · PROD `cultural_heritage_hierarchy` 6-row Title-Case table · legacy `filterConstants.ts` `CULTURAL_HIERARCHY`) into one source of truth + recursive expansion + UI alignment + E2E. NO filter/apply code until that design is written + checkpointed with the user. STILL PENDING from cycle-start ritual: archive Sessions 6-11 to the archive file (housekeeping). Pre-build investigation artifacts were two read-only Agent reports (apply-tooling map + filter-mechanism map) — conclusions captured in this pivot block + the Session-12 log entry; not committed as files.
>
> **▶ UPDATE (Session 13, 2026-06-16): the design pass is DONE + LOCKED.** Design doc `docs/plans/2026-06-16-heritage-filter-rebuild-design.md` (committed `71cfdb6`). Four user decisions: (1) UI scope = **regen from vocab.json `top`+`sub` tiers (~31 nested options)**; (2) mechanism = **DB-side live recursive expansion** from the vocab.json-generated tree, keep Title-Case label storage + `alias_map` bridge; (3) **PR C1 (filter) before C2 (apply)** split confirmed; (4) display = **nested/indented tree**. Full PR C1 task list (C1.0–C1.10, TDD) authored in the impl plan. **NEXT = BUILD PR C1.** NO data writes in C1. The PR-breakdown "C0/C" naming is retired → **C1 (filter) / C2 (apply)**.

## Session 19 (2026-06-16) — PR C2 build start: C2.1 done + verified

- **C2.1 ✅ `f6308a2`** (Opus executor subagent, fresh context; supervisor-verified in the main loop). Heritage corrections manifest + emitter wiring + grade guard, TDD (+21 → 65 tests in `prepare-apply.test.ts`).
- **Manifest** `scripts/stage2-retag/data/heritage-corrections.json` — 4 LOCKED corrections; executor re-cross-checked every id + run-heritage array verbatim against `artifacts/full-run.fable.jsonl` (each present exactly once; matched). Confirmed the wrong Arroz id `055b000b…` has 0 matches; correct = `1VShcRmc…`. Salsa Toasts: no correction.
- **Emitter** `prepare-apply.ts`: `loadHeritageCorrections` (mirrors `loadCorpusExclusions`, Zod, dup-id reject); `applyHeritageCorrection` called INSIDE `comparedToStagingRow` BEFORE the `setsEqual` change check (line ~291) → drop-to-empty rows (U-8/U-12 `[…]`→`[]`) are still detected as changed and staged. Single corrected value flows into CSV + staging SQL + draft migration.
- **Grade guard (Codex finding #2, DATA SAFETY):** `applyUpdate` computes `writeGrades = row.gradeLevels.length > 0` and gates BOTH the `grade_levels` column write AND the `metadata.gradeLevels` jsonb_set on it — empty-run-grade rows keep their existing PROD grades instead of being blanked to `[]`. `academic_concepts` JSONB-only exception intact.
- **Grade-diff review artifact** `artifacts/pr6-retag-grade-diff.md` (committed/force-added — Protocol-B surface for C2.3) + committed `data/prod-grades-census.json` (753-row "before" snapshot from a read-only PROD SELECT, since the corpus snapshot lacks current grades). **MEASURED: 728 grade writes (132 real value-changes + 596 no-op same-value) / 25 preserved.** This refines the plan's approximate 731/33 → impl plan C2.2/C2.3 updated to 728/25.
- **Supervisor verification (load-bearing):** independently re-ran `type-check` (0) + `lint` (0) + `vitest prepare-apply.test.ts` (65/65); spot-checked the regenerated staging CSV — Fattoush = `[Middle Eastern,Levantine,Lebanese,Syrian,Palestinian]` (changed), Arroz = `[Puerto Rican,Caribbean,Latin American]` (changed), U-8/U-12 = `[]` (changed); read the ordering + grade-guard source hunks directly. Confirmed SQL/CSV/draft are gitignored (PR-B "stay local" precedent); only manifest + grade-diff + census + test/source committed.
- **Process note:** the executor's id/heritage verbatim re-cross-check (not trusting the plan's transcription) is the right default for a data-safety apply; it independently re-confirmed the Session-18 ID correction.
- **C2.2 ✅ `3b2ac6c`** (Opus executor; supervisor-verified). Migration `20260617000000_pr6c2_retag_apply.sql` (12,489 lines, floor `20260616000000`), 8 load-bearing sections (header above). 753 per-lesson UPDATEs copied verbatim from the C2.1-regenerated draft (heritage-corrected + grade-guarded). Companion edit: `supabase/seed.sql` canonicalized 3 constrained fields in the 5 demo lessons (`cooking_methods`/`core_competencies`/`social_emotional_learning`) so the new CHECKs don't reject the dev seed during `db reset` — supervisor confirmed every substituted value is in `smaller-fields.vocab.json`, and `db reset` running seed AFTER the CHECKs double-proves the column→value mapping; grep confirmed NO test/E2E depends on the old seed values. Supervisor read every structural section (rollback DDL, snapshot, Who's-Who `$whoswho_body$` dollar-quote with no body collision, fold-fix column+metadata, 6 CHECKs vocab-verbatim, U-1 derive-not-hand-list), re-ran `db reset` + `test:rls` + type-check/lint independently. **Idempotent** (executor re-applied 2nd time: INSERT 0 0, no constraint re-add, no double-effect).
- **U-1 fix-up `b5bf020`** (Opus executor; supervisor-verified). **Why:** the supervisor-verify gate ran the U-1 assertion's logic against REAL data (read-only PROD probe) — something local `db reset` can't do (5 demo rows). Found the assertion would **abort the apply on 8 rows**. Resolution per the two user verdicts: (a) 7 staged rows → `drop:["North American"]` added to the manifest (now 11 corrections; each id's staged heritage cross-verified in the staging CSV before trusting); (b) `v_excluded text[]` of the 12 corpus-exclusions added to §7 with `<> ALL(v_excluded)` on BOTH column + metadata sides + updated NOTICE. Regenerated artifacts → re-extracted §3 (only the 7 rows' heritage lines changed; 753 blocks + grade guard 728/25 unchanged). **Supervisor re-verified:** PROD-simulation post-apply U-1 = **0** (7 fixed + Soul-Food-Sunday excluded); 12 v_excluded ids EXACT-match corpus-exclusions.json; `db reset` NOTICE "excluding 12…"; 68/68 tests; type-check+lint 0.
- **Process learnings:** (1) **A corpus-data integrity assertion in a migration is INVISIBLE to local `db reset`** when the local seed lacks the corpus rows — its real behavior MUST be pre-checked with a read-only PROD probe (simulate post-apply state = staged values for changing rows + current values for the rest) BEFORE push, or the migration aborts on TEST/PROD. This is the load-bearing supervisor-verify pattern paying off. (2) An UNPUSHED migration committed locally can be EDITED in place (the database-migrations "never edit pushed" rule starts at the first PR push) — so the U-1 fix-up amended `20260617…` directly, no fix migration. (3) The re-tag's specific-only output organically healed 33/41 legacy U-1 violations (it drops the legacy ancestor chain) — the residue was the model's own North-American+Indigenous co-tagging (7) + 1 out-of-scope exclusion.
- **GATE 2 (Codex adversarial review of the migration, pre-TEST) — CLEAN.** gpt-5.5/xhigh via the companion (`--base origin/main --scope branch`), focused on idempotency / section-ordering / quoting / rollback completeness / grade guard / U-1 scoping / dual-write / CHECK-install. Verdict: **approve, no material findings**. (Note: local `main` is stale at `44da8f6`; `origin/main`=`f3f419c` is the correct C1 baseline / merge-base, so `origin/main...HEAD` is the C2-only diff.)
- **GATE 3 (independent Claude code-reviewer, pre-push) — SHIP.** Mechanized (not eyeballed): parsed all 753 UPDATEs → 0 dual-write mismatches; rollback snapshot IN-list set-equal to the 754 touched ids; U-1 `v_excluded` set-equal to corpus-exclusions.json with 0 overlap on re-tagged lessons; 6 CHECK vocab arrays byte-equal to `smaller-fields.vocab.json`; Who's-Who dollar-quote safe; seed.sql in-vocab. 0 material findings; below-bar hardening default-rejected. Re-ran type-check/lint/vitest itself.
- **Pre-flight PROD read-only probes (supervisor, data-safety — the same pattern that caught U-1):** (1) post-apply U-1 simulation = **0** violators; (2) **CHECK-install census re-confirmed**: 352 non-canonical (field,row) appearances across 114 lessons = 113 live + 1 retired; ALL 113 live are in the staged-753 set (apply canonicalizes them), the 1 retired = `15T4wU94…` `Food Justice` (fold-fixed) → **0 CHECK-install blockers**. Matches the Session-18 plan census exactly; still current.
- **Pushed + PR #510 opened** (`git push -u`, `gh pr create --base main`). Full unit suite 1112/1112 pre-push. PR body documents the data-apply nature, the 8 sections, the U-1 finding+resolution, the pre-flight verifications, and the Protocol-B review surfaces. Status-doc checkpoint committed locally but NOT pushed (no docs-only CI burn during an open PR — bundle with the first bot fix-up).
- **Process learning:** before pushing ANY migration that adds a table-wide CHECK constraint, re-run the read-only PROD non-canonical census (not just trust the plan's earlier census) — a CHECK install fails the whole migration on TEST/PROD if even one un-migrated live/retired row holds a non-canonical value. Cheap pre-check vs. a failed TEST CI run on a pushed (locked) migration.
- **CI round 1 — E2E FAILED then FIXED (the PROD-census-was-not-enough corollary):** E2E failed at "Apply migrations to TEST" — `valid_academic_integration` violated by **6 PROD-absent stale TEST rows** (legacy kebab values, not in the staged-753 corpus). My pre-flight CHECK-install census was PROD-only (correctly 0 there), but **TEST data diverges from PROD** — the exact "TEST is missing/extra rows" caveat the plan flags. Fix: triggered `reset-test-db.yml --reset_type=full` (PROD→TEST sync) — it ran (success) but the 6 SURVIVED because its restore is `db dump --data-only` + `psql` with **NO truncate**, so it adds PROD rows but never deletes TEST-only drift (the 2026-06-14 weekly sync had "succeeded" with the drift intact — the tell). Confirmed the 6 are FK-isolated (queried all 6 FK cols referencing lessons → 0 refs) + PROD-absent → DELETE'd them on TEST. Re-census: 0 non-staged blockers. Re-ran E2E (`gh run rerun --failed`, no code change — env fix) → PASS. MCP-verified the migration's effects on TEST (6 CHECKs, U-1=0, all corrections, fold, Who's-Who).
- **Bot review round 1: `claude[bot]` *Approve*, 0 code changes.** 5 findings all below the user-visible-bug/DB-risk bar (no-explicit-BEGIN/COMMIT [Supabase auto-wraps; rollback proven by the failed-apply], JSONB-mirror-not-CHECKed [precedent tradeoff], numbered-rollback-comment [don't-edit-pushed], nested jsonb_set [machine-gen+verified], Zod-set-empty-string [already guarded]). Triage reply posted to the PR. Round-cap effectively reached (Approve + 0 changes → no fix-up push → no round 2).
- **Process learnings (this round):** (1) **PROD-only pre-flight is necessary but NOT sufficient for CHECK-constraint migrations** — TEST can carry extra/divergent rows the apply doesn't canonicalize; either pre-census TEST too, or accept that the CI TEST-apply is the gate that catches it. (2) **`reset-test-db.yml --reset_type=full` does NOT make TEST a faithful PROD mirror** — no truncate, so TEST-only rows persist forever; worth a workflow fix (truncate-before-restore) as its own PR. (3) An env-only fix re-runs via `gh run rerun --failed` with NO code change — keeps the migration (already GATE2/GATE3-clean) untouched.
- **NEXT = USER gates:** Protocol-B sign-off on `pr6-retag-grade-diff.md` + diff/spot-check → merge → PROD approval → verbatim-id PROD verify. DB writes this session: 1 TEST DELETE (6 drift rows) + local `db reset`; PROD strictly read-only.

## Session 17 (2026-06-16) — PR C1 bot round 1 triaged + fix-ups pushed

- **Orientation:** PR C1 (#509) OPEN, CI all-green except the known pre-existing `main` Security Audit (CVEs in `@babel/core`/`basic-ftp`/`brace-expansion`/`dompurify`; C1 touches no package.json). Previously-flaky E2E now PASSES (`4442d14` fix held). Bots reviewed latest commit `4442d145` (confirmed via Netlify preview). Git reconciled: local `09de741` (Session-16 final checkpoint) was ahead-1 unpushed — expected (no-docs-push-during-open-PR); bundled into this round's push.
- **Bot round 1 collected from all 4 surfaces:** 3× `claude` + 1× `copilot` review, all "well-engineered, safe to merge." **9 distinct findings, 0 user-visible bugs, 0 DB-safety regressions** (full rebuttal pass written).
- **2 fix-ups ACCEPTED (user verdict via AskUserQuestion) → `e5015cd`:** (a) **globals devDependency** — `eslint.config.js` imports `globals` (added C1.3 for the `.mjs` Node-globals block) but only present transitively; declared `^14.0.0` (already-resolved version, lockfile +1 line, no resolution change, no CVE). Executes the PR-body pre-registered "address only if a bot raises it" rule (Copilot raised it); a fresh install could otherwise break `npm run lint`. (b) **HeritageNode dedup** — `filterUtils.ts` + `IntCulturalHeritageSection.tsx` each re-declared a `HeritageNode` interface structurally identical to the generated `HeritageOption` (3rd copy); both now import `HeritageOption`. Verified `HeritageOption` structurally identical before swapping; generator's internal Zod-derived `HeritageNode` is a different type → untouched.
- **7 findings REJECTED below-bar** (written rationale, internal-tool calibration): renderNode→named-component (bot says non-blocking at 31 nodes), `ancestorsBySlug` rename (doc-comment already states `[self,…ancestors]`), `VOLATILE` vs `STABLE` (signature intentionally preserved to not touch `search_lessons`), unit-test-clicks-input-vs-label (bot: "not a correctness bug"), `getCultureDescendantValues` unused (tested + harmless; design uses DB-side expansion so not "scaffolded for C2"), `GRANT ALL` to anon (one bot tagged Medium but its own line-comment downgrades to "fine/intentional"; **matches the prod baseline this rebuild preserves, RLS restricts writes to admins → no new exposure**), `facetCounts.ts:118` fallback (bot confirms correct).
- **Verification:** type-check + lint clean (whole repo); full unit suite **1089/1089**; targeted filterUtils (10) + IntCulturalHeritageSection (4). No DB-affecting change → no TEST re-verify needed this round.
- **Process learning:** when a PR body pre-registers a contingent fix rule ("fix X only if a bot raises it"), a round-1 bot hit on X converts the deferral into a clean, non-judgment ACCEPT — distinct from the usual default-reject-below-bar posture.

**Round 2 (deeper claude pass; Copilot did not re-review) — TRIAGED, 0 code changes:** all round-2 CI green except the unchanged pre-existing Security Audit; previously-flaky E2E now PASSES on the fix-up commit (stable). Bot verdict "Must-Fix: None / 0 blocking correctness bugs." 8 items raised:
- **Should-Fix #1 DROP-CASCADE pre-flight on TEST → VERIFIED** (read-only TEST MCP after migration applied): 0 inbound FKs from other tables, 0 triggers, 0 views, only `expand_cultural_heritage` references the table, migration `20260616000000` in TEST history, 71 rows / 6 roots. The `DROP … CASCADE` is provably safe. Posted as evidence on the PR.
- **Migration-level (all on the ALREADY-PUSHED migration → would need a NEW fix migration, not an edit):** `CREATE TABLE` "not idempotent" REJECTED (rebutted — `DROP … IF EXISTS … CASCADE` unconditionally clears before CREATE; the suggested `IF NOT EXISTS`+`ON CONFLICT` would be *less* safe, masking stale schema + seed bugs); seed-SQL missing `public.` prefix REJECTED (generated fragment embedded verbatim, never run standalone, resolves under the migration `search_path`); missing inline rollback SQL REJECTED for C1 (schema is deterministically reconstructible from vocab.json + the C1.0 baseline artifact captures the prior function/table verbatim; rolling back to the old 6-row table would re-break the filter).
- **`GRANT ALL`→`SELECT` (+ fold-in `STABLE`) — DEFERRED to C2/grants-hygiene (USER VERDICT 2026-06-16).** TRUNCATE-bypasses-RLS is technically true but ~nil under PostgREST (no TRUNCATE endpoint for anon/authenticated); matches the prod baseline (no new exposure); project-wide posture, not heritage-specific. Address uniformly later (C2 already opens a migration on this table, or a dedicated grants pass) rather than bolt a security-posture change onto the filter rebuild.
- **Frontend:** `aria-hidden` on `int-check-count` (a11y) DEFERRED to the a11y follow-up (real but pre-existing + project-wide across every filter section; internal admin UI; component matches the prior one exactly — a heritage-only fix would be inconsistent); `buildCultureLabelMap` shared-singleton REJECTED (two call sites build the same static map at init but call the *same* function so can't diverge — minimal value); `getCultureDescendantValues`-unused + `renderNode`-closure already triaged in R1.
- **Round-cap reached** (2 rounds). 0 code changes in R2 → nothing to push → no round 3. **C1 is REVIEW-CLEAN.**
- **Process learning:** an already-pushed migration converts every migration-level review nit into "would need a *new* fix migration" — which raises the bar for accepting them on the same PR (a one-line-looking change is actually +1 migration + TEST re-apply + re-verify + another bot round). Worth stating up front when triaging migration findings.
- **MERGED + PROD-APPLIED + PROD-VERIFIED (2026-06-16, user-approved both gates).** User authorized merge → squash-merged #509 to main as `f3f419c` (matched PR-B #508 squash precedent; full C1.0–C1.10 history preserved on the PR). Merge triggered migrate-production run `27650220635`; user approved the `production` environment gate; **Apply to Production succeeded on the FIRST attempt — no SASL flake this time** (Check Migration Changes + Verify Recent Backup both green first). **PROD post-apply MCP verification (read-only, identifiers verbatim from the migration) PASSED:** `20260616000000` in PROD history; 71 rows / 6 roots; `expand_cultural_heritage(['asian'])`→15 labels (incl. Taiwanese/Uzbek deep leaves the old flat table lacked = recursion proven), `['chinese']` slug→`Chinese`, `['Chinese']` label→`Chinese`, `['african-american']`→`African American/Black culinary history/Soul Food`; end-to-end live counts (`cultural_heritage && expand(_alias(...))`, `retired_at IS NULL`) EXACTLY match C1.9 anchors: asian 74 / chinese 15 / mexican 38 / italian 25 / african-american 27 / indigenous-and-diaspora 58 / mediterranean 44. **The heritage filter is healed + live on PROD.** C1 IS DONE.
- **NEXT = PR C2** (data apply). **C2-setup:** `feat/pr6c-stage2-retag-apply` predates C1 → merge/rebase main first. Carry: 3 targeted heritage drops (U-5 Fattoush, U-8 Alternative Proteins, U-12 Intro to Salad — verbatim IDs from `full-run.fable.jsonl`), specific-only heritage storage, deferred `GRANT ALL`→`SELECT` + `STABLE` fold-in, the C1-flagged C2 error candidates (Arroz con Gandules spurious South Asian; two Salsa Toasts → []). Then PR D (synonyms), PR E (drop PR-5 rollback tables). **All merges + PROD approvals remain USER-GATED.**

## Session 16 (2026-06-16) — C1.7 + C1.8 + C1.9 done; PR C1 OPENED (#509)

- **C1.7 ✅ `c5ca119`** — retired orphaned `CULTURAL_HIERARCHY` (whole `src/utils/filterConstants.ts` deleted; supervisor-verified the original had exactly 1 export + zero `.ts/.tsx` importers; type-check+lint green).
- **C1.8 ✅ `eb8bfaa`** — E2E `e2e/cultural-heritage-filter.spec.ts` (6 tests, structural + superset). **User-decided scope** (the literal "deep-descendant-only lesson" assertion is unsatisfiable pre-C2). Supervisor independently re-ran: 6/6 pass.
- **C1.9 ✅ `9dc9bdb` (+ adjudication `ad1b926`)** — backward-compat apply gate, PROD-read-only: **21 HEAL / 9 SAME / 1 vocab-correction** (`mediterranean` −1 = Moroccan re-homed to North African). Supervisor re-verified the lone −1 vs live PROD; **user approved PASS-with-correction**.
- **C1.10 (tail) — PR #509 OPEN, pushed clean.** Pre-push = a **5-dimension review workflow + adversarial per-finding verify** → CLEAN (0 meet-bar; 2 below-bar default-rejected: no-op slug-keyed label map; transitive `globals` dep). Docs checkpoints `e61c34f`, `ad1b926`, + a local Session-16 close commit (unpushed). Next session: TEST-DB verify + bot triage.
- **Process learnings:** (a) **E2E data-fragility** — the pre-C2 corpus stores full ancestor chains, so a deep-descendant-ONLY retrieval proof needs C2 data; the right pre-C2 UI scope is structural + monotonic-superset, with recursion-correctness owned by C1.3 (DB) + C1.9 (gate). (b) **C1.9 PROD-read-only pattern** — derive each option's new expanded label-set from the REAL local `expand_cultural_heritage(_alias_cultural_heritage(...))` (C1.2 is applied locally; expansion is data-independent), then count read-only against PROD — faithful without deploying. (c) The **"never fewer" gate needs a correction-vs-narrowing distinction**: a vocab-aligned fix can legitimately drop a mis-parented lesson from one filter while keeping it discoverable under its correct parent (escalate, don't auto-pass; user adjudicates). (d) Under ultracode, the **fan-out review workflow** (dimension finders → adversarial verify, calibrated to the acceptance bar) is the right pre-push reviewer for a large PR — kept supervisor context light and produced verified, deduped findings.

## Session 11 (2026-06-15) — B6 gate CLEARED: 13 user verdicts + register apply

- **13-question walkthrough COMPLETE** (interactive, one-at-a-time, Session-7 style). All U-1..U-13 settled by the user; recorded in `register-adjudication.md` "USER VERDICT NEEDED" verdict table (`47e6449`). **10 accept-the-re-tag-as-is; 3 targeted PR-C apply-time fixes:** U-5 (Fattoush `1Dz-Jv4…` drop Israeli+Jordanian), U-8 (Alternative Proteins drop Italian/European), U-12 (Intro to Salad Project `1V2Xt4c…` drop Middle Eastern). U-1 carries an apply-time guard: PR-C parent-expansion must NOT add `North American` to AA/Indigenous leaves (identity-anchor confirmed). **User closed EVERY "log a future vocab/filter-UI follow-up" option → NO new future-track items**, only the 3 targeted fixes. Re-tag output is FINAL ($121 B4 run); these were dispositions, not re-runs.
- **74-signal bundled register apply ✅ `2767f83`** (Opus executor, supervisor-verified): all 74 signals across both `*-audit-signal-register.md` moved Open → Resolved/Deferred per the B6 per-row mapping + the 13 verdicts. **69 Resolved** (R 24 re-tag, V 18 vocab-lock, U 19 user-verdict, P 5 partial, X 3 excluded) + **5 Deferred** (D 4 dedup track, B 1 body-content). Independently verified: zero `Open` rows remain (heritage 50→0, concepts 24→0); X-prefix/X-category trap navigated correctly (X-04 = partial, NOT excluded); folded U-rows byte-identical; targeted-fix flags present; commit scope exactly 2 files.
- **B7 ritual DONE + PR B OPEN as #508** (https://github.com/danfeder/esnyc-lesson-search-react/pull/508). Artifact-tracking (user-chosen): `git add -f` diff-report.full-fable.md + pr6-retag-spot-check.md + full-run.fable.jsonl (sole-source) + run-summary.full-fable.json (`ed2abfc`); SQL/CSV/draft-migration stay local. Pre-push fan-out review (4 dimensions + adversarial verify) → 3 LOW/optional findings, all fixed `78bad8e` (F1 grade docstring gap, F2 empty-staging-SQL guard +test, F3 scorer dedup +test). 471 tests, type-check+lint clean. 44 commits, **zero DB writes**.
- **NEXT = bot-review triage** (per-PR ritual steps 3-8): wait for external bots → collect from ALL 4 surfaces (issue-comments, review summaries, line-comments, checks/failed-run logs) → written rebuttal pass on every finding, default-reject hardening that fails the user-visible-bug-or-DB-risk bar → consolidated fix-ups (bundle this uncommitted status-doc edit with the first fix-up push; no docs-only push during open PR) → re-verify → round-cap after 2 bot rounds. **Merge is USER-GATED.** After merge: PR C (apply: staging→migration→embeddings, incl. the 3 targeted fixes with verbatim IDs) and eventually PR E (drop PR-5 rollback tables).
- **3 targeted PR-C apply-time fixes to carry (verify verbatim IDs vs full-run.fable.jsonl):** U-5 Fattoush `1Dz-Jv4cV0N0ntxZ8z0VcRgenI0hpZuOIDJuPbEvr7bI` drop Israeli+Jordanian · U-8 Alternative Proteins (EUR-07) drop Italian/European · U-12 Intro to Salad Project `1V2Xt4c…` drop Middle Eastern. Plus U-1 apply-guard: parent-expansion must NOT add `North American` to AA/Indigenous leaves.
- **PR B bot round 1 ✅ triaged (claude-review; all CI green except pre-existing Security Audit dep-CVE fail — PR B touches no package.json).** 4 findings, **ZERO code changes** (calibrated to no-DB internal-tooling scope; reply posted to PR). **F2 "12 missing lessons" VERIFIED not-a-bug** — the 12 `missingFromRun` IDs == the 12 `corpus-exclusions.json` entries EXACTLY (set-diff identical; intentional deletion-slated non-lessons + 404 tag-cards, per-id reasons in file; PR C builds staging from the 753 run records only → never touches them; 765 − 12 = 753). **F1 + F3 DEFERRED** as post-merge follow-ups (see out-of-scope list). **F4 rejected** (bot agreed safe). **Merge is USER-GATED — PR is review-clean, awaiting the user's merge call.** PR-C clarity note: a run-summary pointer to corpus-exclusions.json would pre-empt the F2 confusion.



## Session 10 (2026-06-15) — B5 apply-artifacts underway

- **CI test-guard fix ✅ `b2a2101`** (supervisor-verified): `it.skipIf(!existsSync(...))` on the 3 assertions reading gitignored artifacts (`answer-key.final.jsonl` in doc-surfaces.test, `corpus.jsonl` ×2 in sample-answer-key.test). CI-sim (artifacts moved away) = 417 passed / 3 skipped / **0 failures**; restored → 420. Closes the Session-9 pre-PR-B blocker.
- **B5a full-corpus diff report ✅ `18763c7`** (supervisor-verified): extended `generate-diff-report.ts` to honor corpus-exclusions (TDD, +5 tests → 425). Repair/fallback selection already correct via `latestRecordById` last-line-wins. Report at `artifacts/diff-report.full-fable.md` (gitignored, 651KB plain-language). **Coverage: 753/753 compared, 0 without usable results, 0 Zod-fails.** All 753 lessons change ≥1 field. Biggest movers: **academic_concepts 736/753 (98%, by-design full re-tag), core_competencies 592, academic_integration 510, garden_skills 400.** Activity Type: **+craft 146 / −garden 105** (cosmetics-craft + tasting-conflation fix direction — flag for Protocol-B spot-check). `tags` empty in ALL 753 corpus rows → run proposes first-ever values (77 adds, nothing removed). Supervisor spot-checks (raw JSONL) exact: Bánh Mì `[cooking,garden]→[cooking]`, 2nd-Grade Decomp Pt2 `[garden]→[academic]`. Scorecard artifacts are git-tracked via `git add -f` (gitignore still covers the rest); diff report stays untracked pending the B5/B7 artifact-commit decision.
- **B5b prepare-apply ✅ `59f0d5c`** (supervisor-verified): `prepare-apply.ts` (TDD, +44 → 469 tests) emits 4 review-only artifacts (all gitignored, regenerate via `npx tsx prepare-apply.ts`): `pr6-retag-staging.{sql,csv}` (753 staged), `pr6-retag-apply.draft.sql` (1.6M DRAFT — NOT under supabase/migrations/; PR C owns the real one), `pr6-retag-spot-check.md` (90 samples, 30/30/30 buckets). Reuses `selectComparedLessons` (extracted from generate-diff-report) so staging tags == diff-report new values. Mirrors PR-5a emitter: snapshot `pr6_retag_rollback` (RLS, `ON CONFLICT DO NOTHING`) FIRST, then 753 idempotent absolute-value UPDATEs. **Independently verified in emitted SQL: academic_concepts column writes = 0 (JSONB-only per `column:null`), academicConcepts JSONB writes = 753, dual-write confirmed (text[] col + jsonb_set both present), grades staged but no old-vs-new (corpus lacks current grades — reviewed at apply).** No STOP-worthy discrepancies. **B5 COMPLETE.**
- **ARTIFACT-TRACKING DECISION DEFERRED to B7:** all PR-B data artifacts (diff report 651K, draft migration 1.6M, staging SQL 593K/CSV 437K, spot-check 97K) currently gitignored; run file `full-run.fable.jsonl` also gitignored (local-only). At PR-open, decide which review artifacts to `git add -f` (candidates: spot-check worksheet + diff report — the human-readable Protocol-B surfaces; SQL dumps stay local/regenerable). Scorecard precedent = force-add.
- **B6 adjudication artifact ✅ `29da364`** (supervisor-verified): `artifacts/register-adjudication.md` (git-tracked via `-f`) classifies all 74 signals. **Counts: R(resolved-by-retag) 24 · V(resolved-by-vocab-lock) 18 · U(needs user verdict) 19 rows→13 questions · P(partial) 5 · D(dedup track) 4 · X(excluded) 3 · B(body-content) 1 = 74.** Independently spot-checked 5 verdicts against raw run file (ASI-01 re-parent, X-02 no-North-American, X-06 Lenape backfill, ME-03 claims-vs-delivery survives, Soul Food excluded) — all hold. **HEADLINE: re-tag emits LEAF heritage tags only (no continental top-parents); AA/Indigenous root under `indigenous-and-diaspora` cluster so they won't get `North American` even post parent-expansion → de-facto identity-anchor convention (U-1). 0/26 AA + 5/30 Indigenous carry North American now (was 17/26, 23/30).** Heritage over-tag caveat applied honestly (ME-03 = clearest claims-vs-delivery survivor; AFR-04/06/07 widened the set). NO register edits yet — those wait for user verdicts then a bundled apply pass.
- **CURRENT GATE: 13 USER-VERDICT questions (U-1..U-13)** from B6 — these are DISPOSITION decisions (re-tag is final/$121 spent; not re-run decisions): accept-current-output-and-mark-resolved vs defer-to-future-track vs targeted-apply-time-fix. Biggest structural weight = U-1 (North American pairing convention, ties AME-01/X-02/X-03/X-07). U-3/U-4 (sub-region creation Irish/Yemeni/Persian) = filter-UI-track deferrals. U-2 = CON-16 cross-field. Full plain-language shortlist in `register-adjudication.md` "USER VERDICT NEEDED" section. **NEXT: user walkthrough of the 13 → record verdicts → bundled register status edits → B7 ritual → PR B.** No DB writes in PR B.

## Current State

> **⚠️ LOAD-BEARING ORIENTATION (C2 cycle, Session 18).** PR C1 (heritage filter) is MERGED + LIVE ON PROD (`f3f419c`); **PR A (#507) + PR B (#508) + PR C1 (#509) all merged.** The C2 branch `feat/pr6c-stage2-retag-apply` is set up — main `f3f419c` merged in (merge commit `0c5fef9`) so it carries C1's live recursive filter + the most-current scaffold docs. The **C2 apply plan is REFINED + CONCRETE** in the impl plan (`## PR C2 — Apply + embeddings`): C2.1 corrections manifest + emitter wiring (TDD) · C2.2 the apply migration `20260617000000_pr6c2_retag_apply.sql` (rollback snapshot → 753 dual-write UPDATEs, heritage specific-only → Who's-Who `content_text` repair → 1-row retired fold-fix → 6 small-field CHECK constraints → U-1 assertion → ANALYZE) · C2.3 TEST+PROD apply · C2.4 1-row Who's-Who embeddings. **All Session-18 decisions are locked in the impl plan** (4 verified heritage corrections w/ verbatim ids — Arroz real id `1VShcRmc…`, Salsa Toasts no-action; CHECK constraints SAFE — apply canonicalizes 113 live rows, only retired `15T4wU94…` needs the Food Justice→Social Justice fold; **embeddings DEFERRED + grades guarded+reviewed + U-1 derived-from-hierarchy — corrected by a Session-18 Codex adversarial review, see impl plan `## PR C2`**). **NEXT = BUILD C2.1.** No DB writes yet this cycle. All merges + PROD approvals USER-GATED. The DIRECTION PIVOT block at the top of this file remains the heritage-design rationale of record. **[Historical C1-build detail follows — superseded by this orientation; retained for reference.]** PR A (#507) and PR B (#508) are both MERGED to main. The heritage **Option B** pivot is decided AND its **design pass is COMPLETE + LOCKED** (Session 13, 2026-06-16; `docs/plans/2026-06-16-heritage-filter-rebuild-design.md`, `71cfdb6`). **BUILD PR C1 — heritage filter rebuild — is UNDERWAY** on branch `feat/pr6c1-heritage-filter` (created Session 14 off the `feat/pr6c-stage2-retag-apply` tip `d0c9e6a`, so it carries the design + pivot doc commits; `feat/pr6c-stage2-retag-apply` continues as the C2/apply branch). No lesson-data writes anywhere in C1; backward-compatible; ships before the C2 apply. The impl plan holds the full C1.0–C1.10 task list. **C1.0 ✅ DONE + supervisor-verified (`2f22dd5`):** read-only PROD baseline at `scripts/heritage/artifacts/heritage-filter-baseline.json` (git-tracked gate evidence) — verbatim defs of the 3 funcs + 6-row hierarchy table + before-counts for all 31 vocab top+sub options via the live `&&` heritage path (`retired_at IS NULL`, faithful to `search_lessons`). Keystone independently re-probed: **Asian=65 today**; 15/31 options return **0** today (slug-vs-label footgun — `_alias_cultural_heritage` only knows the 18 current-UI slugs), which C1.9 will heal (chinese 0→15, mexican 0→38, african-american 0→27, etc.). **C1.1 ✅ DONE + supervisor-verified (`b56f0b5`):** generator `scripts/heritage/generate-heritage-hierarchy.ts` (TDD, 13 tests) emits committed `src/utils/heritageHierarchy.generated.ts` (**31** nested top+sub options, 6 roots, zero internal-leak, zero label-mismatch — cross-checked vs vocab.json) + `scripts/heritage/artifacts/heritage-hierarchy-seed.sql` (**71** rows, 6 NULL-parent roots) for C1.2. Drift guard re-run by supervisor = byte-identical (generator pipes the TS through project Prettier so the committed file stays canonical). Registered `scripts/heritage/**` in `tsconfig.scripts.json` + ESLint. NOTE: existing `FilterOption` already has recursive `children?: FilterOption[]`, so C1.4 may need no type change. **C1.2 ✅ DONE + supervisor-verified (`3d83875` + fix-up `aabbd86`):** migration `supabase/migrations/20260616000000_heritage_recursive_expansion.sql` (LOCAL-applied) recreates `cultural_heritage_hierarchy` as `(key PK, label, parent_key→self)` + reseeds the 71-row vocab tree + rewrites `expand_cultural_heritage` as `WITH RECURSIVE` (seed on key-OR-label → recurse parent_key → return **labels** + defensive passthrough; signature/VOLATILE/INVOKER preserved so `search_lessons` + `_alias_cultural_heritage` unchanged). RLS (4 policies) + grants recreated from baseline. Pre-flight (supervisor PROD scan): **only `expand_cultural_heritage` references the table** — safe recreate. Independent local probes: asian→full 15-label subtree, chinese→`Chinese` (footgun fixed), african-american→`African American/Black culinary history/Soul Food`, NULL handled, 71 rows/6 roots/0 orphans. **Fix-up `aabbd86`:** generator now emits the SQL seed in deterministic topological order (depth ASC, freq DESC, key ASC) so the migration uses it VERBATIM (single-source-of-truth restored; the original hand-reorder rested on a false self-FK rationale — a single multi-row INSERT is order-independent); migration INSERT == fragment verbatim (71 rows, topologically valid), TS artifact byte-identical, +topological-validity test (15 generator tests). KNOWN: 2 pre-existing `test:rls` failures (`archive_duplicate_lesson` — unrelated to heritage; CI re-runs on TEST). **C1.3 ✅ DONE + supervisor-verified (`5e9282f`):** standalone LOCAL-DB script `scripts/heritage/test-heritage-expansion.mjs` (mirrors the `test:rls` idiom — NOT a vitest-with-DB test, which would break CI since the vitest suite is hermetic + CI runs `npm run test:coverage --run`); npm script `test:heritage`. Exercises the REAL `search_lessons` RPC end-to-end with specific-only fixtures; 6 assertions PASS (incl. case 1b `asian`→Taiwanese, a deep leaf absent from the OLD flat Asian children = clean recursion-RED; Soul Food under African American; slug `chinese`→label `Chinese` bridge; internal `black-culinary-history` matches; no false positives). RED demonstrated by reconstructing the old one-tier function in a rolled-back txn. Data-safety guard aborts on any non-local URL (exercised). Fixtures cleaned (independently MCP-confirmed 0 leftover). DB LAYER OF C1 NOW COMPLETE + PROVEN. **C1.4 ✅ DONE + supervisor-verified (`006776a`):** repointed `filterDefinitions.ts` `culturalHeritage.options` at the generated tree (removed the ~45-line hand-maintained 2-level inline block that omitted African American/Indigenous/Indigenous-and-Diaspora + mis-parented Ethiopian/Nigerian). `FilterOption.children` already recursive → **NO type change** (confirmed). Added recursive helpers to `filterUtils.ts` (`buildCultureLabelMap` + `getCultureDescendantValues`, TDD 10 tests RED→GREEN, terminating/acyclic). **DISCREPANCY RESOLVED (carry forward):** the impl-plan-named helpers `isParentCultureSelected`/`getCultureChildren` DO NOT EXIST in code — they're stale references in `src/utils/CLAUDE.md` only (→ doc-cleanup follow-up). `facetCounts.ts` does NO heritage tree walk (reads flat `meta.culturalHeritage`, L61-62) → correctly left untouched (NOT a miss). The REAL 2-level consumers were 3 display value→label lookups — `IntActivePills.tsx`, `IntListRow.tsx`, `IntLessonDetail.tsx` — now all use the shared recursive map (clean de-dup: `culturalLabel` exported from `IntListRow`, reused in `IntLessonDetail`; one-directional, no cycle). Client still sends kebab slugs verbatim (no client-side expansion added). Boundaries C1.5/C1.6/C1.7 untouched. Supervisor re-verified independently: type-check clean + lint clean + full suite 1073 tests green + facetCounts flat-read claim + circular-import check. **C1.5 ✅ DONE + supervisor-verified (`bfa23bd`):** rewrote `IntCulturalHeritageSection.tsx` — `renderNode(node, depth)` recurses over `node.children` to arbitrary depth (replaces the hardcoded 2-level block); indent is inline `paddingLeft: depth * 20px` (depth-1 = legacy 20px, grandchildren = 40px); `int-check--child` CSS dropped its fixed `padding-left` (now per-depth inline), keeps only `font-size:12px`. Per-node behavior unchanged: each node's checkbox toggles `toggleFilter('culturalHeritage', node.value)` with the kebab slug VERBATIM (no client auto-check); facet count `countFor(node.value) || ''` unchanged; caller `IntSidebar` needs no change. +RTL render test (4 cases). **Shared-CSS risk checked + cleared:** `int-check--child` is used ONLY by this component (grep-confirmed); base `int-check` untouched → no regression to other filter sections. **Visual verified by supervisor (chrome-devtools, local dev):** a11y tree shows all 31 nodes across 3 tiers incl. the previously-missing groups (Indigenous and Diaspora › African American / Indigenous › Lenape) + fixed mis-parenting (Ethiopian/Nigerian under W/N/E African); computed-style probe = 0/20/40px tiers; screenshot = clean stepped indent. type-check + lint + render test independently re-run green. **C1.6 FINDING (carry into the decision):** heritage facet counts render BLANK in local demo — BUT so do many other categories (Activity Type, Location, Thematic, Core Competencies, SEL, Cooking Methods); only Seasons + Academic Integration show counts. So blank heritage counts are PRE-EXISTING + broader than heritage (C1.5 renders counts identically to the old component — NOT a regression). Likely a slug(`node.value`)-vs-label(`meta.culturalHeritage` stores Title-Case) keying mismatch in `countFor`/facetCounts; must be characterized against REAL prod data in C1.6, not the sparse local seed. **C1.6 DECIDED 2026-06-16 (user verdict: fix keying + expansion-aware)** — recorded in design doc §3.7 + impl plan C1.6 line. The blank badges are a PRE-EXISTING prod bug (counts keyed by stored Title-Case labels, component looks up by slug → 0/blank; pre-dates C1.4, not a regression). Scope: (1) fix the slug↔label keying → emit a **slug-keyed** heritage count map so `countFor(node.value)` works; (2) **expansion-aware** parent counts = **distinct lessons** matching `{self ∪ all transitive descendants}`. Algorithm: per lesson, label→slug each stored value, expand to `{self ∪ ancestors}`, **union into one Set per lesson**, increment each slug once (NOT sum-of-descendants — double-counts). Maps must cover the **FULL 71-node tree incl. `internal`** (e.g. `Soul Food`→credits African American + Indigenous and Diaspora); preferred = extend `generate-heritage-hierarchy.ts` to emit a committed ancestry/alias-map artifact (existing outputs byte-identical, extend drift guard). **C1.6 ✅ DONE + supervisor-verified (`6805433`):** generator extended to emit a 3rd committed artifact `src/utils/heritageAncestry.generated.ts` (`aliasToSlug` + `ancestorsBySlug` over all 71 nodes incl internal; existing 2 outputs byte-identical — confirmed not in commit; drift guard extended). `facetCounts.ts` special-cases heritage via `tallyHeritage` (per-lesson: label→slug normalize → expand to {self∪ancestors} → union into ONE Set → increment each slug once; distinct-lesson, no double-count; unknown values get best-effort self-only credit). Count map is now **slug-keyed** → component unchanged. Executor's necessary call: `aliasToSlug` augmented with identity entries for all 71 slugs (vocab `alias_map` only had ~13 slug self-keys) so slug-form stored values pass through. Grep-confirmed ONLY the section component reads `counts.culturalHeritage` (no label-key dependency broken). Tests: facetCounts 12 + generator 20 (expansion, no-double-count, internal rollup, unknown best-effort, slug-passthrough). **End-to-end browser-verified by supervisor (local dev):** heritage badges that were BLANK now render with expansion-aware distinct counts matching the 3-lesson seed EXACTLY — Americas **2** (rolled up; no lesson literally tagged Americas), Latin American 2, Mexican 2, European 2 (rolled up), Mediterranean 1, Italian 1; type-check+lint+full suite (1089 tests) green. **C1.7 ✅ DONE + supervisor-verified (`c5ca119`):** retired orphaned `CULTURAL_HIERARCHY` — repo-wide grep confirmed ZERO live module importers (the legacy constant was the file's ONLY export; named consumers `CulturalHeritageFilter`/`VirtualizedCulturalHeritageFilter` don't exist in code), so the whole `src/utils/filterConstants.ts` was removed (59-line pure deletion, not an empty-module stub). Supervisor re-verified independently: original file had exactly 1 export, zero `.ts/.tsx` importers, type-check+lint green, zero `CULTURAL_HIERARCHY` refs remain in code. Stale doc rows at `README.md:143` + `src/utils/CLAUDE.md:8` now point at the deleted file → folded into the existing src/utils/CLAUDE.md doc-cleanup follow-up. **C1.8 ✅ DONE + supervisor-verified (`eb8bfaa`):** E2E spec `e2e/cultural-heritage-filter.spec.ts` (6 tests; scope = "structural + superset, no fixtures" per the Session-16 user decision in recent-decisions). Proves on the public `/` route: nested tree renders across ALL tiers incl. the new groups (Indigenous and Diaspora → African American; Indigenous → Lenape; Asian → East Asian → Chinese/Japanese); parent + nested-child checkboxes toggle/apply/clear via active-filter chip + section count badge (public store syncs filters to NEITHER URL nor localStorage → chip is the state surface; checkbox is `display:none` → click the label + read `toBeChecked()`); parent ⊇ child counts (Asian≥Chinese robust-at-any-size; **European≥Italian non-vacuous even on the 5-lesson local seed — European parent picks up the Italian-only lesson via recursion**, a real local recursion proof); the African-American >0 + strict-superset magnitude probes are GUARDED behind total>50/>10 so the sparse local seed can't falsely fail (CI/TEST DB exercises them). NO component changes (semantic text/role locators sufficed). Supervisor re-ran the spec independently: **6/6 pass (13.7s)**; type-check+lint green; commit is the spec file only. **C1.9 ✅ DONE + supervisor-verified + USER-APPROVED (`9dc9bdb` + verdict edits): backward-compat apply gate = PASS with one documented correction.** PROD-read-only comparison (new label-sets from the REAL local `expand_cultural_heritage(_alias_cultural_heritage(...))`, counted read-only against live PROD via the faithful `&& … retired_at IS NULL` path; PROD anchors 767/788/339 match the C1.0 baseline). Across all 31 options: **21 HEAL** (incl. 13 previously-broken-to-0 options now resolving — chinese 0→15, mexican 0→38, italian 0→25, african-american 0→27, indigenous-and-diaspora 0→58, indigenous 0→31, lenape 0→7; keystones Asian 65→74, Americas 172→199), **9 SAME**, **1 numeric −1** (`mediterranean` 45→44). Executor correctly STOPPED on the −1 per the hard boundary + escalated; supervisor independently re-verified vs live PROD — the lone dropped lesson is `Moroccan Carrot Salad, Two Ways` (`1LyuuuF-GNwUfVIgxrR_3NQLCEbD59GkfFN145WeTk3Q`, tagged `[Moroccan, North African, African]`, NO Mediterranean-proper tag, still surfaced under african/north-african, not retired). Vocab-aligned correction (OLD 6-row table wrongly cross-parented Moroccan under Mediterranean + phantom Turkish; locked §16 vocab homes Moroccan under North African only), NOT filter narrowing. **User approved recording the gate as PASS-with-correction (2026-06-16, Session 16).** Gate-evidence artifacts: `scripts/heritage/artifacts/heritage-filter-after-prod.json` + `heritage-filter-backward-compat.md` (verdicts updated to the adjudicated PASS). TEST-DB stage of C1.9 runs in CI at C1.10 after the migration applies. **C1.10 IN PROGRESS** — per-PR ritual → open PR C1. **Pre-push review = CLEAN** (5-dimension fan-out workflow + adversarial per-finding verify, Session 16; 7 agents): 2 findings raised, **0 meet the acceptance bar** (0 user-visible bugs, 0 DB-safety risks across migration/recursive-fn/generator/component/algorithms), 2 real-but-below-bar default-rejected per the internal-tool calibration — (i) `buildCultureLabelMap` is slug-keyed while stored values are Title-Case labels so the lookup is a no-op today, but the `?? value` fallthrough returns the correct label anyway (inert + harmless, verified vs local+TEST DB; NOT fixed — churn); (ii) `eslint.config.js:9` imports the transitive-only `globals` package (undeclared direct dep; lint passes, lockfile pins 14.0.0; NOT fixed pre-push — documented in PR body, address only if a bot raises it). **PR C1 OPEN as #509** (https://github.com/danfeder/esnyc-lesson-search-react/pull/509), pushed clean — no pre-push fix-ups (Session 16). Deploy preview ready (deploy-preview-509). CI at open: Dependency Review + Netlify pass; **Security Audit FAIL = the KNOWN pre-existing main dep-CVE breakage** (PR C1 changes no package.json — same as PR A/B; NOT PR-introduced); E2E / CodeQL / Coverage / Test&Build / the 4 `claude-*-review` bots / semgrep / Lighthouse all PENDING at close; 0 bot reviews yet at open. **C1.10 POST-OPEN (Session 16, same session — user asked to check CI):** (1) **TEST-DB verification DONE** — C1.2 migration applied on TEST (71-row hierarchy + recursive `expand_cultural_heritage`, text[] heritage col populated 335 rows); the **anon-role** heritage path (the public browser context) returns european=59 / italian=24 / african-american=26 — heritage filter verified correct end-to-end on TEST for the public path = the C1.9 TEST stage. (2) **E2E FAILED → ROOT-CAUSED → FIXED + PUSHED (`4442d14`, bundled the local Session-16 docs commit `9bb9e41`; re-triggers CI on a fresh preview)** — `e2e/cultural-heritage-filter.spec.ts` failed on its 2 hard-magnitude assertions (european>italian strict; aa>0) which read a transient loading `0` while the filtered search was still landing on an unusually slow deploy preview (the pre-existing `accessibility.spec.ts` also hit a 30s load-state timeout that run → preview was slow). **NOT a product bug** — the anon-role DB path returns correct non-zero counts (verified above). Fix: `resultCount` now waits for the count to STABILIZE before reading, and the 2 fragile hard-magnitude assertions were removed (kept structural + monotonic `>=`, which is scope-aligned and robust at any data size — `59>=24` on a healthy preview, `0>=0` on a slow one, never false-fails); 6/6 pass locally, type-check+lint green. Fix-up commit bundles the local Session-16 docs commit (first fix-up push; also re-triggers CI on a fresh preview). The pre-existing `accessibility.spec.ts` timeout is environmental/flaky (not PR-related; my change doesn't touch page load) — watch whether the re-run clears it. **BOT ROUND 1 DONE (Session 17)** — collected all 4 surfaces; 4 review bots (3× claude, 1× copilot) all "safe to merge"; 9 distinct findings, 0 user-visible bugs, 0 DB-safety regressions. User chose 2 fix-ups (`e5015cd`): (a) declare `globals` as a devDependency (executes the PR-body "fix-if-a-bot-raises-it" rule that Copilot triggered; lockfile +1 line, no resolution change, no CVE impact) + (b) dedup the 3rd `HeritageNode` copy → import generated `HeritageOption` (filterUtils + IntCulturalHeritageSection; the generator's internal Zod-derived `HeritageNode` is a different type, left untouched). 7 rejected below-bar with written rationale (renderNode-as-named-component non-blocking@31-nodes, ancestorsBySlug-rename, VOLATILE-vs-STABLE intentional, test-clicks-input-vs-label not-a-bug, getCultureDescendantValues-unused-but-tested-harmless, GRANT-ALL-to-anon matches the prod baseline this rebuild preserves + RLS restricts writes to admins). No DB-affecting change → no TEST re-verify needed this round. Verified before push: type-check+lint clean, full unit suite 1089/1089. The held-back Session-16 docs commit `09de741` + this status edit bundle with the fix-up push (one CI cycle). **Round 2 triaged + round-cap reached → C1 REVIEW-CLEAN (see the Session-17 round-2 block above); NEXT = user merge call.** **Merge + PROD migration approval USER-GATED.** After C1 merges: PR C2 (apply: staging→migration→embeddings, incl. the 3 targeted heritage drops U-5/U-8/U-12 + the C1-flagged error corrections, specific-only heritage storage), then PR D (synonyms) + PR E (drop PR-5 rollback tables). **C1 process note for downstream:** ESLint flat-config IGNORES `--ext`; it lints by `files` globs, so ANY file under the un-ignored `scripts/heritage/` is linted regardless of extension (the C1.3 `.mjs` needed a Node-globals config block) — relevant if C1.4+ add non-`.ts` files there. **STILL PENDING (pure housekeeping, non-blocking):** archive Sessions 6-11 to the archive file. The dense PR-B-cycle detail below (Sessions 6-11) is historical. Inherited substrate unchanged: PROD concepts census 675/119/1923 zero non-canonical; `pr5a_heritage_rollback` (37) + `pr5b_concepts_rollback` (676) live until PR E; sole-source run file `scripts/stage2-retag/artifacts/full-run.fable.jsonl` committed on main.

**PR A (#507) — MERGED 2026-06-12 16:55 UTC as `f982d1a`** (bot round 1: zero accepted findings; Security Audit red = pre-existing main breakage, not PR-introduced; full triage in Session-5 archive entry).

**PR-B CYCLE ACTIVE (started 2026-06-12).** Branch `feat/pr6b-stage2-retag-run` off main at `f982d1a` (= PR A #507 merge; bot round 1 closed with zero accepted findings — triage detail in the archive). Carries local docs commit `de26881`. **Done this cycle:** impl-plan B-section refined (B0-B7, `f3451d8`); **B0 ✅ `7efcf50`** (cache-floor `level:'unknown'` + preflight --help; toolChoice symmetry VERIFIED ALREADY CLOSED in PR A via shared buildMessageRequest — deferral was stale; repair-prompt review: fabrications prompt-immune, rules already present — effective fix is code-side; 286 tests). **B1 ✅ `fb6ab84`** (sampler seed 20260612, 40 stratified + 20 adversarial, worksheet 2,034 lines + parse round-trip, 318 tests; determinism re-verified byte-identical; dry-run-notes L-numberings differ r1-vs-r3 — adversarial IDs anchored to verbatim citations, never L-numbers). **B2 pre-fill ✅ (2026-06-12):** 6 parallel Fable agents drafted all 60 lessons from BODIES ONLY (current PROD tags excluded from context — v3 is a scored contestant); supervisor independently validated all 60 records (JSON, exact ID coverage, keyset = 13 worksheet anchors, ZERO vocab violations — heritage uniformly label-form); drafts merged into the worksheet DRAFT column (780/780 cells); parser round-trip smoke: 60 records, DRAFT-fallback works. Evidence + agent notes at `artifacts/prefill/evidence-notes.md`; full structured drafts (concepts everyday-vocab + synonym pairs — NOT carried by the worksheet cell, scoring joins from draft JSONL) at `artifacts/prefill/draft-{1..6}.jsonl`. **CURRENT GATE: USER verifies the worksheet (OQ8). NEXT SESSION = interactive verification walkthrough (user-chosen format 2026-06-12):** go through the worksheet TOGETHER — start with the ~25 flagged judgment calls (extract them from `artifacts/prefill/evidence-notes.md` "Agent notes", grouped by the 5 ruling clusters below so one user ruling settles each cluster, then per-lesson leftovers; AskUserQuestion rounds per the Session-2/4 walkthrough pattern, plain language); record user answers in the CONFIRMED column; THEN user decides full-pass vs spot-check for the unflagged remainder. After verification: re-run `--parse` → answer-key.final.jsonl, then B3 (needs user cost OK ~$10+$4). Cross-batch rulings the user should set while verifying: (1) CRF stamp policy (3 tiers seen: bracketed-template→empty · unbracketed all-7 stamp→transcribed+flagged · genuine prose→mapped); (2) heritage vocab gaps (Muslim/Eid lessons left EMPTY; Wild Soda raw-tag row); (3) concepts vocab gaps (fossil-fuels/energy, health topics); (4) grade-claim boundary (CCSS citations? adaptation-section claims? variation matrices counted); (5) **R4 verdict is PER-LESSON, not one direction** (Cajun 3-5: drop Math+Arts concepts · Cajun 6-8: ADD Science integration AND drop Math+Arts) — normalize.ts blanket add-integration is wrong at least sometimes; B3 quantifies. Contestants for B3 scoring: **claude-fable-5 primary (`--tool-choice-auto`) + claude-opus-4-7 challenger** (4-8 + sonnet dropped; DEFAULT_MODEL = 4-7), both via the CLIProxyAPI proxy at `http://127.0.0.1:8317/api/provider/anthropic` — **proxy v7.1.68 must be RUNNING** (`~/.local/bin/cli-proxy-api -config ~/.cli-proxy-api/config.yaml`; pull-latest before runs; mid-run 400 → pull latest → `--resume`). Measured full-run projections: fable ~$129 · o47 ~$47-79 w/repairs (Max credits via proxy). **B4 full run = USER GREEN-LIGHT, gated on the B3 scorecard.** No DB writes anywhere in PR B.

**SESSION 7 (2026-06-12, interactive) — B2 VERIFICATION COMPLETE ✅; answer-key.final.jsonl BUILT (57 records).** Full walkthrough done: 8 cluster/heritage rounds + 16 lesson-by-lesson pass rounds, every surviving lesson user-verified against the live Google Doc. **25 policy rulings (A1-A25) + per-lesson corrections recorded at `docs/plans/2026-06-12-pr6b-answer-key-verification-rulings.md` — THE source of truth for the key AND the full-run prompt-rule seeds.** Headlines: CRF AND SEL stamps KEPT (cell-primary, user overrode discount rec); **3 lessons REMOVED as incomplete/non-lesson (L10 Celebrating Eid, L21 Kitchen Appendices, L27 Three Sister Arepas) — corpus-deletion follow-ups + full-corpus completeness screen logged**; **SYSTEMIC: grade/season claims live in Drive FILENAMES + page HEADERS that content_text lacks** (user: both become pipeline inputs; 59-lesson sweep done → `artifacts/prefill/header-filename-sweep.md`, method = inspect_doc_structure / header*.xml); all-seasons headers → all four seasons (A21); cross-surface conflicts → union (A22); both food-miles lessons academic (A23 refines topic-arm). Corrections applied by executor (`9d4b626`: exclusions file + parser exclusion support, TDD, 323 tests; worksheet+drafts edited; --parse → 57 records) — supervisor-verified (6 independent §F spot-checks exact, gates green). **B2.5 ✅ `ff3c7ee`** (doc-surfaces sidecar 57 ids 1:1 with key + runner injects filename/header block; 342 tests). **B3 prep ✅ `c908144` + `41a4f61`** (--ids subset flag; score-answer-key.ts reusing evalMetrics; 379 tests). **B3 RUNS LAUNCHED 2026-06-12 ~16:55 ET** (background: fable --tool-choice-auto conc 5 → `artifacts/answer-key-run.fable.jsonl`; o47 conc 3 → `…o47.jsonl`; proxy v7.1.68 verified running; mid-run 400 recovery = pull-latest + --resume). **NEXT-SESSION PICKUP — BOTH RUNS COMPLETED before session close (57/57 records each): fable 55 Zod-passed / 1 repairable / 1 ERRORED ($9.25); o47 47 passed / 10 repairable ($3.29); total $12.54 of the ~$14 approval.** (1) The fable errored record: re-run `--resume` with the SAME fable flags (proxy running) to fill it, THEN repair passes. (2) `npx tsx scripts/stage2-retag/validate-output.ts` per run file → repair pass `--repair` per model for Zod-failed fields. (3) Score: `npx tsx scripts/stage2-retag/score-answer-key.ts --key scripts/stage2-retag/artifacts/answer-key.final.jsonl --v3-from-corpus scripts/stage2-retag/artifacts/corpus.jsonl --run fable=… --run o47=… --output scripts/stage2-retag/artifacts/answer-key-scorecard.md` (gated winner = FIRST --run label). (4) Present scorecard + 3 gate verdicts + queued B3 decisions (academic-strip routing, L14 heritage-ban rule, garden_skills code-rule; caveat: corpus.jsonl lacks grade_levels/tags so v3 scores 0 there). (5) **B4 full run = USER-GATED**, and BEFORE it: corpus-wide filename/header capture (sweep method proven: inspect_doc_structure for native docs, word/header*.xml for .docx) + the full-corpus completeness screen (3/60 key lessons were incomplete — expect ~35 corpus-wide) + fold the 3 deletion verdicts into the corpus/export.**

**SESSION 8 (2026-06-12) — B3 SCORING COMPLETE ✅; awaiting user gate verdicts.** Fable refusal record: 4× deterministic `stop_reason: refusal` on Seed Dispersal `1X7HadA1WaR-hNBFIHvAUsFKborDGE5eT3Lhg5Dp9CEI` (likely "build seeds that explode" safety pattern-match; sibling variant passed; o47 fine) → fable scores on 56/57. Repairs: fable 1/1 pass, o47 10/10 pass; final spend fable $9.56 + o47 $6.69 = $16.25 (vs ~$14 estimate; o47 repairs added $3.40). **Scorecard (`artifacts/answer-key-scorecard.md`, committed): fable macroF1 0.858 · o47 0.777 · v3 0.532. Gate 1 PASS (fable ≥ v3 all 13 fields), Gate 2 PASS, Gate 3 FAIL as-written** — 5 academic_concepts values at recall 0, ALL support=1 (31 singletons of 68 values; fable hit 26/31; 1 of the 5 is the refusal artifact). **Decision data gathered:** garden_skills code-rule (empty when predicted activity_type lacks `garden`) would fix 3 fable + 11 o47 records with ZERO harm cases in key sample — strong adopt evidence; academic-strip amplification is o47-only this run (fable fired 0 strips; o47 stripped correct Science off Kitchen Tool ID via wrong activity_type); L14-class heritage-ban persists in BOTH models on 4 of 5 ban lessons (mention-vs-about failure: Compost Relay→Indigenous, Hummus→Middle Eastern, Black Bean Burgers→Indian, Food Workers' Rights→Mexican/Ecuadorian) — prompt-rule seed for the full run, fable heritage F1 still 0.709 vs v3 0.407. **USER RULINGS (same session): Gate 3 AMENDED to support≥2 floor (singletons reported non-gating) → FABLE PASSES ALL 3 GATES, confirmed winner · garden_skills rule ADOPTED · heritage prompt rule + retest · refusal fallback = o47.** All four implemented by executors, supervisor-verified: **Gate-3 amendment `cfffdee`** (scorer; 385→tests) + regenerated scorecard `f698ba2` (fable 45/50 singletons all-fields); **garden rule `90208d0`** (normalize.ts R6 `garden-skills-nongarden-clear`, fires only when predicted activity_type lacks `garden` AND gs non-empty; 389 tests); **heritage mention-vs-about prompt rule `088fba0`** (prompts/stage2-retag.md cultural_heritage section; 391 tests); **`--fallback-model` `3374a27`** (refusal-only o47 fallback, `phase:'fallback'` provenance, forced tool_choice always, resume-aware — fallback records match current.fallbackModel; 405 tests). **HERITAGE RETEST RESULT (7 lessons, $1.34, `artifacts/heritage-retest.fable.jsonl`): prompt rule INEFFECTIVE on the 4 ban lessons** (all still over-tagged; Food Workers' Rights got worse +African American; 3 keep-lessons unaffected — no suppression). ROOT CAUSE: bodies CLAIM cultural engagement (Hummus Summary: "learn about its origins and cultural relevance"; CRF cell: "learning within the context of culture") that the agenda never delivers — claims-vs-delivery, not mention-vs-about; rule stays shipped (harmless+correct guidance). **FINAL SESSION-8 RULINGS: heritage over-tagging ACCEPTED** (claims-vs-delivery beyond prompt reach; Protocol-B diff audit + OQ9 register adjudication are the catch surfaces; rule stays shipped). **B4 PREP GREEN-LIT; the $129 RUN gets its OWN gate after prep results.** **B3 IS COMPLETE.** **NEXT SESSION = B3.5 prep (impl plan has the concrete tasks):** B3.5a corpus-wide doc-surfaces capture (~710 docs, chunked background agents, Session-7 method) · B3.5b full-corpus completeness screen (heuristic + agent review → USER verdicts on candidates) · B3.5c fold the 3 deletion verdicts into exclusions. THEN B4 run gate (fable `--tool-choice-auto` + `--fallback-model claude-opus-4-7`; prompt hash changed at `088fba0` so B4 is a fresh run, no resume vs B3 artifacts). Session-8 total spend: ~$1.74 (retries $0.18 + repairs ~$0.22 fable/$3.40-already-counted-o47… authoritative: fable file $9.56 + o47 $6.69 + retest $1.34 ≈ $17.59 cumulative B3).

**SESSION 9 (2026-06-12, in progress) — B3.5 prep underway.** **B3.5c ✅ `7750586`** (supervisor-verified: `data/corpus-exclusions.json` + run-retag loadCorpusExclusions/excludeCorpusIds — exclusions drop BEFORE --ids/--limit/--resume, --repair mirrors; ids verbatim-cross-checked against answer-key-exclusions.json by a test; 412 tests, type-check+lint clean). **B3.5b SCREEN ✅ + USER VERDICTS RULED:** screen read all 765 bodies, flagged 51, caught all 3 calibration docs; artifact `artifacts/completeness-screen.md` (supervisor spot-verified quotes/lengths/ids against corpus). USER rulings: **EXCLUDE 7** (Soul Food Sunday, Flowers, Sandor Katz, Italy/Pesto, Juneteenth [Eid twin], Nigeria/Red Bean Stew, Honduras/Baleadas — all → corpus-exclusions + deletion-review list) · **RE-EXTRACT the 6 "metadata card" bodies** (Equivalent Ratios, Mindful Eating, Fattoush ×2, Plant Part Salad/Stir Fry — corpus body = machine tag-summary, circular to re-tag; user chose re-extract real Drive bodies + include in B4, fallback-to-exclusion if doc is stub/404; Mindful Eating already 404'd in sweep — expect fallback). NOTE: tag-card bodies are ALSO a live PROD data-quality issue (public FTS runs on them) — content_text repair joins the Who's-Who repair in PR C. **B3.5b verdicts APPLIED ✅ `8953bb3`** (supervisor-verified: corpus-exclusions 3→12 — 7 ruled + 2 re-extraction-404 fallbacks (Equivalent Ratios + Mindful Eating, both 404 via two methods); body-overrides mechanism generalizes Who's-Who into manifest `data/body-overrides.json` (4 entries: Fattoush ×2, Plant Part Salad, Plant Part Stir Fry — all real complete lessons re-extracted, 2.8-4K chars) + untracked sidecars `artifacts/body-overrides/<id>.txt`; corpus.jsonl patched in place, exactly 4 lines changed content_text-only, backup at `.tmp/b35b-corpus-backup/`; 420 tests, type-check+lint clean). **B4 run corpus = 753** (765 − 12). **B3.5a ✅ `8a57623` — COMPLETE.** All 9 sweep chunks validated 1:1 (8 original + chunk-9 for the 88 `lesson_*` ids, Drive ids self-resolved via read-only PROD `file_link` → `.tmp/b35a-chunks/lesson-id-map.json`); 58-record filename-fix pass (chunk-9 wrote 'Tab 1' placeholders; fix agent fetched real names). Sidecar `data/doc-surfaces.json` **57 → 758 entries** (607 with real headers); missing only = 5 unswept exclusions + 2 dead-link 404s (Trail Mix Greater-Than `1P8rs1bUmdlj_lKDPQSnMr1QANW-KnTWAuEAc-UbEDVw`, The Curious Garden `1jQ5XtGr20RfoIZU_Ikd-X2dBffAqGbbB2ZklIcT-yYY` — run with no surfaces; their PUBLIC file_link is dead → follow-up). Verification: 9 Session-7 overlaps byte-identical headers (independent cross-validation) + 2 fresh supervisor Drive probes exact; doc-surfaces tests updated to corpus-wide contract; 420 tests green. **B3.5 IS COMPLETE. B4 GREEN-LIT 2026-06-12 (user) — RUNNING IN WINDOW-SIZED BATCHES** (user pacing call: Max 5-hour window had ~54% left; batch then wait for reset). Run shape: 753 lessons (765 − 12 exclusions), fable `--tool-choice-auto` + `--fallback-model claude-opus-4-7`, proxy v7.1.68, fresh run (prompt hash `d561226e9ea2…`), output `artifacts/full-run.fable.jsonl`, batching via `--resume --limit N` (limit is positional over corpus, resume skips done). **BATCH 1 ✅ (--limit 150) + BATCH 2 ✅ (--limit 300): 300/300 mains, 298 clean ALL Zod-passed (100% both batches!), 2 refusals (`1_WlZmDJv8Ql-sCLlOyxr2qL4zeb1GQda`, `18SdRTm5OmYYQT-lfGISMlph5ZMjMttp0m_lrnAwP5ZM`) both auto-fell-back to o47 (phase:'fallback' records), cumulative $48.86 ($0.163/lesson).** **B4 ✅ COMPLETE 2026-06-12 ~20:45 ET — validate-output: 753/753 Zod-passed, 0 failed, 0 unusable; total $121.28** (vs ~$129 projection). Ran as 6 window-paced batches (--limit 150/300/350/390/415/753) + repair. Final tallies: 5 fable refusals (incl. the B3 Seed Dispersal doc `1X7HadA1…` + its sibling), all o47-fallback-recovered; 3 main Zod-fails repaired 3/3; **one stubborn lesson `1_WlZmDJv8Ql-sCLlOyxr2qL4zeb1GQda`: o47 FALLBACK-path returned the whole payload stuffed as a string in `tags` TWICE (deterministic; malformed record quarantined to `full-run.fable.rejected.jsonl`), resolved via direct o47 MAIN-pass (--ids) + per-field repair** → process learning: fallback-path o47 has a payload-stuffing failure mode the main path doesn't; repair pass covers main-phase records only. Artifacts (local, gitignored): `full-run.fable.jsonl` (764 records: 760 main + 4 repair), `run-summary.full-fable.json`. The 12 validate-output "missing" = exactly the 12 corpus-exclusions. **NEXT: B5 apply artifacts (generate-diff-report + staging SQL emitter per OQ7), B6 register adjudication (74 signals vs new tags), B7 ritual → PR B.** Protocol-B diff audit + user spot-check still gate the eventual APPLY (PR C). DEFERRED FINDINGS (new): ~~stage2-retag tests read gitignored artifacts~~ CLOSED Session 10 `b2a2101` (skip-if-missing guards; CI-sim 417 passed / 3 skipped / 0 failures) · `lesson_1753316245157_flurdiez2` (PROD title '---') shares doc `1HX-izHnD2BJ…` with `lesson_cc0a5cb454…` — stub row, dedup track · 2 dead public file_links (above) · tag-card content_text PROD repair rides PR C with Who's-Who.

**PR-B deferred findings to fold into B-tasks:** R4 reconcile-direction conflict (prompt says drop-concept, code adds-integration — answer key adjudicates) · ~~repair-pass toolChoice gate asymmetry~~ (B0: already closed in PR A) · ~~fable cache-floor labeling~~ + ~~preflight --help~~ (B0 `7efcf50`) · NEW: garden_skills code-side rule candidate (empty when activity_type lacks `garden`, or anchor repair with chosen activity_type — B0 diagnosis; decide at B3 alongside academic-strip recheck routing) · `strict:true` optional hardening (API rejected uniqueItems) · o47 garden_skills fabrications concentrated in REPAIR records (review repair prompting) · **L14 heritage ban fails ALL FOUR models** (answer-key adversarial item; possible code-side rule) · academic-strip can amplify when `academic` was correct (consider recheck routing) · even fable fabricated one season on a [Table] body (L10 — answer-key check on [Table] bodies).

**Operational notes:** exploration doc has non-UTF8 bytes (`grep -a` required) · `.env.local` `ANTHROPIC_API_KEY` = proxy-side key (direct API uses `ANTHROPIC_CONSOLE_API_KEY`) · corpus snapshot `artifacts/corpus.jsonl` is gitignored + Who's-Who sidecar requires authenticated re-fetch · answer-key inputs: ~40 stratified-random + ~20 adversarial from `oq6-v3-baseline-and-eval-protocols.md` failure gallery; grades = source-doc claim only; "beats v3" = per-field F1 ≥ v3 everywhere AND macroF1 ≥ 0.7 AND per-value recall ≥ 0.5; eval reuses `scripts/lib/evalMetrics`.

**Pre-next-PR verification (if any):** none pending. Inherited substrate: PROD concepts census 675 / 119 / 1923 zero non-canonical; `pr5a_heritage_rollback` (37) + `pr5b_concepts_rollback` (676) live until PR E.

## Recent decisions worth carrying forward

- 2026-06-16 (Session 18, interactive — **C2 cycle start: plan refined + 3 user decisions**): the C2.1–C2.4 task plan was made concrete from a deep recon + PROD censuses. (1) **Embeddings = DEFERRED out of C2** and **grades = guarded + reviewed** — both REVISED after a **Session-18 Codex adversarial review (gpt-5.5/xhigh) of the C2 plan** caught that (a) the embedding recipe is metadata-INCLUSIVE (the initial "1-row, metadata-free" call was WRONG — `prepareLessonText` embeds culture/concepts/skills/grades; THREE inconsistent recipes exist: live `process-submission`=title+content, `generate-embeddings.mjs`=metadata-rich, `regenerate-all`=content-only), so settling the canonical recipe + full regen is its own task; and (b) the B5 emitter writes `grade_levels` UNCONDITIONALLY → would overwrite 731 rows + BLANK 33 rows' grades (a user-facing filter) with no review → C2.1 now guards (never write empty) + emits a before/after grade diff for Protocol-B signoff. Codex also refined U-1 (derive the forbidden set from the hierarchy + assert column & metadata). All 3 findings verified against code + accepted. The "embed metadata for search" follow-up subsumes the deferred embeddings task. (2) **6 small-field CHECK constraints INCLUDED in C2** — PROD census proved safe: the apply canonicalizes all 113 live non-canonical rows (residue 0); the lone blocker is 1 retired row `15T4wU94…` fixed by a `Food Justice`→`Social Justice` fold. (3) **Salsa Toasts = no action** (the third row's `[Latin American]` is defensible); Arroz correction targets the verified real id `1VShcRmc…` (design doc's `055b000b` doesn't exist). Heritage emitter needs NO change (already specific-only). Also: **PR-B cycle (Sessions 6-11) archived** to the archive file (no new promotable learnings).
- 2026-06-16 (Session 17, interactive — **PR C1 bot rounds 1+2 triaged; C1 review-clean**): R1 → 2 user-approved fix-ups (`e5015cd`): declare `globals` devDep (executes the PR-body "fix-if-a-bot-raises-it" rule Copilot triggered) + dedup `HeritageNode`→generated `HeritageOption`; 7 rejected below-bar. R2 (deeper claude pass, Must-Fix none) → **0 code changes**: DROP-CASCADE pre-flight VERIFIED on TEST (0 deps); migration-level nits rejected/rebutted (and each would need a NEW migration since `20260616000000` is pushed); **`GRANT ALL`→`SELECT` + `STABLE` DEFERRED to C2/grants-hygiene** (user verdict — not a regression, project-wide posture, ~nil exploit path under PostgREST); `aria-hidden` a11y deferred to the project-wide a11y batch. Round-cap reached. **NEXT = user merge call; merge + PROD approval USER-GATED.**
- 2026-06-16 (Session 16, interactive — **C1.9 apply gate PASS-with-correction APPROVED**): the backward-compat gate found 21 HEAL / 9 SAME / 1 numeric −1 (`mediterranean` 45→44). The −1 is the lone lesson `Moroccan Carrot Salad, Two Ways` (`1LyuuuF…Tk3Q`) leaving the Mediterranean filter because the OLD 6-row hierarchy wrongly cross-parented Moroccan under Mediterranean (+ phantom Turkish); the locked §16 vocab homes Moroccan under North African only, and the lesson stays discoverable under african/north-african. Supervisor re-verified the root cause against live PROD; **user approved recording the gate as PASS with this one documented correction** (not narrowing — a vocab-aligned fix). Alternatives (multi-parent Moroccan / re-home it) declined as out-of-scope (single-parent vocab is locked; multi-parent deferred). Gate-evidence artifacts' verdicts updated accordingly.
- 2026-06-16 (Session 16, interactive — **C1.8 E2E scope DECIDED**): user chose **"structural + superset, no data fixtures"** (Option 1 of 3). Rationale surfaced by a read-only E2E recon: the impl-plan's literal "select Asian → a lesson tagged ONLY with a deep descendant shows up" is unsatisfiable pre-C2 (current corpus stores full ancestor chains; specific-only lessons land in C2) AND the repo's E2E convention deliberately avoids per-lesson data assertions (no seed/fixture mechanism). Recursion-correctness stays proven by C1.3 (DB-layer, RED-against-old) + C1.9 (backward-compat baseline diff); C1.8 becomes the **UI integration/smoke** proof — nested tree renders across tiers incl. new groups, parent+child checkboxes toggle/apply, parent "Asian" ⊇ child "Chinese" (monotonic, no exact counts), data-magnitude checks CI-only (robust to sparse local seed). Impl-plan C1.8 line rewritten to this scope. Options 2 (seed a fixture lesson) + 3 (defer to C2) declined. Deep-descendant-only UI proof deferred to C2.
- 2026-06-16 (Session 13, interactive — **heritage-filter-rebuild design LOCKED**): 4 user decisions in a brainstorming walkthrough — UI scope = **regen from vocab.json `top`+`sub` tiers (~31 nested options)** (adds the missing Indigenous and Diaspora / African American / Indigenous groups; fixes mis-parented nodes); mechanism = **DB-side live recursive `expand_cultural_heritage`** from a vocab.json-generated `cultural_heritage_hierarchy` (full tree), **keep Title-Case label storage** + `alias_map` slug↔label bridge (the keystone footgun); **PR C1 (filter) ships before C2 (apply)**; display = **nested/indented tree** (component rewritten to recurse). Evidence: read-only Understand workflow + keystone verify (regression CONFIRMED 65→36 strip; backward-compat CONFIRMED 65→74 superset; all 61 live labels exist in vocab.json). Design `2026-06-16-heritage-filter-rebuild-design.md` (`71cfdb6`); C1 tasks in the impl plan; PR-naming standardized **C1/C2** (retires "C0/C"). NEW surfaced detail: facet-count coherence post-C2 (decide in C1.6). NEW apply-time error candidates to verify in C2: Arroz con Gandules spurious `South Asian`; two Salsa Toasts → `[]`.
- 2026-06-16 (Session 12, interactive — **HERITAGE PIVOT to Option B**): apply-as-is would break the hierarchical heritage filter (PROD-verified: single-tier expansion + incomplete 6-row hierarchy table; filter works only because lessons store full ancestor chains). User chose **Option B** — specific-only heritage storage + rebuild the live filter for complete recursive expansion from one source of truth (§16 `cultural-heritage.vocab.json`); **NOT** Option A (full-family storage). **Filter-first** build order; **open-ended** maintenance-window runway (site closed to users → transient inconsistency harmless). Re-opens design §7/OQ7/OQ13. U-1 is satisfied structurally by the §16 tree (AA/Indigenous root under `Indigenous and Diaspora`). Next = heritage-filter-rebuild design pass. See the pivot block at top + the Session-12 log entry.
- 2026-06-11 (scaffolding): **field scope (OQ2) deferred to the Session 2 walkthrough** by user choice, with a Session 1 PROD census of the ~8 smaller vocab fields as the evidence input.
- 2026-06-11 (scaffolding): track scaffolded in design-lock mode — the mechanism lock reopening is the walkthrough's first question (OQ1), per the exploration doc; nothing pre-decided.
- 2026-06-11 (scaffolding, post-review): **execution model = supervisor + fresh-context subagents** (user directive, generalized from the PR 5 kickoff into the templates + this track's kickoff). Session boundary = supervisor context budget, not task count; status header checkpointed after each verified task; OQ1-OQ13 tagged `[user-verdict]` vs `[evidence-lockable]` — verdict questions are never locked without the user.

## Done

- 2026-06-11 — Session 1 evidence items E1 (OQ2 smaller-fields PROD census), E2 (OQ5 content_text audit), E4 (OQ6 v3 baseline + eval protocol sketches), E5 (OQ4 Batch retention fact-check), E6 (call-shape confirmation). All adversarially verified (4× confirmed-with-caveats, 1× confirmed, zero refutations) + supervisor-spot-checked via independent PROD probe. Artifacts: `docs/plans/pr6-stage2-retag-evidence/oq{1,2,4,5,6}-*.md`.
- 2026-06-11 — E3 token-economics dry-run (OQ3): 15 lessons, claude-opus-4-7, $3.58 actual. V1 monolithic + V2 per-field complete; V3 shared-prefix 5/153 calls (Console credits exhausted) but the 5 calls answered the headline cache question. Supervisor verified raw JSONL reproduces every claimed number. Artifact: `oq3-token-economics-dryrun.md`; harness + raw data relocated to `/Users/danfeder/cCode/pr6-dryrun-tmp/` (outside the repo — ESLint doesn't ignore `.tmp/**` and the harness `.ts` files broke the lint baseline; rerun instructions in artifact postscript).
- 2026-06-11 — E7 `claude -p` flag re-verify (supervisor-run, claude 2.1.174): flag surface intact EXCEPT `--max-turns` gone from `--help`.

## In flight

(C2 cycle: branch set up + C2.1–C2.4 plan refined. NEXT = build C2.1 — corrections manifest + emitter wiring, TDD. No code written yet.)

## Blocked

(none)

## Decisions made during execution

- 2026-06-12 (Session 4, interactive) — **cooking_skills + main_ingredients vocab DECIDED by user walkthrough; curriculum team SKIPPED entirely (user call); two-rounds confirmed (PR F stays separate, now unblocked).** Headline divergences from the overnight draft: ONE general **Knife skills** entry absorbing the five specific cuts AND knife safety (user: "specifics can get too messy"); Kitchen & food safety separate (oven/food safety/washing); ingredients = two levels (C), 24 groups, NO "Other vegetables" — distributed instead (squash group renamed **Squash, cucumbers & melons** absorbing Cucumbers + Melons; **Mushrooms** own group; green beans/snow peas → Beans & legumes; Celery/Fennel group-less specifics); Potatoes → Root vegetables (not Grains & starches); Avocado under Tropical fruits; all nine near-miss specifics pulled up (34 starter specifics); pantry-staples featured-only rule. **`guyanese` verdict: Both** — hierarchy parent stays `latin-american`, re-tag prompt rule dual-tags `caribbean` (vocab json is single-parent; multi-parent deferred to filter-UI track). Full record: `2026-06-12-metadata-rebuild-stage1-cooking-skills-ingredients-worksheet-decided.md`. Probe note: all 88 "Following directions" uses are on cooking lessons (66 cooking + 22 cooking/garden).
- 2026-06-12 (Session 3, supervisor, overnight; **RATIFIED by user 2026-06-12 ~10:05 ET** — design doc + impl plan lock lines amended) — **OQ2 observances count corrected 17 → 16.** The A3 executor proved the lock's "17 after End-of-year merge" unsatisfiable: `filterDefinitions.ts` has exactly 17 observances options INCLUDING both `End of year` and `End of year celebrations`; merging the pair yields 16; no 17th value exists anywhere on disk. "17" was the pre-merge census headline propagated into the lock text. Resolution: **16 canonical values; surviving spelling `End of year celebrations`** (stored usage 15 lessons vs 1); `Earth Month` Title-Case canonical (absorbs stored case-twin `Earth month`). Design doc deliberately NOT edited overnight — user ratifies first; if overridden it's a one-line vocab change + test rerun, and the full run is user-gated regardless. Also accepted (evidence-backed spec carve-outs, not design changes): `academic_concepts` is JSONB-only — no `lessons` text[] column exists — so the vocab mapping carries an explicit `column: null` exception (other 11 fields map 1:1); garden_skills canonical form = Title-Case labels (22 configured incl. `Crop rotation` + the 2 walkthrough adds); cooking_methods canonical form = kebab (`basic-prep`/`stovetop`/`oven`, the stored convention; UI labels update in PR E).
- 2026-06-11 — **OQ5 LOCKED from evidence** (tagged evidence-lockable): `lessons.content_text` is the re-tag body source as-is. Rationale: 100% live coverage, no truncation/corruption, 5/5 seeded-random Google-Doc comparison fresh. Handling in export step: exclude 2 ghost stubs, re-extract/hand-tag Who's Who in the Food System, strip VT + normalize CR (verbatim IDs in design doc + oq5 artifact).
- 2026-06-11 — **Session-2 walkthrough: ALL remaining OQs locked by the user.** OQ1 TS+Zod runner (lock reopened-and-changed; foundation plan + decision journal amended) · OQ2 hybrid 12-field main pass + small-field vocab locks (Food Justice→Social Justice, no-cook→basic-prep, End-of-year merge, garden_skills 24 = +Stewardship tasks +Sensory exploration, crop rotation kept) · OQ3 monolithic · OQ4 sync, Batch dropped, **retention accepted by user** · OQ6 Protocol A+B, **grades = always source-doc claim (silent docs gradeless)**, model picked empirically Opus-vs-Sonnet · OQ7 staging + emitter migration + `pr6_retag_rollback` + dual-write · OQ8 **user gates everything** (answer key = agent pre-fill + user verify; team gets only the mini-worksheet) · OQ9 post-run adjudication of all 74 register signals · OQ10 synonyms from the same call · OQ11 embeddings ride apply PR · OQ12 full check-surface lift + tsx pin · OQ13 five PRs A-E + rider F · observances field closes to fixed list. Full rationale per lock in design doc §4.

## Out-of-scope follow-ups captured here

- **(NEW, Session 18; CORRECTED post-Codex) Settle the embedding recipe + full-corpus regen — subsumes C2's deferred embeddings.** The `content_embedding` recipe is **INCONSISTENT across 3 code paths** (Codex-surfaced, verified against code): live `process-submission` embeds `title+content` only; the batch `generate-embeddings.mjs`/`prepareLessonText` embeds title+summary+grades+theme+**culture+skills+ingredients+concepts**+content (metadata-RICH); `regenerate-all-embeddings.mjs` = content only. So prod vectors are likely internally inconsistent, and because C2 changes metadata corpus-wide, any metadata-rich vectors go stale. **Deferred from C2** (user decision 2026-06-16; open maintenance window → temporary staleness harmless). The dedicated task: (1) pick ONE canonical recipe (and align `process-submission` so new submissions match); (2) full-corpus regen (~$0.03, trivial $); (3) ideally a before/after search-relevance check + a duplicate-detection-impact check (same `content_embedding` column `detect-duplicates` is tuned on). SUBSUMES the earlier "embed metadata for semantic search" idea; overlaps PR 3b/PR D synonyms (the FTS-side metadata→search bridge). Run AFTER C2's metadata apply. **(My Session-18 "embeddings are metadata-free → 1-row regen" claim was WRONG — only caught by the Codex adversarial-review gate.)**
- **`activityType` facet-count slug-vs-label bug** (surfaced Session 15 during C1.6 investigation) — same class as the heritage count bug: options use slugs (`cooking-only`/`garden-only`/`academic`) but `lessons` store Title-Case (`Cooking`/`Garden`/`Academic`), so `counts.activityType[node.value]` misses → activity-type badges render blank/0 on prod. NOT fixed in the heritage track (C1.6 only fixes heritage). Verify the exact stored form against prod before fixing; likely a small normalize-at-tally fix in `facetCounts.ts` mirroring the heritage keying fix. (Other filters whose options match stored vocab on prod — seasons, academic integration, etc. — render fine; the local-seed blanks for those were a toy-data artifact, not a bug.)
- ~~`guyanese` parent (`latin-american`)~~ — **RESOLVED 2026-06-12** (user verdict: Both; see Session-4 decision entry). Residual for the filter-UI track: consider true multi-parent support in the heritage hierarchy schema.
- Generic "following directions" as a cross-activity classroom skill — noted for any future SEL/competencies vocab pass (Cooking Skills is not its home; surfaced in the Session-4 walkthrough).
- **`src/utils/CLAUDE.md` doc cleanup** (grew across C1.4 + C1.7) — (a) the C1.4-stale helper names `isParentCultureSelected`/`getCultureChildren` (never existed in code; the real recursive helpers are `buildCultureLabelMap`/`getCultureDescendantValues`); (b) the C1.7-stale table row `filterConstants.ts | Cultural hierarchy, ingredient groups` + the README.md:143 row now point at a DELETED file. Pure docs; no code impact. Batch into one src/utils/CLAUDE.md + README.md pass (not on the PR-C1 critical path).
- Seed Bursts near-duplicate pair (`1HuffJuy…` + `1NqjpqXV…`) — dedup track (inherited from PR 5 follow-ups; not this track's to fix).
- **(PR B bot round 1, deferred) F1 — `lessonBodyById` module-level mutable global** (`run-retag.ts` ~1884): assigned in `main()`'s `if (args.repair)` branch, read in unexported `runRepairPass`; zero current risk (only caller is `main()` post-init). Thread it as a parameter to `runRepairPass` if the runner is ever revived/extended. Low priority — runner is feature-complete post-run.
- **(PR B bot round 1, deferred) F3 — validate-output error-summary de-dup** (`run-retag.ts` validate-output path): the one lesson that failed both main (refusal) + fallback appears twice in `errors[].ids`, so `count` reflects error records not distinct lessons. Cosmetic, on a frozen artifact for a completed run. De-dup the error display by ID if the summary generator is touched again.
- **(PR C1 bot round 2, DEFERRED — user verdict 2026-06-16) `GRANT ALL`→`GRANT SELECT` on `cultural_heritage_hierarchy` for `anon`/`authenticated`** (+ optional `VOLATILE`→`STABLE` on `expand_cultural_heritage`). Not a regression (matches the prod baseline C1.2 preserves) and a project-wide grant posture, not heritage-specific; under PostgREST the anon/authenticated roles have no TRUNCATE path, so practical risk ~nil. Address least-privilege uniformly — preferably **in C2** (which already opens a migration on this table; `REVOKE ALL … ; GRANT SELECT … TO anon, authenticated; GRANT ALL … TO service_role;` + fold `STABLE` into the same `CREATE OR REPLACE`) or a dedicated project-wide grants-hygiene migration. Must be a NEW migration (`20260616000000_*` is pushed — cannot edit).
- **(PR C1 bot round 2, DEFERRED) `int-check-count` a11y — count leaks into the checkbox accessible name** (screen reader announces e.g. "Chinese 7 checkbox"). Add `aria-hidden="true"` to the `int-check-count` span. PRE-EXISTING + PROJECT-WIDE across every internal filter section (not heritage-specific; the heritage component matches the prior one exactly) → belongs in the known frontend/a11y P1 follow-up batch, fixed across all sections at once, not partially here.
- **(PR C1 bot round 2, noted) `buildCultureLabelMap` called in 2 modules** (`IntActivePills` + `IntListRow`) — optional micro-DRY (export a shared `CULTURE_LABEL_MAP` const). Both call the same function so they can't diverge; only saves one trivial init. Low value; pick up if those modules are refactored.
- **(PR C1 bot round 2, noted, PRE-EXISTING) `IntActivePills` pill-removal type-dispatch fragility** — `Array.isArray(filters[pill.key]) ? removeFilter : setFilters({key:''})`; a future array-type filter key added without updating this guard would silently break multi-select pill removal. Not introduced by C1. Add a comment/type-assertion when next touching `IntActivePills`.

## Pointers to durable context

- Kickoff prompt: 2026-06-11-metadata-rebuild-pr6-stage2-retag-kickoff.md
- Design doc: 2026-06-11-metadata-rebuild-pr6-stage2-retag-design.md (LOCKED 2026-06-11; OQ1-OQ13 + 2026-06-12 amendments)
- Implementation plan: 2026-06-11-metadata-rebuild-pr6-stage2-retag-implementation.md (concrete tasks; PR A done, B refined at cycle start)
- Mechanism exploration (mandatory pre-walkthrough read): 2026-05-13-stage2-retag-mechanism-exploration.md
- Parent initiative status: 2026-05-03-metadata-rebuild-foundation-execution-status.md
- Vocab inputs: 2026-05-10-metadata-rebuild-stage1-heritage-worksheet.md (§16) + 2026-06-11-metadata-rebuild-stage1-concepts-worksheet-returned.md
- PR 5 follow-ups inherited: 2026-06-11-metadata-rebuild-pr5-canonicalization-execution-status.md ("Out-of-scope follow-ups" section)
- Archive: 2026-06-11-metadata-rebuild-pr6-stage2-retag-execution-status-archive.md (Sessions 1-5 + PR-A Current-State snapshots)

## Session log

<!-- New session entries land here. When a PR ships and the next PR
     begins, prior PR's entries move to the archive file. -->

### (Sessions 1-5 — PR A cycle — moved to the archive file 2026-06-12 at PR-B cycle start)

New session entries land below.

### Session 6 — 2026-06-12 (interactive, ~12:45–14:15 ET, same calendar session as the Session-5 merge) — PR-B cycle start: B0 + B1 + B2 pre-fill — COMPLETE

- **Cycle start:** branch `feat/pr6b-stage2-retag-run` off `f982d1a`; Sessions 1-5 archived; impl-plan B-section refined into B0-B7 (`f3451d8`) folding the 9 PR-A deferrals into tasks.
- **B0 ✅ `7efcf50`** (286 tests): cache-floor `level:'unknown'` for unknown families + preflight `--help`. Two deferrals dissolved on investigation: toolChoice asymmetry ALREADY CLOSED in PR A (shared buildMessageRequest); repair-prompt review found garden_skills fabrications PROMPT-IMMUNE (rules already present in repair prompt; effective fix is code-side — normalize rule or repair anchored on chosen activity_type; decide at B3 with data).
- **B1 ✅ `fb6ab84`** (318 tests): deterministic sampler (seed 20260612, mulberry32), 40 stratified (activity_type × body-length quartile) + 20 adversarial (`data/answer-key-adversarial.json`, all class floors met), worksheet emitter + filled-worksheet parser with CONFIRMED-falls-back-to-DRAFT semantics. Supervisor re-ran determinism: byte-identical. Executor caught that the two dryrun-notes files use DIFFERENT L-numberings (r1 vs r3 order) — anchored every adversarial ID to verbatim citations, never L-numbers.
- **B2 pre-fill ✅:** 6 parallel Fable agents, 10 lessons each, BODIES ONLY (current PROD tags excluded — v3 is a scored contestant; anchoring-bias defense). Supervisor independent validation: 60/60 JSON-valid, exact sample coverage, single keyset = 13 anchors, ZERO vocab violations (heritage all label-form). Merged 780/780 DRAFT cells; `--parse` round-trip → 60 records. Evidence + notes at `artifacts/prefill/evidence-notes.md`; structured drafts (concepts everyday+synonym pairs — worksheet cell carries framework-only; B3 scoring must JOIN the draft JSONL for dual-vocab) at `prefill/draft-{1..6}.jsonl`.
- **Cross-batch findings for the user verification walkthrough:** 5 ruling clusters — CRF stamp tiers; Muslim/Eid heritage vocab gap; concepts gaps (fossil fuels, health topics); grade-claim boundary (CCSS citations / adaptation appendices / variation matrices); R4 adjudication came out PER-LESSON OPPOSITE directions on the Cajun twins (3-5 drop concepts; 6-8 ADD Science integration) — blanket normalize rule wrong at least sometimes.
- **Process learnings:** (1) the pre-fill independence rule (strip current tags before agent reading) is cheap and prevents the key from being anchored on the contestant it scores; (2) B0's "already fixed" outcome validates the executor STOP-don't-improvise rule again — deferral lists go stale across fix rounds; (3) merging agent drafts into a single artifact supervisor-side (python, by lesson-id anchor) avoided 6 agents contending over one file.
- **Next session (user-chosen format):** interactive worksheet-verification walkthrough — 25 flagged calls first (grouped by the 5 clusters), then user decides full-pass vs spot-check. Then `--parse` → final key → B3 cost OK.

### Session 7 — 2026-06-12 (interactive, ~15:00–17:10 ET) — B2 VERIFICATION WALKTHROUGH COMPLETE + B2.5 + B3 LAUNCHED

- **The whole walkthrough ran in one session:** 8 cluster/heritage AskUserQuestion rounds (~70 flagged calls → rulings A1-A20 + 22 heritage verdicts) + user chose FULL lesson-by-lesson pass → 16 more rounds covering all 60 lessons, each presented plain-language with doc links + filename/header info. Full ruling record: `docs/plans/2026-06-12-pr6b-answer-key-verification-rulings.md` (A1-A25 + §F corrections).
- **Format lesson (2× user clarification):** records must be narrated plain-language ("the draft says…"), AND the per-lesson summary must be INSIDE the AskUserQuestion text (dialog hides preamble). Memory updated (`feedback_plain_language.md`).
- **Two pre-fill-invisible discoveries, both user-caught:** (1) L11 grades/season lived in the Drive FILENAME + page HEADER — surfaces content_text lacks → systemic ruling A20 (both become pipeline inputs); background agent swept all 59 docs (49 carried signals!, 9 grade/season cell changes + 2 conflicts resolved by union rule A22). (2) Three docs were incomplete/non-lesson (L10, L21, L27) → removed from key (57 remain), corpus-deletion follow-ups + completeness-screen follow-up logged.
- **Notable rulings:** CRF AND SEL verbatim stamps KEPT (cell-primary; user overrode discount rec twice); core competencies stay evidence-based (CRF/SEL are special); all-seasons headers → all four (A21); both food-miles lessons academic (A23); per-lesson exceptions L2 (CCSS grade 3), L36 (+Italian), L6 (+Climate Change).
- **Commits:** `8ac6b24` rulings checkpoint · `9d4b626` exclusions + §F apply (executor, TDD, 323 tests; supervisor-verified 6/6 spot-checks) · `0591008` verification-complete docs · `ff3c7ee` B2.5 doc-surfaces sidecar + runner injection (TDD, 342 tests; 57 ids 1:1 with key) · `c908144` --ids flag (TDD) · `41a4f61` score-answer-key.ts (TDD, 379 tests total) · `ff16317` launch checkpoint.
- **B3 runs launched ~16:55 ET** (background, proxy v7.1.68 verified running): fable `--tool-choice-auto` conc 5 → `artifacts/answer-key-run.fable.jsonl`; o47 conc 3 → `…o47.jsonl`. ~Halfway at session close. Resume-capable (`--resume` + same flags).
- **Process learnings:** (1) supervisor independent spot-checks again earned their keep (caught my own probe misreading doc-surfaces nesting — the executor was right); (2) the user-as-verifier catches what body-only agents CANNOT (filename/header claims, incomplete docs) — the OQ8 "user gates everything" lock is load-bearing for exactly this reason; (3) launching real-money runs supervisor-side with executor-built mechanics kept cost control and TDD separated cleanly.

### Session 8 — 2026-06-12 (interactive, ~17:15–18:05 ET) — B3 COMPLETE: scoring, gate rulings, 4 follow-up tasks shipped

- **B3 finish:** fable refusal record retried 4× total — deterministic `stop_reason: refusal` on Seed Dispersal `1X7HadA1…` (likely "build seeds that explode" safety false-positive; sibling variant + o47 fine) → fable 56/57. Repairs: fable 1/1, o47 10/10 → 100% Zod-pass on usable records. Scoring (`b417694` + scorecard): **fable macroF1 0.858 · o47 0.777 · v3 0.532; fable ≥ v3 on all 13 fields.**
- **User rulings (AskUserQuestion rounds):** (1) **Gate 3 AMENDED** — recall floor gates support≥2 values only; singletons reported non-gating (the as-written gate flunked fable on 4 single-occurrence concept misses out of 31 singletons hit 26/31 + 1 refusal artifact — small-sample technicality, not a blind spot) → **fable passes all 3 gates, confirmed B4 winner**; (2) garden_skills code rule ADOPTED (B3 evidence: 14 fixes, 0 harm); (3) heritage mention-vs-about prompt rule + $1.34 7-lesson retest; (4) refusal fallback = o47 provenance-marked; (5) heritage retest came back INEFFECTIVE (bodies CLAIM cultural engagement the agenda never delivers — claims-vs-delivery) → user ACCEPTED, Protocol-B/OQ9 catch surfaces; (6) **B4 prep green-lit, run separately gated.**
- **Commits:** `b417694` scoring + scorecard · `cfffdee` Gate-3 amendment (TDD, 385) · `f698ba2` scorecard regen (all gates PASS) · `90208d0` garden_skills R6 rule (TDD, 389) · `088fba0` heritage prompt rule (391) · `3374a27` `--fallback-model` refusal-only fallback (TDD, 405) · `9deed81` checkpoint · impl plan gains B3.5a/b/c prep tasks.
- **Process learnings:** (1) AskUserQuestion preamble-hiding bit AGAIN even after the Session-7 memory note — the full explanation must live INSIDE the question field, every time, no exceptions; (2) per-value recall floors need a support floor when the key is small — gate definitions written before the key exists should expect a calibration amendment; (3) prompt-immune failure modes recur (o47 garden fabrications, now fable heritage claims-vs-delivery) — the pattern "diagnose → try prompt → measure → fall back to code rule or accept-with-catch-surface" is the house style now, and cheap targeted retests (~$1) make the measure step affordable.

### Session 9 — 2026-06-12 (interactive, ~18:00–20:50 ET) — B3.5 COMPLETE + B4 FULL RUN COMPLETE

- **B3.5c ✅ `7750586`** (executor, TDD): `data/corpus-exclusions.json` + run-retag exclusion loading (drops before --ids/--limit/--resume; --repair mirrors). 412 tests.
- **B3.5b ✅:** screen agent read all 765 bodies, flagged 51, judged each — 5 EXCLUDE + 8 BORDERLINE + 35 false-positives-kept; all 3 calibration docs caught; artifact `artifacts/completeness-screen.md` (supervisor-verified quotes/ids/lengths). **USER verdicts (4 AskUserQuestion rounds): exclude 7** (Soul Food Sunday, Flowers, Sandor Katz, Italy/Pesto, Juneteenth, Nigeria + Honduras drafts), **re-extract the 6 tag-card bodies** (fallback-to-exclude on 404). Applied by executor **`8953bb3`** (TDD): exclusions 3→12 (2 tag-cards 404'd: Equivalent Ratios, Mindful Eating), body-overrides manifest mechanism generalized from Who's-Who (4 overrides re-extracted, corpus.jsonl patched 4-lines-only, backup kept). 420 tests.
- **B3.5a ✅ `8a57623`:** 9 background sweep agents (8 chunks × ~88 + chunk-9 self-resolving the 88 `lesson_*` ids via PROD file_link) → sidecar 57→758 (607 real headers). 58-record filename-fix pass (chunk-9 'Tab 1' placeholders). Verification: all chunks 1:1, 9 Session-7 overlaps byte-identical, 2 fresh Drive probes exact. Tests updated to corpus-wide contract.
- **B4 ✅ green-lit then run in 6 window-paced batches** (user call: Max window had ~54% left, then reset): 415 lessons pre-reset ($66.44, zero cap-trips), 338 post-reset, repair pass → **753/753 Zod-passed, $121.28 total**. 5 refusals all o47-fallback-recovered. One lesson needed direct o47 main-pass + per-field repair (fallback-path payload-stuffing, deterministic 2×, quarantined to `full-run.fable.rejected.jsonl`).
- **Process learnings:** (1) sweep agents ignore "fetch real filename" instructions at scale — a dedicated correction pass with explicit target lists fixed 58 records cheaply; byte-identical overlap rows are a free cross-validation surface, design for them; (2) the o47 FALLBACK path has a deterministic payload-stuffing failure mode the MAIN path doesn't — and --repair only covers main-phase records, so the escape hatch is quarantine + re-run main-pass with --ids; (3) window-paced batching via --resume --limit worked flawlessly (6 batches, zero lost records, zero double-spend) — the resume-hash design earned its keep; (4) corpus tests reading gitignored artifacts will fail in CI — guard before PR B opens.
- **Follow-ups logged:** 2 dead public file_links (Trail Mix `1P8rs1bUmdlj…`, Curious Garden `1jQ5XtGr20Rf…`); `lesson_1753316245157_flurdiez2` stub row shares a doc with `lesson_cc0a5cb454…` (dedup track); 9 corpus-deletion candidates for the cleanup track (7 ruled + 2 tag-card 404s); tag-card content_text PROD repair rides PR C.

### Session 10 — 2026-06-15 (interactive) — CI guards + B5 (apply artifacts) + B6 (adjudication) COMPLETE; 13 user-verdict gate

- **Context:** session opened on Fable 5; user switched to Opus 4.8 (1M) mid-session. **Fable 5 + Mythos 5 are SUSPENDED globally** by a US government export-control directive issued 2026-06-12 17:21 ET (national-security citation over a narrow code-review jailbreak; Anthropic disputes it and is working to restore — https://www.anthropic.com/news/fable-mythos-access). Indefinite external suspension, NOT self-clearing and NOT a config issue on our end; all other models unaffected. Executors ran on **Opus** (judgment-heavy fallback while Fable is suspended; Sonnet floor stands). **Consequence for this track: the B4 full run was done on claude-fable-5 BEFORE the suspension and its 753 records are saved to disk — PR B consumes saved artifacts, so it is NOT blocked. But the run is no longer reproducible as-Fable (the runner's fallback `claude-opus-4-7` would produce different output), so `artifacts/full-run.fable.jsonl` is now the SOLE source of truth for the Fable tagging — do not lose it (gitignored; back it up before any artifacts-dir cleanup).**
- **CI test guards ✅ `b2a2101`** (executor, supervisor-verified): `it.skipIf(!existsSync(...))` on the 3 assertions reading gitignored artifacts (answer-key.final.jsonl, corpus.jsonl ×2). CI-sim (artifacts moved away) = 417 passed / 3 skipped / **0 failures**. Closes the Session-9 pre-PR-B blocker.
- **B5a diff report ✅ `18763c7`** (TDD, +5 → 425): extended generate-diff-report to honor corpus-exclusions; repair/fallback selection already correct via latestRecordById. 753/753 compared, 0 Zod-fails. academic_concepts 98% change (by-design), Activity Type +craft 146 / −garden 105. Supervisor spot-checks vs raw JSONL exact.
- **B5b prepare-apply ✅ `59f0d5c`** (TDD, +44 → 469): emits staging SQL/CSV + draft apply migration (PR-5a precedent: pr6_retag_rollback snapshot ON CONFLICT DO NOTHING, then 753 idempotent dual-write UPDATEs) + 90-lesson spot-check worksheet. **Supervisor independently verified emitted SQL: academic_concepts column writes = 0 (JSONB-only per `column:null`), academicConcepts JSONB writes = 753, dual-write present, grades staged-without-diff.** B5 COMPLETE.
- **B6 adjudication ✅ `29da364`** (executor on Opus, supervisor-verified): `artifacts/register-adjudication.md` (`-f` tracked). 74 signals → R 24 · V 18 · U 19rows/13q · P 5 · D 4 · X 3 · B 1. 5 independent spot-checks vs raw run file all hold. **HEADLINE: re-tag emits leaf heritage tags only; AA/Indigenous root under indigenous-and-diaspora cluster → no North American even post parent-expansion (de-facto identity-anchor convention = U-1).** Honest heritage over-tag caveat applied (ME-03 clearest claims-vs-delivery survivor).
- **GATE (user-chosen: fresh session):** 13 disposition questions U-1..U-13 in the artifact's "USER VERDICT NEEDED" section. Re-tag is FINAL ($121 spent) → these are accept/defer/targeted-fix dispositions, NOT re-runs. ~5 genuine (U-1 North-American pairing = biggest, U-2 CON-16 cross-field, U-5 Levantine over-reach, U-9/10 empanadas Mediterranean, U-12 ME-03 thin-tag); ~5 deferrals to filter-UI/vocab-expansion tracks; ~3 accept-the-cleanup. **NEXT SESSION = the 13-question walkthrough (Session-7 style, plain language, AskUserQuestion rounds, full explanation INSIDE each question field) → record verdicts → bundled register status edits (supervisor owns; both `*-audit-signal-register.md`) → B7 ritual → open PR B.**
- **Artifact-tracking decision still DEFERRED to B7:** diff report (651K), draft migration (1.6M), staging SQL/CSV, spot-check (97K) all gitignored; register-adjudication.md `-f`-tracked. At PR-open decide which review artifacts to `git add -f` (lean: spot-check worksheet + diff report; SQL dumps stay regenerable-local).
- **Process learnings:** (1) deferral-list staleness struck again in a GOOD way — the CI-guard "blocker" was real and cheap; clearing known pre-PR blockers FIRST kept B5/B6 from compounding risk. (2) B6's leaf-only-heritage headline is the kind of structural finding only a full-corpus adjudication surfaces — the OQ9 pass earned its place as a Protocol-B catch surface exactly as Session 8 predicted. (3) artifact-only executor dispatch (no register edits) cleanly separated "produce evidence" from "user decides + supervisor applies" — the right shape when output gates on user judgment.

### Session 11 — 2026-06-15 (interactive) — B6 gate CLEARED: 13 user verdicts + 74-signal register apply

- **13-question walkthrough (U-1..U-13) COMPLETE** — interactive, one-at-a-time (user-chosen pacing), Session-7 style with full explanation INSIDE each AskUserQuestion field. Verdicts recorded in `register-adjudication.md` verdict table (`47e6449`). Outcome: 10 accept-re-tag-as-is + 3 targeted PR-C fixes (U-5 Fattoush trim, U-8 Alternative Proteins trim, U-12 Salad Project drop ME). U-1 (the structural keystone, folds AME-01/X-02/X-03/X-07) = **accept identity-anchor** — no `North American` re-added to AA/Indigenous leaves. User declined every "log future follow-up" option → no new future-track items.
- **74-signal bundled register apply ✅ `2767f83`** (Opus executor; supervisor-verified zero-Open + spot-checks). All 74 across both registers Open → Resolved (69) / Deferred (5). The B6 per-row mapping (R 24 · V 18 · U 19 · P 5 · X 3 · D 4 · B 1) drove the categories; the 13 verdicts drove the U-row status text.
- **Process learnings:** (1) the X-prefix-vs-X-category trap (signal IDs X-01..X-09 are NOT all category-X "excluded"; X-04 is category-P) is exactly the kind of latent confusion a spec'd executor handles cleanly when the supervisor hands it the authoritative per-row mapping + explicit per-signal lists — verification was deterministic because the spec was complete. (2) committing the verdict record (`47e6449`) BEFORE dispatching the register-apply executor removed any git-index race and gave the executor a stable on-disk spec to read. (3) one-at-a-time walkthrough pacing made U-1's keystone role visible and let later empanada/Levantine questions inherit the convention the user had just set — sequencing as a teaching aid.
- **NEXT: B7 ritual → open PR B** (artifact-tracking `git add -f` decision, pre-push reviewer dispatch on the full B0-B6 diff, push, `gh pr create`). No DB writes in PR B.

### Session 12 — 2026-06-16 (interactive, supervisor) — PR-C cycle start + HERITAGE PIVOT to Option B

- **PR B (#508) confirmed merged** to main as `44da8f6`; on `main`, baseline clean (type-check+lint). Verified sole-source `full-run.fable.jsonl` + apply tooling present on main; migration date floor `20260613000000_…` (today 2026-06-16 sorts clean).
- **Branch `feat/pr6c-stage2-retag-apply` created off main.** User chose (cycle-start gate) "refine PR C plan then checkpoint" + "targeted-fixes manifest" for the U-5/U-8/U-12 drops.
- **Pre-build investigation (2 read-only Agent dispatches, supervisor-verified):** (1) apply-tooling map — staging chokepoint is `prepare-apply.ts:193` (`fields[field]=newValues`); NO parent-expansion exists anywhere in the pipeline (grep-confirmed by supervisor); `body-overrides.json` is the manifest precedent to mirror; 12-field dual-write mapping confirmed (`academic_concepts` JSONB-only); draft migration already mirrors PR-5a snapshot-then-update; **Who's-Who `content_text` PROD repair is net-new PR-C migration work (export side handles re-tag input only).** (2) filter-mechanism map — **leaf-only apply = filter regression**, VERIFIED on PROD: `cultural_heritage_hierarchy` = 6 rows, no `Americas`/`North American`/`Caribbean`, single-tier expansion in `search_lessons` RPC (`expand_cultural_heritage`); current corpus stores full ancestor chains (Chinese=`["East Asian","Asian","Chinese"]`, Mexican=`["Mexican","Latin American","Americas"]`); THREE divergent hierarchy sources (UI slugs / PROD table / legacy `filterConstants.ts`). §16 `cultural-heritage.vocab.json` = complete correct multi-tier tree (6 roots incl. `Indigenous and Diaspora`; AA + Indigenous root there, never North American).
- **HERITAGE PIVOT decided with user (4 plain-language rounds):** Option B (specific-only storage + smart recursive filter), filter-first, open-ended maintenance window. Re-opens §7/OQ7/OQ13. Pivot block written at top of this file + recent-decisions entry + design/impl-plan pivot notes (this session).
- **NEXT SESSION:** heritage-filter-rebuild **design pass** (brainstorming-first) — unify 3 hierarchy sources → 1 (§16 vocab), recursive expansion in `search_lessons`, UI alignment, E2E; checkpoint design with user BEFORE code. Then build filter (verify backward-compat on current PROD data) → then the apply (12 fields, specific-only heritage, Who's-Who repair, embeddings). STILL PENDING: archive Sessions 6-11 to archive file.
- **Process learning:** the supervisor-verify gate caught a track-altering issue BEFORE any code — the locked plan's one-line "apply dual-writes from staged data" hid a filter-breaking assumption (leaf-only storage) that only a pre-build investigation + PROD probe surfaced. Refine-then-checkpoint earned its keep on the highest-stakes PR.

### Session 13 — 2026-06-16 (interactive, supervisor) — heritage-filter-rebuild DESIGN PASS complete + PR C1 tasks authored

- **Orientation:** git matched the status file (branch `feat/pr6c-stage2-retag-apply`, tip `525ddbf`, PR #508 merged `44da8f6`); baseline type-check+lint clean. Confirmed with user, then proceeded into the design pass (brainstorming-first per the pivot).
- **Understand workflow (`wf_a507e074-da2`, 6 agents, read-only):** 4 parallel readers (hierarchy sources / RPC mechanism / UI contract / storage shapes) + 1 adversarial keystone verify + 1 synthesize. Supervisor-grounded against `data/vocab/cultural-heritage.vocab.json` (71 nodes, 6 roots incl. `indigenous-and-diaspora`, `filter_ui_tier` + `alias_map`) and `IntCulturalHeritageSection.tsx` (53-line, 2-level, non-recursive) + `filterDefinitions.ts` heritage block (5 regions + 13 children). **Keystone:** regression CONFIRMED (specific-only under flat RPC: Asian 65→36), backward-compat CONFIRMED (recursive from vocab.json: Asian 65→74, strict superset), all 61 live labels exist in vocab.json (zero orphans). Footgun: slug-vs-label (storage=labels, UI=slugs; expansion must resolve to labels).
- **Brainstorming walkthrough (skill-driven, 4 AskUserQuestion rounds, plain language + ASCII previews):** UI scope = **vocab `top`+`sub` regen (~31 nested)**; mechanism = **DB-side live recursive**, keep label storage + alias bridge; **PR C1/C2 split** approved; display = **nested/indented tree**. Then presented the 6-section design; user approved all.
- **Artifacts:** design doc `docs/plans/2026-06-16-heritage-filter-rebuild-design.md` (LOCKED) + cross-links into the PR-6 design doc's re-opened OQ7/OQ13/§7 — committed **`71cfdb6`**. Impl plan gained the full **PR C1 task list (C1.0–C1.10, TDD)** + relabel C0→C1 / C→C2 (apply tasks renumbered C2.1–C2.4) + heritage portion of C2 specified — committed with this status update.
- **Newly surfaced (logged, not scope-crept):** (1) facet-count coherence — after C2's specific-only storage a parent's *direct-tag* count understates the expanded result set; decide in **C1.6**. (2) Apply-time heritage error candidates to VERIFY against `full-run.fable.jsonl` in C2: Arroz con Gandules spurious `South Asian`; two Salsa Toasts → `[]`.
- **Process learnings:** (1) the Understand-workflow's synthesized brief + targeted supervisor reads (vocab.json, the actual component) kept supervisor context light enough to carry orientation → design → plan in ONE session; (2) AskUserQuestion **previews** (flat-vs-nested ASCII mockups) made an abstract UI-depth choice concrete in one round — reach for previews when the decision is visual; (3) authoring the plan surfaced the C1/C2-vs-task-label naming collision — resolving it in the doc (not at build time) avoided a mid-execution rename.
- **NEXT SESSION = BUILD PR C1**, starting **C1.0** (read-only baseline). Create branch `feat/pr6c1-heritage-filter`. Still-pending housekeeping: archive Sessions 6-11.

### Session 14 — 2026-06-16 (interactive, supervisor) — BUILD PR C1 started: C1.0 baseline ✅

- **Orientation:** git matched the status file (branch `feat/pr6c-stage2-retag-apply`, tip `d0c9e6a`, PRs #507 + #508 merged); baseline type-check+lint clean. User confirmed orientation.
- **Branch `feat/pr6c1-heritage-filter` created** off `d0c9e6a` (carries the design + pivot commits; the apply branch stays separate per the design's filter-first split).
- **C1.0 ✅ `2f22dd5`** (Opus executor, supervisor-verified): read-only PROD baseline → `scripts/heritage/artifacts/heritage-filter-baseline.json` (git-tracked gate evidence, 21.5KB). Verbatim defs of `expand_cultural_heritage`/`_alias_cultural_heritage`/`search_lessons` (no overloads) + 6-row `cultural_heritage_hierarchy` (shape is `(id,parent,children[])` Title-Case, not the guessed `culture/parent_culture`) + before-counts for all 31 vocab top+sub options via the faithful live heritage path (`&&` with `retired_at IS NULL`). Sanity anchors: 767 live lessons / 339 with heritage.
- **Supervisor independent re-probe (PROD MCP, verbatim):** Asian=65 (keystone CONFIRMED), chinese filter 0 / label-stored 15, african-american 0/27, mexican 0/38, total 788 all / 767 live — all match the artifact exactly.
- **Key finding (expected, not a blocker):** 15/31 options return 0 today because `_alias_cultural_heritage` hardcodes only the 18 current-UI slugs; other slugs fall through `ELSE ARRAY[x]` and stay slug-form, never matching Title-Case-label storage (the design's slug-vs-label footgun, empirically proven). C1.9 will verify these heal (chinese 0→15, mexican 0→38, african-american 0→27, etc.). All 18 current-UI values exist as vocab keys (zero slug drift); drift is structural (UI omits the Indigenous/AA/Indigenous-and-Diaspora groups + mis-parents Ethiopian/Nigerian).
- **Process note:** executor correctly used the `retired_at IS NULL` faithful path (the task's literal SQL omitted it) — matched live `search_lessons` rather than the spec's shorthand, and captured both variants. Right call; flagged it instead of silently diverging.
- **C1.1 ✅ `b56f0b5`** (Opus executor, TDD, supervisor-verified): generator `scripts/heritage/generate-heritage-hierarchy.ts` emits committed `src/utils/heritageHierarchy.generated.ts` (31 nested top+sub options) + `scripts/heritage/artifacts/heritage-hierarchy-seed.sql` (71 rows). 13 generator tests incl. two byte-identical drift guards. Generator pipes the TS artifact through the project Prettier API so the committed file is canonical AND drift-stable (clean solution to `src/` being lint-enforced). Registered `scripts/heritage/**` in `tsconfig.scripts.json` + re-included in ESLint. Commit scope = exactly 6 files; zero migration files; zero out-of-scope edits.
- **Supervisor independent verify:** re-ran generator → no git diff (byte-identical); cross-checked artifacts vs vocab.json — 71 nodes / 12 top + 19 sub = 31 top+sub / 40 internal / 6 roots; UI artifact 31 options 6 roots, zero internal-leak, zero label-mismatch, soul-food correctly internal-excluded; seed 71 rows / 6 NULL-parent. Ran the test (13/13) + type-check + lint myself: all green. Bonus finding: `FilterOption` already recursive → C1.4 likely needs no type change.
- **C1.2 ✅ `3d83875`** (Opus executor, `database-migrations` skill, supervisor-verified): migration `20260616000000_heritage_recursive_expansion.sql` (LOCAL only) — recreate `cultural_heritage_hierarchy (key PK, label, parent_key→self)` + reseed 71 rows + recursive `expand_cultural_heritage` (seed key-OR-label, recurse parent_key, return labels + passthrough; signature/VOLATILE/INVOKER preserved). RLS+grants recreated from baseline. Also removed an obsolete old-shape sample insert in `supabase/seed.sql` (broke `db reset`; local-dev-only, in-scope). Pre-flight (supervisor PROD `pg_get_functiondef` scan + executor local scan): only `expand_cultural_heritage` reads the table; 0 views/FKs/triggers.
- **Supervisor verify (C1.2):** independent local MCP probes — 71 rows / 6 roots / 0 orphan parent-refs; `expand(asian)` = full 15-label subtree; `expand(chinese)` = `Chinese` (slug→label bridge proven); `expand(african-american)` = `African American, Black culinary history, Soul Food`; NULL→NULL; function VOLATILE+INVOKER. Footgun fixed, recursion confirmed.
- **C1.2 fix-up `aabbd86`** (supervisor-initiated; Opus executor, TDD): the C1.2 executor had hand-reordered the migration's seed rows away from the C1.1 generated fragment on a FALSE rationale ("to satisfy parent_key references" — but a single multi-row INSERT satisfies a self-FK at statement-end regardless of order), diverging from the design's single-source-of-truth principle. Fix: generator now emits the SQL seed in deterministic **topological order** (depth ASC, freq DESC, key ASC) — TS/UI ordering unchanged (display freq-DESC) — and the migration uses the fragment VERBATIM; misleading comment corrected; +topological-validity test (15 generator tests). Supervisor verify: regenerate→no diff (both artifacts byte-identical), migration INSERT == fragment IDENTICAL, all 71 rows topologically valid, migration diff touches only the reseed comment + INSERT (function/DDL/RLS untouched), `supabase db reset` clean, type-check+lint green.
- **Process learning:** the supervisor-verify gate earned its keep again — reading the migration surfaced a false-premise hand-edit (correct output, wrong reasoning + a latent single-source-of-truth drift) that a green-tests-only check would have passed. Worth fixing immediately while context was fresh rather than deferring to the C1.10 reviewer round.
- **C1.3 ✅ `5e9282f`** (Opus executor, TDD, supervisor-verified): standalone LOCAL-DB test `scripts/heritage/test-heritage-expansion.mjs` + npm `test:heritage`. **Harness decision (supervisor recon):** vitest suite is hermetic + CI runs `npm run test:coverage --run`, so a DB-connecting vitest test would break CI → C1.3 mirrors the `test:rls` standalone-script idiom; CI-side RPC coverage comes from C1.8 E2E. Exercises the REAL `search_lessons` RPC end-to-end with specific-only fixtures; 6 assertions. Executor sharpened case 1 with **case 1b (`asian`→Taiwanese)** — plain Chinese was ALSO an old flat-table direct child so case 1 alone isn't a clean recursion-RED; Taiwanese (deep leaf absent from old flat list) is. RED via rolled-back-txn reconstruction of the old one-tier function. Data-safety guard (local-URL assertion, exercised) + fixture cleanup.
- **Supervisor verify (C1.3):** ran `npm run test:heritage` myself (6 passed, exit 0); independent MCP query = 0 leftover `c13-heritage-test-%` rows; vitest unchanged (1063 passed); type-check+lint green; scope = 3 files.
- **Process learning:** ESLint flat-config ignores `--ext`; lints by `files` globs (any file under un-ignored `scripts/heritage/` is linted regardless of extension). Logged in Current State for C1.4+.
- **Session 14 close:** delivered the entire DB layer of PR C1 (C1.0 baseline · C1.1 generator · C1.2 migration + fix-up · C1.3 expansion tests), 8 commits on `feat/pr6c1-heritage-filter` off `d0c9e6a`, every task supervisor-verified + checkpointed. type-check+lint green; nothing pushed; no PR yet. **NEXT SESSION = C1.4** (UI options wiring + recursive helpers). Still-pending housekeeping: archive Sessions 6-11 to the archive file (non-blocking).

> (Sessions 15-17 detail lives in the `## Session 16` / `## Session 17` blocks near the top of this file — not duplicated here. PR-B Sessions 6-11 detail moved to the archive file 2026-06-16.)

### Session 18 — 2026-06-16 (interactive, supervisor) — C2 cycle start: branch setup + C2 plan refined + decisions locked

- **Orientation:** git on `feat/pr6c1-heritage-filter` (dead C1 branch, ahead-1 docs); fetched → real `origin/main` = `f3f419c` (C1 merge). Confirmed C1 done + live on PROD. Baseline type-check+lint green. User confirmed → proceed with cycle-start mechanics.
- **Codex plugin setup** (user ran `/codex:setup` mid-session): installed `@openai/codex` 0.140.0 (npm), companion `ready:true`, ChatGPT auth active. `codex:codex-rescue` agent now available. Review-gate left OFF.
- **C2 branch setup ✅ `0c5fef9`:** checked out `feat/pr6c-stage2-retag-apply` (local-only, 3 docs-only commits, never pushed); merged `origin/main` (`f3f419c`) → C1 code arrived clean, only docs conflicts; resolved all 4 scaffold docs to the most-current C1-branch state (`51bc312`, a strict superset). `npm ci` + type-check + lint green on the merged tree. Branch now `0 4` vs main (contains all C1 code + the live filter).
- **PR-B cycle archived ✅** (cycle-start ritual): appended a condensed "PR B cycle (#508, Sessions 6-11)" section to the archive file (commit hashes + decisions + headlines preserved; full verbatim recoverable from git at `51bc312`). Audited for promotable learnings → **none new** (the durable ones already in `feedback_plain_language.md` / `feedback_workflow_orchestration.md`; MEMORY.md is also over its size cap). PR-C1 Sessions 12-17 detail kept in the live doc one more cycle (archives at the D-cycle start).
- **C2 recon (Agent, read-only, `a37c3d07…`) + supervisor verification:** mapped `prepare-apply.ts` + the apply migration shape + targeted-fix data. **Independently re-verified the high-stakes claims:** heritage arrays for U-5/U-8/U-12/Arroz/Salsa via the run file (all exact); `055b000b` = 0 grep matches (design doc's Arroz id is wrong → real `1VShcRmc…`); migration floor `20260616000000`; `prepare-apply.ts` `FLAT_FIELDS` confirms heritage staged as-tagged (no expansion).
- **PROD censuses (read-only) for the CHECK-constraint data-safety landmine:** the 6 small fields are NON-canonical on PROD (113 live rows carry kebab twins + a few strays). Anti-joined against the 753 run ids → **all 113 are in-corpus (apply fixes them); live residue = 0.** Incl. retired rows: exactly **1 retired blocker** `15T4wU94…` (`Food Justice` in core_competencies) → 1-row vocab-aligned fold makes the table canonical. Verified embedding input excludes metadata → C2 embeddings = 1 row.
- **3 user decisions (AskUserQuestion):** embeddings 1-row + metadata-embed deferred; CHECK constraints IN C2 + 1-row retired fold; Salsa Toasts no-action. (Recorded in recent-decisions + out-of-scope.)
- **C2.1–C2.4 task plan AUTHORED** into the impl plan (`## PR C2`, supervisor-owned): concrete file paths, the 4-correction manifest w/ verbatim ids, the migration section order (load-bearing), the small-field vocab + CHECK precedent, the U-1 assertion, 1-row embeddings. Migration target `20260617000000_pr6c2_retag_apply.sql`.
- **Process learnings:** (a) supervisor-verify caught the design doc's wrong Arroz id (`055b000b` doesn't exist) — a recon agent's claim that, unverified, would have hard-coded a non-existent id into the C2 manifest. (b) The "feared" CHECK-constraint landmine dissolved under evidence (residue 0 + 1 retired fold) — gathering the PROD census BEFORE asking the user turned an abstract scary question into a concrete safe one. (c) Resolving the apply tooling's heritage handling early confirmed the $121 run + B5 emitter need NO rework for the pivot.
- **Codex plugin adopted + first adversarial review (gpt-5.5/xhigh) on the C2 plan:** user installed the Codex plugin + asked how to use it; configured (already gpt-5.5; effort `xhigh` kept — exceeds the requested "high"). Ran `codex-companion.mjs adversarial-review --base 0c5fef9` on the C2-plan commit (background). **Codex caught 3 real issues the recon + 4 PROD censuses + supervisor-verify all MISSED:** (1) embeddings recipe is metadata-INCLUSIVE (`prepareLessonText`; 3 inconsistent recipes across the codebase) → the "1-row, metadata-free" call was WRONG → embeddings DEFERRED out of C2; (2) `grade_levels` written UNCONDITIONALLY by the emitter → 731 overwrites + 33 BLANKS of a user-facing filter with no review → C2.1 now guards (never write empty) + emits a before/after grade diff for Protocol-B signoff; (3) U-1 hand-listed label set incomplete vs the hierarchy → derive it + assert column & metadata. All 3 independently verified against code, accepted, folded into the impl plan PR C2 + this status doc. **Codex wired in as standing gates ✅** (plan-lock + pre-TEST migration + parallel pre-push reviewer) — added to this track's kickoff (`b5b02d9`) AND the generic opt-in block added to the `~/.claude/templates/multi-session-execution/` kickoff template + README (user-approved). Note: `/codex:*` commands are `disable-model-invocation` (user-invoked); the supervisor runs the wrapped `codex-companion.mjs` when driving a gate, and pulls background results via `/codex:status`+`/codex:result`. **Process learning (promote candidate): a different-model-family adversarial reviewer is LOAD-BEARING — it surfaced a reversed decision + a 33-row data-loss risk that a Claude-only review chain (4 PR bots + recon + supervisor-verify) did not.**
- **Session 18 close:** branch set up, PR-B archived, C2 plan concrete + **Codex-corrected**, decisions locked. Docs bundled into the cycle-start commit + a follow-up plan-correction commit. **NEXT SESSION = BUILD C2.1** (corrections manifest + emitter wiring + grade guard, TDD). No DB writes; nothing pushed; no PR. All merges + PROD approvals USER-GATED.
