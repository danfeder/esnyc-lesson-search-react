# Concepts Worksheet Wizard — Execution Status

> **FROZEN 2026-06-10.** This doc is history only — no further session logs land here.
> The remaining work (old M3.7/M3.8/M3.9 + the B2 reasoning sidecar + handoff) moved to the slim
> **`2026-06-10-concepts-wizard-finish-plan.md`**, which `/concepts-batch2` now points at.

> **Plan:** `2026-05-15-concepts-worksheet-wizard-plan.md`
> **Design:** `2026-05-15-concepts-worksheet-wizard-design.md`
> **Per-milestone kickoff:** `2026-05-15-concepts-worksheet-wizard-execution-kickoff.md` ← paste at start of each fresh session
> **Original brainstorm kickoff:** `2026-05-15-concepts-tool-simplification-kickoff.md` (design phase only; do not re-execute)

## Current state

- Branch: `tools/concepts-worksheet-form` (not pushed; no PR)
- Batch 1 status: **SHIPPED** (M1.17 smoke gate passed 2026-05-15)
- Batch 2 plan: **FINALIZED 2026-05-28** — 6 self-review revisions applied; **M2.0 plain-language pass added**; design §4.1 + W22 + global plain-language convention recorded
- **Batch 2 status: SHIPPED 2026-05-28.** All milestones M2.0 → M2.7 complete. The wizard is gated (8/8 smoke, M2.6) and documented (M2.7 README). Branch not pushed; no PR.
- **Batch 3 (tool polish) IN PROGRESS — opened 2026-05-28 from a pre-handoff dry run.** Two role-play agents (non-technical first-time reviewer + curriculum domain skeptic) walked the real tool; structure validated, but found 2 blockers + several should-fixes. Batch 3 fixes the bounded display-layer ones (M3.1–M3.9 — see plan "Batch 3" section). Two findings are out of Batch-3 scope: **B2** (internal jargon in the per-concept reasoning — the dominant usability risk + the known M2.0 deferral → its own `concepts-reasoning-rewrite` initiative, to be scaffolded) and **W3** (worksheet-data content errors like `measurement`'s "anatomically" copy-paste → folded into B2 / metadata-rebuild track).
- Last milestone completed: **M3.4 — group-override stale rationale** (`e748640`) — reframe the now-stale per-concept note under a group-override headline + suppress the per-concept suggestion block during the "hold off" state (keep the ✓ Agree button + "Pick something else" per user choice). Prior Batch 3: M3.2+M3.3+M3.5 = `6ba3df6`; M3.1 legend fix = `e4cdcd2`. Batch 2: M2.7 README (`f56f124`), M2.6 smoke gate (`cd143b1`).
- Next milestone: **M3.7 — friendly labels over snake_case keys** (reviewer-facing, multi-site → workflow-orchestrate; prose → show the user). Then M3.8 (nits bundle), M3.9 (re-gate). **M3.6 (keyboard-shortcut footgun) — SKIPPED / won't-fix per user decision 2026-05-29** ("i dont care about keyboard shortcuts for this"): the single-key shortcuts (k/m/n/d verdict keys, L = decide-later, Enter = save·next, ← = previous) stay as-is; the silent accidental-override risk is accepted as a known limitation. Same safety floor as Batch 2 (empty-export SHA `0c49a7a7…cd03` byte-identical, parser untouched, plain-language locked, no generated-HTML commits, no push/PR).
- **RESOLVED DECISION (2026-05-28) → M2.1b SCOPE ADDITION — ✅ SHIPPED in M2.1b (Session 19):** info-only cluster captions (CON-16 opt-1 heritage-reframe; CON-24 opt-2 pre-parent-pick) currently render **only in `renderDecideStep`**, but every info-only member in the real corpus is a §13/Confirm-routed entry — so those "verdict here pending …" notices are invisible to reviewers. **User decided: make them visible.** → **M2.1b must also wire the `.cluster-info-caption` render into `renderConfirmStep`** (the entry calls `displayedPrefillForEntry`, which already carries `clusterInfoCaption` from M2.1 — render it the same way `renderDecideStep` does, ~6 lines). Keeps the "hold off, this is pending another decision" safeguard working in the common (Confirm) path. The note text stays plain/curriculum-voice (W22) — but note the existing `caption_text` strings carry `CON-16:`/`CON-24:` prefixes (data in `CLUSTER_DERIVATIONS`), which are reviewer-visible once shown in Confirm mode → de-jargon them when wiring (drop the `CON-xx:` prefix; say e.g. "Hold off — this is pending the heritage review.").
- **Run mode (set 2026-05-28):** resume via the project command **`/concepts-batch2`** (re-primes a fresh session: loads state + next milestone; preferred over re-pasting the kickoff). **Review cadence relaxed to "prose + smoke only"** — auto-proceed through mechanical milestones (M2.1/M2.1b/M2.2/M2.3/M2.5), full review only on prose (M2.4/M2.7) + the smoke gate (M2.6). Always-on safety floor (empty-export SHA, no generated-HTML commits, no push/PR, parser untouched, plain-language locked) is never relaxed. Details in the kickoff's 2026-05-28 cadence note.
- **WORKFLOW-BY-DEFAULT (standing user directive, reaffirmed Sessions 19–21):** under ultracode, **orchestrate each milestone's execution as a sequential dynamic Workflow — do NOT run milestones inline** (the user course-corrected when M2.1b ran inline). Pattern: executor → adversarial-verifier; the workflow does NOT commit; the **main loop independently reviews the diff + re-confirms the empty-export SHA + browser-checks the risky bits, then commits.** The main-loop verify is load-bearing — M2.2/M2.3 agents missed a "verdict" W22 slip + a skipped edit that the main loop caught. Inline is the exception (trivially small change + healthy context only). Authoritative how-to: the `/concepts-batch2` command's "Execution via workflow" section.

## Branch baseline at M1.0

```
dd7d45b docs(concepts-worksheet): tighten design clarifications     ← pre-flight clarifications
aeafcb0 docs(concepts-worksheet): patch round-2 nits + display priority fix
6d4ef5f docs(concepts-worksheet): patch design contradictions before impl plan
671447b docs(concepts-worksheet): design doc for wizard redesign
94fc66c tools(concepts-worksheet): apply Codex review feedback (F1-F4)
d284144 tools(concepts-worksheet): add HTML template + wire --build-html
de21973 tools(concepts-worksheet): add parser + builder scaffold
```

Parser baseline: `Parsed 208 entries (§11=32, §12=39, §13=137).`

## Session log

### Session 1 (2026-05-15)

- Read design end-to-end (`2026-05-15-concepts-worksheet-wizard-design.md`).
- Drafted plan; 3 review rounds with the user surfacing 11 findings total (6 P1/P2 fixes in round 1, 3 in round 2, 2 P3 nits in round 3). Plan v4 signed off for execution with per-milestone review.
- Committed design-doc clarifications standalone (`dd7d45b`) before plan finalization.
- **M1.0 complete (`c849fff`):** plan + status doc committed; branch verified clean against plan baseline.
- **M1.1 complete (`6ae69ef`):** parser emits `claude_notes_summary`; 208/208 entries have non-empty summaries with terminal punctuation.
- Per user request, scaffolded `2026-05-15-concepts-worksheet-wizard-execution-kickoff.md` as a durable per-milestone kickoff prompt so future sessions can `/clear` between milestones to keep context fresh.

### Session 2 (2026-05-15)

- **M1.2 complete (`b22e06a`):** parser emits `suggested_merge_target` via `Recommend merg(e|ing) into [§N ]`target`` regex with valid-key filtering plumbed through `entry_to_json` / `build_payload`. Extraction matched W19's empirical prediction exactly: 53/78 (68%) merge recommendations resolved, 25 fall through to picker by design.
- **M1.3 complete (`08e42d1`):** `--verify-only` (and full payload runs) now print `merge-target extraction: 53 of 78 merge recommendations (68%) — 25 fall through to picker` after the existing tier-distribution line; warns if rate ever drops below 60% floor. **Smoke check #8 baseline recorded: 53/78 (68%).** Matrix will be filled at M1.17 gate.
- Bundled in same session per Group rule (trivial parser-only TDD).

### Session 3 (2026-05-15)

- **M1.4 complete (`53024ca`):** template rewritten end-to-end (2198→1085 lines; -1462/+349). CSS replaced with ~290-line wizard-shell draft (single-card layout, top-bar decision-debt region, mode-chip + tier-strip tokens for M1.5+); body HTML swapped to `<header.top>` + single-card `<main>` + three-button nav + simplified onboarding modal. JS deletions: filter-state quartet, `buildSubjectChips`/`subjectCounts`/`setFilter`/`applyFilters`/`updateStatusCounts`, `parseReviewerNotesFromMarkdown`, `renderAllEntries`, `renderClusterSignalCard`, `updateClusterSignalMembers`, `updateProgress`, `scrollToEntry`/`scrollToClusterSignal`, `updateEntryCardChrome`. JS stubs added: `renderEntryCard(entry, mode)`, `renderClusterSignalsPanel`, `buildStepSequence`, `renderCurrentStep`, `navStep`, `navDefer`, `onJumpToEntry`, `updateDecisionDebt`. `reviewer_note` channel fully removed (W5): `buildExportMarkdown` no longer emits the HTML comment; `onImport` no longer parses it; legacy files import without alert noise. `init()` rewritten per plan Step 5; `onClearState` + `onVerdictChange` + `onMergeIntoChange` redirected to the new stubs. Preserved utilities: `el`, `escapeHtml`, `renderInlineMarkdown`, entry status helpers (`entryFilled`, `entryVerdict`, `entryMergeInto`, `entryNeedsMergeTarget`), `verdictLabel`, `tierReviewDepth`, `ensureCanonicalKeysDatalist`, `setBadgeDirty`, `clusterSignalCommentBlock`, `downloadFile`, `onExport`, `onDownloadJson`, `parseEntryEditsFromMarkdown`, `parseClusterSignalsFromMarkdown`, onboarding helpers, `toggleAdvancedMenu`.
- **Smoke check #1 (Empty-export SHA invariant) PASS** — `buildExportMarkdown()` SHA-256 = `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03`, byte-identical to source `2026-05-12-metadata-rebuild-stage1-concepts-worksheet.md` (264,428 bytes, 3547 lines). Verified via chrome-devtools-mcp `evaluate_script` against the built artifact.
- Browser smoke (chrome-devtools-mcp): zero console errors; onboarding modal renders + dismisses; top bar shows all expected pieces (brand, saved badge, 0/0/0 debt counters, jump-to-entry, disabled Review-so-far, ⚙ Advanced); placeholder body line "Wizard step machine not yet wired. 208 entries loaded." renders; Advanced menu opens with all 5 items including "Save & Export markdown ↓".
- `tierReviewDepth()` refreshed to W10 wording ("review carefully" / "confirm or adjust" / "quick pass; pause when unsure") since renderers in M1.7+ will consume it.

### Session 4 (2026-05-15)

- **M1.5 complete (`e25e8bc`):** added `entryMode()` (design §3 routing), `clusterPrefillCandidate()` Batch-1 stub returning null, and the real `updateDecisionDebt()` body replacing the M1.4 stub. Wired `updateDecisionDebt()` into `setClusterState()` (newly added) and `init()` (after `renderCurrentStep()`); existing wires in `onVerdictChange` / `onMergeIntoChange` / `onImport` / `onClearState` carried over from M1.4 stub period.
- Browser smoke (chrome-devtools-mcp `evaluate_script`): live DOM counters at baseline = **decide=46, confirm=162, resolve=5, bar=0%**; total reconciles to 208 entries + 5 cluster signals = 213. Plan estimated ~35 Decide; actual 46 because the third Decide branch (`!hasAiRec && !hasClusterRec`) catches **12 §12/§13 entries with `suggested_verdict: null`** in the worksheet (e.g., `biodiversity`, `observation`, `phases_of_matter`, `creative_writing`). Spot-checked — these legitimately lack an AI verdict, so routing them to Decide is spec-correct, not a parser bug. Decide tier breakdown: 32 §11 + 8 §12 + 6 §13. State-change wiring exercised via 4 paths (Confirm verdict commit 162→161, Decide verdict commit 46→45, `setClusterState` 5→4, manual clear back to baseline); bar advances 1/213 per commit; zero console errors.
- M1.4 status doc bundled into M1.5 commit per kickoff default.

### Session 5 (2026-05-15)

- **M1.6 complete (`1232bba`):** added `state.wizard = { step_index, deferred }` to `defaultState()` + `loadState()` migration for pre-wizard state. Replaced `buildStepSequence()` stub with real impl: walks `ENTRIES` in `TIER_ORDER`, interleaves each cluster Resolve step before its earliest tier-order member (clamps stale `step_index`). Added `currentStep()` lookup. Replaced `renderCurrentStep()` stub with real dispatcher (entry / cluster / end-screen sentinel) + `updateNavButtonState()` (Prev disabled at step 0). Added three stub renderers — `renderEntryStep` (delegates to M1.4 `renderEntryCard`), `renderClusterStep` (M1.10 placeholder), `renderEndScreen` (M1.15 placeholder).
- Browser smoke (chrome-devtools-mcp `evaluate_script`): `stepSequence.length = 213` (208 entries + 5 clusters). All 5 cluster Resolve steps fire before their first tier-order member, including the W12 test case: **CON-22 emits at idx 44, immediately before `reading_comprehension` at idx 45** (not before `reading`, which sits at §13 well after the reading-comprehension §12 row). Cluster positions: CON-23@8 → measurement@9; CON-12@38 → writing@39; CON-22@44 → reading_comprehension@45; CON-24@128 → descriptive_language@129; CON-16@157 → indigenous_knowledge@158. Current step at index 0 = `entry:plant_parts` (§11 · Decide). Cluster step probe at index 8 renders `wizard-step cluster-step` class + M1.10 placeholder text. End-screen sentinel at index 213 renders `wizard-step end-screen` + M1.15 placeholder. Nav-button states: prev disabled at step 0, enabled at step 8. M1.5 decision-debt counters unchanged (46/162/5/0%). Zero console errors throughout. `state.wizard` migration from prior schema-1 state confirmed: `{step_index: 0, deferred: []}` initialized.
- M1.5 status doc bundled into M1.6 commit per kickoff default.

### Session 6 (2026-05-15)

- **M1.7 complete (`de0d787`):** replaced M1.6 `renderEntryStep` stub with mode dispatcher (Confirm → `renderConfirmStep`; Decide → `renderDecideStep`). Implemented full §6.1 Confirm layout: `displayedPrefillForEntry` (saved → cluster → AI → null source-of-truth), `renderStepHead` (mode chip + step counter + h2 title + key + freq/subject meta), `renderTierStrip` (W10 calm copy literals), `renderClaudeRecommendation` (headline + one-liner summary + ▸ more expand on `claude_notes`), `renderPrimaryAgreeAction` (atomic `{verdict, merge_into}` patch via `setEntryState` → `onCommit`; merge-with-no-target routes to picker stub), `renderPickSomethingElse` (radios for the two non-displayed verdicts + "Less common: add new concept" link → `onPickOtherVerdict`), `renderMergeAliases`. Commit handlers: `onCommit` (refreshes counters + re-renders), `onPickOtherVerdict` (auto-routes merge through picker), `openMergePicker` (M1.11 stub — commits immediately if `defaultTarget` present, else alert), `maybeAutoOpenNotesDrawer` (M1.12 stub), `renderNotesDrawer` (M1.12 stub returning empty div so `renderConfirmStep` append stays unconditional). §6.3 cluster caption swap wired but unreachable in Batch 1 (`clusterPrefillCandidate` returns null).
- **Two deltas from plan code** surfaced + applied during execution:
  1. `renderDecideStep` placeholder stub added in M1.7 (not in plan). Without it, init at step 0 = `plant_parts` (§11 · Decide) throws `ReferenceError` since the real `renderDecideStep` is M1.8. Stub renders mode chip + step head + tier strip + "(Decide renderer — M1.8.)" placeholder so the data-safety floor (tool builds + exports at every commit) holds across the standalone M1.7 ship. M1.8 will replace the stub with the real impl.
  2. `reviewerLabelWithTarget(verdict, target)` helper added because plan's straight `REVIEWER_LABELS["merge"] = "Fold into another"` produced grammatically awkward "Fold into another `plant_identification`" in both the recommendation headline and the primary Agree button. Design §6.1 / §6.3 mockups show "Fold into `X`" (no "another") when a target is named. Helper drops "another" when target known; standalone "Fold into another" preserved for Pick Something Else radios where no target is yet picked. User-decided 2026-05-15 ("Match design").
- **Orphan cleanup:** removed M1.4 stubs `renderEntryCard` (callers: just the M1.6 stub that the new dispatcher replaces), `onVerdictChange` / `onMergeIntoChange` / `onNotesChange` (no callers — were forward-looking handler stubs from M1.4, now superseded by atomic `setEntryState` + `onCommit` paths).
- **Added constant:** `REVIEWER_LABELS` (W8) near `VERDICTS`. Distinct from `verdictLabel()` (export vocab `keep / merge → / new / drop`), which stays untouched.
- Browser smoke (chrome-devtools-mcp): on `plant_id` (§13 · Confirm, step 184/213): mode chip "Confirm", tier strip "§13 Long-tail: quick pass; pause when unsure.", headline "💡 Claude recommends: Fold into `plant_identification`", summary present, ▸ more expands 279 chars of body, primary button "✓ Agree — Fold into `plant_identification`". Click ✓ Agree → `state.entries.plant_id = {verdict:"merge", merge_into:"plant_identification"}` atomic write confirmed; caption flips to "💡 Saved: Fold into `plant_identification`". On `recipe_writing` (§13 · Confirm with target `how_to_writing`): same layout, "Fold into `how_to_writing`" copy. On `drawing` (§13 · keep) Pick Something Else: radios `["Fold into another", "Remove"]` — standalone "Fold into another" preserved when target unknown. On `plant_parts` (§11 · Decide, step 1/213, init landing): M1.8 placeholder renders with chip + counter + title + key + tier strip + "(Decide renderer — M1.8.)" line, zero throw. Override path (Pick Something Else → "Keep as concept" radio on plant_id) commits `{verdict:"keep"}` and entryMode flips to Decide per W14 revisit rule (committed ≠ AI rec). Zero console errors throughout.
- M1.6 status doc bundled into M1.7 commit per kickoff default.

### Session 7 (2026-05-15)

- **M1.8 complete (`944f830`):** replaced M1.7 `renderDecideStep` placeholder with real §6.2 layout — step head + tier strip + `renderWhyAttention` row + optional theme-overlap callout + `renderMergeAliases` (when proposed) + Claude recommendation block (source label + full `claude_notes` body expanded; no summary, no ▸more — Decide always shows full notes) + notes-only fallback when AI/cluster rec absent but `claude_notes` present + blank verdict radio group with `"Your call (no default[ — high-impact tier]):"` legend + "Less common: add new concept" linkish + notes drawer stub. Added `renderWhyAttention(entry)` helper (priority order per design §3: theme_overlap → cluster (Batch 2) → §11 → no-rec). Added `onDecideVerdict(entry, v)` handler in commit/override section sibling to `onPickOtherVerdict`: merge → picker; `new`/`drop` auto-open notes drawer (M1.12 stubs); override of AI rec auto-open notes drawer; else commit + advance.
- Browser smoke (chrome-devtools-mcp, zero console errors throughout): **plant_parts** (§11 init step 1/213) Decide chip + tier "§11 High-impact: review carefully." + "Why this needs attention: §11 High-impact concept." + Claude body 506 chars expanded (no summary/▸more) + legend `Your call (no default — high-impact tier):` + 3 blank radios + Confirm-mode elements absent + notes drawer stub present. Click Keep → `state.entries.plant_parts = {verdict:"keep"}` saved to localStorage (verified via `beforeChecked: true` on next probe). **plant_growth** (§12 theme-overlap, step 36/213) Decide-mode forced by theme_overlap routing (not tier-11) + "Why this needs attention: Theme overlap with themes worksheet." + ⚠ callout visible + Claude rec "Keep as concept" + legend `Your call (no default):` (no tier-11 suffix). **biodiversity** (§12 no-rec, step 41/213) Decide via `!hasAiRec && !hasClusterRec` branch + "Why this needs attention: No confident recommendation." + no theme callout + notes-only block fires (no headline, 1653-char body). **ecosystems** (§11 + theme-overlap, step 6/213) priority order verified — theme_overlap reason fires before §11 reason on tier-11 entry, confirming `renderWhyAttention` priority chain.
- **Plan-text discrepancy noted (no code change):** plan verification §2 says "Click 'Keep' → 'Saved' caption appears" — that wording is M1.7-Confirm-specific (via `displayedPrefillForEntry`'s `captionSource`). Decide-mode never uses "Saved" caption; saved verdicts surface as the checked radio. On §11 entries (which stay Decide per `entryMode`'s tier-11 short-circuit), the headline keeps saying "Claude recommends:" while the radio shows the user's pick. Plan code (followed exactly) is consistent with actual behavior; verification text appears to be M1.7 copy-paste.
- **M1.7 status doc NOT bundled into M1.8 commit `944f830`** (plan's verbatim `git add` was template-only). Both M1.7's pending status doc edit (Session 6 log + header bump M1.6→M1.7) and this M1.8 edit (Session 7 log + header bump M1.7→M1.8) landed together as a tiny standalone status-doc commit (user-chosen — M1.9 may produce no commit per spec, so bundling would have deferred to M1.10).

### Session 8 (2026-05-15)

- **M1.9 complete (no commit — verification milestone):** mode-routing distribution + 3-entry walk both confirm spec-correct behavior. No routing bug, no code change.
  - **Distribution probe (Step 1):** 208 entries → **162 Confirm + 46 Decide** (matches M1.5 baseline exactly; under the 60-Decide investigation ceiling). Decide reason breakdown via the plan's probe: **§11=31, theme-overlap=3, no-rec=12** (sum 46). Decide tier breakdown: **§11=32, §12=8, §13=6** (sum 46). The §11/reason 31-vs-32 delta is the one §11 entry that's also `theme_overlap.flagged=true` (`ecosystems`, already noted in Session 7's M1.8 log — priority order routes it as "theme-overlap" not "§11" because theme_overlap is checked first in `renderWhyAttention`).
  - **3-entry walk (Step 2)** via direct `state.wizard.step_index` + `renderCurrentStep()` (no commits, no localStorage writes):
    - `plant_parts` (§11 · step 1/213): mode **Decide**, tier strip "§11 High-impact: review carefully.", "Why this needs attention: §11 High-impact concept.", "💡 Claude recommends: Keep as concept", blank radio group (no pre-fill button). ✓ matches plan §1592.
    - `plant_id` (§13 · step 184/213): mode **Confirm**, tier strip "§13 Long-tail: quick pass; pause when unsure.", "💡 Claude recommends: Fold into `plant_identification`", primary button **"✓ Agree — Fold into `plant_identification`"**. ✓ matches plan §1593 verbatim.
    - `photosynthesis` (§12 · step 38/213, picked from the 29 §12+keep+no-theme-overlap candidates): mode **Confirm**, tier strip "§12 Mid-tier: confirm or adjust.", "💡 Claude recommends: Keep as concept", primary button **"✓ Agree — Keep as concept"**. ✓ matches plan §1594 verbatim.
  - **Zero console errors/warnings** throughout the probe.
  - **No anomalies surfaced** → no investigation triggered, no code commit. This status-doc edit pending bundle into the M1.10 commit per kickoff default.

### Session 9 (2026-05-15)

- **M1.10 complete (`07dad7f`):** replaced M1.6 `renderClusterStep` stub with full §9 Resolve UI — Resolve mode chip + step counter + h2 cluster label + `<code>signal.id</code>` + member count meta + verbatim cluster question + verbatim option radios (no rephrase per audit-register contract) + `Members (N)` block with click-to-jump peek buttons rendered as `mono` per-member entries showing current verdict (or `(no decision yet)` / `(not in worksheet)`). Added `onClusterCommit(signalId)` (refresh counters + re-render — Batch 2 will also recompute downstream member pre-fills); added `renderClusterNotesDrawer(signal)` placeholder (cluster_notes persistence deferred to Batch 2 — distinct from per-entry `curriculum_notes` channel); added `jumpToStepByEntryKey(key)` helper that walks `stepSequence` to find the matching `entry:` step and updates `state.wizard.step_index` + `saveState()` + `renderCurrentStep()`. Cluster→member pre-fill propagation deliberately deferred to M2.1 per design — Batch 1 just renders the question + collects the answer + lets reviewer jump to members.
- **M1.9's pending status-doc edit (Session 8 log) bundled into the M1.10 template commit** — but the header bump M1.9→M1.10 was missed in that bundle. (Caught + corrected in M1.11 status-doc edit; see Session 10 entry.)

### Session 10 (2026-05-15)

- **M1.11 complete (`759785b`):** replaced M1.7 `openMergePicker` stub with the real picker UI + `commitMerge` helper. Picker renders inside `#wizardCard` as an in-card overlay: H3 `Fold \`${entry.canonical_key}\` into…` + (if `defaultTarget && ENTRIES_BY_KEY.has(defaultTarget)`) a `merge-shortcut` block with "Claude suggests:" + single-click `→ ${defaultTarget} (high-confidence target) — Confirm` button + muted "Or pick a different target ▾" caption + autocomplete input bound to the existing `all-canonical-keys` datalist (built once at init via `ensureCanonicalKeysDatalist()` in M1.4) + Cancel + Confirm (disabled until input is a known non-self canonical_key). `commitMerge` validates target (alerts on unknown / self-ref / empty), writes `{verdict:"merge", merge_into:target}` atomically via `setEntryState`, removes the picker, then runs `maybeAutoOpenNotesDrawer` BEFORE `onCommit` (per the §1808-1815 comment — auto-open marker must be set before re-render reads it). Auto-open trigger: `isOverride = entry.parsed.suggested_verdict !== "merge" || (entry.parsed.suggested_merge_target && target !== entry.parsed.suggested_merge_target)` — so agreement-with-suggestion does NOT auto-open the drawer (M1.12 is currently a no-op stub but the call is traced).
- **Surprise discovery — high-confidence shortcut UI is data-unreachable via real wizard flow in Batch 1:** all 53 entries with `suggested_merge_target` populated are Confirm-mode, where `renderPrimaryAgreeAction` (line 977-989) commits the merge directly without opening the picker. Tier-11/theme-overlap/no-rec entries (the only Decide modes) happen to have zero merge-with-target. Pick Something Else paths only reach the picker when displayed verdict is NOT merge, but those entries don't have the target. So the shortcut UI block is correct code (verified via direct invocation in probe #3) but won't be exercised by any real reviewer click in Batch 1. Becomes reachable in Batch 2 once cluster propagation can write a target onto a Decide-mode member entry.
- Browser smoke (chrome-devtools-mcp `evaluate_script`, zero console errors throughout): **(1) Picker fallback** — `social_justice` (§13, no target, picker reached via "✓ Agree — Fold into another"): picker opened with H3 `Fold \`social_justice\` into…`, no shortcut block, autocomplete input + Cancel + Confirm (disabled at start). Type `seasonality` → Confirm enables → click → state writes `{verdict:"merge", merge_into:"seasonality"}`, picker removed, step re-renders. **(2) Validation** — `commitMerge('social_justice', 'definitely_not_a_key', fakePicker)` + self-ref + empty all alert and leave state untouched. **(3) Shortcut UI** — `openMergePicker(plant_id_entry, 'plant_identification')` direct call: shortcut block renders with "Claude suggests:" + button "→ plant_identification (high-confidence target) — Confirm" + muted "Or pick a different target ▾"; click commits to `{verdict:"merge", merge_into:"plant_identification"}`. **(4) Cancel** — picker removed, state untouched. **(5) Pick Something Else routing** — on `drawing` (§13 keep): radios `["Fold into another", "Remove"]`; click "Fold into another" → picker opens with `Fold \`drawing\` into…`, no shortcut (drawing has no target). **(6) Auto-open semantics** — drawing keep→merge override fires `maybeAutoOpenNotesDrawer({key:'drawing', verdict:'merge', trigger:'override'})` BEFORE onCommit; social_justice merge → user picks target fires zero auto-open calls (agreement, not override).
- M1.10 status-doc edits (Session 9 log + header bump M1.9→M1.10) NOT bundled into M1.10 commit `07dad7f` — caught here. Both M1.10's missed edit + this M1.11 edit + smoke-matrix #5 fill bundled into the next milestone (M1.12) commit per kickoff default.
- **Smoke check #5 (Merge picker high-confidence shortcut) baseline recorded:** UI verified via direct `openMergePicker(entry, target)` invocation @ M1.11; data-unreachable via end-to-end wizard click in Batch 1 (all 53 with-target entries are Confirm-mode and route to direct primary-agree commit). Re-test for end-to-end reachability at Batch 2 M2.1 once cluster propagation can put a target into a Decide-mode entry.

### Session 11 (2026-05-15)

- **M1.12 complete (`2ae194c`):** replaced the M1.7 `maybeAutoOpenNotesDrawer` no-op stub + M1.4/M1.7 `renderNotesDrawer` empty-div stub with the full §8 implementation. `renderNotesDrawer(entry, forceOpen)` builds a `<details class="notes-drawer">` with `open = forceOpen || autoOpenSet.has(key) || cur.length > 0`, summary toggles between `"+ Add a note"` and `"✎ Note added (click to edit)"` based on saved `curriculum_notes`, optional `<p class="drawer-prompt">` rendered when `autoOpenPrompts.get(key)` is set, `<textarea class="notes-input">` wired to `oninput → onNotesChange(key, "curriculum_notes", ev.target.value)`. Added module-level `autoOpenSet = new Set()` + `autoOpenPrompts = new Map()` — UI-only state (NOT persisted to localStorage), cleared on reload; saved `curriculum_notes` is what keeps a drawer open across sessions. `maybeAutoOpenNotesDrawer(entry, verdict, trigger)` dispatches on trigger: `"override"` → "Worth noting why?", `"new-or-drop"` → "Optional rationale for the team.", `"cluster-mismatch"` → "Heads up — this differs from the cluster shape you picked. Worth a quick note?" (Batch 2 only; dispatch ready). Re-added `onNotesChange(key, field, value)` (was orphan-cleaned in M1.7 when it had no callers — has one now). All call sites from M1.7/M1.8/M1.11 (`onPickOtherVerdict`, `onDecideVerdict`, `commitMerge`) already trace through `maybeAutoOpenNotesDrawer` — no caller changes needed in M1.12.
- **Visual delta from plan: drawer is functional but UNSTYLED.** No `.notes-drawer` / `.notes-input` / `.drawer-prompt` rules exist in the template CSS yet — drawer renders with native `<details>` chrome. Matches plan: M1.16 (CSS polish + visual pass) is where these get styling. Verification text in the plan is behavioral-only, which passed cleanly.
- Browser smoke (chrome-devtools-mcp `evaluate_script`, zero console errors throughout): **(1) Confirm + override (drawing §13 keep → drop via `onPickOtherVerdict`):** drawer open=true, prompt="Worth noting why?", verdict=drop, summary="+ Add a note" (cur still empty pre-typing). **(2) Decide + drop (plant_parts §11 → drop via `onDecideVerdict`):** drawer open=true, prompt="Optional rationale for the team.", verdict=drop. `onDecideVerdict`'s priority chain routes drop/new through `"new-or-drop"` trigger before falling through to the `"override"` branch, so plant_parts (suggested=keep, picked=drop) gets the team-rationale prompt rather than the override prompt — matches plan copy. **(3) Confirm + Agree (plant_id §13 merge target=plant_identification, click `"✓ Agree — Fold into \`plant_identification\`"`):** drawer open=false, verdict=merge + merge_into=plant_identification, autoOpenSet stays empty. **(4) Type → state propagation:** dispatched synthetic `input` event on `.notes-input` with text "Reviewer note: confirming drop on second pass." — in-memory `state.entries.drawing.curriculum_notes` matched immediately, localStorage snapshot matched after 250ms debounce. **(5) Reload preservation:** after `location.reload()` (in-memory `autoOpenSet` + `autoOpenPrompts` cleared), drawing's drawer re-rendered with open=true (driven by `cur.length > 0`), summary flipped to "✎ Note added (click to edit)", textarea pre-filled with saved note, no prompt rendered (correct — prompt is in-memory only). **Sanity:** plant_id post-reload (no notes + not in autoOpenSet) → drawer open=false, summary="+ Add a note".
- **Hash-record follow-up:** M1.12 entry was bundled into this milestone's own commit per kickoff Bundle default; M1.12 commit hash was chicken-and-egg unknowable at write time. Following the M1.3 precedent (`f6aab5d docs(concepts-worksheet): record M1.3 commit hash in status doc`): committed with `4a565ca` placeholder, then tiny follow-up commit replaced both placeholder occurrences (header + Session 11 entry + smoke matrix #4 row) with the real hash `2ae194c`.

### Session 12 (2026-05-15)

- **M1.13 complete (`626bca4`):** replaced M1.6-stub `navStep`/`navDefer` (lines 1714-15) with real impls — `navStep(delta)` clamps at 0 and treats `step_index === stepSequence.length` as the M1.15 end-screen sentinel; `navDefer()` adds `currentStep().id` to `state.wizard.deferred` (UI-only set; preserved across reloads via existing `saveState` path on `state.wizard`) before advancing. Replaced M1.6-stub `updateNavButtonState()` with mode-aware logic (Decide → next disabled until `entryFilled`; Cluster → next disabled until `getClusterState !== undefined`; Confirm → next always enabled because Save·Next doubles as primary commit; Defer → enabled on every wizard step; no-step → both disabled at end-screen). Replaced the trivial `nextBtn` click handler in `init()` with the commit-then-advance flow that mirrors `renderPrimaryAgreeAction`: reads `displayedPrefillForEntry`, routes merge-with-no-target to `openMergePicker(entry, null)`, otherwise atomic `setEntryState` + `onCommit` + `navStep(+1)`. Added `document.addEventListener("keydown", ...)` after `maybeShowOnboarding()` per Design §14: ArrowLeft = Previous, Enter = `nextBtn.click()`, L = Decide later, K/M/N/D = `onPickOtherVerdict` (Confirm) / `onDecideVerdict` (Decide). Tag/contentEditable/modifier early-returns guard against intercepting normal typing.
- **One delta from plan applied:** patched `renderCurrentStep()` to drop the early-return at the end-screen branch so `updateNavButtonState()` always runs after every render. The M1.13 plan code in `updateNavButtonState()` correctly disables Next + Defer when `currentStep()` returns undefined, but the pre-existing M1.6 `renderCurrentStep()` early-returned at the end-screen branch before that call ever fired. Without the patch, the end-screen sentinel left Next + Defer enabled, letting a reviewer click past `stepSequence.length`. Six lines, on-scope correctness for the new mode-aware nav state. Plan §1589-1606 had `if (!step) { card.appendChild(renderEndScreen()); return; }` → restructured to `if (!step) { ... } else if (step.kind === "entry") ...` so control flow falls through to `updateNavButtonState()` at the end.
- Browser smoke (chrome-devtools-mcp `evaluate_script`, zero console errors throughout): **(1) Initial nav state @ step 0 (plant_parts §11 Decide):** prev disabled, next disabled (Decide unfilled), defer enabled. **(2) Decide K shortcut:** dispatch `keydown` `key:"k"` on document → `state.entries.plant_parts = {verdict:"keep"}` written via `onDecideVerdict`; nextBtn enables. **(3) ArrowLeft @ step 0:** clamped, step_index stays 0. **(4) Enter:** advances 0→1 (cultural_traditions). **(5) ArrowLeft after Enter:** back to 0 (plant_parts). **(6) L (defer):** `deferred = ["plant_parts"]` written + advances to step 1 (cultural_traditions). **(7) Confirm Save·Next commits-then-advance:** at first fresh Confirm step (drawing §13, suggested keep at idx 33), `state.entries.drawing` was null before click; after `nextBtn.click()`, `{verdict:"keep"}` written atomically + step_index 33→34 (fractions). **(8) Confirm M shortcut routes to picker:** at problem_solving §13 (Confirm, suggested keep, no merge target), dispatch `keydown` `key:"m"` → `.merge-picker` element appears in DOM, `state.entries.problem_solving` stays null (commit gated on target selection per M1.11). **(9) Modifier guard:** Ctrl+K dispatched on plant_parts → no state write. **(10) Input/textarea guard:** focus jumpInput, dispatch keydown `key:"k"` with bubbles → no state write, `document.activeElement.tagName === "INPUT"`. **(11) Cluster step Next-gated:** at CON-23 (idx 8), unanswered → next disabled, defer enabled; click first radio + dispatch `change` event → next enables, `getClusterState("CON-23")` returns the picked option index. **(12) End-screen sentinel:** `navStep(+1)` from `stepSequence.length - 1` lands at step_index 213, card class `wizard-step end-screen`, **next disabled, defer disabled**, prev enabled (post-fix; pre-fix this would have left Next/Defer enabled). **(13) End-screen Prev:** `navStep(-1)` from sentinel returns to step 212 (yeast, confirm-mode card class).
- **No M1.12 hash-pending follow-up needed this session** — M1.12's `2ae194c` already recorded in the prior commit `0b0c1e9`. Status-doc edit (Session 12 + header bump M1.12→M1.13) pending — bundle default into M1.14 per kickoff.

### Session 13 (2026-05-15)

- **M1.14 complete (`616357a`):** replaced M1.4 stub `onJumpToEntry(key) {}` with the full plan §2152-2175 impl — walks `stepSequence` once, matches entries on `s.id === value` first then `s.entry.parsed.canonical_label === value`, matches clusters on `s.id === value` (e.g., `CON-23`); on miss alerts ``No entry/cluster found for `${value}` `` and leaves state untouched; on hit writes `state.wizard.step_index`, `saveState()`, `renderCurrentStep()`. No other changes — the `#jumpInput` change-event wire (line 1540) + `ensureCanonicalKeysDatalist()` (line 1539) were already in place from M1.4. Per plan Step 2 the Advanced menu was just re-verified, no code change.
- Browser smoke (chrome-devtools-mcp `evaluate_script`, zero JS errors): **Jump probes (6/6 PASS):** (1) `plant_parts` → idx 0/entry; (2) `plant_id` → idx 183/entry; (3) `CON-23` → idx 8/cluster (matches Session 5 baseline); (4) `Plant Parts` (canonical_label fallback path) → idx 0/entry; (5) junk `definitely_not_a_key` → alert + `state.wizard.step_index` unchanged; (6) empty string → no alert, no movement (early-return at `if (!value) return`). **Wiring through #jumpInput change event:** typed `CON-22`, dispatched `change` → idx 50→44 (cluster CON-22 — matches Session 5 baseline CON-22@44), input cleared post-call (existing `ev.target.value = ""` from M1.4 still runs). **Advanced menu re-verify:** all 5 buttons present with expected labels (Save & Export ↓ / Import a saved markdown file… / Download progress as JSON / Show wizard intro / Clear all progress…); `toggleAdvancedMenu(true|false)` flips `.shown` correctly; all 5 handler functions defined (`onExport`, `onImport`, `onDownloadJson`, `showOnboarding`, `onClearState`); `showOnboarding()` opens the `<dialog id="onboardingModal">` (`dlg.open === true`) and `dismissOnboarding(false)` closes it; `onClearState` properly guarded by `confirm()` (overrode `window.confirm` to return false; `state.wizard` deep-equal unchanged). Console check showed only the pre-existing form-field-id cosmetic issue (jumpInput + importInput both have `id` but lack `name` — unchanged from prior milestones).
- **No deltas from plan applied this session.** Plan §2152-2175 code dropped in verbatim; smoke check called for exactly the 3 cases (canonical_key / cluster id / junk) and added 3 supplementary probes (canonical_label fallback, empty input, end-to-end through #jumpInput) for thoroughness.
- M1.13's pending status-doc edit (Session 12 + header bump M1.12→M1.13) was NOT bundled into M1.14 commit `616357a` (plan's verbatim `git add` was template-only). Both M1.13's pending edit and this M1.14 edit (Session 13 + header bump M1.13→M1.14→M1.15) pending — bundle default into M1.15 per kickoff.

### Session 14 (2026-05-15)

- **M1.15 complete (`aa66856`):** replaced M1.6 `renderEndScreen()` stub (6 lines, single placeholder paragraph) with the full plan §2227-2266 impl (40 lines) — `<h2>Done — Save & Export</h2>` + `.end-summary` block (committed/deferred/blank entry counts + cluster-shapes-resolved count) + `.end-actions` row with prominent **Save & Export ↓** button (font-size 16px / padding 12px 24px / `onclick: onExport`) and ghost **← Back to wizard** button (sets `step_index = stepSequence.length - 1`, saves, re-renders) + Batch-2 footer note. Counts gate-check the deferred/blank lines on `> 0` so a clean baseline shows just `committed + clusters` lines. Replaced M1.13's inline-sentinel `navStep` (15 lines) with plan §2273-2282's simpler clamp `[0, stepSequence.length]` inclusive (9 lines, same observable behavior — `> stepSequence.length` no-op preserved). Replaced M1.13's `renderCurrentStep` (with the M1.13 patch that always-ran `updateNavButtonState` after end-screen render) with plan §2287-2306's version that hides the bottom nav (`#wizardNav.classList.add("hidden")`) on end-screen and early-returns from that branch — buttons being disabled or enabled becomes moot when the nav is hidden.
- **One delta from plan applied:** dropped M1.13's defensive `if (!card) return;` from `renderCurrentStep` to follow plan verbatim. `#wizardCard` is a static template element so the nullcheck was never exercised in practice; kept for plan-fidelity.
- Browser smoke (chrome-devtools-mcp `evaluate_script`, zero console errors throughout): **(1) End-screen sentinel via Save·Next:** at step 212 (yeast §13 confirm), `nextBtn.click()` commits yeast (`{verdict:"keep"}` via M1.13 commit-then-advance) + advances to step_index 213; `currentStep() === undefined`; card class `wizard-step end-screen`; `<h2>Done — Save & Export</h2>` rendered; `nav_hidden = true`. **(2) End-screen counts (clean baseline):** `["0 of 208 entries committed.", "208 entries not yet visited (export as <to_fill>).", "0 of 5 cluster shapes resolved."]` — no Decide-later line (correct gating on `deferredEntries > 0`). **(3) Save & Export button wiring:** stubbed `window.onExport` to record calls without triggering download; clicking the prominent button fired `onExport` exactly 1×; restored original. `el()` helper wires `onclick:` props via `addEventListener("click", v)` so `b.onclick` getter returns null but the handler is bound — Chrome semantics quirk, not a missing handler. **(4) ← Back to wizard:** click → step_index 213→212, kind=entry, key=yeast, card class `wizard-step entry-step confirm-mode`, `nav_hidden = false` (re-shown via `nav.classList.remove("hidden")` in the non-end-step branch). **(5) navStep upper-bound clamp:** `navStep(+1)` from sentinel (213) stayed at 213 — correctly refused to advance to 214. **(6) navStep lower-bound clamp:** `navStep(-10)` from step 0 stayed at 0. **(7) navStep -1 from sentinel:** step 213 → step 212 (yeast), kind=entry. **(8) Dynamic recount with deferred:** added plant_parts to `state.wizard.deferred`, jumped to end screen → summary rendered `["1 of 208 entries committed.", "1 marked \"Decide later\" (export as <to_fill>).", "206 entries not yet visited (export as <to_fill>).", "0 of 5 cluster shapes resolved."]` — reconciles 1 + 1 + 206 = 208 ✓; the `1 marked "Decide later"` line gated correctly on `deferredEntries > 0`. State restored to clean baseline (`deferred = []`, `entries.yeast` cleared, `step_index = 0`) at end of probe so reviewer reopen lands on plant_parts.
- **Three milestones of pending status-doc edits accumulated** (Session 12 M1.13 + Session 13 M1.14 + this Session 14 M1.15) since plan's verbatim `git add` was template-only for M1.13/M1.14/M1.15. Per kickoff Step 5, asked user whether to bundle into M1.16's commit (default for routine progress) or land as tiny standalone status-doc commit (clears the 3-milestone backlog before M1.16's CSS polish work). User chose standalone — sessions 12/13/14 logged in commit `182311b` before M1.16 work began.

### Session 15 (2026-05-15)

- **M1.16 complete (`ff78407`):** CSS-only polish pass on `concepts-worksheet-tool.template.html` (+477/-25). All 8 plan §2349-2360 visual targets addressed: card max-width preserved at 720px; mode-chip token tweaks (Confirm `--mode-confirm` `#5e5e58`→`#4a5a6a` muted gray-blue + softer tint; Decide `--mode-decide` `#a4143c`→`#b0331d` shifted from crimson toward red-orange per plan literal wording; Resolve amber unchanged); tier-strip switched from inline pill to full-width block band inside card with `border-left: 3px solid currentColor`; `.primary-action-row .btn.primary-agree` now `width: 100%` + padding 10×16 + font-size 14.5px + weight 600; `details.pick-other` muted with dotted-underline `linkish` link styling; `details.notes-drawer` cream `--notes-bg`/`--notes-bg-strong` tokens + strong-bg-when-open; end-screen `<h2>` bumped to 24px + bordered `.end-summary` panel; top-bar `.decision-debt` adds size hierarchy `decide 16px (accent-colored) > confirm 14px > resolve 13px`. Baseline styling also added for all previously-unstyled components: `.claude-rec` block (soft border + bg), `.verdict-options-group` fieldset (legend muted + radios with accent color + hover bg), `.why-attention` row (soft border), `.theme-overlap-callout` (warn-tinted), `.merge-picker` (bordered panel + shadow), `.merge-shortcut` (accent-bordered subpanel with full-width green button), `.merge-aliases-block` (dashed border), `.cluster-question` (amber-tinted with left border), `.cluster-options` `.cluster-option` (bordered option cards with `.selected` state), `.cluster-members` (bordered panel + bulleted list), `.member-peek` (pill button). Responsive `@media (max-width: 720px)` updated to soften step-title size, collapse merge-picker margin, stack verdict-option radios vertically.
- **Two design judgment calls surfaced to user before commit:** (a) tier-strip block-level vs inline pill — user OK'd block-level. (b) Decide chip color shift from crimson toward red-orange per plan wording — user OK'd.
- Browser smoke (chrome-devtools-mcp): 8 wizard states screenshotted before/after (step 1 Decide / step 184 Confirm / step 9 cluster Resolve CON-23 / merge picker fallback / merge picker shortcut / notes drawer filled+open / end screen / Decide with theme-overlap). Zero console errors across all probes. Pick-other expand-open state separately verified — dotted-underline summary + tinted radio panel renders cleanly.
- Plan's verbatim `git add` was template-only; this status-doc edit (Session 15 + header bump M1.15→M1.16) pending — bundle default into M1.17 per kickoff.

### Session 16 (2026-05-15)

- **M1.17 complete (`5630a69`):** all 6 Batch-1-relevant smoke checks from design §15 (#1-5, #8) PASS end-to-end against the M1.16 build; zero console errors throughout.
  - **#1 Empty-export SHA invariant:** PASS — `buildExportMarkdown()` SHA-256 `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03`, byte-identical to source `2026-05-12-metadata-rebuild-stage1-concepts-worksheet.md` (264,428 bytes / 3547 lines). Re-verified post-fix (see below) — SHA unchanged.
  - **#2 Decide-later semantics:** PASS — defer on `plant_id` (§13 Confirm step 183/213): `state.entries.plant_id` undefined, `state.wizard.deferred = ["plant_id"]`, step_index advances 183→184, export shows `verdict: <to_fill>` for the deferred entry.
  - **#3 Pre-fill non-commit:** PASS — `recipe_writing` (§13 Confirm step 91/213) displays pre-fill "✓ Agree — Fold into `how_to_writing`"; click Previous → step 91→90, `state.entries.recipe_writing` undefined, export shows `verdict: <to_fill>`.
  - **#4 Commit roundtrip:** PASS — 5 entries committed via `setEntryState` covering both modes (3 Confirm-agree shapes: plant_id→merge/plant_identification, recipe_writing→merge/how_to_writing, photosynthesis→keep; 2 Decide picks: plant_parts→keep, cultural_traditions→drop with curriculum_notes "Roundtrip smoke test note."). `buildExportMarkdown` → md1 (SHA `d1038e2b904a50674e93a4eb8b6813e6e4f70c537867ce46b1a274f24c5d1a12`, 262,995 bytes). State cleared. `parseEntryEditsFromMarkdown(md1)` + `parseClusterSignalsFromMarkdown(md1)` re-applied via `onImport`'s inline path. `buildExportMarkdown` → md2 byte-identical to md1, 0 diff lines.
  - **#5 Merge picker high-confidence shortcut:** PASS — (A) `plant_id` (§13 Confirm, suggested_merge_target=`plant_identification`) primary-agree click writes `{verdict:"merge", merge_into:"plant_identification"}` atomically + caption flips "Claude recommends:" → "Saved:". (B) low-confidence `social_justice` (§13 Confirm, suggested_verdict=merge, no target): primary-agree opens picker with H3 "Fold `social_justice` into…", no shortcut block, Confirm button disabled at start; typing junk "definitely_not_a_key" keeps Confirm disabled; typing "seasonality" enables Confirm; click commits `{verdict:"merge", merge_into:"seasonality"}` + picker removes. End-to-end shortcut-block UI path remains data-unreachable in Batch 1 (no Decide-mode entries have suggested_merge_target populated); shortcut block re-tests at Batch 2 M2.1 once cluster propagation can write a target onto a Decide-mode member.
  - **#8 Merge-target extraction rate:** PASS — `merge-target extraction: 53 of 78 merge recommendations (68%) — 25 fall through to picker` (matches M1.3 baseline exactly; W19 prediction held).
- **Parser fix folded into M1.17:** Smoke #4 surfaced an import-bloat side effect — `parseEntryEditsFromMarkdown` was creating `{curriculum_notes: ""}` shells for every worksheet row (~207 keys after a 5-entry import roundtrip) because the old code created the result shell at line 1839 BEFORE the verdict-validation gate at line 1840, AND line 1838 normalized `<to_fill>` to `""` so the curriculum_notes line then wrote an empty string into the existing shell. All downstream consumers (`entryFilled` / `entryVerdict` / `entryMergeInto` / `entryNeedsMergeTarget` / export) gracefully degraded — the only observable cost was localStorage bloat. Fix: replaced `if (value === "<to_fill>") value = "";` + early shell-creation with `if (value === "<to_fill>") continue;` + lazy shell-creation moved AFTER the verdict-validation gate. Re-ran Smoke #4 post-fix: imported key count now **5** (was 207), `state.entries` contains exactly the 5 committed shapes (`plant_parts` + `cultural_traditions` + `photosynthesis` + `recipe_writing` + `plant_id`), md1 === md2 SHA unchanged (`d1038e2b…`). Smoke #1 invariant SHA also unchanged (`0c49a7a7…`) — the fix touches import path only.
- **Batch 1 SHIPPED.** M1.0 → M1.17 all complete. Ready to start Batch 2 (M2.1 onward) in a separate session.

### Session 17 (2026-05-28) — Batch 2 begins

- **M2.0 complete (`4a565ca`):** plain-language UI pass — all 9 plan steps applied to `concepts-worksheet-tool.template.html` (+26/−19, display-only). Added `MODE_CHIP_LABELS` map (Confirm→"Quick check" / Decide→"Your call" / Resolve→"Group decision"); swapped the chip render in `renderStepHead` + `renderClusterStep` to use it (internal mode names stay in routing/CSS/`dataset.mode`). Relabeled top-bar counters ("your call" / "to check" / "group decisions"); rewrote tier strips to calm copy with no §-number/tier-name ("Worth a careful look." / "A quick confirm or adjust." / "Quick pass — pause if something seems off."); plain-ified the Decide legend ("Your call — there's no suggested answer here:"), the 3 `renderWhyAttention` reasons, and the theme-overlap callout headline ("⚠ Also appears as a theme."); dropped the raw `CON-xx` `<code>` + relabeled "members" → "concepts in this group" in the Resolve header + members heading; plain-ified the end-screen summary ("…group decisions made.", "left blank for now"). Left the `"(Cluster reason — Batch 2.)"` why-attention placeholder for M2.2 and the export `<to_fill>` token untouched.
- **Acceptance test PASS — empty-export SHA-256 `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03`, byte-identical to M1.17** (264,428 bytes; clean state: 0 entries / 0 clusters). Confirms the pass was display-only — nothing leaked into the export path. Parser output unchanged (`208 entries (§11=32, §12=39, §13=137)`; `53 of 78 (68%)`).
- **No-jargon sweep PASS** (chrome elements only, `jargonClean: true`, zero hits against `/§|High-impact|Mid-tier|Long-tail|CON-\d|cluster shape|<to_fill>|\btier\b/i`). Verified via chrome-devtools-mcp across 5 states: Your-call (`plant_parts` §11, step 1), Quick-check (`drawing`, idx 33), Group-decision (`CON-23`, idx 8 — header shows "5 concepts in this group", no CON-code), theme-overlap Decide (`ecosystems`, idx 5), and the end screen. Top bar reads "46 your call / 162 to check / 5 group decisions". **Zero console errors.**
- **Three out-of-scope residuals flagged (all by-design; M2.0 is display-only template sweep):**
  1. **Onboarding modal** (`line 936`, "Confirm Claude's calls, resolve five clusters") — **dealt with by M2.4** (intro rewrite); code placeholder confirms ("Full intro rewrite lands in Batch 2.").
  2. **Cluster Resolve step DATA** — all 5 `signal.label`s contain "cluster" ("Measurement-cluster boundary", "Figurative-language cluster", …); some `question`/`options` contain "canonical", "§9.1", "heritage worksheet". **NOT fixed by any milestone** — options are hard-locked (audit register = source of truth); labels/questions are locked worksheet data (editing breaks the SHA invariant). M2.3's review summary will *re-display* these labels. Only future home: the parked, unscheduled "soften in-UI shorthand" decision or a worksheet-data / D4 canonicalization pass.
  3. **`claude_notes` / `theme_overlap.note` bodies** — analyst reasoning occasionally cites internal artifacts (e.g. `ecosystems` note → `filterDefinitions.ts`, "D-C5"). **Deliberately excluded** (plan line 2710); analyst data, same future-data-pass home as #2.
- Minor: the end-screen line "N of 208 entries committed" keeps "committed" (plan Step 9 didn't include it; M2.3 replaces this whole screen).
- Status-doc edit bundled into the M2.0 commit per kickoff default; hash recorded via the M1.12 `4a565ca` follow-up pattern.

### Session 18 (2026-05-28) — M2.1 via calibration workflow

- **M2.1 complete (`9c3bfb6`):** cluster auto-prefill matrix (design §17) + carry-over P2 fix. Executed via a **dynamic workflow** (executor agent + independent adversarial verifier; calibration run — first time trusting a workflow on logic-heavy work). The workflow did NOT commit; main loop verified the diff + evidence, then committed.
- **Edits (template only, parser untouched):** added `CLUSTER_DERIVATIONS` (verbatim §17, all 5 clusters; CON-24 opt-2 = `"CON-24_PICK_ONE"` sentinel) + `computeMemberDerivation` + a **`getClusterParent` M2.1 stub** (returns `(state.cluster_parents && state.cluster_parents[id]) || null`); replaced `clusterPrefillCandidate` stub body; extended `displayedPrefillForEntry` (info_only carries caption + falls through; actionable returns plain `captionSource:"Based on your earlier group decision"`, replacing the old `Suggested from CON-xx`); rewrote `renderDecideStep`'s headline via `reviewerLabelWithTarget(v, v==="merge"?(t||null):null)` (P2 fix → "Fold into \`X\`", no "another") + added the `.cluster-info-caption` render. Diff +197/−20, in-scope (no export-path / parser / routing churn).
- **Verification (both agents, independent; main-loop diff review):** empty-export SHA `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03` byte-identical (checked clean-state before probing AND after clearing probing state); parser byte-exact (208 / 53-of-78); 0 console errors. Probe 1 (CON-12 opt-2 → `narrative_writing`) = `{merge, writing, "Based on your earlier group decision"}` exact. P2 fix live ("💡 Based on your earlier group decision: Fold into \`writing\`"). **CON-24 opt-2 crash trap (the `getClusterParent`-undefined ReferenceError I flagged pre-execution) confirmed prevented** — stub returns null, info_only caption fires, 0 errors.
- **Two findings (plan-vs-corpus, NOT execution errors):**
  1. **Stale plan probe:** plan Probe 2 assumed `writing` is §11; in the real corpus `writing` is §12 (Confirm). The genuine sole §11 cluster member is `measurement` (CON-23) — W17 re-tested on it: decide-mode card, plain caption, **zero radios checked** ✓.
  2. **Info-caption invisibility (logged as OPEN DESIGN DECISION above):** CON-16/CON-24 info-only captions are Decide-only-wired but their members are all §13/Confirm-routed → invisible in the live path. Needs a decision before/within M2.1b.
- **Calibration outcome: workflow judged trustworthy** for the mechanical chain — executed faithfully, handled the flagged trap, adapted around stale probes, and the adversarial verifier corroborated + surfaced the design gap rather than rubber-stamping.

### Planning session (2026-05-28)

- Recovered context after an interruption; confirmed Batch 1 SHIPPED and the Batch 2 plan was drafted-but-uncommitted.
- Validated the prior session's 9-item self-review of the Batch 2 draft and applied the **6 verified revisions**: M2.3 deferred-collect simplification + test-seed fix (the keys are bare canonical_keys, not `entry:`-prefixed); M2.1 Step 5/6 concreteness + a real `displayed`/`target` → `v`/`t` ReferenceError trap; dropped the `M` prefix from Batch 2 headings to match Batch 1; global line-drift caveat; M2.5 review-line wording; sequence/dependency note.
- **Added Milestone 2.0 — Plain-language UI pass** (curriculum-team voice, per user directive): de-jargon every reviewer-visible string — mode chips → **Quick check / Your call / Group decision**; drop `§`-codes, `CON-xx` ids, `<to_fill>`, "cluster shapes," "themes worksheet" from the UI. **Display-only**: export/markdown format + the empty-export SHA invariant are unchanged (the acceptance test). Recorded as design **§4.1 + W22** + a global plain-language convention applied to every Batch 2 milestone that emits new on-screen text.
- Reconciled the downstream ripple: M2.1 caption probes + M2.5 fully (incl. fixing pre-existing M2.5 draft bugs — wrong `.decide-counter` selector, wrong `decideUnfilled` locals, `entryMode === "decide"` case bug); M2.2/M2.3 user-facing labels covered by the convention + per-milestone notes (de-jargoned at their execution).
- **No code touched — planning only.** Scaffolded `2026-05-15-concepts-worksheet-wizard-batch2-execution-kickoff.md`.

### Session 19 (2026-05-28) — M2.1b (auto-proceed; main-loop exec + independent adversarial verifier)

- **M2.1b complete (`0da9cfd`):** CON-24 pick-one canonical Resolve UI (inline reveal) + the scope-addition Confirm-mode info-caption render. Template only (+109/−9), parser untouched. Ran under the relaxed "prose + smoke only" cadence (mechanical → auto-proceed): main loop executed the edits (full context already loaded), drove the chrome-devtools-mcp smoke directly, then dispatched **one independent adversarial verifier (opus)** that re-derived expectations from the diff + re-ran every probe in a fresh page; main loop reviewed the diff + re-confirmed the empty-export SHA before committing.
- **Edits:** `defaultState()` + `loadState()` gain `cluster_parents` ({} default + idempotent migration; **`schema_version` stays `1`** — the plan's illustrative `schema: 2` was a trap and was NOT adopted); added `setClusterParent` (next to the existing M2.1 `getClusterParent`, comment de-stubbed); `renderClusterStep` gains the CON-24 reveal (parent radios figurative_language / descriptive_language → `setClusterParent` + re-render); `onClusterCommit` clears the stale parent when CON-24's committed option ≠ 2; `renderConfirmStep` now renders `.cluster-info-caption` (mirrors M2.1's `renderDecideStep`); de-jargoned the 4 `caption_text` strings (dropped `CON-16:`/`CON-24:` prefixes + "verdict"/"canonical" → "Hold off — this is pending the heritage review." / "…the group's main-concept pick."); added `.con24-parent-picker` + `.cluster-info-caption` CSS.
- **KEY ARCHITECTURE DELTA from plan (adapted, not a scope change):** the plan's Steps 3–5 assume a *staged-option + separate Confirm-button* cluster model with a `currentStagedOption()` helper and a `.cluster-confirm` button. **The real M1.10 `renderClusterStep` commits immediately on radio change — there is no Confirm button and no staging.** So the reveal keys off the **committed** `getClusterState("CON-24") === 2`; the parent pick commits immediately (no gate); `currentStagedOption()` was not added. Building a CON-24-only Confirm button would have created an inconsistent cluster UX and reworked smoke-verified M1.10 code — out of scope. The milestone goal (reveal → parent → member derivations → stale-parent clear) is fully met within the real architecture.
- **One deliberate enhancement beyond the plan's Step 6 CSS:** the plan only specced `.con24-parent-picker`. The `.cluster-info-caption` was a bare unstyled `<p>` (M2.1 added the render with no CSS). Since the scope addition's whole purpose is to make the "hold off" notice *visible*, I added a modest warn-tinted style (house tokens, matches `.theme-overlap-callout` family) that styles it in BOTH Decide and Confirm modes. Display-only / SHA-safe / W22-safe.
- **Verification (main loop + independent verifier, both PASS):** empty-export SHA `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03` byte-identical (checked clean-state before probing AND after clearing all probe state); parser byte-exact (208 / 53-of-78); **zero console errors/warnings** (only the pre-existing form-field-id `[issue]`). Probes (real globals; the plan's `window.__test_*` helpers don't exist): CON-24@128; option 0/1 no reveal, option 2 reveal (no `.cluster-confirm`, prompt "Now pick the main concept to keep:", parents figurative_language/descriptive_language); **pre-pick** → `similes` (Confirm) shows "Hold off — …main-concept pick." + its Claude rec; **post-pick figurative_language** → figurative_language=keep, similes/sensory_details/**descriptive_language**=merge→figurative_language (**W21 cluster-over-AI confirmed**: descriptive_language's own AI target `vocabulary_development` was overridden), info-caption gone, "✓ Agree — Fold into `figurative_language`" (P2 label fix holds); **reload** preserves option 2 + parent; **flip to option 0** clears parent (in-memory immediately, localStorage after the 250ms debounce flush — verified no zombie); **CON-16 opt-1** → `indigenous_knowledge` (Confirm) shows "Hold off — this is pending the heritage review." + its keep rec + primary action. Verifier verdict: "M2.1b VERIFIED — safe to commit."

### Session 20 (2026-05-28) — M2.2 via dynamic workflow (executor → adversarial verifier) + main-loop catch

- **M2.2 complete (`2e92d4b`):** cluster-mismatch detection + de-jargoned yellow entry-step callout. **Executed as a strictly-sequential dynamic Workflow** (run `wf_168f814a-6b6`) — user pushed for real workflow orchestration under ultracode so the heavy spec-reading/edits/browser-probes live in agent contexts and we can chain milestones without `/clear`. Executor agent edited the template + ran chrome-devtools probes; adversarial verifier independently re-checked; **workflow did NOT commit** — the main loop reviewed the diff + re-confirmed the SHA + caught two misses + committed.
- **Edits (template only, parser untouched):** `checkClusterMismatch` / `recomputeMismatchForEntry` / `recomputeMismatchForCluster` helpers after `clusterPrefillCandidate`; recompute wired into `onCommit` (adapted to the real `onCommit(key)` signature — plan said `onCommit(entry)`), `commitMerge` (reordered so the cluster-mismatch auto-open prompt wins over the override prompt via `!state.entries[key]?.cluster_mismatch`), `setClusterState`, and `setClusterParent`; `renderClusterMismatchCallout` wired before the Claude-rec block in both `renderConfirmStep` + `renderDecideStep`; `.cluster-mismatch-callout` CSS (house tokens). `cluster_mismatch` is a UI-only field — `buildExportMarkdown` reads only verdict/merge_into/curriculum_notes (never spreads the entry), so it can't leak (verified).
- **TWO main-loop catches the workflow agents BOTH missed** (the main-loop-verifies-before-commit step earning its keep):
  1. **W22 violation — "verdict" in the callout headline.** The executor followed the plan's own draft copy ("your verdict differs…") instead of the briefed de-jargoned string; the verifier's `w22JargonScan` literally quoted that headline and still declared it plain-language (self-contradicting false PASS). Fixed in the main loop → "⚠ Heads up — this differs from the group decision you made for these related concepts." (no "verdict").
  2. **Skipped edit — the `(Cluster reason — Batch 2.)` why-attention placeholder** (NOT in the M2.2 plan section, but flagged by the M2.0 log as deferred-to-M2.2 and reviewer-visible on §11 group members once a group is resolved, e.g. `measurement` after CON-23). The executor never touched it; the verifier's adversarial-extra never actually tested it. Filled in the main loop → "Part of a group you've already made a decision about — make sure this concept fits." **(Scope addition beyond the plan section — user may adjust wording.)**
- **Process note:** the verifier ran a Node logic-harness (correct on the pure logic + SHA + export-leak) but **skipped the browser smoke**, which is partly why it didn't visually catch the rendered headline. The executor DID run real chrome-devtools probes on the core mismatch logic. The main loop did the final browser re-check of the two fixes.
- **Verification (main-loop browser, post-fix):** empty-export SHA `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03` byte-identical before AND after probing; parser byte-exact (208 / 53-of-78); zero console errors. Probes: narrative_writing (CON-12 opt 2 + commit keep) → `{verdict:"keep", cluster_mismatch:"CON-12"}` + callout headline "this differs…" (no "verdict") + plain detail "Fold into \`writing\`" + de-jargoned auto-open prompt; CON-12→opt 0 clears the flag; CON-23 opt 1 + agree merge→measurement on volume → no flag/no callout; reload persists the flag; `measurement` (§11, CON-23, Decide) why-attention shows the plain group reason (placeholder gone); cluster_mismatch absent from the export. Non-blocking observations accepted: dual-mechanism prompt precedence (cosmetic), template is ESLint-ignored by design (matches all 19 prior milestones), co-author → 4.8.

### Session 21 (2026-05-28) — M2.3 via dynamic workflow + main-loop review + user-approved plain group labels

- **M2.3 complete (`9d872fd`):** review summary screen (design §11) — 7 collapsible buckets (KEEP / FOLD / ADD NEW / REMOVE / GROUP DECISIONS / ⚠ DECIDE LATER / ⚠ DIFFERS FROM A GROUP DECISION), attention buckets default-open when count>0, FOLD grouped by target, edit/resume links jump+close, mid-flow "Review so far" button wired, Save & Export from footer, end-screen delegates to the summary. **Executed as a sequential executor→verifier Workflow** (run `wf_a8d6c526-812`); workflow did NOT commit; main loop independently verified diff + SHA + screenshot before committing.
- **Six plan traps pre-briefed + applied** (avoided M2.2-style misses): (A) sort uses `TIER_ORDER.indexOf(entry.tier)` — the plan's `parsed.tier`/`{"§11":0}` map doesn't exist (`entry.tier` is `"11"/"12"/"13"`); (B) dropped the `§11` tier-tag from rows (design mock has none + §-code is W22-forbidden); (C) de-jargoned section headers + mismatch row ("you chose … the group decision points to …", no "verdict"/CON-id); (D) `renderReviewSummary` returns a plain `<article>` with NO `id="wizardCard"` (plan's `id` would duplicate the container id) — verified exactly one `#wizardCard`; (E) dispatch via the existing `innerHTML=""`+append, not `replaceWith`; (F) `closeReviewSummary` clamps `step_index` to the last step at the end sentinel so "Back to wizard" works there. Verifier returned VERIFIED/0 bugs but with thin structured detail (and ran a Node harness, not browser) — main loop did the authoritative browser re-check.
- **USER-APPROVED SCOPE ADDITION — plain group labels (`CLUSTER_DISPLAY` map):** the GROUP DECISIONS rows initially re-displayed the LOCKED signal labels/options ("Writing-cluster canonical shape", "Pick one canonical …") — an accepted M2.0 residual, but it clashes with the plain-language goal. User chose to de-jargon **and extend to the Resolve step** (not just the summary). Added a tool-owned `CLUSTER_DISPLAY` map (plain name + question + index-aligned option recaps for all 5 groups), reviewed + approved by the user in two rounds (names/options, then questions). Wired into `renderClusterStep` (h2 title, question, option radios) AND `renderClusterBucket` (summary rows) via `clusterDisplayName/Question/Option` accessors with fallback to locked data. **The locked `signal.label`/`signal.question`/`signal.options` are NEVER edited** — the export comment block (`clusterSignalCommentBlock`) + import matcher (`parseClusterSignalsFromMarkdown` `findIndex`) still use the locked strings, so the empty-export SHA stays byte-identical and the markdown roundtrip is unaffected. This closes the M2.0-flagged "signal labels contain 'cluster'/'canonical'" residual for the wizard UI (display-only; the worksheet data + audit register are untouched).
- **Verification (main-loop browser, post-everything):** empty-export SHA `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03` byte-identical (clean state); parser byte-exact (208 / 53-of-78); zero console errors. Resolve step (CON-24) renders plain ("Figurative-language concepts" / "How should the figurative-language concepts be organized?" / plain options) WITH the M2.1b pick-one reveal intact; W22 scan of the Resolve step + the summary cluster bucket both clean (no cluster/canonical/CON/§). Summary: 7 sections with de-jargoned labels + live counts, attention buckets default-open, mismatch row plain, group rows plain ("Writing concepts — Fold the specific writing types into the general \`writing\`"), exactly one `#wizardCard`, Back-to-wizard clamps at the end sentinel.

### Session 22 (2026-05-28) — M2.5 via dynamic workflow (executor → adversarial verifier) + main-loop verify

- **M2.5 complete (`5259427`):** zero-state color cue for the top-bar decision-debt counters. **Executed as a strictly-sequential dynamic Workflow** (run `wf_ad40bf53-4de`, executor → adversarial verifier, single shared browser); workflow did NOT commit; the main loop independently re-read the diff + re-confirmed the empty-export SHA + browser-checked + committed.
- **Edits (template only, +17/−0, parser untouched):** (1) `updateDecisionDebt()` gains three `document.querySelector(".debt-{decide,confirm,resolve}").dataset.zero = count === 0 ? "true" : "false"` toggles, appended AFTER the three `debt*Count` `textContent` writes (which stay untouched — the count lives in the inner `<b id="debt*Count">`; setting `span.textContent` would clobber it) and BEFORE the progress-bar block; (2) CSS rule `.debt-decide[data-zero="true"], .debt-confirm[…], .debt-resolve[…] { color: var(--keep); opacity: 0.7; }` inserted before `.debt-bar`. Display-only: `cluster_mismatch`-style — `buildExportMarkdown` never reads `data-zero`, so the export path can't move.
- **Verification (executor + independent verifier + main-loop browser, all PASS):** empty-export SHA `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03` byte-identical (264,428 bytes; clean state); parser byte-exact (208 / 53-of-78 / 68%); zero console errors. Main-loop probe drove all three buckets both directions: clean baseline = 46/162/5, `data-zero="false"`, muted gray `rgb(94,94,88)`, opacity 1; all-committed = 0/0/0, `data-zero="true"`, green `rgb(31,90,71)` (= `var(--keep)` #1f5a47), opacity 0.7. **No clobber confirmed** — inner bold numerals (`debtDecideCount`/`debtConfirmCount`/`debtResolveCount`) all present + correct ("0"), labels intact ("0 your call" / "0 to check" / "0 group decisions"). The only diff line mentioning `debt*Count` is the new comment, not a `textContent` change. **Probe 3 (M2.2 contract):** `recomputeMismatchForEntry` does NOT change the top-bar counts (mismatch stays out of the calm top bar per design §10 — surfaced in the review summary instead).
- **M2.5 ends the auto-proceed chain** — M2.6 (smoke gate) + M2.7 (README) + M2.4 (intro) are all review-gated. Suggested next: `/clear` + re-run `/concepts-batch2` to start M2.6 with fresh context.

### Session 23 (2026-05-28) — M2.4 (floating prose milestone) via dynamic workflow + user-approved copy

- **M2.4 complete (`b5326b3`):** 3-step wizard intro modal rewrite (design §14), replacing the M1.4 single-paragraph onboarding stub. The floating prose milestone — done now (before M2.6) so the smoke gate verifies the final UI. **Copy drafted in the main loop + user-approved BEFORE implementation** (prose-review gate); implementation **executed as a sequential dynamic Workflow** (run `wf_017df89b-b82`, executor → adversarial verifier). Workflow did NOT commit; main loop independently re-read the diff + re-confirmed the SHA + browser-walked the 3 steps + re-confirmed `maybeShowOnboarding` gating both directions, then committed.
- **Plain-language copy (W22 / design §4.1) — reconciled from the plan's jargon-laden draft.** The plan's draft Step 2 cited "§12 / §13", "32 high-impact concepts (§11)", "verdict", "5 clusters" and taught Confirm/Resolve/Decide as named modes. Rewrote in curriculum-team voice aligned to the *visible* UI labels: Step 1 frames "208 concepts" + the four plain outcomes (keep / fold into / add new / remove); Step 2 describes the three on-screen labels plainly (**Quick check / Your call / Group decision**) with no counts that could drift; Step 3 references the real **Saved locally** badge + **⚙ Advanced** menu's **Save & Export markdown ↓** + **Import a saved markdown file…**.
- **Edits (template only, +121/−25, parser untouched):** (A) `#onboardingModal` body → `class="wizard-intro"` 3-step shell (3 `.intro-step` divs + `.intro-footer` with step-dots + Skip/Back/Next); (B) orphaned `dialog.onboarding` CSS block fully swapped for `dialog.wizard-intro` + `.intro-modes`/`.intro-footer`/`.step-indicator`/`.intro-actions` (kept the box-shadow + `--radius` for cohesion; added a border); (C) `showOnboarding()` expanded + new `introStep`/`syncIntroStep()`/`introNext()`/`introBack()` (loop var `stepEl`, NOT `el` — avoids shadowing the DOM-builder helper); `maybeShowOnboarding()` + `dismissOnboarding(persist)` untouched; (D) init wiring: `dismissOnboardingBtn` handler → optional-chained `introNext`/`introBack`/`introSkip`. Plus a 1-line stale CSS comment fix (`Onboarding dialog (preserved)` → `Wizard intro modal (3-step onboarding)`).
- **KEY RECONCILIATION from the plan's draft JS:** the plan hardcoded a NEW `localStorage` key `"intro_shown"` and its probes used it. The existing code already has `ONBOARD_KEY = "concepts-worksheet-onboard-v1"`. Kept `ONBOARD_KEY` (no second key introduced) and adapted the probes — verified `intro_shown` appears 0× in the template.
- **Verification (executor + independent verifier + main-loop browser, all PASS):** empty-export SHA `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03` byte-identical (264,428 bytes; re-confirmed on the freshly rebuilt artifact after the comment fix); parser byte-exact (208 / 53-of-78 / 68%); **zero console errors**. Orphan sweep all 0 (`dialog.onboarding` / `dismissOnboardingBtn` / `modal-body` / `class="onboarding"` / `intro_shown`). Nav walk: step 1→2→3 with active dot tracking + Back disabled only on step 1 + Next→"Get started" on step 3 + back-nav; Skip and Get-started both close + set seen="1"; reopen via showOnboarding resets to step 1. `maybeShowOnboarding` gating confirmed BOTH directions via real reloads (unseen → auto-opens on step 1; seen="1" → stays closed). W22 scan of the rendered modal: **0 jargon hits**. Copy matches the approved text verbatim.

### Session 24 (2026-05-28) — M2.6 full smoke gate (8/8 PASS) via dynamic workflow + load-bearing main-loop browser re-check

- **M2.6 complete (`cd143b1`):** the full 8/8 design §15 smoke gate — a **verification-only** milestone (no code changes; commits the status doc only). **Executed as a strictly-sequential dynamic Workflow** (run `wf_fda1c965-486`, executor → adversarial verifier, single shared browser). The workflow did NOT commit; the main loop independently re-verified the risky bits in a real browser + confirmed the source diff is empty before committing.
- **All 8 checks PASS** against the M2.4/M2.5 build (committed artifact byte-identical to a fresh `--build-html`, SHA `76c1d733…`):
  - **#1 Empty-export SHA invariant:** PASS — clean-state `buildExportMarkdown()` = `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03` (264,428 bytes), byte-identical to source; **re-confirmed AFTER all probing** (0 entries/0 clusters) — no probe state leaked.
  - **#2 Decide-later:** PASS — `plant_id` defer → uncommitted + in `deferred` + step +1 + export `verdict: <to_fill>`.
  - **#3 Pre-fill non-commit:** PASS — `recipe_writing` shows AI prefill (merge→how_to_writing, `isCommitted:false`); Previous → state uncommitted; export `<to_fill>`.
  - **#4 Commit roundtrip:** PASS — 5 mixed-mode commits → md1 SHA `d1038e2b…` (= M1.17 baseline) → clear → reimport (exactly 5 keys, no empty shells) → md2 byte-identical (0 diff). **NEW Batch-2 claim verified:** `cluster_mismatch` is UI-only and never appears in the export markdown.
  - **#5 Merge picker:** PASS — high-conf `plant_id` atomic write; low-conf `social_justice` picker rejects junk + self-ref, commits valid `seasonality`. **Plan-vs-corpus finding (stable, NOT a regression):** the high-confidence shortcut BLOCK remains data-unreachable through natural reviewer flow even in Batch 2 — the sole §11 cluster member `measurement` (CON-23) derives `keep`, never a merge target, so no Decide-mode member ever receives a cluster-derived merge target. Verified via direct `openMergePicker(entry, target)` invocation (as in M1.11/M1.17). The plan's "now reachable" expectation for #5 does not hold against this corpus.
  - **#6 Cluster Resolve + member walk:** PASS — CON-12 opt 2 → `narrative_writing` (§12 Confirm) renders headline "Based on your earlier group decision" + primary "✓ Agree — Fold into `writing`"; click → `{merge, writing}` atomic write. **W17 (tier-11 text-only):** the plan's "walk to `writing` (§11)" is **stale** — `writing` is §12; the genuine sole §11 cluster member is `measurement` (CON-23). On `measurement` (Your-call/Decide): plain "Keep as concept" suggestion as TEXT, **0 of 3 radios pre-checked**. (W21 cluster-over-AI override also re-confirmed on CON-24 pick-one: `descriptive_language`'s own AI target `vocabulary_development` overridden by the cluster derivation `figurative_language`.)
  - **#7 Mismatch flag:** PASS — CON-12 opt 2 + commit `narrative_writing` keep → `{verdict:"keep", cluster_mismatch:"CON-12"}` + `.cluster-mismatch-callout` visible + `autoOpenSet` has the key; change group answer (CON-12 → opt 0) → `{verdict:"keep"}` (flag cleared). Export contains no `cluster_mismatch` token.
  - **#8 Merge-target extraction rate:** PASS — `--verify-only`: `Parsed 208 entries (§11=32, §12=39, §13=137).` + `merge-target extraction: 53 of 78 merge recommendations (68%) — 25 fall through to picker` (= M1.3/M1.17 baseline; parser last touched `08e42d1`, untouched through all of Batch 2).
- **Three verification layers:** (1) executor real-browser run → ALL_PASS, 0 console errors; (2) adversarial verifier → VERIFIED, 0 discrepancies (logic/payload re-derivation — methodology caveat: it did NOT re-run a live browser, mirroring the M2.2/M2.3 verifier pattern, which is exactly why the main-loop browser re-check is load-bearing); (3) **load-bearing main-loop browser re-check** of #1 SHA + #6 render+click + #6b W17 on `measurement` + #7 set/clear + #4 export-safety — all independently confirmed; post-probe SHA holds; 0 console errors; `git diff` on parser + template empty.
- **Batch 2 functional work complete** at the gate; **M2.7 (README handoff) is the only remaining milestone** (review-gated prose).

### Session 25 (2026-05-28) — M2.7 README + curriculum-team handoff (prose user-approved) — BATCH 2 SHIPPED

- **M2.7 complete (`f56f124`):** rewrote `docs/plans/concepts-worksheet-form/README.md` as the curriculum-team handoff for the wizard. **Prose drafted in the main loop, adversarially critiqued via a two-lens dynamic Workflow** (run `wf_eeef6fa5-bc2`: a plain-language critic + an accuracy-vs-shipped-UI critic, both opus, read-only), then **user-approved before the file was written** (prose-review gate).
- **Reconciled the plan's own draft (same trap as M2.4):** the plan's M2.7 Step-2 draft README *violated* the plain-language constraint stated directly above it — it taught "Confirm/Resolve/Decide" as named modes and cited "32 high-impact entries (§11)", "5 clusters", "verdict", "Differs from cluster decision". Rewrote in the shipped plain voice instead: the four outcomes (keep it / fold it into another / add a new one / remove it), the three on-screen labels (**Quick check / Your call / Group decision**), and the real button/menu names. Anchored to the M2.4 intro-modal copy for voice.
- **Replaced the entire stale Batch-1 README**, which documented the *removed* long-scroll tool: a right sidebar of `CON-xx` cluster signals, the §11/§12/§13 frequency-tier table, filter-by-tier / "Needs my call" filter / search box, the W5-removed per-entry "Reviewer note" channel, and a top-bar `Save & Export` button. None of those exist in the wizard.
- **Two-lens critique outcome:** both critics independently verified the draft against the *actual built tool* (an args glitch delivered an empty brief, but both re-derived the correct lens from context and the template — which hardened the validation). Verdict: **voice clean** (W22 grep clean; labels match the tool verbatim), **all load-bearing claims correct**, **zero blockers**, 5 nits. All 5 folded in: (1) reframed the "~1 MB" claim (that's the file size; saved answers are small); (2) "quietly flags it" → "flags it — both on the concept itself and on Review so far"; (3) **added the top-bar search box** (jump-to-concept) that the draft had omitted; (4) "top corner" → "at the top of the page" (badge is top-left); (5) Enter shortcut now notes you must choose first on *Your call* / *Group decision* steps.
- **Parser untouched** (`--verify-only` = `208 entries (§11=32, §12=39, §13=137)`; `53 of 78 (68%)`). README is docs-only — no template/parser/HTML change.
- **Git-tracking note:** the README had **never been committed** — the whole `docs/plans/concepts-worksheet-form/` dir has been untracked since Batch 1 (not gitignored, just never `git add`-ed). Staged the README path explicitly (`git add docs/plans/concepts-worksheet-form/README.md`), NOT the directory, so the ~1.1 MB generated `concepts-worksheet-tool.html` stays untracked per the hard rule. This is the README's first commit.
- **BATCH 2 SHIPPED.** M2.0 → M2.7 all complete. Tool is gated + documented. Branch not pushed; no PR (awaiting explicit direction).

### Session 26 (2026-05-28) — pre-handoff dry run → Batch 3 opened + B2/W3 scoped

- **Post-ship dry run (workflow run `wf_e98174fd-aff`, sequential two-agent):** a fresh non-technical first-time reviewer (UX lens) then a curriculum domain skeptic (content lens), both driving the real built tool via chrome-devtools-mcp; both reset state at end (tool left pristine; SHA invariant intact). Purpose: catch what "8/8 smoke" can't — whether the tool is actually usable by the curriculum team before handoff.
- **Validated (positives from both agents):** the 3-step intro, the Group-decision cards' plain-language translation, Review-so-far bucketing + resume/edit links, live auto-save, faithful export, and the carry-down + mismatch-warning behavior. Structure/flow is good.
- **2 blockers + should-fixes found → Batch 3 (tool polish) opened** (plan "Batch 3" section, M3.1–M3.9):
  - **B1 (blocker, → M3.1):** "Your call" cards show a `💡 Claude recommends: …` block AND a legend "there's no suggested answer here:" — a contradiction I introduced in M2.0 (mistranslated "no default" = "no pre-filled radio" into "no suggested answer"). W22 term-scans can't catch it (the wording is plain, just false in context). Fix = conditional legend.
  - **Should-fixes (→ M3.2–M3.7):** verdict + "decide later" not mutually exclusive (one concept in two buckets); group page shows "(no decision yet)" despite carry-down; group-override leaves a stale per-concept "keep" note under a "fold" headline; silent export (duplicate downloads); hidden destructive keyboard shortcuts; `snake_case` keys shown in reviewer-facing spots.
  - **Nits (→ M3.8):** "▸ ▸ more" double-arrow; "Agree — Fold into another" (unnamed target); "1 appearances" plural; "Download JSON" clarity; 208-vs-213 count.
- **B2 (blocker, OUT of Batch 3 → own initiative):** the per-concept reasoning (`claude_notes`) is saturated with internal jargon (code paths like `filterDefinitions.ts`, `D-Cx`/`CON-xx` codes, `§`-numbers, raw field names, lesson-id hashes, "canonical/corpus/v3 baseline"). BOTH agents ranked this the #1 issue. It's the deliberate M2.0 residual #3 (notes are worksheet data, deferred). The dry run is the evidence the deferral has high real-world impact. Realistic fix = a plain-language rewrite of the 208 notes (parser emits a `plain_reasoning` field from an LLM pass; tool shows that; source/SHA decision pending — SHA-safe sidecar vs. rewrite-the-source). **To be scaffolded as `concepts-reasoning-rewrite` (kickoff-feature) with a design pass before any code.**
- **W3 (content data errors):** `measurement`'s reasoning says its sub-types are "anatomically more specific" — a copy-paste artifact from `plant_parts` (no anatomy in math). Lives in source worksheet data → folded into B2 / owned by the metadata-rebuild track; an LLM rewrite pass naturally fixes such artifacts, and genuine factual errors that ride into the export get a targeted source correction.
- **Also this session:** `.gitignore`'d the generated `concepts-worksheet-tool.html` (commit `d7fe7e0`) so a stray `git add -A` can't commit the ~1.1 MB artifact (README/template/parser stay tracked).
- **User chose (2026-05-28):** pursue all three workstreams (tool polish + plan the reasoning rewrite + fix worksheet data errors); NOT "ship as-is." Proceeding: execute Batch 3 starting at M3.1, scaffold B2 as its own initiative.
- **M3.1 complete (`e4cdcd2`, inline — trivially small, healthy context; main-loop browser-verified):** `renderDecideStep` legend made conditional on `prefill && prefill.verdict`. When a recommendation is shown the legend now reads **"Your call — confirm the suggestion above, or choose differently:"**; truly no-rec entries keep "there's no suggested answer here:". Verified in-browser: `plant_parts` + `measurement` (have a rec) show the new legend with the `💡 Claude recommends` block above; `biodiversity` (no rec) keeps the original. Empty-export SHA `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03` byte-identical (display-only); zero console errors.
- **M3.2 + M3.3 + M3.5 complete (bundled, inline pre-designed edits — main-loop browser-verified):**
  - **M3.2 (verdict ↔ Decide-later mutual exclusivity):** `setEntryState` clears the entry from `state.wizard.deferred` when a verdict is committed; `navDefer` un-decides an already-committed entry (clears verdict/merge_into/cluster_mismatch, preserves any note, refreshes counters); `renderReviewSummary`'s `deferredEntries` also filters out any committed entry (belt-and-braces). Verified: commit-undefers ✓, defer-undecides (entry deleted, counters refresh) ✓, and a force-double-stated `volume` now shows in exactly ONE bucket (REMOVE (1); DECIDE LATER (0)) ✓.
  - **M3.3 (group page reflects carry-down):** cluster-step member rows now show the derived suggestion (`computeMemberDerivation`) instead of "(no decision yet)". Verified CON-23 opt 1: "measurement — group suggestion: keep", sub-types "group suggestion: fold into measurement"; after committing `volume` the row shows "volume — fold into measurement". Plain wording, no backticks.
  - **M3.5 (export confirmation):** `onExport` now `alert`s the downloaded filename + "attach this to your email" so reviewers don't silently re-click (dry run found duplicate downloads).
  - Empty-export SHA `0c49a7a7…cd03` byte-identical (all display/UI-state); zero console errors. (M3.4 — the subtler group-override stale-note suppression — deferred to its own milestone next.)

### Session 27 (2026-05-29) — M3.4 via dynamic workflow (executor → adversarial verifier) + load-bearing main-loop browser re-check

- **M3.4 complete (`e748640`):** group-override stale rationale + "hold off" suppression. **Executed as a strictly-sequential dynamic Workflow** (run `wf_969eb1ab-25e`, executor → adversarial verifier, single shared browser); the workflow did NOT commit — the main loop independently reviewed the diff + re-confirmed the empty-export SHA + re-ran the risky probes in a real browser, then committed (template + this status edit only).
- **User decisions captured pre-build (AskUserQuestion):** (1) reframe copy = "You grouped this with the concepts above, so it follows that group decision. Its own earlier note no longer applies."; (2) the "hold off" card hides ONLY the suggestion block — the "✓ Agree" button + "Pick something else" stay visible (reviewer can still continue with these answers in mind).
- **Edits (template only, +53/−5, parser untouched):** added `clusterOverridesOwnSuggestion(entry, prefill)` + the `GROUP_OVERRIDE_REFRAME` constant after `displayedPrefillForEntry`; `renderConfirmStep` gates `renderClaudeRecommendation` on `!holdOff` (`holdOff = !!prefill.clusterInfoCaption`) and passes an `override` flag; `renderClaudeRecommendation` gains an `override` param → early-returns the reframe line in place of summary/▸more; `renderDecideStep` computes `holdOff`/`override` up top, gates its rec block (hold-off → no rec; override → reframe instead of the `claude_notes` body), and the M3.1 legend's `hasShownRec` is now `&& !holdOff` so it never claims "confirm the suggestion above" when the rec is suppressed; `.claude-rec-reframe` CSS (muted italic). The override gate fires ONLY on the W21 group-decision source (`captionSource === "Based on your earlier group decision"`) AND a differing verdict/target — members whose own suggestion already matches the group keep their original notes (e.g. `similes`, `sensory_details`). Override and hold-off are mutually exclusive (info-only prefills set `clusterInfoCaption` + `captionSource:null`).
- **Verification (executor + independent verifier + load-bearing main-loop browser, all PASS):** empty-export SHA `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03` byte-identical on clean state, re-confirmed AFTER all probing (262,937 bytes); parser byte-exact (208 / 53-of-78 / 68%); **zero console errors**. Main-loop browser probes: **P2** `how_to_writing` (CON-12 opt2) → reframe present, `.claude-rec-summary`/`▸more` ABSENT, group headline "Fold into `writing`", Agree button kept; **P3** `similes` (CON-24 opt1, own==group) → own note intact, NO reframe; **P4** `descriptive_language` (own target `vocabulary_development` ≠ group `figurative_language`) → reframe present; **P5** `similes` hold-off (CON-24 opt2, no parent) → NO `.claude-rec`, caption "Hold off — this is pending the group's main-concept pick.", Agree kept; **P6** `indigenous_knowledge` (CON-16 opt1) → NO `.claude-rec`, caption "Hold off — this is pending the heritage review."; **P7** after parent-pick `figurative_language` → `similes` no reframe (own==group), `descriptive_language` reframe present. Reframe copy character-exact (116 chars). The diff is exactly the 7 planned edits — no drift.

### Session 28 (2026-05-29) — M3.6 skipped (won't-fix; user decision)

- **M3.6 (keyboard-shortcut footgun) — SKIPPED, closed as won't-fix.** User decision 2026-05-29: "i dont care about keyboard shortcuts for this." Surfaced three paths (remove all single-key shortcuts / drop only the dangerous k/m/n/d verdict keys / leave as-is and skip); user chose **leave as-is**. **No code change.** The dry-run footgun — an accidental `k`/`m`/`n`/`d` keypress silently sets a verdict (overriding the suggestion, flipping the card type, ticking a counter) with no on-screen hint and no undo — is **accepted as a known limitation** for this internal, low-stakes tool. The keydown layer (ArrowLeft = Previous, Enter = Save·Next, L = Decide later, K/M/N/D = verdict) is unchanged from M1.13.
- **M3.4 hash folded in** (the prior session's `<commit-pending>` placeholder → `e748640`) as part of this status edit.
- Next: **M3.7** (friendly labels over `snake_case` keys — reviewer-facing, multiple render sites → workflow-orchestrate).

## Open questions / parked concerns

- **P2 — Decide-mode merge label regression (queued for M2.1):** `renderDecideStep`'s Claude-recommendation block (template ~line 1298) builds the merge label inline as `${REVIEWER_LABELS[v] || v}${targetSuffix}` → "Fold into another `target`", whereas `renderConfirmStep` uses `reviewerLabelWithTarget(v, t)` (template ~line 968) → "Fold into `target`". Dormant in Batch 1 (no Decide-mode entries have suggested_merge_target). Surfaces in M2.1 when `clusterPrefillCandidate` returns `{verdict:"merge", merge_into}` for Decide-mode cluster members (e.g., the CON-12 "Fold into `writing`" case at design §555). Captured inline in plan M2.1 so the fix lands with the cluster propagation work.
- **P3 — `renderClusterNotesDrawer` stub comment rewritten (closed):** prior comment claimed "Batch 2 wires cluster_notes to the cluster comment block" — false per the locked Batch 2 scope (auto-prefill, mismatch detection, review summary, intro rewrite, counter polish, smoke gate, README handoff). Rewrote in Session 16 follow-up to neutrally document the stub and stop inviting an unplanned cluster_notes channel; call site at template line 2226 stays unconditional so the cluster-step renderer's append chain is stable. Remove the function + call site if cluster Resolve never gains a notes affordance.

## Smoke check matrix

Recorded at M1.17 (Batch 1 gate) and again at Batch 2 conclusion.

| # | Check | M1.17 result | M2.6 result (Batch 2 gate) |
|---|---|---|---|
| 1 | Empty-export SHA invariant | PASS — SHA `0c49a7a720d6e703d995bab9969e0a98d8f582aad7655dab1d3513bf4d06cd03` (264,428 bytes / 3547 lines); first baseline at M1.4, re-verified post parser fix | **PASS** — clean-state SHA identical (`0c49a7a7…`, 264,428 bytes); re-confirmed AFTER all probing (0 entries/0 clusters) — no probe state leaked |
| 2 | Decide-later semantics | PASS — `plant_id` defer → entry state undefined, deferred list updated, export `verdict: <to_fill>` | **PASS** — `plant_id` defer → uncommitted + in `deferred` + step +1 + export `<to_fill>` |
| 3 | Pre-fill non-commit | PASS — `recipe_writing` Previous → state undefined, export `verdict: <to_fill>` | **PASS** — `recipe_writing` prefill `isCommitted:false`; Previous → uncommitted; export `<to_fill>` |
| 4 | Commit roundtrip | PASS — 5 entries (3 Confirm-agree + 2 Decide-pick), md1=md2 SHA `d1038e2b904a50674e93a4eb8b6813e6e4f70c537867ce46b1a274f24c5d1a12`, 0 diff lines; post fix to import parser: 5 imported keys (was 207), no empty shells | **PASS** — 5 mixed-mode commits, md1 SHA `d1038e2b…` (= M1.17), reimport 5 keys (no shells), md2 byte-identical (0 diff); **`cluster_mismatch` is UI-only, never exported** |
| 5 | Merge picker high-confidence shortcut | PASS — `plant_id` agree atomic write + "Saved:" caption; low-conf `social_justice` picker validates junk/valid + commits; shortcut-block UI data-unreachable in Batch 1 (re-test at M2.1) | **PASS** — `plant_id` atomic write; `social_justice` picker rejects junk/self-ref + commits `seasonality`. Shortcut BLOCK still data-unreachable via natural flow (sole §11 member `measurement` derives `keep`, never a merge target) → plan's "now reachable" expectation does **not** hold; verified via direct invocation |
| 8 | Merge-target extraction rate | PASS — 53/78 (68%) | **PASS** — 53/78 (68%); parser untouched since `08e42d1` |
| 6 | Cluster Resolve + member walk (Batch 2) | PARTIAL (M2.1b) — actionable prefill PASS (CON-12 opt-2 → narrative_writing); W17 PASS on §11 `measurement`; **M2.1b: CON-24 pick-one reveal + parent-pick → member derivations (incl. W21 override on descriptive_language) + reload-persist + flip-clears-parent all PASS; info-only caption now visible in BOTH Decide and Confirm (CON-16 opt-1 + CON-24 pre-pick verified)**. Full 8/8 gate (this + #7 mismatch) runs at M2.6. | **PASS** — CON-12 opt-2 → `narrative_writing` (§12 Confirm) renders "Based on your earlier group decision" + "✓ Agree — Fold into `writing`" → click → `{merge, writing}`. W17: `writing` is §12 (plan stale); genuine sole §11 member `measurement` (CON-23) → "Keep as concept" as TEXT, **0/3 radios checked**. W21 cluster-over-AI override re-confirmed (CON-24 pick-one, `descriptive_language`). |
| 7 | Mismatch flag (Batch 2) | PASS (M2.2) — commit-disagrees-with-group fires flag + de-jargoned yellow callout + auto-open prompt; matching commit doesn't flag; flag clears on group-answer change; persists across reload; `cluster_mismatch` is UI-only (never exported — SHA byte-identical). Full 8/8 re-run at M2.6. | **PASS** — CON-12 opt-2 + commit `narrative_writing` keep → `{verdict:keep, cluster_mismatch:CON-12}` + `.cluster-mismatch-callout` + `autoOpenSet`; CON-12 → opt-0 clears → `{verdict:keep}`; export has no `cluster_mismatch` token |
