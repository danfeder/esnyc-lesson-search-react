# PR 6 — Stage 2 Corpus Re-tag (PR 3b folds in) — Design Document

**Date:** 2026-06-11
**Status:** LOCKED 2026-06-11 — all 13 open questions decided in the Session-2 walkthrough (user = decision authority) on Session-1 evidence; see §4 for per-question locks + rationale
**Related:** `docs/plans/2026-05-13-stage2-retag-mechanism-exploration.md` (MANDATORY pre-read — primary input to the mechanism re-decision), `docs/plans/2026-05-03-metadata-rebuild-foundation-design.md` (parent initiative), `docs/plans/2026-05-03-metadata-rebuild-foundation-implementation.md` (lines 11 + 856-867 hold the currently-locked-but-reopen-recommended mechanism text), `docs/plans/2026-06-11-metadata-rebuild-pr5-canonicalization-execution-status.md` (PR 5 follow-ups this track inherits)

---

## 1. Why this exists

The corpus vocabulary is now fully canonical (PR 5a heritage + PR 5b concepts + #506 fold: PROD census 675 / 119 / 1923, zero non-canonical values) — but the **tag assignments themselves** are still mostly the output of the 2025 v3 GPT-4.1 run, which enum-enforced only 3 of 17 fields and whose tags on ~670 of 772 lessons no human reviewer ever validated. PR 5 rewrote the *spelling* of what's there; it did not re-derive *whether the tags are right* from the lesson bodies. The documented judgment-error classes remain live: tasting conflated with cooking (`both`-tagged garden lessons), cosmetics/craft lessons tagged `cooking`, stovetop lessons graded 3K-8, same-titled lessons classified inconsistently across variants.

Stage 2 is the cross-cutting Scope 3 commitment from the foundation walkthrough (2026-04-30): re-read every live lesson body against the validated canonical schema and re-tag with per-field enum enforcement on **all** re-tagged fields this time — "the first truly canonical retag pass," not "rerun v3 with Claude." It also carries the known one-shot backfills decided in the walkthrough: ~5 cosmetics lessons → `craft`, the 3 tasting-conflation `both` → `garden` fixes, consistent classification across the Seed Dispersal variants, `orientation` tag backfill (~15-30 lessons), `bilingual_handouts` tag backfill (6 lessons), and concepts re-tagged in both framework AND everyday vocabularies (the D5 decision) so PR 3b can populate `search_synonyms`.

**PR 3b folds into this track** (decided 2026-05-08): `search_synonyms` population with concept-derived everyday↔framework pairs from the re-tag outputs. The smart-search edge function already reads synonyms from the DB (PR 3a, Option B), so the TS-layer half of that work is done; only population remains. Full corpus embedding regeneration is also deferred to this track (foundation status doc: the natural batching boundary is when the underlying content meaningfully changes — that's here).

## 2. Constraints

1. **Data safety supersedes velocity.** The re-tag writes corpus-wide canonical data to PROD. Every apply step needs: local/TEST rehearsal first, a rollback snapshot, idempotency, and a dry-run/diff-review gate before any PROD write. Per `feedback_data_safety_top_priority.md`.
2. **Auditability and replay.** Every pipeline step's inputs and outputs must persist as inspectable artifacts (the exploration's JSONL-at-every-step property). Partial failure must be recoverable per-lesson, not per-run.
3. **Enum enforcement at generation time.** All re-tagged fields with locked vocab are constrained via schema-forced output (the `tool_choice` + `input_schema` enum pattern already running in PROD in `process-submission`), not validated-after-the-fact only.
4. **The mechanism decision precedes implementation.** The locked mechanism ("Anthropic SDK + Pydantic via Python adapter from `taggingv3/gpt_tagger/`") is recommended for reopening on five pieces of concrete file-cited evidence (exploration §3). The walkthrough decides; this scaffold does not pre-decide. No pipeline code before the design doc flips to Locked.
5. **Beat v3 measurably.** v3 output quality was mediocre (user calibration 2026-05-13). The eval/QA protocol must define what "better" means and must probe confident-but-wrong cases, not just v3-flagged-low-confidence cases.
6. **Cost bounded and estimated before the full run.** Original estimate ~$200-300; the per-field-vs-monolithic and Batch-vs-sync decisions are token-economics decisions requiring a small dry-run first, not aesthetics.

## 3. The chosen shape

**Decided 2026-06-11 (Session-2 walkthrough):** a **TypeScript + Zod batch runner at `scripts/stage2-retag/`** (export-corpus / run-retag / validate-output / generate-diff-report / prepare-apply), making **one monolithic enum-forced call per lesson** over the **767-lesson live corpus** via the **synchronous API** (Opus or Sonnet — picked empirically by answer-key scoring; **Opus contestant updated to `claude-opus-4-8` 2026-06-12**, user call: 4.7 superseded mid-track, 4.8 has identical pricing/tokenizer/cache-floor so all projections carry over), reading `lessons.content_text` as the body source, with `taggingv3` mined as reference material (baseline-to-beat, failure-case seeds, prompt ideas, cost calibration) rather than ported. Projected full-run cost ≈ $24 (Opus) / $14 (Sonnet). Main pass covers 12 fields (the 6 already-locked + the 5 cheap smaller fields + garden_skills, all vocab-locked in the walkthrough); cooking_skills + main_ingredients follow in a ~$15-20 second pass after a curriculum-team mini-worksheet. The runner mirrors (not extends) the canonical PROD call shape; per-field calls survive only as the repair-pass mechanism for Zod-failed fields.

### What is already settled (do NOT re-debate)

| Settled | Source |
|---|---|
| Stage 2 re-tag happens; full live corpus; spot-check ~50-100 sampled lessons before/alongside apply | Scope 3 commit, foundation walkthrough session 1 |
| Foundation decisions D1-D9, Stage 1 decisions D-C1—D-C15 | Decision journals; exploration doc reopens ONLY the mechanism lock |
| Heritage canonical vocab = the §16 88-row table; concepts canonical vocab = the returned 208-entry worksheet + `urban revitalization` → Advocacy addendum | `2026-05-10-...-stage1-heritage-worksheet.md`, `2026-06-11-...-stage1-concepts-worksheet-returned.md`, PR #506 |
| Concepts re-tagged in framework + everyday vocabularies; synonyms feed `search_synonyms` (PR 3b) | D5 decision |
| `claude -p` is NOT the bulk mechanism (SDK wins on cost/auditability/determinism); plausibly useful for periphery (audit-signal adjudication, spot-check probing) | Exploration §1-§2 |
| Do not extend `process-submission` into the bulk runner; mirror its call shape into a standalone script | Exploration §4 |
| Drop `pr5a_heritage_rollback` + `pr5b_concepts_rollback` in a cleanup migration after PR 6 ships | PR 5 design §4.8 (locked) |
| Future vocab censuses run on PROD, not TEST (TEST missing 13 live PROD concepts rows) | PR 5b follow-up |

## 4. Design questions — ALL LOCKED 2026-06-11

<!-- Sessions 1-2 ran 2026-06-11 (compressed into one day): Session 1 =
     evidence (workflow fan-out + adversarial verify, see "Session 1
     evidence" below); Session 2 = the re-decision walkthrough with the
     user deciding every [user-verdict] question via explicit choice
     prompts. Each question below retains its original framing for
     historical traceability; the LOCKED line at its end is the decision. -->

1. **OQ1 — Bulk mechanism: TypeScript+Zod runner vs Python/Pydantic adapter** `[user-verdict]`. The reopen-recommendation rests on: Zod schema header names Stage 2 as a TS consumer; Pydantic hand-sync is acknowledged debt; `taggingv3` reuse is ~15-20% (3-5 sessions realistic, not 1-2); two PROD-running TS demonstrations of the canonical pattern exist; v3 quality was mediocre. Leading: TS+Zod at `scripts/stage2-retag/`. Walkthrough decides; if reopened-and-changed, amend implementation plan line 11 + lines 856-867 + decision journal. → **LOCKED: TypeScript+Zod runner at `scripts/stage2-retag/`.** Lock reopened-and-changed on the five-point evidence (user choice); foundation impl plan + decision journal amended same session. `taggingv3` = reference material only.
2. **OQ2 — Field scope of the re-tag** `[user-verdict]` (user-deferred 2026-06-11 to this walkthrough). Only heritage + concepts vocab are locked; the ~8 smaller vocab fields (cooking_skills, garden_skills, SEL, core_competencies, cooking_methods, main_ingredients, observances/holidays, academic subjects) have no Stage 1 worksheets. Options: (a) complete smaller-field worksheets first, one all-field pass; (b) re-tag locked-vocab fields now (heritage, concepts, activity_type, tags, CRF, season_timing), second pass later; (c) hybrid. **Session 1 evidence: PROD census of the ~8 smaller fields' distinct-value counts + drift shapes** so this is decided with real numbers. → **LOCKED: hybrid, 12 fields in the main pass** — the 6 already-locked + academic subjects (6 values) + SEL (5) + core competencies (6; `Food Justice`→Social Justice fold) + cooking methods (3; `no-cook`→basic-prep fold) + observances (16 after End-of-year merge — **ratified 2026-06-12**: the configured 17 options included BOTH End-of-year variants, so the merge yields 16; `End of year celebrations` survives, 15 stored lessons vs 1; field closes to fixed list) + garden_skills (24 = configured 22 + `Stewardship tasks` + `Sensory exploration`; crop rotation kept). cooking_skills + main_ingredients → ~$15-20 second pass (deferred rider PR). All small-field vocab locked by user in the walkthrough. **AMENDED 2026-06-12: the curriculum-team mini-worksheet step was replaced by an in-session user walkthrough (user = decision authority; team skipped entirely, user call). Both vocabularies are now DECIDED — cooking_skills 23 entries, main_ingredients 24 groups + two-level specifics — see `2026-06-12-metadata-rebuild-stage1-cooking-skills-ingredients-worksheet-decided.md`. The second pass stays a separate rider (user choice, two-rounds confirmed) but is no longer blocked on any return.**
3. **OQ3 — Per-field vs monolithic call shape** `[user-verdict]`. Token-economics decision (body 3-10K tokens is the dynamic part; cache dynamics in the middle; Batch API caching is best-effort). **Session 1 evidence: 10-20 lesson dry-run in both shapes capturing input tokens, cache-hit rates, projected full-run cost** (exploration §5 item 8). → **LOCKED: monolithic** — measured $23.76 vs $220.60 (Opus sync, 767 lessons); per-field prefixes sit under Opus's 4,096-token cache minimum and cannot cache. Per-field survives only as the repair-pass for Zod-failed fields.
4. **OQ4 — Batch API vs synchronous SDK** `[user-verdict]` (the retention-acceptability half is a data-safety call). Batch = guaranteed 50% discount + `custom_id` replay, but async polling and **not ZDR-eligible (~29-day retention)** — verify current Anthropic data-handling policy is acceptable for lesson-body curriculum content before opting in (exploration §5 item 7). Likely shape: sync for dry-run/iteration, Batch for full run if retention is acceptable. → **LOCKED: synchronous for everything; Batch dropped.** With monolithic caching deterministic, Batch's bracket ($13-26) straddles sync's $24 while adding async complexity. **Retention verdict (user, 2026-06-11): the ≤30-day Anthropic-side retention of lesson bodies is ACCEPTABLE** (curriculum content, no student data, training-excluded, same exposure class as the PROD process-submission flow).
5. **OQ5 — Body-source readiness** `[evidence-lockable]`. `lessons.content_text` is the single canonical source (100% coverage at 772 rows as of 2026-05-06; stale measurement). **Session 1 evidence: freshness + quality audit on the ~751 live rows** — null counts, length distribution (truncation), control characters (corruption), random-sample comparison vs Google Doc canonical (staleness). Output: a body-source readiness statement + documented fallback for known-bad rows. → **LOCKED (Session 1, evidence): `lessons.content_text` as-is** — see the OQ5 entry in "Session 1 evidence" below for the 3-row handling + export normalization.
6. **OQ6 — Eval / QA spot-check protocol** `[user-verdict]`. Must include random sampling (catches confident-but-wrong), v3-flagged failures (biased but useful), and adversarial probes against open audit signals; must define "beat v3" concretely. **Session 1 evidence: inspect 5-10 `taggingv3` sample outputs to convert "v3 was mediocre" into specific failure examples; sketch 2-3 candidate protocols** (exploration §5 items 3-4). Who performs spot-check (Claude periphery agent / curriculum team / both) is part of the answer. → **LOCKED: Protocol A + B.** A = pre-run ~60-lesson answer key (~40 stratified-random + ~20 adversarial from the failure gallery), agent pre-filled + USER-verified (no team dependency per OQ8); v3 AND the new pipeline (Opus AND Sonnet variants) scored against it; run model picked empirically. "Beats v3" = per-field F1 ≥ v3 on every re-tagged field AND macroF1 ≥ 0.7 AND per-value recall ≥ 0.5. B = post-run plain-language diff audit + sampled three-bucket spot-check gating the apply. **Grade ground-truth policy (user verdict): ALWAYS source-doc claim** — record exactly the grades the lesson states; silent docs get NO grades (accepting weaker grade-filter coverage in exchange for never inventing grades).
7. **OQ7 — Apply mechanism + rollback** `[evidence-lockable]` (PR 5's emitter/rollback precedent guides; the PROD apply itself stays user-gated regardless). How re-tag output reaches PROD: staged temp table + diff review + apply migration is the exploration's sketch; the PR 5 emitter-generates-migration pattern is precedent. Rollback snapshot shape (PR 5-style in-migration backup tables) + idempotency + the pre-delete/pre-update checklist apply. → **LOCKED: staging + emitter-generated migration.** Runner emits apply-ready SQL artifacts + diff report (no live writes in PR B); the apply migration (PR C) snapshots every about-to-change row into `pr6_retag_rollback` first, then applies, dual-writing both `lessons` text[] columns AND the matching camelCase metadata JSONB keys (the census-confirmed dual representation). Idempotent; local + TEST rehearsal; PROD apply behind the CI approval gate.
8. **OQ8 — Reviewer-validation flow** `[user-verdict]`. Original plan named "Re-tag DIFF view vs fresh-tag review (TBD)" and the foundation plan gates PR 6 on a "Stage 2 reviewer-validation UX walk." Decide: what the curriculum team actually reviews (diff report artifact? worksheet-style doc? UI?), at what depth (spot-check-only per D4's lists-first sequencing), and what their sign-off gates. Keep curriculum-facing artifacts plain-language per `feedback_curriculum_facing_copy_plain.md`. → **LOCKED: the USER gates everything.** No curriculum-team involvement this track except the cooking_skills + main_ingredients mini-worksheet (which also carries the `guyanese`-parent question). Answer key = agent pre-fill + user verification; post-run diff audit + spot-check reviewed by the user (with agent help); USER sign-off gates the PROD apply. The mini-worksheet stays plain-language per the curriculum-facing-copy rule. **AMENDED 2026-06-12: the mini-worksheet did NOT go to the team — the user decided all of its questions (incl. `guyanese` → Both, implemented as hierarchy-parent latin-american + always-dual-tag caribbean) in an in-session walkthrough and chose to skip the team entirely. Curriculum-team involvement this track is now zero.**
9. **OQ9 — Audit-signal adjudication** `[user-verdict]` (which of the 74 signals resolve in this track is a scope call). 24 open CON-NN (concepts) + 50 open heritage register signals carry Stage 2 action columns. Decide which resolve in this track, the mechanism (agent-loop periphery work is the exploration's §2 fit — re-verify `claude -p` flag surface live if used), and where resolutions get recorded (the registers). Includes CON-16 (Indigenous cluster cross-field alignment with heritage §9.1). → **LOCKED: post-run adjudication pass over ALL 74 signals.** The re-tag resolves many implicitly (tags re-derived from bodies); after the full run an agent pass walks every register row against the NEW tags, marks resolved-by-retag vs still-open, records resolutions in the registers, and surfaces genuine judgment calls (CON-16 included) to the user. Rides PR B (docs-only register edits).
10. **OQ10 — PR 3b synonym extraction shape** `[evidence-lockable]`. How everyday↔framework pairs come out of the re-tag output (same tool call? second pass?) and the `search_synonyms` population migration shape; both search layers already read from DB. → **LOCKED: same tool call.** The monolithic response already carries concepts in both framework + everyday vocabularies (D5); everyday↔framework pairs are distilled from the run artifacts into the `search_synonyms` population migration in PR D. No second LLM pass.
11. **OQ11 — Embedding regeneration sequencing** `[evidence-lockable]`. Full-corpus regen after re-tag apply (cost trivial, ~$0.03/regen at current rates); decide TEST/PROD ordering and whether it rides the apply PR or follows. → **LOCKED: rides the apply PR (PR C)** — regen immediately after apply, TEST first then PROD, following the existing regen script precedent.
12. **OQ12 — Check surface for `scripts/stage2-retag/` (if TS path wins OQ1)** `[evidence-lockable]`. `tsconfig.json` includes only `src`; ESLint ignores `scripts/**`. The re-tag runner crosses a blast-radius threshold that deserves `tsconfig.scripts.json` + ESLint override + unit tests for parsing/validation/diff/apply-prep. Also revisit the Stage-1 worksheet `Notes`-field human-only-prose parsing convention if a worksheet parser is needed (foundation status doc out-of-scope item). → **LOCKED: full check-surface lift** — `tsconfig.scripts.json` + ESLint override covering `scripts/stage2-retag/**` + Vitest unit tests (TDD) for vocab assembly, schema construction, output parsing/validation, diff generation, and apply-prep. Pin `tsx` in devDependencies (currently unpinned).
13. **OQ13 — PR breakdown + count** `[user-verdict]`. Authored after OQ1-OQ12 lock. Candidate skeleton in the implementation plan (pipeline+dry-run → full run+diff+staging → apply+embeddings → PR 3b synonyms → cleanup/rollback-drops); the walkthrough may merge or split. → **LOCKED: 5 PRs as sketched (A-E).** A: runner + check surface + dry-run (no DB writes) · B: answer key + full run (user green-light) + diff + apply artifacts + register adjudication · C: apply migration + rollback + embeddings regen · D: `search_synonyms` population · E: cleanup (drop `pr5a_heritage_rollback`, `pr5b_concepts_rollback`, `pr6_retag_rollback` + staging; observances field closes to fixed list; filterDefinitions vocab updates). Plus a deferred rider PR for the cooking_skills/main_ingredients second pass when the mini-worksheet returns.

### Session 1 evidence (2026-06-11) — gathered + adversarially verified

All artifacts in `docs/plans/pr6-stage2-retag-evidence/`; every item passed an adversarial verifier and the supervisor independently reproduced the load-bearing PROD numbers. **Cross-cutting correction: the live corpus is 767 lessons** (788 total − 21 `import:*` retired; the ~751 figure reproduces only on TEST) — all references and cost projections resize to 767.

- **OQ1:** "mirror, don't extend" holds — both canonical call sites unchanged since the exploration; `scripts/eval-llm-tagging-prompt.ts` already proves the exact PROD call shape standalone on Node; a Stage 2 runner is additions only (corpus fetch, vocab adapter, JSONL+resume, usage accounting, optional Batch path, dry-run/apply gates). Edge enums come from `_shared/metadataSchemas.ts`, not `enums.json` (consumed by neither call site). (oq1-call-shape-confirmation.md)
- **OQ2:** the 8 smaller fields tier cleanly: **5 walkthrough-lockable without worksheets** (academic subjects 12 distinct / SEL 10 / core competencies 13 / cooking methods 5 / observances 17 — each ~90%+ canonical, drift is kebab twins + 3 single-value judgment calls); garden_skills in the middle (47 distinct, ~9-entry add-or-fold list); **cooking_skills (122 distinct, 93% of tagged rows off-vocab, top values absent from the 27-option configured list) + main_ingredients (230 distinct, 81% off-vocab, category-vs-specific abstraction mix) genuinely need worksheets.** All 8 fields: single camelCase JSONB key + text[] column dual representation → the apply must dual-write. Kebab drift is submission-era-concentrated (88/105 vs 7/662 legacy). (oq2-smaller-fields-census.md)
- **OQ3:** dry-run measured (15 lessons, claude-opus-4-7, $3.58 actual): **monolithic = $23.76 sync for the full 767-lesson run** with perfect prefix caching (14/14 cache reads of a 6,274-token prefix); **per-field today's shape = $220.60** — its 1.3-2.2K-token per-field prefixes sit below Opus's 4,096-token cache minimum so it cannot cache at all; shared-prefix per-field DID cache fully (rotating `tool_choice` did not invalidate the prefix incl. the body block — exceeds documented behavior; 5-call sample, rerun pending credits) but showed ~18× output inflation (enums not server-enforced without `strict: true`), making it the most expensive variant anyway. Output tokens ≈ 57% of monolithic per-lesson cost → output-verbosity control matters as much as caching. Sonnet 4.6 monolithic: $14.26. (oq3-token-economics-dryrun.md)
- **OQ4:** ≤29-day batch retention + not-ZDR-eligible confirmed against live docs 2026-06-11; **batches are deletable on demand post-download** (effective storage window → hours); sync is also retained ≤30 days under standard commercial terms, so the retention delta is artifact-shape, not duration; neither path trains on the data. 50% discount confirmed, stacks with best-effort caching (30-98%) → for monolithic, Batch ($25.72 / $18.80 / $13.26 at 0/50/90% hits) **straddles** sync ($23.76) — not automatically cheaper. 767 lessons fit one batch trivially. Retention acceptability = user verdict at walkthrough. (oq4-batch-retention.md)
- **OQ5 — LOCKED 2026-06-11 (evidence-lockable):** `lessons.content_text` is the body source, as-is. Rationale: 100% live coverage, smooth length distribution with no truncation cliff, no corruption (48 harmless vertical tabs), 5/5 seeded-random sample verbatim-fresh vs live Google Docs. Handling baked into the export step: exclude ghost stubs `1l9KH63QBe2xhyH0zp6VIavtj0uKSRZtd` + `1nFbpkwlujk8fIO8RkeIcDoO2fuO83Iil2Srf8t5iqm8`; re-extract or hand-tag `1n8wS0X-dXAw9sfQuLFgsMg_kNvACph3cT4yd9p2i1eg` (Who's Who in the Food System); strip VT + normalize `\r`; note to prompt that ~95 bodies carry `---`/`[Table]`/pipe markers. (oq5-content-text-audit.md)
- **OQ6:** v3 flagged **zero** of 831 lessons for review → the "v3-flagged" sampling bucket is empty, not merely biased; v3 confidence is highest exactly where it failed (64 lessons grade-smeared to 10+ grades at ≥90 confidence; 82 incoherent Stovetop+"Basic prep only" pairs; cosmetics lesson with cooking tags at 88-95). Protocol sketches: **A** = pre-run ~60-lesson answer key (~40 stratified-random + ~20 adversarial seeded from the failure gallery; curriculum team labels with agent pre-fill; v3 AND the new pipeline both scored against it) + **B** = post-run three-bucket diff audit as the apply gate; "beats v3" = per-field F1 ≥ v3 on every re-tagged field AND macroF1 ≥ 0.7 AND per-value recall ≥ 0.5. Pre-labeling user decision required: grade-level ground-truth policy (source-doc claim vs age-appropriateness — corpus contains both fabricated and source-faithful 3K-8 spans). (oq6-v3-baseline-and-eval-protocols.md)
- **OQ9 input (E7, claude 2.1.174):** `--bare`, `--tools`, `--json-schema`, `--max-budget-usd`, `--no-session-persistence`, `--output-format`, `--allowedTools`/`--disallowedTools`, `--system-prompt` all present; **`--max-turns` is gone from `--help`** — if periphery use needs loop caps, re-probe alternatives then.
- **OQ12 input:** confirmed `scripts/` has zero tsc/ESLint coverage today (Vitest does reach it); `tsx` is unpinned; the Anthropic SDK version is a shared caret spec (`^0.95.0`) across Deno + Node manifests, not a pin.

Operational notes for future sessions: this track's exploration doc contains non-UTF8 bytes — plain `grep` sees it as binary and silently returns nothing; use `grep -a`. `.env.local`'s `ANTHROPIC_API_KEY` is the CLIProxyAPI proxy-side key and 401s against the direct API — direct-API scripts must use `ANTHROPIC_CONSOLE_API_KEY`. The Console account ran out of credits mid-V3 (top-up needed before the ~$5.50 V3 rerun or any future dry-run).

## 5. Migration / shipping strategy

**Locked with OQ13: five PRs (A-E) + one deferred rider.** A (runner + check surface + dry-run, code-only) → B (answer key + user-green-lit full run + diff/staging artifacts + register adjudication, no DB writes) → C (apply migration with `pr6_retag_rollback` snapshot, dual-write columns+JSONB, Who's-Who content_text repair, SQL CHECK constraints added AFTER data is canonical, embeddings regen) → D (`search_synonyms` population from run artifacts) → E (cleanup: drop the three rollback tables + staging, close observances to fixed list, filterDefinitions vocab updates). Rider PR: cooking_skills/main_ingredients second pass — vocab DECIDED 2026-06-12 (user walkthrough, team skipped), so the rider is unblocked and gated only on the B/C-style miniature cycle. The global Zod enum-closing for the 6 newly-locked small fields rides C/E (schema constrains only after data conforms — PR 5 ordering precedent); until then the locked lists live in a runner-local vocab file.

Fixed points:

- Schema/data changes reach PROD only through migration files + CI (never `mcp__supabase-remote__apply_migration`).
- TEST rehearsal note: TEST is missing 13 live PROD concepts rows — TEST rehearsal validates *mechanics*, but PROD MCP verification validates *coverage*. Apply-step probes must run against PROD post-apply with verbatim identifiers per `feedback_verbatim_identifiers_in_probes.md`.
- The cleanup migration dropping `pr5a_heritage_rollback` + `pr5b_concepts_rollback` ships at the END of this track (after the re-tag is PROD-verified), per PR 5 locked design §4.8.
- Known CI flakes inherited: migrate-production SASL (both Apply + Verify variants; rerun pattern), deploy-edge-functions esm.sh 522 (if any edge function is touched).

## 6. Testing strategy

Locked with OQ1/OQ12/OQ6: TDD (failing-test-first) for the runner's vocab assembly, schema construction, output parsing/validation, diff generation, and apply-prep modules, under the new `tsconfig.scripts.json` + ESLint override + Vitest surface; dry-run (≤20 lessons, sync) before the full run; every run record post-hoc Zod-validated (enum adherence is NOT server-guaranteed without `strict: true`); Zod-failed fields go through the per-field repair pass; Protocol A answer-key scoring (v3 + Opus + Sonnet contestants, "beats v3" bar) before the full run is green-lit; Protocol B diff audit + user spot-check before apply; `npm run test:rls` unchanged unless a migration touches policies; PROD MCP verification with verbatim identifiers after every apply.

## 7. Out of scope (captured for future work)

- **Seed Bursts near-duplicate pair** (`1HuffJuy…` + `1NqjpqXV…`, different metadata) — dedup track, not this one. The re-tag treats both as live rows.
- **Phase-2 reviewer UX redesign** (guided pickers, per-field guidance, concepts editor + its clearing semantics) — only the minimal reviewer-validation flow needed by OQ8 is in scope.
- **Resend email setup** — unrelated; still deferred per user.
- **`guyanese` parent under `latin-american`** — RESOLVED 2026-06-12 (user verdict: **Both**; hierarchy parent stays `latin-american`, re-tag prompt rule dual-tags Guyanese lessons with `caribbean`; true multi-parent schema change deferred to the filter-UI track, which itself stays out of scope).
- **Filter UI redesign** (5-value Activity Type group, Lesson Type tag filter, heritage hierarchy surfacing) — Phase 2.
- **The ~8 smaller-field Stage 1 worksheets** — in scope ONLY if OQ2 lands on "worksheets first"; otherwise a sibling track.

## 8. References

- `docs/plans/2026-05-13-stage2-retag-mechanism-exploration.md` — primary mechanism input; §5 lists the walkthrough prerequisites this doc's OQ evidence items mirror
- `docs/plans/2026-05-03-metadata-rebuild-foundation-{design,implementation}.md` — parent initiative; impl plan lines 11 + 856-867 are the text OQ1 amends if reopened-and-changed
- `docs/plans/2026-05-10-metadata-rebuild-stage1-heritage-worksheet.md` (§16) + `docs/plans/2026-06-11-metadata-rebuild-stage1-concepts-worksheet-returned.md` — locked vocab inputs
- Audit signal registers: heritage worksheet register (50 open) + concepts worksheet register (24 open CON-NN)
- `docs/plans/2026-06-11-metadata-rebuild-pr5-canonicalization-execution-status.md` — inherited follow-ups
- Memory: `project_metadata_rebuild_initiative.md`, `project_stage2_mechanism_exploration.md`, `feedback_data_safety_top_priority.md`, `feedback_curriculum_facing_copy_plain.md`, `project_crf_stamp_theater.md` (D9 re-tag design for CRF), `project_dedup_third_state.md`
