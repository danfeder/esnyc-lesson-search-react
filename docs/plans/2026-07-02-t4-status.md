# T4 Corpus Dedup — Status

**Design:** `2026-07-02-t4-dedup-design-decisions.md` (all decisions D1–D12 locked with the
user 2026-07-02). Briefs: `2026-07-02-brief-t4a-candidate-sweep.md`,
`2026-07-02-brief-t4b-review-reshape.md`. Deck/decisions artifacts land in
`docs/plans/t4-dedup/`.

## Steps

| # | Step | Who | Status |
|---|------|-----|--------|
| 1 | Design session (probes, user decisions, briefs) | Fable + user | ✅ DONE 2026-07-02 |
| 2 | t4a: sweep script + evidence deck (Sonnet 4.6 fan-out, pinned) | Opus | **✅ DONE 2026-07-02 — PR #576 MERGED (squash `f6513c1`, Fable-reviewed); 113 groups, deck on `main`** |
| 3 | Live walkthrough → `decisions.json` | Fable + user | **✅ DONE 2026-07-02 — all 113 groups decided with user; 61 retires; decisions.json committed** |
| 4 | t4c: retire migration (brief written by step-3 session) | Opus | **PR #577 OPEN 2026-07-02 (`feat/t4c-dedup-retire`)** — migration `20260702160000_t4_dedup_retire.sql` (+`.sql.rollback`) generated from decisions.json by `scripts/dedup-sweep/generate-retire-migration.mjs`. Local gates green; PROD re-probe 764/61-live/0-retired ✓. **TEST verified: 57 retired, snapshot 57, 0 survivors, search excludes retired (685 live) — matched pre-registration.** CI green + both claude bots reviewed. **Atomicity fix (bot+Codex, [[project_supabase_migration_autocommit]]):** wrapped the body in `BEGIN;`/`COMMIT;` + `LOCK TABLE lessons SHARE ROW EXCLUSIVE` (c02 precedent) so the post-asserts can roll back the write; validated locally. After the fix, a controlled reversible TEST re-validation (rollback → drop snapshot table → delete TEST history row → CI re-run) applied the WRAPPED file fresh against TEST — same numbers. Rollback hardened to the 61 ids. **Fable pre-merge review 2026-07-02: APPROVED, no changes** — generator↔migration byte-identical; decisions.json integrity 61/250/0-overlap; wrapper definitively confirmed via TEST's `schema_migrations` statements (BEGIN→LOCK→…→COMMIT); TEST re-probed 685/57/57/0-outside-snapshot; PROD re-probed 764/61-present-live/0-retired/no-table (STOP clear); Codex #2/#3 rebuttals upheld; TEST surgery ruled appropriate; `.sql.rollback` verified safe + idempotent. **✅ DONE 2026-07-03: merged (squash `badaadf`, user-authorized) + user approved the PROD gate (run 28629708389 success) + post-apply PROD verify ALL GREEN — live 703 exactly, 61 `dedup:%`, rollback=61 (all-NULL prior), 0 outside-snapshot, wrapped txn confirmed in PROD `schema_migrations`, fattoush search clean, retired stub linkable (queries + raw results on PR #577).** |
| 5 | t4b: review-screen reshape + detection rewrite + admin-page removal | Opus → Fable | **PR OPEN 2026-07-03** — Opus first cut (on-brief, mid-flight when stopped; snapshot `695e0bb`) salvaged + completed by Fable: D7 publish guard, complete-review embedding-block removal (brief gap), MANAGE_DUPLICATES removal, migration ACL-comment fix, full test pass (1958/1958; 3 new integration tests + label-map suite). TEST verified (2 DB rounds: ACLs, retired-exclusion live-fire, hash-honest edge smoke on v10 — queries+results on PR). 3 bot rounds converged (R1 note-leak+casefold `e6d63fd`+mig `20260703010000`; R2 card-switch note rebind `856fcd6`; R3 clean) + 2 permanent RLS guardrail scenarios. Deploy-to-TEST reds = documented cosmetic no-op. 3 deferred follow-ups on tracker. **✅ DONE 2026-07-03: merged (squash `9e0cc4e`) + both PROD gates user-approved (success) + PROD verify ALL GREEN — 2 migrations recorded, ACLs locked, retired-exclusion clean vs 61 retired rows, corpus 703 untouched, 3 edge fns advanced w/ byte-exact shas, no-auth 401 (posted to #578). T4 CLOSED.** |

## Key session-1 facts (don't re-derive)

- Live corpus 764 (785 − 21 retired imports); 46 same-title groups / 50 excess copies;
  1 hash-identical pair (Fattoush); content trigram sim separates copies (≥0.92) from
  families (≤0.75); pools: title-sim>0.5 → 324 pairs.
- Soft-retire precedent = imports cleanup migrations `20260520000000/020000/030000`.
- `archive_duplicate_lesson` HARD-deletes (UI lied) → revoked in t4b, page removed.
- Reject already supported server-side; teacher-side gap is `UserProfile.tsx:43/:62`.
- Dup-panel disconnect anchor: `ReviewDetail.tsx:190`.
- User decisions: dup-clean now + family labels (families project post-sprint); live
  walkthrough run by Fable (authorized 3rd Fable session); one-decision-list screen shape;
  plan ratified incl. admin-page removal; **Sonnet fan-out pinned to 4.6, not Sonnet 5.**

## t4a outputs (for the walkthrough)

- **Sweep pipeline:** `scripts/dedup-sweep/` (export-corpus → generate-candidates → run-deck →
  build-deck-md; all read-only, zero DB writes). Corpus export gitignored; reproduce steps in
  the dir README.
- **`docs/plans/t4-dedup/candidates.json`** — 113 deterministic groups (Tier **A=21, B=41,
  C=51**), calibration-gate-locked (46 same-title groups/96 rows, 1 hash pair, all 8 named-pair
  tiers hold; TS trigram matches SQL pg_trgm ≤0.001). Byte-identical on re-run.
- **`docs/plans/t4-dedup/deck.json` + `deck.md`** — one Sonnet 4.6 verdict per group
  (pinned `claude-sonnet-4-6`; pin asserted). Tally: **retire_duplicate 37 · keep_family 74 ·
  unrelated 2**. deck.json is a point-in-time LLM artifact; candidates.json is the deterministic
  spine.
- **7 flagged "please confirm" groups** (all Tier C): AI recommends retire_duplicate but wording
  overlap < 75% — the "same lesson, one is a fuller/table-import copy" pattern (per-brief
  re-run-once done; 1 of the original 8 flipped to keep_family; 7 persist and are surfaced, not
  auto-shipped or overridden — decision is the user's). Listed in a deck callout + per-group
  flags. Groups: all-about-birds, all-about-pumpkins, black-panther-party…, kitchen-garden-intro,
  sweet-and-sour-roots, three-sisters-puppet-show, tortilla-espanola.
- **Mixed groups** (a true-copy pair embedded in a real family, e.g. Fattoush, Sun Printing,
  3-Sisters-Tacos) come out **keep_family** with the redundant copies named in the text — the
  single-verdict schema can't retire a subset, so the walkthrough retires those copies by hand.

## Walkthrough outputs (step 3, for t4c)

- **`docs/plans/t4-dedup/decisions.json`** — all 113 groups: verdicts retire_duplicate 39 ·
  keep_family 72 · unrelated 2; **61 lessons to retire** (live corpus 764→703), 250 named
  survivors. Every verdict user-confirmed; every lesson_id pulled verbatim from
  candidates.json by `scripts/dedup-sweep/record-decisions.mjs` (letters→IDs, committed).
- **Same-doc discovery (load-bearing):** many "duplicate pairs" are two DB entries pointing
  at ONE Google Doc (Sept 2025 import re-ingested docs already in the library). Checked all
  113 groups via `file_link` doc-ID match; this confirmed most Tier-A/B retires, resolved 3
  of the 7 "please confirm" flags outright (Black Panther, Puppet Show, All About Pumpkins
  + both Decomposition pairs), and **flipped three deck keep-both verdicts to retire-one**
  (Plants and Music, BCCS Empanadas, Street Vendors).
- **User overrides of deck picks (4):** Kitchen Tool Identification (kept the
  reflective-discussion copy), All About Birds (kept July summary copy), Kitchen & Garden
  Intro (kept September Intro outline w/ Pre-K), Tortilla Española (kept streamlined copy).
  Plus 1 keep-both override: Three Sisters Soup pair kept as grade-band family to preserve
  3K-5 tagging.
- **PROD pre-validation (read-only, 2026-07-02):** all 61 retire IDs exist, all live, none
  already retired; live corpus 764 → expected 703 post-retire.

## Session log

- **2026-07-02 (Fable, design session — 2nd planned Fable session):** PROD recon (read-only),
  all design decisions locked live with user, design doc + both briefs + this doc written.
  Tracker updated. Docs ride the t4a PR (nothing pushed this session). NEXT = t4a (Opus).
- **2026-07-02 (Opus, t4a execution):** built the sweep (`scripts/dedup-sweep/`), ran it
  read-only against live PROD (764 rows, anon key), generated 113 candidate groups (all
  calibration gates green), fanned out 113 Sonnet-4.6 agents (0 schema failures) for the deck,
  spot-checked 5 groups against real content (all correct). `npm run check` + `test:run`
  (2059/2059) green. Docs on the PR. NEXT = **Fable walkthrough with the user** (read
  `deck.md`, fill `decisions.json`, then write the t4c retire-migration brief).
- **2026-07-02 (Fable + user, live walkthrough — 3rd authorized Fable session):** all 113
  groups adjudicated (Tier A batched, Tier B batches + 7 doc-pairs eyeballed one at a time,
  Tier C with the same-doc evidence). decisions.json written incrementally via
  `record-decisions.mjs` (checkpoint after every batch). Same-doc check across all groups
  surfaced mid-session and reshaped several verdicts (see Walkthrough outputs). All 61
  retire IDs pre-validated against PROD read-only. t4c brief written. NEXT = **t4c (Opus,
  brief ready)**; t4b can run in parallel (Opus). PROD gate on t4c = user-only.
- **2026-07-02 (Opus, t4c execution — retire migration):** wrote a committed generator
  `scripts/dedup-sweep/generate-retire-migration.mjs` that reads decisions.json and emits
  (byte-deterministically, zero hand-typed ids) the migration
  `20260702160000_t4_dedup_retire.sql` + its `.sql.rollback`. Integrity gate re-run: 61
  distinct retired, 250 distinct survivors, no overlap, all slugs/ids charset-clean.
  Mechanism per D5: snapshot table `t4_dedup_retire_rollback` (RLS-on-no-policies, precedent
  pattern) captures prior state, then soft-retire (`retired_at=now()`,
  `retired_reason='dedup:<group_id>'`); in-migration guards (compile-time 61-distinct,
  refuse-if-any-already-retired, post-update all-present-targets-retired + zero-survivors-
  touched). Also fixed `eslint.config.js` to give `scripts/dedup-sweep/**/*.mjs` Node globals
  (mirrors the heritage override) — the walkthrough's `record-decisions.mjs` had left `check`
  red. Local gates all green (db reset, test:rls exit 0, check exit 0, test:run 2071/2071).
  PROD re-probe (read-only): 764 live, all 61 present+live, 0 already retired → 703 expected.
  TEST rehearsal pre-registered: **57 of 61 present+live** (4 absent from TEST), 0 already
  retired → TEST 742→685, snapshot 57 rows. PR `feat/t4c-dedup-retire` opened (carries the
  walkthrough commit `c586c1d`). NEXT = CI→TEST verify (assert 57 retired + snapshot 57 + 0
  survivors), then 🔴 **USER-only PROD gate**; after PROD, run the brief's read-only
  verification (703 live, 61 dedup:%, 0 survivors, fattoush spot-check). Then **t4b** (Opus,
  independent) → **T5** launch smoke.
