# C02 — Cooking Skills & Main Ingredients Re-tag — Design Document

**Date:** 2026-06-22
**Status:** **PIVOT RE-LOCKED 2026-06-23 (Session 9)** — the P2 pilot failed (blind full-LLM re-read → over-tagging); the method was re-designed and re-locked. **The authoritative method is now §3·PIVOT below**; §3 / §4-Q3 / §4-Q5 / §4-Q6 and the apply scope are superseded by it. The original Session-1 lock (Q1–Q11) stands for everything §3·PIVOT does not touch (vocab Q1, casing Q8, expand/contract Q9, Opus-only Q11). GATE 1B re-run on the re-authored pivot tasks.
**Review:** GATE A adversarial review (Codex cross-family + a Claude anchor-verify agent, 2026-06-22) folded — 2 HIGH (close the vocab into a byte-identical manifest; PROD expand/contract deploy choreography) + ~11 MED/LOW; anchors corrected against current code.

> **✅ PIVOT RE-LOCKED 2026-06-23 (Session 9).** The P2 pilot failed — both `claude-opus-4-8` and the `claude-opus-4-7` fallback over-tagged (raw-LLM precision **66.5%** / recall **89.7%** on the 69-lesson tight gold key; `Tasting` predicted on 78% of lessons, `Kitchen & food safety` 43%) because the harness was a *blind* full-LLM re-read (LLM got body only; floor rules R7/R8 fired **0×**; apply wrote all ~14 fields). The method has been re-designed and re-locked in **§3·PIVOT** below, validated by a Codex cross-exam (agent `ab877ed343f682131`, gpt-5.5). Headline: **anchored verify-and-diff** (show the LLM the current provenance-annotated tags; KEEP/DROP/ADD, not re-tag-from-scratch) + **one canonical floor** + **reconcile in code** + **C02-only apply scope** + **per-value gates with a recomputed baseline** + a **3-layer test strategy** (69-key workhorse / fresh-25 canary / corpus-wide prevalence). §3 / §4-Q3 / §4-Q5 / §4-Q6 + the apply scope are **SUPERSEDED by §3·PIVOT — read it first.** Full pilot findings + scorecards: the execution-status doc + `scripts/stage2-retag/artifacts/c02-pilot-scorecard.opus-4-{8,7}.md` + `c02-filter-usefulness.md`.

**Related:**
- Discussion kickoff: `docs/plans/2026-06-22-c02-vocab-retag-discussion-kickoff.md`
- Decided vocabulary worksheet: `docs/plans/2026-06-12-metadata-rebuild-stage1-cooking-skills-ingredients-worksheet-decided.md`
- Wave-4 carry-forward findings: `docs/plans/2026-06-22-wave4-data-corpus-cleanup-design.md` §3.1 / §4 Q11–14a / §8
- Re-tag harness: `scripts/stage2-retag/` (export → normalize → run-retag → validate → diff → apply + answer-key scoring)
- Memory: `project_metadata_rebuild_initiative` ("PR F" definition), `project_deferred_work_campaign`, `feedback_data_safety_top_priority`, `feedback_per_round_test_db_verification`, `feedback_codex_over_crossexamine`, `feedback_multi_session_execution`

> **Provenance of the file:line anchors below:** captured 2026-06-22 from a read-only grounding pass (3 agents) + a PROD read-only census + a Codex cross-exam. Line numbers may have drifted; **Session 1 verifies every anchor against current code before authoring tasks.**

---

## 1. Why this exists

The 2026 "metadata rebuild" canonicalized every lesson-metadata field except two — `cooking_skills` and `main_ingredients` — which were deliberately carved out as the rebuild's deferred **"PR F"** (the two largest, messiest vocabularies). C02 is that second pass. As of the **live PROD census 2026-06-23** (`retired_at IS NULL`, **764 live rows**): **`cooking_skills` = 122 distinct values** (1,758 appearances, 435 lessons); **`main_ingredients` = 230 distinct** (1,847 appearances, 430 lessons) — versus the rebuilt `cultural_heritage` at ~70. **[CORRECTED 2026-06-23 Session 6 — the "121/202 re-census" previously recorded here was a TEST-DB query mislabeled as PROD (the cooking_skills `121` and an appearance-sum of `1,709` match TEST exactly; PROD is reproducibly 122/230, confirmed against the 2026-06-11 `oq2` census's verbatim SQL and a fresh live query). The 121/202 figure is retracted; see the forensic note in the status doc + `q1-vocab-census.md`'s correction banner.]** The sprawl is years of free-form reviewer entry with no enforcement.

**A grounding pass settled the central reframing** (it is *not* a SQL value-rename like the rebuild's 5a/5b migrations):
- The prior ~$121 fable-5 re-tag run (rebuild PR 6, `20260617000000_pr6c2_retag_apply.sql`) **produced ZERO output for these two fields** — they were excluded from the harness schema, prompt, vocab, corpus input, and apply migration. There is **no reusable model output**; whatever method we pick is fresh work. ("The dry-run was shippable" is true only for the *other* 12 fields.)
- By volume, a deterministic find-and-replace built from the worksheet's alias map covers **~94% of `cooking_skills` appearances and ~92% of `main_ingredients` appearances**. But rules can only *re-label* existing tags — they cannot realize the worksheet's design: the **two-level "starred specifics" tier is largely unpopulated** in legacy data (must be *added* by reading), the old **`Herbs & Aromatics`** catch-all (48 lessons) conflates two new groups (Fresh herbs vs Alliums) and can't be split without reading, and the vague tags (`Basic Skills`, `Cooking Techniques`) must be *replaced by the real skill taught*.
- All three reviewer write-surfaces are **unguarded** for these two fields (free-form `CreatableSelect`, open `z.array(z.string())`, no DB CHECK) — so any clean-up re-pollutes immediately unless enforcement ships with it. `garden_skills` is the already-shipped closed-loop template.

This is therefore a **content re-tag + an enforcement lockdown**, done via the proven `scripts/stage2-retag/` harness, in its own multi-session track.

## 2. Goals & constraints

1. **Clean *and* complete, not just consistent.** Canonicalize both fields to the decided vocabulary AND realize the worksheet's intent — replace vague cooking tags with real skills, split conflated ingredient groups, add the 1–3 starred specifics, drop cosmetic/craft noise. Consistency alone (rules-only) is explicitly insufficient (it leaves the two-level tier empty).
2. **Don't regress the clean core.** The worksheet's alias map already gets ~92–94% right; the re-tag must not *lose* correct-but-clean tags to LLM drift. A deterministic floor protects them.
3. **Prove quality before paying for the full run.** A pilot on a hand-adjudicated gold subset must clear explicit, *separate* gates before the full-corpus run is greenlit.
4. **Lock the surfaces so it can't re-pollute.** Close both fields end-to-end (dropdown + Zod enum + DB CHECK), mirroring `garden_skills` — *after* the data is canonical, and shipped so reviewers never hit save failures.
5. **Data safety supersedes velocity** (`feedback_data_safety_top_priority`). 3-tier local→TEST→PROD-with-approval; snapshot-before-mutate; the apply is a reviewed, idempotent migration with `.rollback`; dual-write the typed column AND the `metadata` JSONB; touch the corpus once.

## 3·PIVOT. The re-locked method (Session 9, 2026-06-23) — AUTHORITATIVE

**Why this exists.** The P2 pilot (§6) ran Opus-4.8 + Opus-4.7 over the 69-lesson tight gold key and **both failed all 4 gates by over-tagging**. Root cause, measured: the harness as built is a *blind* full-LLM re-read — `run-retag.ts` `loadCorpus` (~L1115) strips the existing tags (`z.object({id, content_text})`), the LLM is sent the body only (~L1322/1590), the alias-floor rules R7/R8 (`normalize.ts` `applyAliasFloor` ~L267-290) fire **0×** on canonical LLM output, and `prepare-apply.ts` (~L540-582) writes the raw LLM output across **all ~14 metadata fields**. The "floor anchors the clean core" intent never reached the run/apply path. Direction re-validated by a Codex cross-exam (agent `ab877ed343f682131`, gpt-5.5, read-only).

**The decisive forensics** (counterfactual policies replicated over the failed run's output vs. the tight gold key, combined both fields):

| Policy | Precision | Recall |
|---|---:|---:|
| Raw LLM (the failing run) | 66.5% | 89.7% |
| Pure floor over existing tags, no LLM | 80.1% (≈87% canonical-only) | 78.6% |
| LLM ∩ floor (confirm-only) | 90.9% | 71.8% |
| Floor ∪ LLM (additive — what R9 resembles) | 62.7% | 96.6% |

- **84%** of false positives are **pure-LLM additions** absent from existing tags → anchoring suppresses these.
- **67%** of misses **were already in existing tags** → anchoring recovers these.
- **27%** of gold cooking tags / **17%** of gold ingredient tags are **genuinely body-derived** and absent from existing tags → pure-floor / strict-intersection structurally caps recall; the LLM is still required for the new-tag tail.
- The worst over-tags are **body-plausible, not hallucinated** → the LLM's *bar* is too low; the anchor + negative few-shots raise it while keeping body evidence. The additive policy (Floor∪LLM) is *worse than raw* → reconciliation must be subtractive-capable, **never append-only**.

**Conclusion: both halves are required.** Floor-first reconciliation fixes the inherited-anchor half (recovers the 44 regression-misses, suppresses the 244 pure-LLM FPs); the anchored prompt + negative few-shots + per-value gates fix the additive-judgment half (`Tasting` 11% precision, `Kitchen & food safety` 10%, `Sweeteners` 36%). Neither alone clears the gates.

### Locked decisions (Session 9)

- **D-P1 `[user-verdict]` — LLM add-leash = "gentle fixes, then measure."** Pilot-1 controls over-tagging via anchoring + per-label ADD criteria + negative few-shots + per-value gates, with **no** deterministic keep-only lock on the universal catch-alls (`Tasting`, `Kitchen & food safety`). Add the lock in a later iteration **only if** the re-pilot still shows them over-predicted. *Rationale:* 84% of FPs are pure-LLM adds, which the anchor directly attacks; fix the root cause first, measure before adding machinery.
- **D-P2 `[user-verdict]` — Held-out = fresh ~25, the anti-overfit CANARY, not the sole gate.** 3-layer test strategy: (a) the **69-key = per-value workhorse** we tune against; (b) a **fresh ~25** disjoint, independently-labeled slice = the honest final check (prompt frozen + IDs committed *before* labeling; two body-only derivations that see neither model output nor anchors; user adjudicates disagreements); (c) a **corpus-wide prevalence review** at full-run (per-tag firing rate across all ~700) = the scale gate. *Rationale (Codex Q4):* 25 rows cannot measure 46-value precision (the 69-key already has 22/46 specifics under 3 support); the 25 guards prompt-overfit, the 69 carries per-value, the prevalence review catches "Tasting on 78%."
- **D-P3 `[evidence + Codex]` — One canonical floor.** A single deterministic function: fold aliases → **execute the `drops` list** (currently inert per `normalize.ts:134-139`) → drop unmapped junk (`Basic Skills`, `Cooking Techniques`) → **remove the `Herbs & Aromatics` literal** (it becomes a *split* decision, not a fold). Used identically by the reconciler, scorer, gold-key builder, and baseline. *Rationale (Codex Q7 #1/#2):* today `drops` is ignored and the scoring-floor (`predictMembership`) vs gold-key-floor (`build-c02-answer-key.ts`) disagree on unmapped values — building against the wrong one invalidates the eval loop.
- **D-P4 `[evidence + Codex]` — Anchored verify-and-diff.** A **dedicated 2-field tool** (not the all-14-field schema). The user turn carries the body **plus** the current tags annotated with provenance; the task is **KEEP / DROP / ADD** against the body, not re-tag-from-scratch. The prompt carries explicit per-label ADD criteria (e.g. `Tasting` = sensory comparison/vocabulary/assessment, *not* eating the dish; `Kitchen & food safety` = taught/practiced/assessed or a dedicated agenda segment, *not* incidental knife/wash/heat) + negative few-shots (garnish-isn't-an-ingredient, pantry-staple precision). No hard lock (per D-P1).
- **D-P5 `[Codex]` — Confidence-typed anchors.** Each anchor tag is annotated with provenance (`exact-canonical` / `alias-fold` / `parent-derived` / `ambiguous`); deterministic drops and unmapped values **never enter the anchor**. *Rationale (Codex Q2):* measured anchor precision ranges **78%–97%** by provenance (parent-derived 97%, ingredient-already-canonical only 78%); a flat anchor makes the LLM keep the junky ones.
- **D-P6 `[evidence]` — Reconcile in code (new `reconcile.ts`).** Floor-anchored core + the LLM's keep/drop/add, enforcing the specific→group invariant and **rejecting parent/child conflicts** (drop-parent-keep-child is contradictory). Persist **both** the raw decisions and the final arrays (`finalC02`); scoring and apply read `finalC02` only. **Never append-only** (the existing R9 direction; the additive policy scored worse than raw).
- **D-P7 `[Codex]` — Recompute the baseline against the cleaned floor.** Cleaning the floor (D-P3) raises the rules-baseline F1 (≈0.77→0.81 cooking / ≈0.72→0.78 ingredients per Codex's recompute), which raises Gate 2's +0.05 bar. Lock Gate 1/2 to the **cleaned** baseline; do not greenlight against the artificially-weak old one.
- **D-P8 `[Codex]` — Per-value gates AUGMENT the pooled check.** Pooled added-specific precision ≥ **0.70**; per-specific precision ≥ **0.60** only where `truthCount ≥ 3` AND `predictionCount ≥ 3`; absent-value rate ≤ **0.05**; named sentinels `Tasting`/`Kitchen & food safety` ≥ **0.70**, `Sweeteners` ≥ **0.80**. Below-support values stay informational but still contribute their FPs to the pooled gate. Implement a support-aware wrapper (not raw `evalMetrics.minPrecisionPerValue`). All thresholds re-tunable at the pilot.
- **D-P9 `[Codex]` — Run identity hashes the full effective input** (body + raw current tags + floored anchor + manifest version + reconciliation policy), not body-only. Anchored runs start cold; resume/repair against the failed pilot is invalidated (expected).
- **D-P10 `[Codex, P3 apply]` — Apply safety.** (a) **C02-only write scope** — an explicit `C02_APPLY_FIELDS = [cooking_skills, main_ingredients]` const drives both the column writes and the JSONB `jsonb_set` chain; the other 12 fields, `grade_levels`, and `academicConcepts` are **not** written. (b) **Optimistic concurrency guard** — each UPDATE matches the source C02 arrays/hash captured at export; skip + report any row a reviewer changed since export. (c) **C02-targeted rollback** — the snapshot stays full-row for forensics, but the rollback UPDATE touches only the two C02 JSONB keys + columns. (d) **Dual-write lockstep** + a post-update column↔JSONB equality assertion. *Rationale (Codex Q6):* the current all-fields apply would stomp the completed metadata rebuild and a full-metadata rollback could clobber unrelated post-apply edits.
- **D-P11 `[user-verdict, Session 14, 2026-06-25]` — Per-field ship policy (RESOLVES the OPEN decision "(a)").** After the P2′.6 ceiling, the full run ships a **different source per field** — the LLM does NOT ship for both:
  - **`main_ingredients` → deterministic floor ONLY** (no LLM judgment). Coherent with the Session-13 accept-ceiling: specifics ship at cleaned-existing-tag quality. Measured (69-key clean-core F1): floor **0.918** vs LLM+keep-only **0.831**; the LLM's only ingredient edge is +0.012 (CI includes 0) and it leaks group-level FPs the per-value gate cannot catch (Codex agent `ae4414a5…`).
  - **`cooking_skills` → LLM with a FLOOR-RETENTION policy ("option 3").** The reconciled LLM output is **unioned with the floor**: the LLM may KEEP/ADD but **cannot DROP a floored skill**. Measured honest all-69 (Session-14 analysis): floor-only F1 **0.807** / LLM-as-is **0.836** / floor-retention **0.872** (**+0.065** over floor, P 0.833 / R 0.915). It recovers ~50 real body-derived skills the floor structurally cannot add (replacing vague `Basic Skills`/`Cooking Techniques` junk with `Dough making`/`Roasting`/`Creating sauces & dressings`) AND restores the ~28 high-confidence skills the LLM otherwise wrongly drops. **Zero clean-core DATA LOSS** (floor fully retained) → satisfies the §6 re-framed greenlight's "no harmful clean-core regression"; precision cost (~0.83: keeps ~14 floor FPs the LLM would prune + ~28 noise adds) is accepted as "much cleaner than today."
  - **Floor-retention is a NEW reconcile/materialization policy**, NOT the existing add-suppressing keep-only lock (which blocks ADDs while *allowing* DROPs — the opposite direction; Codex-confirmed it cannot be expressed by extending `C02_KEEP_ONLY_LOCK`). It is append-only **for `cooking_skills` only**, formally re-opening D-P6's "never append-only" — sanctioned by user-verdict because D-P6's additive-is-worse evidence was measured on the *combined fields* (62.7% precision, INGREDIENT-dominated), whereas cooking-only floor-retention measures **0.872 F1 / 0.833 P**.
  - **P3 MUST field-isolate run-time validation** so an off-vocab value in one field can never discard the OTHER field's valid output. The pilot's 4 zero-`finalC02` records were valid-cooking lessons killed by an off-vocab **ingredient** (whole-record validation coupling, Codex CLAIM 2 + supervisor-verified from the raw r4 decisions); without the decoupling this recurs at corpus scale.
  - *Provenance:* validated by `scripts/stage2-retag/c02-cooking-floor-vs-llm.ts` (all-69, no cross-round backfill) + a Codex adversarial cross-exam (agent `a1be3c2d…`) that REFUTED an earlier flawed r2-backfill reconciliation; the corrected numbers above stand. Still gated downstream by the **§6/§8 re-framed greenlight** (the §8 "Greenlight criterion" subsection: prevalence review + cooking-scoped fresh-25 canary + user spot-check) — D-P11 sets the *method*, not the greenlight.

- **D-P12 `[user-verdict, Session 15, 2026-06-25]` — Fresh-25 canary = cooking-scoped.** Under D-P11's per-field ship, the D-P2 anti-overfit canary is **labeled + scored on `cooking_skills` only.** Ingredients ship the deterministic floor, so the 25 cannot surface an ingredient over-tag the P3.1 all-700 prevalence review wouldn't also catch (redundant); cooking ships via the LLM and the prevalence report is unlabeled firing-rate, so the 25 is the **only fresh, independently-labeled cooking-precision check** before the full run. P2′.7 freezes the prompt/reconciler/ship-policy/thresholds, then labels cooking on the 25 (two body-only derivations + user adjudication — the Q6 protocol) and scores cooking aggregate precision/recall on the **shipped** (floor-retention) cooking output. *Rationale (Session-15 Codex cross-family + Claude audit, finding C3):* keep the unique anti-overfit signal where it has teeth (cooking); drop the redundant ingredient labeling.

### What carries forward unchanged
Vocab (Q1, 93 canonical values), casing (Q8 Title-Case `value===label`), expand/contract deploy choreography (Q9), Opus-4.8-only pilot (Q11), the harness skeleton, the 201-key alias map (gets the D-P3 hygiene top-up), the **69-lesson gold key**, the 764-row PROD corpus.

### Superseded by this section
§3 (the "hybrid-floor full re-read" framing as built), §4-Q3 (floor as R7/R8 in `normalize.ts` is the *mechanism* — now a unified floor + `reconcile.ts`), §4-Q5 (gate plumbing — now per-value + baseline recompute), §4-Q6 (gold-key protocol — now the 3-layer strategy), and the §7/§8 apply scope (now C02-only + D-P10).

---

## 3. The chosen shape: **hybrid-floor full re-read** — ⚠️ SUPERSEDED by §3·PIVOT (kept for history)

The LLM reads **every** lesson and proposes canonical `cooking_skills` + `main_ingredients` (groups + conditional starred specifics) against the locked vocabulary. A **deterministic alias-map floor** then anchors the clean core: where the worksheet's fold-in map gives an unambiguous canonical value, that value is authoritative and the LLM cannot regress it; the LLM's contribution is *kept* for the judgment work — vague-tag replacement, the `Herbs & Aromatics` split, **adding** starred specifics, and dropping noise. A full read is unavoidable because the two-level specifics can only be *added* by reading each lesson; the floor is a guardrail on that read, not an alternative to it.

`main_ingredients` carries the heavier judgment load; `cooking_skills` is mostly floored (94% deterministic, vocabulary effectively locked).

> **Architecture nuance (lock the precise split in Session 1 / at the pilot gate):** "how much the LLM is trusted vs. the floor overrides" is tuned by the pilot's AI-vs-rules scores. The *default* is floor-first (Codex's "reverse the burden of proof": deterministic on the clean core, LLM on the residual). The pilot may widen LLM coverage only if it demonstrably beats rules without regressing the core.

### Why this shape over the alternatives

| Option | Why rejected |
|---|---|
| **Rules-only (Method 1, deterministic find-and-replace)** | Covers ~92–94% of appearances and is cheap/reversible, but *cannot* add the starred specifics (the whole point of the two-level design), split `Herbs & Aromatics`, replace vague tags with real skills, or drop cosmetic noise — it only re-labels what exists. Leaves the corpus "consistent but not complete," and a later content pass would touch the corpus twice (violates Goal 1 + Constraint 5). |
| **Full-LLM with no deterministic floor** | A blind LLM re-read can *regress* the ~94% clean core that the worksheet alias map already gets right (drop/alter a correct-but-clean tag, hallucinate). Wastes the alias-map work the user already did, and trusts the model on exactly the easy cases where rules are strictly safer. |
| **Per-field method (rules for cooking_skills, LLM for ingredients)** | Reasonable, but the full read needed for ingredients' specifics is the cost driver; `cooking_skills` rides that same read nearly free. Floor-first within one full read subsumes this with less special-casing — and the floor still makes `cooking_skills` ~94% deterministic in practice. |
| **Reuse the PR-6 fable run** | Impossible — the run produced zero output for these two fields (verified). |

## 4. Mechanism questions — RESOLVED & LOCKED (Session 1, 2026-06-23)

> **All eleven resolved 2026-06-23** by a 6-agent read-only discovery fan-out (every anchor re-verified against current code) + user verdicts on the four highest-value choices (pantry disposition, pilot size, invariant mechanism, freeze-candidate specifics). Each question below carries its **LOCKED** answer + rationale inline. `[evidence-lockable]` = locked from discovery evidence; `[user-verdict]` = decided by the user 2026-06-23. The **provisional** canonical manifest (census + value set + parent map) lives at `docs/plans/c02-session1-discovery/q1-vocab-census.md`.

1. **Final canonical VALUES manifest + parent map + open-specifics freeze** `[user-verdict]` (**BLOCKING — GATE A D1/D6/Codex-7**). Materialize ONE committed artifact that the harness enum (Q2), the Zod enums (§7.2), and the DB CHECKs (§7.3) all derive from byte-identically. It must contain: (a) the **exact closed value set** = groups ∪ specifics ∪ any pantry-staple literals; (b) the **specific→group parent map** — `null` parent for the confirmed group-less specifics (worksheet: Celery, Fennel; C02 additions: Seaweed (nori), Cocoa & chocolate — Melons is parented under *Squash, cucumbers & melons*), a concrete parent for every other specific; (c) a decision to **FREEZE** the worksheet's "open to adding further specifics as the re-tag reveals true counts" door (a closed CHECK cannot ship against an open-ended list — re-tag-discovered new foods get added to the manifest *before* the freeze, then it's closed); (d) the **pantry-staple disposition** (Salt / Oil / Soy sauce / Sugar: canonical value, map-to-group e.g. Sugar→Sweeteners, or never-stored — wired to gate ④). This is the prerequisite to half of §4 and to all of §7.

   **LOCKED 2026-06-23 [user-verdict]:** Provisional manifest = `docs/plans/c02-session1-discovery/q1-vocab-census.md` (built from the live 2026-06-23 census). **cooking_skills = 23** flat Title-Case values. **main_ingredients = 24 groups + 46 specifics = 70 provisional values** — the 42 worksheet+§5 specifics (37 worksheet = the 34-row table + 3 always-available Celery/Fennel/Melons; + 5 §5-new) **plus 4 user-pre-added high-count specifics** (Apples→*Apples & pears*, Coconut→*Tropical fruits*, Oranges→*Citrus fruits*, Lime→*Citrus fruits*). Parent map per artifact §2c + those 4; **group-less (`null` parent, 4):** Celery, Fennel, Seaweed (nori), Cocoa & chocolate (Melons is parented under *Squash, cucumbers & melons* per the worksheet). **[Corrected 2026-06-23 Session 2, user-confirmed: count 43→46 / 67→70 — the original lock dropped the 3 always-available worksheet extras; and Melons `null`→*Squash, cucumbers & melons* — the original mis-parented it vs. the worksheet.]** **Pantry = B-lite:** Sugar→*Sweeteners* group; Salt/Oil/Soy sauce dropped (never-stored). **Freeze:** ship the provisional set into the harness now; **freeze the manifest at the end of P3** (post-apply, pre-enforcement); P4 Zod + DB CHECK derive byte-identically from the frozen set. Casing = Title-Case `value===label` (Q8). *Rationale:* a closed CHECK can't ship against the worksheet's open list and enforcement is the last phase anyway, so freeze-after-discovery costs nothing; B-lite keeps the set lean while honoring the one staple (Sugar) that maps to a real group.
2. **Two-level output shape in the harness** `[evidence-lockable]`. How to represent "group tags + conditional 1–3 specifics in one `main_ingredients` value set" in `scripts/stage2-retag/schema.ts` (output/tool schema), the emitter prompt, `vocab.ts` (`MAIN_PASS_FIELDS`), and `validate-output.ts`. NOTE (GATE A/Codex-1): the harness already emits one **structured** field — `academic_concepts` is a subject-keyed object (`schema.ts:411-413`), so structured output is not new ground; the real choice is **flat `string[]` over {groups ∪ specifics} + a parent-map validation refinement** (default) vs. a second structured shape. *Discovery:* `schema.ts:174`, `vocab.ts:36`, `validate-output.ts`, `run-retag.ts`.

   **LOCKED [evidence-lockable]:** **Flat `string[]` over {groups ∪ specifics}** (Option A), NOT a second structured shape. cooking_skills is a plain flat enum array like `garden_skills`; main_ingredients is a flat enum over the closed union + a Zod `.superRefine` (driven by the Q1 parent map) that rejects an orphan specific. Mechanism: add both fields to `MAIN_PASS_FIELDS` (`vocab.ts:36-49`) + two `loadVocab` `FieldVocab` entries → auto-propagates through tool schema / Zod / repair / validate / score; the parent-map refinement attaches only to main_ingredients in `buildResultSchema`. "1–3 specifics" stays prompt-guidance only (no schema cap — consistent with §10). *Rationale:* reuses the entire generated `enumArray`/`uniqueEnumArray` pipeline and keeps main_ingredients' `text[]` column + flat DB CHECK intact; the `academic_concepts` structured precedent is **heavier than the design implied** (its own repair branch, JSONB-only, no `text[]` column) — flat is the genuinely low-cost path.
3. **Deterministic floor mechanism** `[evidence-lockable]`. Where the alias-map floor lives and how it reconciles with LLM output — a new reconcile module vs. extending `normalize.ts` (which already runs deterministic cross-field rules R1/R4/R5/R6) vs. applied in `prepare-apply.ts`. Encodes the worksheet's fold-in map (cooking_skills + main_ingredients aliases) as data. *Discovery:* `normalize.ts`, `prepare-apply.ts` (`ROLLBACK_TABLE`:89, snapshot DDL/INSERT 490-529, `applyUpdate` dual-write 531-582/552-575).

   ⚠️ **[SUPERSEDED by §3·PIVOT — D-P3 one canonical floor + D-P6 `reconcile.ts`; R7/R8 proved inert on canonical LLM output.]** **LOCKED [evidence-lockable]:** New **data-driven alias-floor R-rule(s) in `normalize.ts`** (Option b) — e.g. R7 `cookingSkillsAliasFloor` / R8 `mainIngredientsAliasFloor`, alias map loaded from a committed data file (mirroring `vocab.ts`'s `data/vocab/*.json` loading), registered as ordered calls inside `normalizeRecordInput`. Floor-first: where the alias map gives an unambiguous canonical value it **overwrites** the LLM value; the LLM is kept only for judgment work (vague-tag replacement, Herbs/Alliums split, *adding* specifics, dropping noise). **Idempotency invariant — no canonical value is itself an alias key** (stated task requirement + unit test). *Rationale:* `normalize.ts` is the deterministic chokepoint `run-retag` routes every output through **pre-Zod** (and `generate-diff-report` re-runs), so the floor inherits diff-consistency, the idempotency contract, and automatic edge-case spot-checking (`prepare-apply` `isEdgeCase` keys off `normalizationCount`) with the least new wiring. *Anchor correction:* `applyUpdate` is **L540-582** (531 is the doc-comment); the typed-column write is L552-554 and the metadata `jsonb_set` chain L562-575 (two separate SET clauses).
4. **Pilot sampling strata + size** — strata `[evidence-lockable]`, **final size `[user-verdict]`** (cost/coverage tradeoff). Extend `sample-answer-key.ts` (today strata = `activity_type × body-length quartile`, ~L243) to **over-sample hard cases** (vague cooking tags, `Herbs & Aromatics`, the orphan foods) **+ cover every group and every canonical specific ≥2×** + a **clean-core slice** (regression detection). Codex flagged ~40–60 too small for 24 groups + ~39 specifics → present ~60–80 as the recommendation; user picks. *Discovery:* `sample-answer-key.ts:4,243`.

   **LOCKED:** **Strata [evidence-lockable]** = three-layer hybrid — (1) hard-case quotas keyed off current-tag values (vague cooking tags, the `Herbs & Aromatics` 48 catch-all, orphan foods), (2) a deterministic seeded **set-cover** guaranteeing every group + every specific appears ≥2×, (3) a clean-core regression slice retaining `activity_type × body-length-quartile` balance. **Size [user-verdict 2026-06-23] = 70** (≈25 clean-core / 25 coverage / 20 hard-case). *Subtlety:* ≥2× coverage for **added** specifics (near-zero current carriers) is defined against **expected post-retag membership** (seed from the floor's alias predictions / a quick pre-scan), not current tags. *Anchor:* `stratumKey` L244; only a set-cover layer guarantees the ≥2× a blind proportional draw cannot.
5. **4-gate scoring — metrics + thresholds** — plumbing `[evidence-lockable]`, **thresholds + gate-② definition `[user-verdict]`** (GATE A D3/D4). Extend `score-answer-key.ts` (today: per-field micro-F1 + per-value recall; singletons informational/non-gating at `SUPPORT_FLOOR=2`, L260 + L288-298). Gates ③ (false-positive rate on *added* specifics) and ④ (pantry-staple precision) need **new precision/false-positive metric families** — per-value *recall* structurally cannot see false positives; budget two new metrics. Gate ② ("beats rules on judgment rows") must be **defined as a test**: judgment-row eligibility set, the metric, the minimum delta, tie behavior, per-field-independent or combined. Decide singleton gating for ingredient specifics. *Discovery:* `score-answer-key.ts:6,66,260,288-298`.

   ⚠️ **[SUPERSEDED by §3·PIVOT — D-P7 recompute the baseline against the cleaned floor + D-P8 per-value gates augment the pooled check.]** **LOCKED:** **Plumbing [evidence-lockable]** — restructure `GateResults` into 4 gates that read the **already-computed** `scripts/lib/evalMetrics.ts` per-value `precision` / `fp` / `predictionCount` + `evaluateThresholds`'s existing `minPrecisionPerValue` & `maxPredictionRateForAbsentValues` (**CORRECTION to the design's framing: the false-positive primitives already exist — `evalMetrics.ts` L16-26 / 164 / 172 — gates ③④ are new *wiring*, not new metric math; supervisor-verified 2026-06-23**), plus a rules-baseline contestant (the P1 floor) and a per-lesson clean-core/judgment-row label. **Thresholds + gate-② definition [user-verdict 2026-06-23, starting values, re-tunable at the pilot]:** ① winner ≥ floor micro-F1 (strict, per field) on the clean-core slice; ② winner ≥ rules **+0.05** micro-F1, scored **per-field-independent, both must pass**, a tie (δ=0) **fails**, judgment-row eligibility = vague-tag ∪ `Herbs & Aromatics` ∪ added-specific-in-gold ∪ orphan-remap; ③ added-specifics aggregate precision ≥ **0.7** + any never-in-key specific predicted in ≤ **5%** of rows; ④ Sweeteners/Sugar precision ≥ **0.8** + a "no Salt/Oil/Soy-sauce literal survives" check (per Q1 B-lite). **Singleton gating:** keep the support≥2 non-gating rule for recall checks, but FP gates **count** a singleton over-prediction. *Anchor:* `SUPPORT_FLOOR=2` L260.
6. **Gold-key adjudication protocol** `[user-verdict]` (NEW — GATE A D5). The rules baseline is **blind on exactly the judgment rows** (it can't add specifics, split `Herbs & Aromatics`, or replace vague tags), so "AI-drafts + rules-baseline check" carries the clean core but NOT the hard cases. Define an **independent hard-case adjudication pass** (Herbs/Alliums split, added specifics, vague-tag replacement, pantry staples) so the gold key isn't AI-marking-its-own-homework on the rows that matter.

   ⚠️ **[SUPERSEDED by §3·PIVOT — D-P2 3-layer test strategy: 69-key workhorse / fresh-25 canary / corpus-wide prevalence review.]** **LOCKED [user-verdict 2026-06-23]:** Gold key = **AI drafts all rows → user adjudicates**; for the **judgment rows** specifically (vague-tag replacement, Herbs/Alliums split, added specifics, pantry), an **independent second pass** — the user, or a **different AI lens than the drafting/bake-off model** — re-derives the answer from the lesson body before it's accepted. *Rationale:* the rules baseline is blind on exactly these rows, so without an independent derivation the key would be the model marking its own homework on the rows gate ② scores; this gate and Q6 are **coupled** (gate ② is sound only if its judgment-row answers come from this pass).
7. **Specific→group invariant — enforcement MECHANISM only** `[user-verdict]` (policy is LOCKED in §7: enforce specific→group; "1–3" is guidance, not a cap — GATE A D4-split). Open: app-layer Zod `superRefine` only vs. also a DB trigger vs. a harness `normalize.ts`-style deterministic rule. Recommendation to present: harness-emit-correct + Zod `superRefine` (+ optionally a normalize R-rule), **no DB trigger** (overkill for an internal-reviewer field) — surface the tradeoff. The parent map from Q1 is the source of truth.

   **LOCKED [user-verdict 2026-06-23] = Option B:** a harness `normalize.ts` R-rule that **auto-ADDS** the missing parent group (so the apply can never emit an orphan) **+ a Zod `superRefine` on all four schemas** (`lessonMetadata.zod` 215/217, `reviewFormPayload.zod` 67/68, edge `metadataSchemas` 170/172 + 209/210) that **rejects** an orphan on reviewer save. **No DB trigger.** One `INGREDIENT_PARENT_MAP` const (from the Q1 manifest) feeds both; add orphan-specific negative fixtures to both invalid blocks in `edgeSharedSchemas.equivalence.test.ts`. *Rationale:* matches the trigger-free `garden_skills` precedent and §10's lean; each write-path gets the right-shaped guard — append for the auto-pipeline, reject for the reviewer.
8. **Vocab value casing** `[evidence-lockable]`. Stored form: **Title-Case `value===label`** (mirrors shipped `garden_skills` L231-268 / `observances_holidays`) vs. kebab (like `cooking_methods`). Default: Title-Case `value===label` (closest analog; round-trips through the closed enum). Whatever is chosen, the manifest (Q1), the Zod `VALUES`, the DB CHECK arrays, and the dropdown options are **byte-identical**. *Discovery:* `filterDefinitions.ts` (gardenSkills L231-268, cookingSkills L271, mainIngredients L174); `lessonMetadata.zod.ts`.

   **LOCKED [evidence-lockable]:** **Title-Case `value===label`**, mirroring shipped `garden_skills` (`filterDefinitions.ts` L240-267 / `GARDEN_SKILLS_VALUES`). The stored value is the exact worksheet label string incl. diacritics/parens/ampersands (`Sautéing & stir-frying`, `Fresh herbs`, `Seaweed (nori)`, `Cocoa & chocolate`). Carry the gardenSkills `value===label` justification comment onto the new configs; update the existing equivalence-test fixtures to real canonical values. *Rationale:* these are **not search facets**, so `cooking_methods`' kebab-for-badge-bucketing rationale doesn't apply; Title-Case round-trips the worksheet labels with zero lossy slugging, and byte-identity across manifest→Zod VALUES→edge VALUES→DB CHECK→dropdown is trivial when value===label. (`cooking_methods` stays kebab — different field.)
9. **PROD deploy choreography (expand/contract)** `[user-verdict]` (NEW — GATE A D2, **blocking for P3/P4**). The frontend auto-deploys on merge but migrations need manual PROD approval (`reference_ci_flakes`), so deploy order ≠ merge order. Lock the explicit sequence: **P3 PROD data canonical (approved migration, no CHECK) → P4 closed frontend/Zod deploys → P4 CHECK migration (separate approval, after a drift re-census)**. Do NOT bundle the closed frontend + the CHECK migration as one PR. Decide whether the brief "data clean but UI still free-form" window is accepted (low edit volume) or mitigated.

   **LOCKED [user-verdict 2026-06-23] = Option A (the design's 3-step, verbatim):** P3 PROD data canonical (approved migration, **no CHECK**) → P4a closed dropdown + Zod enums + all 4 edge-mirror lines + `superRefine` merge → **frontend auto-deploys** → P4b the two `valid_*` CHECKs as a **separate** migration PR (separate PROD approval) **after a fresh TEST+PROD drift re-census** that re-canonicalizes any window drift **in the same P4b migration** before `ADD CONSTRAINT`. The brief "data clean / UI free-form" window is **accepted** — re-pollution-only (never a save failure), bounded by low internal edit volume, self-healed by the P4b re-census. **Never bundle P4a + P4b.** *Rationale:* the three deploy events are independently timed (2 manual approvals + 1 auto-deploy), so only data-canonical → closed-UI-deployed → CHECK never lets a table-wide CHECK coexist with a free-form save surface.
10. **PR / branch breakdown + artifact placement** `[user-verdict]` (policy portion). Confirm the phase→PR mapping (§8): harness extension + floor + pilot tooling = **scripts-only, no DB** (own PR, mergeable independently); full-run **apply** = migration PR (snapshot + dual-write + `.sql.rollback`); enforcement split per Q9. Decide whether pilot artifacts/run live on the harness PR or a throwaway branch.

   **LOCKED [user-verdict 2026-06-23]:** Confirm the §8 phase→PR mapping. **P1** harness + floor + pilot-tooling = scripts-only, own branch `feat/c02-harness`, mergeable independently. **P2** pilot artifacts live in the harness `artifacts/` (bulk gitignored, a committed scorecard summary) — **not** a throwaway branch. **P3** apply = migration PR `feat/c02-apply` (snapshot + dual-write + `.sql.rollback`). **P4a** frontend `feat/c02-enforce`; **P4b** CHECK `feat/c02-check` — separate PRs, P4a deploys before P4b (Q9).
11. **Model bake-off + cost estimate** `[evidence-lockable]`. fable-5 (PR-6 winner) is **suspended** (export-control freeze) — re-run a small **Opus 4.8 vs Sonnet 4.6** bake-off on the pilot and project full-run cost for the winner (the ~$121 figure was fable). Note the repo's prior **Opus-4.8 fabrication notes** (`schema.ts:40`, `run-retag.ts:175`) — Opus is not assumed safe; the bake-off decides on fresh evidence.

   **⚠️ SUPERSEDED 2026-06-23 (user decision, immediately post-P1-merge) → P2 USES OPUS ONLY, NO BAKE-OFF.** Run **Opus 4.8** (`--model claude-opus-4-8`) over the 70-lesson key **once**; no Sonnet contestant, no comparison, no model tie-break. Opus must still **earn** the greenlight on the **same 4 gates** (the `schema.ts:42-44` fabrication caveat is exactly why the gates remain the quality safety net); **`claude-opus-4-7`** stays the fallback if Opus 4.8 doesn't clear. Net: one model run instead of two (cost/complexity drop; accepted trade-off = no Sonnet data point). Recorded in memory `project_c02_p2_opus_only`. Cost projection + `preflight-token-mass` pre-check are unchanged (just for the single run). *The original Q11 bake-off lock below is retained for history — it documents WHY the 4 gates exist, which still holds.*

   **LOCKED [evidence-lockable] (HISTORICAL — superseded above 2026-06-23):** **Opus 4.8 vs Sonnet 4.6 bake-off** via the existing `--model` flag over the 70-lesson key, both scored on all 4 gates; **if both clear → take Sonnet 4.6** (cheaper $3/$15 vs $5/$25, lower cache floor). **Opus 4.8 must *earn* selection** given the on-disk fabrication caveat (**correct anchor: `schema.ts:42-44`**, not `run-retag.ts:175` which is the pricing/eval-drop note); **`claude-opus-4-7`** (current `DEFAULT_MODEL`, the surviving forced-tool contestant) is a third baseline if neither contestant clears. Cost = pilot per-lesson `totalCostUsd` × ~700, separating `cache_creation` (first call) from `cache_read` (0.1×); run `preflight-token-mass` first to confirm the grown tool-schema prefix stays under `TOKEN_MASS_BUDGET` (12K) and above the Opus 4096 floor.

## 5. Vocabulary — the canonical target (LOCKED)

The decided worksheet lists are the canonical target, **plus the C02-discussion amendments** (user-decided 2026-06-22). Sign-off is **solo** (the user; no curriculum-team round — consistent with how the worksheet itself was decided).

**Cooking skills:** the worksheet's **23**, with one change — **"Frying" / "Stovetop frying" fold into `Sautéing & stir-frying`** via the alias map (no 24th skill; the re-read assigns the real technique). Vocabulary is otherwise effectively locked (bucket-c was 13 trivial singletons, ~0.9% of appearances).

**Main ingredients:** the worksheet's 24 groups + 42 specifics (37 worksheet [34-row table + 3 always-available Celery/Fennel/Melons] + 5 §5-new), **+ 4 user-pre-added high-count specifics (Apples, Coconut, Oranges, Lime — §4 Q1) = 46 specifics / 70 provisional values**, two-level, **plus three amendment groups (five new specific values) and one remap**:
- **`Seaweed (nori)`** — new group-less specific; merges `Nori` / `Nori (seaweed)` / `Seaweed` (stars in all 7 sushi/kimbap/seaweed lessons; same "distinct category" logic the worksheet used for Mushrooms).
- **`Cocoa & chocolate`** — new group-less specific; merges `Chocolate` / `Cocoa` / `Cocoa powder`.
- **`Sunflower butter`, `Tahini`, `Peanut butter`** — new specifics under the **existing `Nuts & seeds` group** (so they inherit a real parent; allergy-substitute relevance + tahini stars).
- **`Hummus` → re-map to the existing `Chickpeas` specific** (a chickpea dish, not a new term).
- Everything else in the orphan tail is **drop-or-remap, handled by the re-read** (cosmetic/craft noise — Shea butter, Lavender, Rose petals, Peppermint oil — *dropped*; composites — Ice cream, Marshmallows, Jelly, Jam, Granola, Applesauce, Agar agar — dropped or folded; Kimchi→Cabbage/Cruciferous, Beyond Sausage→Tofu & plant proteins, Vanilla→Spices).

The pantry-staple rule (worksheet decision-table #1) holds: salt/oil/soy sauce/sugar tagged only when the lesson is *about* them — disposition **LOCKED 2026-06-23 = B-lite** (§4 Q1): **Sugar → existing `Sweeteners` group; Salt / Oil / Soy sauce dropped (never-stored).**

**The closed value set is LOCKED as PROVISIONAL** (§4 Q1, 2026-06-23): the worksheet left the ingredient vocab *"open to adding further specifics as the re-tag reveals true counts"* — which a byte-identical closed DB CHECK cannot ship against. Resolution: ship the **provisional** manifest (23 cooking_skills + 70 main_ingredients: 24 groups + 46 specifics) into the harness now, surface any genuinely-missing high-frequency food during the P2/P3 run, and **freeze the manifest at the end of P3** (post-apply, pre-enforcement); P4 Zod + DB CHECK derive byte-identically from the frozen set. Known group-less specifics (`null` parent, 4): Celery, Fennel (worksheet) + Seaweed (nori), Cocoa & chocolate (C02 additions); Melons is parented under *Squash, cucumbers & melons*; every other specific maps to its group. Full provisional manifest + parent map: `docs/plans/c02-session1-discovery/q1-vocab-census.md`.

## 6. The pilot (the quality gate)

- **Gold answer key — AI drafts, user adjudicates.** The AI proposes tags for the pilot lessons; the user reviews and corrects each (faster than from-scratch). `[user-verdict resolved 2026-06-22]`. **Circularity caveat (GATE A D5):** the rules baseline is *blind on exactly the judgment rows* (it can't add specifics, split Herbs & Aromatics, or replace vague tags), so on the hardest rows the genuine-user-correction carries the weight — §4 Q6 defines an independent hard-case adjudication protocol for those rows. (`normalize.ts`'s cross-field rules R1/R4/R5/R6 are a precedent for deterministically catching specific→group violations.)
- **Subset** (§4 Q4): stratified to over-sample hard cases + a set-cover sweep guaranteeing every group/specific ≥2× + a clean-core slice; **size = 70** (locked 2026-06-23).
- **Model** (§4 Q11, **superseded 2026-06-23**): a single **Opus 4.8** run (no Sonnet bake-off — user decision, `project_c02_p2_opus_only`); Opus must still clear all 4 gates or fall back to `claude-opus-4-7`.
- **Four separate gates** (§4 Q4), NOT a single macro score: ① no regression on the clean core · ② beats the rules baseline on judgment rows · ③ low false-positive rate on *added* specifics · ④ pantry-staple precision. The full-corpus run is greenlit only when all four pass.

  ⚠️ **AMENDMENT (Session 13, 2026-06-24 — `[user-verdict]`, supersedes the "greenlit only when all four pass" clause).** The P2′.6 re-pilot proved the 4 gates are **structurally unreachable** on ingredient *specifics*: round 4 (specifics keep-only, `46b3ddf`) lifted pooled added-specific precision 0.621→0.773 but all four gates still failed, because the residual over-tags are **incidental specifics already present in the legacy corpus tags** (the model rubber-stamps them, and the deterministic floor carries them forward) — no mechanical re-tagger prunes them without the subtle per-lesson centrality judgment the case review (Session 12) showed is hard. With LLM adds locked, the LLM no longer beats a pure deterministic floor on ingredients (0.831 < 0.918 clean-core). The user therefore **re-framed the greenlight** from "all 4 gates pass" → **"the re-tag is much cleaner than the current corpus + no harmful clean-core regression on the SHIPPED method + cooking-skills sentinels hold (Tasting/KFS = 1.0) + a corpus-wide prevalence review + a user spot-check."** Specific foods ship at **cleaned-existing-tag quality (~0.77 vs the tight key), not curated-to-centrality** — the two-level "AI adds 1–3 central specifics" goal (§3 Goal 1 / D-P1) is dropped for the LLM. cooking_skills + ingredient GROUPS are unaffected (they clear their bars). The 4 gates remain the *diagnostic* instrument; they are no longer the *pass/fail ship criterion*. The concrete re-framed criterion is now **§8 "Greenlight criterion (re-framed Session 13–14)"** (finalized Session 15, a Codex cross-family + Claude audit folded — which corrected three first-draft overclaims: the retired-gate "+0.05" framing, "no regression *by construction*" → *no loss relative to the floor*, and sentinels-as-sufficiency → necessary-only). The floor-only-vs-LLM ingredient-method choice is **D-P11**; the fresh-25 canary scope is **D-P12** (cooking-only).

## 7. Enforcement — close the surfaces (LOCKED scope; mechanism in §4 Q7, casing in Q8)

Mirror the shipped `garden_skills` closed loop, for BOTH fields, **after** the data is canonical:
1. **Reviewer dropdown** — swap the two `CreatableSelect` controls (`ReviewDetail.tsx` mainIngredients ~L1000, cookingSkills ~L1027) to non-creatable `Select` (copy the gardenSkills justification comment ~L1058–1061); replace the old kebab option value-sets in `filterDefinitions.ts` (cookingSkills L271, mainIngredients L174) with the canonical vocab (casing per §4 Q6).
2. **Zod** — add `CookingSkillsEnum` + `MainIngredientsEnum` (VALUES + `z.enum`), replacing the open `z.array(z.string())` at `lessonMetadata.zod.ts:215,217` and `reviewFormPayload.zod.ts:67,68`. **The edge mirror `_shared/metadataSchemas.ts` has open arrays in BOTH schemas** (GATE A/Codex-2): the lesson-metadata schema at `:170,172` AND the review-payload schema at `:209,210` — close **all four**, or off-vocab survives the metadata path. The `edgeSharedSchemas.equivalence` test enforces parity (move them together; add off-vocab negative fixtures for both). Plus the specific→group `superRefine` (§4 Q7), whose deterministic machinery can mirror `normalize.ts`'s cross-field rules.
3. **DB CHECK** — `valid_cooking_skills` + `valid_main_ingredients`, byte-identical arrays to the Zod VALUES, added in the **P4b CHECK migration** (NOT the P3 apply migration) **after** the canonical data is written AND the closed frontend is deployed (Q9) — a table-wide CHECK can only follow canonicalization.

**Sequencing guard (GATE A D2):** the CHECK can only land AFTER both (a) the data is canonical AND (b) the closed frontend/Zod is *deployed* — else reviewer edits of legacy values hit constraint violations. Because the frontend auto-deploys on merge but migrations need manual PROD approval (the expand/contract hazard, `reference_ci_flakes`), this is a **deploy-order** choreography, not just merge-order — locked in §4 Q9. Do NOT bundle the closed frontend and the CHECK migration in one PR.

## 8. Migration / shipping strategy (PR skeleton — confirm in §4 Q7)

| Phase / PR | Contains | DB? | Risk | Reversibility |
|---|---|---|---|---|
| **P1 — Harness extension + floor + pilot tooling** | Add the two fields + two-level shape to schema/prompt/vocab/export/normalize/validate; deterministic alias-map floor; extend sample/score for the 4 gates + rules baseline | **No DB** (scripts only) | low | git revert |
| **P2 / P2′ — Pilot + re-pilot** | Sample 70 → 69-key; AI-draft gold key → user adjudicate; **single Opus-4.8 run** (no bake-off, superseded 2026-06-23). Original **P2 failed all 4 gates** (blind re-read → over-tagging) → method pivoted (§3·PIVOT); **P2′** re-piloted the anchored verify-and-diff method. Greenlight = the **re-framed criterion below**, NOT "4 gates pass". | No DB | low | n/a (artifacts) |
| **P3 — Full run + apply** | The **frozen per-field ship config** over ~700 lessons → **materialize the per-field ship output** (floor-only `main_ingredients` + floor-retention `cooking_skills`, D-P11) → staging + diff + **the ② pre-apply gate (prevalence review + user spot-check)**; **one migration**: snapshot → **C02-only** dual-write column + `metadata` JSONB → idempotent → `.sql.rollback`; **NO CHECK yet** (frontend still free-form → no save failures) | **migration** | **highest** (corpus-wide rewrite) | snapshot table + `.sql.rollback` |
| **P4 — Enforcement lockdown** (expand/contract, §4 Q9) | First: non-creatable dropdowns + Zod enums (2 app files + **4** edge-mirror lines) + `superRefine` → merge → frontend deploys. THEN, as a **separate** migration after a drift re-census: the `valid_cooking_skills` + `valid_main_ingredients` CHECKs | **frontend + migration (split)** | medium | git revert + drop-constraint rollback |

### Greenlight criterion (re-framed Session 13–14 — supersedes "all 4 gates pass")

> Supersedes §6 / §4-Q5 ("greenlit only when all four gates pass"). History + rationale: the §6 ⚠️ amendment. The per-field ship method it rests on: **D-P11**. *(This block was audited Session 15 by a Codex cross-family pass + a Claude lens, which corrected three overclaims in the first draft — see the §8/greenlight notes in the execution-status session log.)*

**Why re-framed.** The P2′.6 re-pilot proved gate ③ (LLM-**added**-specific precision) + the per-specific precision floors are **structurally unreachable** on ingredient *specifics* — the residual over-tags are incidental specifics already carried in the legacy corpus tags, which no mechanical re-tagger prunes without the per-lesson centrality judgment the Session-12 case review showed is hard. **D-P11 responds by not shipping the LLM for `main_ingredients` at all** (floor-only), so gate ③ and the **LLM-precision half** of gate ④ now measure output we no longer ship. The binary "4 gates pass" is therefore **retired as the ship test**; the 4 gates stay on as the **diagnostic instrument** (computed + reported on every scorecard, **over the shipped per-field output** — raw-LLM diagnostics labelled separately), not the pass/fail criterion. *(The floor's own properties the gates still touch — gate ④'s never-stored-literal rule "no Salt/Oil/Soy-sauce survives" + Sweeteners handling, both deterministic in the floor — DO still ship and stay in the diagnostics.)*

**The ship gate is now two USER-gated checkpoints, both evaluated on the SHIPPED per-field output** (floor-only `main_ingredients` + floor-retention `cooking_skills` — D-P11; **NOT** raw `finalC02`):

**① Pilot greenlight (P2′.7 → proceed to the P3 full run).** Scored on the 69-key (per-value) + the fresh-25 canary (**cooking-scoped — D-P12**; labeled + scored on `cooking_skills` only, since floor-only ingredients are deterministic and subsumed by ②'s all-700 prevalence review):
- **No clean-core loss *relative to the canonical floor* — by construction.** `main_ingredients` ships exactly the floor; `cooking_skills` ships floor ∪ LLM, so the floor is always retained (`uniqueOrdered([...flooredCooking, ...cookingFinal])`). Asserted on the stored run: `ingredients == floor` (P2′.8 re-score gate) + `cooking ⊇ floor` (by construction in `materializeC02Ship`; `ship-policy.test.ts`). **Caveat — this guarantees no loss *vs the floor*, not floor *correctness*:** a floor mis-fold (wrong alias target, a clean tag mis-dropped as junk, a bad `Herbs & Aromatics` split) is a clean-core regression the construction cannot catch and both fields inherit identically. Floor fold-correctness is covered by P2′.1's unit tests on the 69-key sample; **at corpus scale it is re-confirmed by ②'s floored-row spot-check**, not assumed.
- **Cooking floor-retention is worth shipping over floor-only — diagnostic evidence, not a gate.** Honest all-69: floor-only F1 **0.807** (P 0.900 / R 0.732) → floor-retention **0.872** (P 0.833 / R 0.915) — it **trades cooking precision 0.900→0.833 for recall 0.732→0.915** (+0.065 F1), recovering ~50 real body-derived skills the floor cannot add and restoring ~28 the LLM otherwise drops. *(This +0.065 is **all-69 cooking F1** — evidence the policy beats floor-only, NOT a re-introduction of the retired Gate ② "+0.05 judgment-row" bar.)*
- **Cooking sentinels — a necessary check on the two historically-worst catch-alls, NOT a sufficiency proof.** `Tasting` + `Kitchen & food safety` precision must hold at their sentinel threshold where supported (the keep-only lock cured their over-prediction — Tasting is amply supported on the 69-key; KFS sits below the support floor there → informational). **These cannot certify cooking precision in general:** the lock pins *these two* labels high, so other skills (Roasting, Dough making, …) could over-predict on fresh data while the sentinels still read clean. **Cooking precision at scale is guarded by ②'s prevalence review, not the sentinels.**
- **Much cleaner than today — BOTH consistency AND correctness, user-judged on the committed scorecard.** (a) *Consistency (measurable):* the vocabulary collapses 122 cooking / 230 ingredient free-form values → the closed canonical set (**23 / 70, provisional** until the P3 freeze). (b) *Correctness (judged):* the shipped F1 sits above the raw current tags **AND** ②'s diff spot-check finds no systematic mis-assignment. Consistency alone cannot carry the ship.
  - *D-P11 basis, for reference:* `main_ingredients` ships the floor because the LLM does not beat it — **clean-core F1 0.831 < floor 0.918**, and on **judgment rows** the LLM edge was only **+0.012** (CI ∋ 0) while leaking group-level FPs the per-value gate can't catch. Ingredient SPECIFICS therefore ship at **cleaned-existing-tag quality (~0.77, specifics-tier-vs-tight-key)** — distinct from the 0.918 whole-field floor F1 — and **not** curated-to-centrality. The "AI adds 1–3 central specifics" goal (§2 Goal 1 / D-P1) is dropped for the LLM.

**② Pre-apply gate (P3.1 → proceed to the apply migration).** All USER-gated:
- a **committed corpus-wide per-value prevalence report** — per-tag firing rate across all ~700 live lessons (the scale check that would have caught "Tasting on 78%"); **this is the real cooking-precision-at-scale guard.**
- a **user spot-check of the diff**, explicitly including a **floored / ship-changed row sample** — selected by an explicit **ship-policy provenance bucket** in the diff report, **NOT** `normalizationCount` (C02 fields bypass `normalize`, and the floor is applied later by `materializeC02Ship`, so `normalizationCount` does not flag floored C02 rows — **P3.1 carry-forward**).
- a **calibration tripwire:** the pilot accepted cooking precision ≈ **0.833**; if the prevalence report's cooking signal reads materially worse than the pilot, **STOP** — "much cleaner than today" was calibrated at ~0.833, not at any cooking precision.
- **field-isolated run-time validation confirmed** in the full run (P2′.8 part 2 built it; re-confirm no row dropped a valid field to an off-vocab sibling — else that row silently degrades cooking to floor-only).
- an **explicit user stop** before the apply. **Remedy if an over-tag surfaces:** abort-and-re-pilot (un-freeze the prompt → re-score the 69-key → fresh canary → renewed approval) **or** a deterministic post-filter — **never** a silent threshold relax (that would be gaming, per the Session-12 case review).

**Dual-write requirement:** the apply must write BOTH the typed `text[]` column and the `metadata` JSONB (the `complete_review_atomic` precedent dual-writes both — JSONB keys at `…tags_side_channel.sql:166`(mainIngredients)/`:168`(cookingSkills), column writes at `:376`(main_ingredients)/`:386`(cooking_skills)); an UPDATE to either auto-fires the BEFORE-UPDATE `search_vector` trigger (`20260618000000_search_vector_add_sel.sql` — `main_ingredients` is a standalone **B-weight** at `:62`, `cooking_skills` rides a **combined C-weight** block at `:65-72`) so no manual poke is needed, but both surfaces must be written for lockstep.

### TEST DB rehearsal / rollback / per-PR ritual
- **P3/P4 migrations:** apply to TEST → MCP-verify (distinct-count drop, sample rows, CHECK present, search still returns) → PROD approval → PROD MCP-verify. Re-verify per round (`feedback_per_round_test_db_verification`). Snapshot table populated BEFORE any UPDATE; rollback artifact = a sibling **`*.sql.rollback`** restoring prior column+JSONB from the snapshot (the Wave-4 corpus-mutation pattern — `20260622000000_*` used `.sql.rollback` + a retained snapshot table; **not** the older PR-6 comment-block style), rehearsed on TEST before PROD.
- **Per-PR ritual:** pre-push reviewer agent + GATE 3 Codex (parallel) → baseline checks → push + `gh pr create` → four-surface triage → rebuttal-pass every finding + GATE 4 Codex on real suggested changes → consolidated fix-ups → per-round TEST re-verify → round-cap after 2. **GATE 2 Codex on the apply + enforcement migration SQL before TEST.**
- **Known flakes:** `migrate-production.yml` SASL apply/verify flake → PROD MCP verify mandatory + rerun (`reference_ci_flakes`).

## 9. Testing strategy

- **Harness unit tests** (P1): the two-level validation refinement (group↔specific), the deterministic floor (alias map → canonical), the rules baseline, the 4-gate scoring — extend the existing `*.test.ts` alongside each module (the harness is already heavily unit-tested).
- **Zod fixtures** (P4): post-canonical values parse against the new enums; the specific→group `superRefine` rejects an orphan specific; edge mirror parity test stays green.
- **Migration** (P3/P4): local `supabase db reset` + `npm run test:rls` unchanged; TEST-DB MCP probes for distinct-count + CHECK + search.
- **Manual smoke:** reviewer dropdown shows canonical values only (no free-form), a known multi-ingredient lesson shows group + specifics, public search still returns expected results.
- **No new E2E**; existing search E2E must stay green after the apply.

## 10. Out of scope (captured for future work)

- **Embeddings regeneration** (the rebuild's C2.4) — a metadata-inclusive embedding recipe + full-corpus regen after the re-tag changes metadata. Separate track; relates to `project_embedding_pipeline_mismatch`.
- **Filter-UI surfacing of the two ingredient tiers** (group vs. specific in the public filter menu) — a filter-UI-track concern, not this track (worksheet QUESTION A note).
- **Hard "1–3 specifics" cap** — deliberately left as guidance, not enforced.
- **Curriculum-team validation round** — user chose solo sign-off.
- **DB trigger for the specific→group invariant** — leaning app-layer only (§4 Q7); revisit only if reviewer drift appears.

## 11. References

- Worksheet (decided vocab): `docs/plans/2026-06-12-metadata-rebuild-stage1-cooking-skills-ingredients-worksheet-decided.md`
- Wave-4 carry-forward: `docs/plans/2026-06-22-wave4-data-corpus-cleanup-design.md` §3.1 / §4 Q11–14a / §8
- Harness: `scripts/stage2-retag/` (+ `artifacts/answer-key-scorecard.md` — the 13-field precedent that excludes these two)
- PR-6 apply precedent: `supabase/migrations/20260617000000_pr6c2_retag_apply.sql` (snapshot + CHECK pattern)
- Dual-write precedent: `supabase/migrations/20260519000000_complete_review_atomic_tags_side_channel.sql`
- gardenSkills closed-loop template: `src/utils/filterDefinitions.ts` (L231–267), `src/pages/ReviewDetail.tsx` (L1058–1062), `src/types/lessonMetadata.zod.ts`
- Memory: `project_metadata_rebuild_initiative`, `project_deferred_work_campaign`, `project_vocabulary_drift_scope`, `feedback_data_safety_top_priority`, `feedback_per_round_test_db_verification`, `feedback_codex_over_crossexamine`, `feedback_multi_session_execution`
