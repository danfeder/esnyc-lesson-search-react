# Phase 8b Execution Status

**Last updated:** 2026-04-28 21:30 UTC by Session 17
**Current PR:** **PR 3 = #470** — Reviewer flow redesign. **OPEN, awaiting bot review + manual TEST DB smoke.** Branch `feat/phase-8b-reviewer-flow` pushed to `origin`. PR carries 21 commits (10 task/feature + 11 docs status updates including this Session 17 entry), 901 additions / 85 deletions across 9 source files + status doc.
**Current task:** Task 3.11 step 1-4 ✅ done (pre-push reviewer agent dispatch → 6 findings, all 6 rejected with documented rationale, 0 fix-ups; `npm run type-check && npm run lint` clean; `git push -u origin feat/phase-8b-reviewer-flow`; `gh pr create` opened **PR #470** at https://github.com/danfeder/esnyc-lesson-search-react/pull/470 with title `feat: Phase 8b — reviewer flow (binding-intent banner, pre-selection, search escape, mismatch helper, queue badges)` and the captured 7-bullet body + reviewer notes documenting the rejected pre-push findings). Next session resumes at **Task 3.11 step 5-6** — wait for external bots (claude-review, claude-component-review, claude-database-review, performance-review) to land, four-surface triage per `feedback_pr_comment_surfaces.md`, manual TEST DB smoke against the Netlify deploy preview, accept/reject + apply fix-ups, round-cap after 2 rounds, then merge.
**Branch:** `feat/phase-8b-reviewer-flow` pushed and tracking `origin/feat/phase-8b-reviewer-flow`. After this Session 17 status commit lands, branch will have 21 commits ahead of origin/main on remote (the 20 commits I pushed + 1 status-doc commit for this session). Current head: `8139df4` (Session 16 doc) at PR-open time; will become Session-17-doc commit after this edit + commit cycle.
**Last commit on branch:** `8139df4` (Session 16 doc, at the time PR #470 was opened); next commit = Session 17 status doc.

## Done

- ✅ **Task 1.1** — created `supabase/migrations/20260504000000_phase_8b_fk_on_delete_set_null.sql`
  - Local apply via `supabase db reset`
  - Local FK confirmed via MCP: `confdeltype = 'n'`, def shows `ON DELETE SET NULL`
  - `npm run test:rls` baseline-clean (2 pre-existing `archive_duplicate_lesson` failures confirmed pre-existing on `main`)
- ✅ **Task 1.2** — pushed branch + opened PR #468
  - Pre-push code-reviewer agent dispatch: 0 critical, 1 doc nit rejected
  - `npm run type-check && npm run lint` clean
- ✅ **Bot triage on PR #468** — 3 findings from `claude-review`, all 3 rejected with documented rationale (BEGIN/COMMIT wrapper rejected as post-push edit hazard + Supabase auto-wraps; NOT VALID lock pattern rejected as low-traffic non-issue and bot itself flagged non-blocking; docs-drift advice rejected as already covered by ritual)
- ✅ **TEST DB verification** via `mcp__supabase-test__execute_sql`: `confdeltype = 'n'`, `ON DELETE SET NULL` confirmed on TEST
- ✅ **PR #468 merged** via rebase as `9a6b09e` (preserves the 9 doc-commit history alongside the migration)
- ✅ **PROD apply** — first attempt failed with documented SASL Apply-step flake (run `25032406625`); rerun via `gh run rerun --failed` succeeded after second approval. PROD verified via `mcp__supabase-remote__execute_sql`: `confdeltype = 'n'`, def shows `ON DELETE SET NULL`.
- ✅ **Task 2.1 (Session 2)** — created `src/utils/titleSimilarity.ts` + co-located test (`src/utils/titleSimilarity.test.ts`). TDD cycle followed: 10/10 failing → implement → 10/10 passing. Commit `edbc48a`.
- ✅ **Task 2.2 (Session 3)** — created `src/components/LessonSearchPicker.tsx` + co-located test (`src/components/LessonSearchPicker.test.tsx`). TDD cycle: RED (module not found) → implement → first run 7/8 (test pollution: test 5's `mockImplementation` leaked into test 6 because `vi.clearAllMocks()` only clears call history, not implementations) → fixed `beforeEach` to restore default mock → 8/8 passing. Type-check + lint clean (lint:fix removed an unused eslint-disable directive on the `onSelect` prop — see decisions). Commit `887449b`.
- ✅ **Tasks 2.3–2.6 (Session 4)** — landed as one commit `06ce2de`. Task 2.3: added two lazy imports + two `<Route>` lines to `src/App.tsx` (used `@/pages/...` aliases to match existing pattern, not the plan's `./pages/...`). Task 2.4: rewrote `src/pages/SubmissionPage.tsx` (413 → 91 lines) as a two-button intent picker per the plan's Tailwind layout. Task 2.5: created `src/pages/NewSubmissionForm.tsx`. Task 2.6: created `src/pages/RevisingSubmissionForm.tsx` consuming `LessonSearchPicker`. Type-check clean throughout; lint flagged 3 prettier formatting errors (`IntStatusBadge` prop wrapping + `<p>` text wrapping), all auto-fixed by `lint:fix`. 18/18 unit tests still pass (titleSimilarity 10 + LessonSearchPicker 8). Single grouped commit per plan (avoid broken intermediate states from the deferred-references chain).
- ✅ **Task 2.7 (Session 5)** — created `supabase/functions/process-submission/normalizeSubmissionInputs.ts` + co-located test (`normalizeSubmissionInputs.test.ts`). TDD cycle: RED (module-not-found) → implement → GREEN (9/9). Then wired into `index.ts`: added `import { normalizeSubmissionInputs } from './normalizeSubmissionInputs.ts';` (line 5), inserted normalization + FK-target existence check between line 171 and the INSERT block (line 173), and replaced both INSERT fields (`submission_type` and `original_lesson_id`) with the normalized values. Type-check clean, lint clean (no prettier flags), full test suite 457/457 pass. Commit `1232d1a`.
- ✅ **Task 2.7.5 (Session 6)** — added minimal reviewer safety banner in `src/pages/ReviewDetail.tsx`. Two edits: (1) added `AlertTriangle` to existing `lucide-react` import on line 4 (was: `import { ExternalLink } from 'lucide-react';` → now: `import { AlertTriangle, ExternalLink } from 'lucide-react';`); (2) inserted a 33-line block of JSX above the decision-panel `<div className="adm-card">` at line 913, gated on `submission?.submission_type === 'update'`, with two copy variants (`original_lesson_id` non-null → shows raw ID; null → "Submitter could not find the target lesson in the picker"). Yellow chrome via inline styles per plan (`borderColor: '#fbbf24'`, `background: '#fffbeb'`, `color: '#92400e'`/`#b45309`). Type-check + lint clean. No unit test (rendered banner verified manually via deploy preview at PR-open per plan §6.1 PR 2 minimal vs PR 3 enriched). Commit `a2287b2`.
- ✅ **Task 2.8 (Session 6)** — created `e2e/submission-flow.spec.ts` with 3 structural smoke tests covering `/submit`, `/submit/new`, `/submit/revising`. All three test the unauthenticated render shape (none of these routes are wrapped in `ProtectedRoute` — auth is handled in-page via `AuthModal`, so structural tests don't need credentials). Verified plan's text expectations against actual rendered text first: `IntPageHeader title="Submit a lesson"` ✓, "Add a new lesson to the library" ✓, "Update a lesson that's already in the library" ✓, `IntPageHeader title="Add a new lesson"` on `/submit/new` ✓, placeholder `https://docs.google.com/document/d/...` matches `/docs\.google\.com\/document/i` ✓, "Adding a new lesson · Change" breadcrumb ✓, "Step 1 · Find the lesson you're revising" matches `/Find the lesson you're revising/i` ✓, URL field disabled on initial render via `targetReady = Boolean(selectedLesson) || cantFind` (both start false) ✓. 3/3 pass against local dev server (4.1s). Type-check + lint clean. Commit `f10be9d`.
- ✅ **Task 3.1 (Session 8)** — extended `loadSubmission` in `src/pages/ReviewDetail.tsx` to fetch off-list submitter-target lessons. Three edits: (1) added `SubmitterTargetLesson` interface (lines 46-66) with the documented nullability commentary; (2) added `submitterTargetLesson?: SubmitterTargetLesson | null` field to `SubmissionDetail` (line 79); (3) inserted ~35-line fetch block between similarities-with-lessons assembly and the reviews query — gated on `submitterTargetId && !targetInRenderedTopFive` against the SLICED top-5 (per impl plan's CRITICAL note about `topDuplicates.slice(0, 5)` mismatch); (4) attached `submitterTargetLesson` to the `setSubmission(...)` payload. Verified `lessons_with_metadata` has both `summary` and `file_link` columns in `database.types.ts` before adding them to the select. `npm run type-check && npm run lint` clean. Commit `6e5c11c`.
- ✅ **Task 3.2 (Session 9)** — replaced PR 2's throwaway minimal yellow banner in `src/pages/ReviewDetail.tsx` (lines 969-999, the `submission?.submission_type === 'update' && (...)` block from Task 2.7.5) with the full four-state IIFE per impl plan §3.2: **green** (`bg-emerald-50`) `(new)`, **blue** (`bg-blue-50`) `(update, X, title-known)`, **yellow** (`bg-amber-50`) `(update, X, title-FAILED-to-load)`, **yellow** (`bg-amber-50`) `(update, null)`. `targetTitle` resolved via `submission?.submitterTargetLesson?.title` (Task 3.1's off-list fetch) with fallback to `topDuplicates.find((d) => d.lesson_id === targetId)?.lesson?.title` for the in-list case. Pure render swap — no new state, no hooks, no useEffect deps. Type-check clean; lint flagged 2 prettier wrapping nits (`<AlertTriangle ... />` should be single-line) — `lint:fix` cleared. +61 / -28 lines. Commit `8c4c354`.
- ✅ **Task 3.11 step 1-4 (Session 17)** — pre-PR check + pre-push code-reviewer dispatch + push + open PR. **PR #470 OPEN** at https://github.com/danfeder/esnyc-lesson-search-react/pull/470 (901 additions / 85 deletions / 21 commits). Dispatched `feature-dev:code-reviewer` agent on `git diff main...HEAD` (the full PR scope minus the docs status file); agent returned **6 findings (0 critical / 2 high / 2 medium / 2 low), all 6 rejected with documented rationale**: (1) `showSearch` auto-expand "fights manual toggle" — REJECTED as factually wrong (`useEffect` deps fire on CHANGE not on every render, so user-close doesn't re-fire the effect); (2) banner IIFE "falls through to green for unknown type" — REJECTED as defensive hardening, the upstream coercion at `ReviewDetail.tsx:348` (`submissionData.submission_type || 'new' as 'new' | 'update'`) plus the narrowed `SubmissionDetail` interface make the banner contract safe; (3) `onClear` "stale closure race" — REJECTED, synchronous event handler reads closure value once, no race; (4) `similaritiesResult.error` swallowed in `ReviewDashboard.tsx` — REJECTED as out-of-scope (pre-existing pattern shared by `profilesResult` + `similaritiesResult`, the new `lessonsResult` follows the same convention; captured as a follow-up); (5) `candidateCards` memo deps include whole `submission` — REJECTED, agent's own statement was "not urgent given current code path"; (6) missing test for `original_lesson_id = undefined` — REJECTED as redundant (the `??` operator coalesces null/undefined identically; testing both is testing the language not the helper). Zero fix-up commits. `npm run type-check && npm run lint` clean immediately before push. `git push -u origin feat/phase-8b-reviewer-flow` succeeded with branch tracking set. `gh pr create` opened **PR #470** with the captured title + 7-bullet body + reviewer notes documenting the rejected pre-push findings (so external bots see the analysis already done). **Net session output:** PR open, branch pushed (no local-only state remaining for the 20 task/feature/doc commits). Next session resumes with Task 3.11 step 5+ — wait for external bots, four-surface triage, manual TEST DB smoke against Netlify deploy preview.
- ✅ **Task 3.10 (Session 16)** — E2E scaffolding for reviewer flow. New file `e2e/review-flow.spec.ts` (88 lines, +88/-0). Three `test.skip(...)` markers per impl plan §3.10 — one per intent state (green NEW / blue update-with-target / yellow update-no-target). Each body has the assertion logic spelled out in commented-but-realistic Playwright calls (`page.getByText`, `page.getByRole('radio')`, `page.getByPlaceholder` etc.) including specific text patterns matched to the actual rendered banner copy from Sessions 9-14, so a future implementer uncomments + adds `page` arg + drops `.skip` rather than rewriting from scratch. `expect` import retained with a single `void expect;` since it's only referenced in comments today. Pre-flight verified no `auth.setup.ts` or seeded-submission fixture exists in `e2e/` (existing specs all defer login per `submission-flow.spec.ts:27` comment); plan tone explicitly accepts `.skip` for v1 ("E2E for reviewer is best done manually for v1 if the auth fixture isn't already established"). `npx playwright test e2e/review-flow.spec.ts --list` reports 3 tests parsed; type-check + lint clean. **Adaptation worth noting:** plan snippet had `async () => {...}` (no `page` arg) and empty bodies — first attempt added `page` for forward-compat, but lint flagged unused `page`. Removed `page` from all three signatures to match plan literal; comments still illustrate `page.getByText(...)` calls so the param is added back when uncommented. Commit `50fa1c9`.
- ✅ **Task 3.9 (Session 15)** — unit tests for two Phase 8b pure helpers + extraction of `computePreselection` from `ReviewDetail.tsx`. +135 / -7 lines, commit `733e14e`. Three new files: (1) `src/pages/reviewPreselect.ts` — pure helper with `PreselectionInput` (input typed `string | null | undefined` for `submission_type` so the DB row shape flows without a cast at the call site; helper still only branches on the literal `'update'` value, fallback to `approve_new` for anything else including legacy `'reject'`). (2) `src/pages/reviewPreselect.test.ts` — 4 cases: new, update+target, update-no-target, legacy unknown (covers both undefined and arbitrary stored strings). (3) `src/pages/reviewMismatch.test.ts` — 5 cases per impl plan §3.9 step 2: no selection, submitter-bound, dup-detector, manual-pick, manual-pick-coincident-with-dup. One file edit: `src/pages/ReviewDetail.tsx` — added `computePreselection` import + replaced inline 8-line pre-selection logic at lines 397-407 with 7-line helper call, preserving identical behavior (same `setDecision` shape, same `setSelectedDuplicate` only-when-target-non-null guard). **TDD cycle followed for `computePreselection`:** wrote test → confirmed RED ("Failed to resolve import @/pages/reviewPreselect") → implemented helper → confirmed GREEN 4/4 → refactored caller → re-verified 4/4. **`reviewMismatch.test.ts` is post-hoc** (helper was extracted in Task 3.7 explicitly to enable §3.9 testing — not retro-fitting). First type-check after refactor surfaced TS2322 because `submissionData.submission_type` is `string` from the DB row but my initial helper input was `'new' | 'update' | null | undefined`; widened to `string | null | undefined` (with explanatory comment). Two prettier fix-ups via `lint:fix`. Final run: 9/9 new tests pass, full unit suite 467/467 pass, type-check + lint clean. Test files co-located alongside source per Sessions 2/3/5 precedent (every `src/**/*.test.*` is co-located; `src/__tests__/` exists at repo root but only holds `setup.ts`/`helpers/`/`integration/`, not unit tests).
- ✅ **Task 3.8 (Session 14)** — three-state queue badge. +59 / -2 lines, commit `a8c4da7`. Two file edits: (1) `IntQueueRow.tsx` — extended `IntQueueRowSubmission` with `originalLessonId?: string | null` + `originalLessonTitle?: string | null`; replaced the existing `<span className="adm-pill">{type === 'new' ? 'New lesson' : 'Update'}</span>` at line 65 with the three-state IIFE per impl plan §3.8 (green NEW for `type==='new'`; blue `bg-blue-100 text-blue-800` UPDATE chip + title-truncated-to-40chars span with `title=` tooltip + `aria-label="Updating lesson: {fullTitle}"` when `originalLessonId` set; amber UPDATE? + "needs reviewer search" with `aria-label` on the chip when `originalLessonId` null). (2) `ReviewDashboard.tsx` — added `targetLessonIds` distinct collection, third entry to existing `Promise.all` fetching `lessons.lesson_id, title` for those IDs, build `lessonTitleMap` Record via `.reduce()`, attach `originalLessonTitle` to each merged Submission row in the `setSubmissions` mapping. `Submission` interface line 36 extended with `originalLessonTitle?: string | null`. IntQueueRow call at lines 257–268 passes both new props with `?? null` coercion (legacy `original_lesson_id` is `string | undefined`, plan's prop is `string | null`). **Manual smoke** via chrome-devtools-mcp against local dev server (was already signed in as a reviewer from prior sessions): navigated to `/review`; a11y tree at uid=4_19/4_20/4_21 surfaced all three label variants; DOM probe via `evaluate_script` queried each row's `span[class*="rounded"]` chip computed background — `oklch(0.962 0.059 95.617)` (Tailwind `bg-amber-100`) on UPDATE? chip, `oklch(0.932 0.032 255.585)` (`bg-blue-100`) on UPDATE chip, `oklch(0.962 0.044 156.743)` (`bg-green-100`) on NEW chip. Title resolution correct: blue UPDATE row's title-span text = "Garden to Table: Growing Tomatoes" (matches LESSON-001 in local DB); tooltip + aria-label both populated. Type-check + lint clean on first try (no prettier nits). Screenshot at `/tmp/phase8b-task38-three-state-badges.png`.
- ✅ **Task 3.7 (Session 13)** — title-mismatch helper. New file `src/pages/reviewMismatch.ts` exports pure `shouldShowMismatchWarning({ selectedTarget, submitterTargetId, topDuplicateIds, searchPickedId })` (auto-pick gate: returns true only when target was submitter-bound or surfaced by dup detector AND not the reviewer's search pick). `src/pages/ReviewDetail.tsx`: added two imports (`titlesAreSimilar` from `@/utils/titleSimilarity`, `shouldShowMismatchWarning` from `@/pages/reviewMismatch`); added `showMismatch` derived value next to the Task 3.6 derived block; inserted yellow IIFE warning block between candidate-matches list (line 1096) and search escape hatch (line 1098), gated on `showMismatch && both titles present && !titlesAreSimilar(...)`. **Decision: created the helper file in §3.7 (option b), not inline.** Plan §3.9 step 2 explicitly says "Then in Task 3.7's render code, gate the actual mismatch banner on `shouldShowMismatchWarning(...)`" — i.e., §3.9 only adds the test file, not the helper. **Manual smoke** via chrome-devtools-mcp signed in as `reviewer@test.com` against local dev server: (a) **fire path** on `bbbbbbbb-...` (update, LESSON-001 = "Garden to Table: Growing Tomatoes", extracted_title = "Test Update With Target", Jaccard 0) → amber inline warning text confirmed in DOM via `evaluate_script + body.innerText` substring "Heads up: submitter linked to "Garden to Table: Growing Tomatoes" but submission's extracted title is "Test Update With Target""; (b) **suppress path** by temporarily updating extracted_title to "Garden to Table: Growing Tomatoes (2026 Refresh)" (Jaccard ~0.71) → reload + probe confirmed "Heads up:" text absent from DOM. extracted_title restored to "Test Update With Target" before commit. Manual-pick suppression branch (search escape hatch active) covered by helper unit tests (Task 3.9). Type-check + lint clean on first try (no prettier nits). +35 / +26 lines. Commit `ac75e95`.
- ✅ **Task 3.6 (Session 12)** — search escape hatch in `src/pages/ReviewDetail.tsx`. +118 / -24 lines, commit `26cfb0f`. Five coupled changes: (1) two new useState (`selectedSearchLesson: LessonSearchResult | null`, `showSearch: boolean`) declared BEFORE `candidateCards` useMemo so the memo can read `selectedSearchLesson` via deps without TDZ; (2) refactored Task-3.5 candidateCards useMemo to compute intermediate `base` (Cases 1-3) then conditionally append a Case-4 synthetic card with `matchLabel: 'Reviewer searched'` when `selectedSearchLesson` is set and not in `base` — deps extended to `[submission, topDuplicates, selectedSearchLesson]`; (3) three derived plain values (`needsSearch`, `noDups`, `searchHelpText`) computed during render + two useEffects: `setShowSearch(needsSearch || noDups)` on `[needsSearch, noDups]` deps and `setSelectedSearchLesson(null)` on `[submission?.id]`; (4) disclosure JSX (toggle button + collapsed/expanded panel rendering `LessonSearchPicker` with `cantFindOption=false`) placed below candidate-matches list, above binding-intent banner — `onClear` captures `selectedSearchLesson?.lesson_id` BEFORE the reset so the `selectedDuplicate` cleanup compares against the right id; (5) import `LessonSearchPicker + type LessonSearchResult`. **Card meta string deviation from impl plan**: plan's `'(found via library search)'` parenthetical replaced with the existing card shape `Grades X · LESSON-XXX` so all candidate-matches cards share visual structure — the REVIEWER SEARCHED badge already provides the provenance signal. **Manual smoke** via chrome-devtools-mcp against local dev server, signed in as `reviewer@test.com`: (a) `cccccccc-...` (update, null, 0 sims) → AUTO-EXPANDED, help text "Use this to find the lesson the submitter couldn't"; (b) `aaaaaaaa-...` (new, 0 sims after temp deleting LESSON-003 sim) → AUTO-EXPANDED, help text "Use this when no card above is the right match"; (c) `bbbbbbbb-...` (update, LESSON-001, 2 sims) → DEFAULT COLLAPSED. Manual expand → help text "Use this if you disagree with the submitter's pick" (override branch). Search "compost" → pick Composting 101 → new card appended at tail with REVIEWER SEARCHED badge + 0%, picker in chip mode (Selected: Composting 101 + Clear button), LESSON-001 SUBMITTER'S CHOICE card retained but no longer pressed; (d) onClear → REVIEWER SEARCHED card removed, picker back to search input, `selectedDuplicate` cleared to null, MERGE & ARCHIVE disabled with inline guidance returns. **Restored** `aaaaaaaa-...`'s LESSON-003 similarity (0.52, medium) after the smoke so next-session seed state matches Session 11's. Type-check + lint clean (one round of `lint:fix` to fold 2 prettier nits). Screenshot at `/tmp/phase8b-task36-override-path.png`. **chrome-devtools-mcp click() oddity**: clicking the search-result button via the `click` tool reproducibly triggered the MERGE & ARCHIVE handler (validation alert) instead of `onSelect` — twice, on fresh reloads. Same button via `evaluate_script + dispatchEvent(new MouseEvent('click', { bubbles: true }))` worked perfectly. Code is correct; the `click` tool's coordinate-based simulation may misfire when buttons are stacked vertically with form controls below — workaround when this recurs: dispatchEvent via evaluate_script.
- ✅ **Task 3.5 (Session 11)** — unified candidate-matches list with "Submitter's choice" badge in `src/pages/ReviewDetail.tsx`. +81 / -26 lines, commit `f4a1d71`. Two edits: (1) `candidateCards` `useMemo` placed between `topDuplicates` and `parsedContent` (line ~490), depends on `[submission, topDuplicates]`. Three branches: empty if no submission; `fromDups` baseline mapping `SimilarityWithLesson` → `IntDuplicateCard.dup` shape; if `submitterTargetId` present and `findIndex` finds it in `fromDups` → hoist with `matchLabel: "Submitter's choice"`; else if `submission.submitterTargetLesson` (Task 3.1's off-list fetch) is non-null → prepend synthetic card with `similarity: 0`, `matchType: null`, badge; else return `fromDups`. (2) Replaced lines 951-984 (the `topDuplicates.map` block) with `candidateCards.map` and changed wrapper from `topDuplicates.length > 0` to `candidateCards.length > 0` so the off-list synthetic card renders when topDuplicates is empty. Section header conditional: "Candidate matches" when `candidateCards[0]?.matchLabel === "Submitter's choice"`, else "Possible duplicates". **Manual smoke** via chrome-devtools-mcp signed in as `reviewer@test.com` against local dev server (3 cases): (a) bbbbbbbb-... off-list (no similarities, original_lesson_id=LESSON-001) → "CANDIDATE MATCHES" header, single Garden to Table card with SUBMITTER'S CHOICE badge + 0%; (b) bbbbbbbb-... in-list with flipped scores (LESSON-001 @ 20% with match_type='low', LESSON-002 @ 80% with match_type='medium') → "CANDIDATE MATCHES" header, **LESSON-001 (20%, badge) at position 0** despite LOWER score, LESSON-002 (80%, no badge) at position 1 — proving the hoist actually re-orders rather than just preserving natural order; (c) badge anchoring: clicking LESSON-002 to select it → LESSON-001 keeps its SUBMITTER'S CHOICE badge while LESSON-002 becomes pressed; (d) aaaaaaaa-... new-type with one similarity (LESSON-003 @ 52%) → "POSSIBLE DUPLICATES" header (reverts because no submitter's choice card), LESSON-003 card no badge. Type-check + lint clean on first try (no prettier nits). Screenshots at `/tmp/phase8b-task35-case[1-3]-*.png`.
- ✅ **Tasks 3.3 + 3.4 (Session 10)** — pre-selection logic + fixed enable/disable in `src/pages/ReviewDetail.tsx`. Three coupled edits, +24 / -6 lines, commit `64a9feb`. (1) **Pre-selection block** added after the existing `if (reviews && reviews.length > 0)` prior-review restoration at line 358-381: when `!reviews || reviews.length === 0`, set `decision='approve_update'` + (if `original_lesson_id`) `setSelectedDuplicate(...)` for `update`, else `setDecision('approve_new')` for `new`. Guard prevents clobbering the existing decision/notes/metadata restoration. (2) **Approve_update radio cleanup** — removed `disabled={!selectedDuplicate}`, the `style={!selectedDuplicate ? { opacity: 0.5 } : undefined}` dimming on the label wrapper, AND the `{!selectedDuplicate && ' (select a duplicate first)'}` parenthetical hint. All three are coupled to the old "radio is gated" semantics; keeping them under the new "radio always selectable" design would be visually misleading. (3) **Inline guidance** placed OUTSIDE `IntDecisionBar` (whose actions container is flex-styled for buttons) but inside the same parent column, gated on `decision === 'approve_update' && !selectedDuplicate`. The existing `disabled={saving || !selectedDuplicate}` on the merge button was already correct in the current rendering structure (the button only renders inside `{decision === 'approve_update' && (...)}`), so no change there. **Manual smoke** via chrome-devtools-mcp against local dev server: seeded `(update, X)` row pointing at `LESSON-001` ("Garden to Table: Growing Tomatoes") and `(update, null)` row, signed in as `reviewer@test.com` / `password123`, verified both flows visually + in the a11y snapshot — yellow banner + radio checked + button disabled + inline guidance for null case; blue banner with title resolved + radio checked + button enabled + no guidance for X case. Screenshots at `/tmp/phase8b-update-null.png` and `/tmp/phase8b-update-x.png`. Type-check + lint clean.
- ✅ **Task 2.9 (Session 7)** — PR 2 shipped end-to-end. Pre-push code-reviewer agent flagged 1 high (LessonSearchPicker stale-async race via debounce-not-cancelling-in-flight) + 1 high (rejected: ilike wildcard hardening) → applied request-id counter fix + regression test (commit `168f52f`). Pushed branch, opened PR #469. **Round 1 bots** (claude-review broad + claude-component-review CHANGES_REQUESTED + claude-database-review pass + performance-review pass) — 4 of ~15 findings accepted as one bundled fix-up `c7b4417`: ARIA combobox attrs on picker, `!!` 0-render guard at L131, `role="alert"` on error divs, auth re-trigger via `pendingSubmitRef + requestSubmit()` in both forms; 11 rejected (DRY, hardening, intentional UX, type-clarity-only). Eslint config got `HTMLFormElement` added to globals list to lint clean. **Round 2 bots** caught two regressions I introduced in round 1: missing URL regex check in `RevisingSubmissionForm.handleSubmit` (round 1 `requestSubmit()` bypassed the canSubmit gate) + ARIA combobox role without keyboard-nav = WCAG 2.1 fail. Both fixed in `3a18277`: added URL check + reverted combobox/listbox/option roles, kept just `aria-label`. **Round 3 bots** ran on `3a18277` — 0 critical findings (re-flags + DRY + UX polish + already-acknowledged-PR-3 items); per round-cap rule no fix-up. **TEST DB MCP smoke** — confirmed deploy preview wired to TEST (`rxgajgmphciuaqzvwmox`), signed in as `admin@test.com`, exercised all 3 paths via chrome-devtools-mcp against the deploy preview UI: `/submit/new` → row `submission_type='new', original_lesson_id=NULL` ✓; `/submit/revising` + Apple Pie picked → row `submission_type='update', original_lesson_id=1r325MQ...` resolves to "Apple Pie" via FK ✓; `/submit/revising` + "can't find it" → row `submission_type='update', original_lesson_id=NULL` ✓. Edge function 400s on fake URLs (extraction fails) but INSERT happens BEFORE extraction so row shape is correct. Live `role="alert"` confirmed in DOM. Three TEST rows left in `lesson_submissions` (clutter only). **Merged via rebase** as `898545e`; local main reset to origin/main; feat branch deleted locally.

## In flight

- **PR #470** — Reviewer flow redesign. **OPEN.** Tasks 3.1–3.10 ✅ shipped + Task 3.11 step 1-4 ✅ done (pre-push reviewer dispatch with 0 accepted findings; pushed; PR opened). **Awaiting:** external bot reviews (claude-review et al. typically land within 5-15 min of PR open), four-surface triage, manual TEST DB smoke against Netlify deploy preview, possible 1-2 fix-up rounds, merge. **No PROD migration needed** for PR 3 (no schema changes — PR 1's FK migration was the only DB change in Phase 8b and was applied to PROD on 2026-04-28 via `9a6b09e`).

## Blocked

(none — user approval gate is expected, not a blocker)

## Decisions made during execution

- **Test-file path = co-located, not `__tests__/` subdir** (Task 2.1, Session 2). Implementation plan specified `src/utils/__tests__/titleSimilarity.test.ts` but the repo convention is `src/utils/titleSimilarity.test.ts` (alongside source — see `duplicateDetection.test.ts`, `facetCounts.test.ts`, `logger.test.ts`). Adapted as a small repo-conformance change. Same will apply to Task 2.2 (`src/components/LessonSearchPicker.test.tsx`, not under a `__tests__/` subdir).
- **Per-PR ritual corrected mid-session.** Original kickoff phrased step 1 as "Pre-push self-review: read every line of `git diff main...HEAD`" — implying I do the read myself. User clarified: pre-push review = agent dispatch (the agent does the line-by-line read; you cannot impartially review your own work). User further clarified: the second `feature-dev:code-reviewer` dispatch I'd added between push and external bots is redundant — external bots ARE the second pass; my role post-bot is investigation/triage with accept/reject recommendations, optionally spawning a subagent for deeper verification. Updated kickoff prompt + implementation plan accordingly.
- **PR comment surfaces — must check ALL of them.** I missed the `claude-review` substantive review on PR #468 by querying only `gh api .../pulls/468/comments` (line-attached comments, returned `[]`). User pushed back: "did you read the bot's comment?" The bot's full report was an issue-comment surfaced via `gh pr view --comments`. New feedback memory created: `feedback_pr_comment_surfaces.md`. Kickoff + impl plan updated with the four-surface checklist (issue-comments, review summaries, line-comments, CI/check failures).
- **TEST DB re-verification is per-round, not one-time.** User FYI: any post-PR round that produces DB-affecting fix-up commits requires re-running the TEST DB MCP verification after CI re-applies the migration. New feedback memory: `feedback_per_round_test_db_verification.md`. Kickoff + impl plan rituals extended with this step.
- **Pre-push agent finding rejected** (rollback-block sub-comment) — chrome below the user-visible-bug-or-DB-safety bar.
- **Wasted second-agent dispatch on PR #468** before bots: cost ~90s. Wrong finding (date-prefix "further than necessary") rejected. No code changes from it. Future PRs follow corrected ritual.
- **PR #468 merge strategy = rebase, not squash.** Repo convention is squash-merge, but PR carried 9 valuable doc commits from prior sessions plus the migration; rebase preserved each commit's individual message rather than collapsing the doc-iteration history into a single squash.
- **PROD apply hit the SASL Apply-step flake on first attempt.** Run `25032406625` failed with `failed SASL auth (invalid SCRAM server-final-message)` at the "Connecting to remote database..." step (the Apply step's second pooler handshake within ~2s of "Initialising login role..."). Verified PROD was unchanged via MCP (`confdeltype = 'a'` pre-rerun); confirmed clean failure with no partial state. `gh run rerun --failed` succeeded on second approval. Memory entry updated to capture the Apply-step variant of the flake (was previously documented only for Verify-step) and the rerun mitigation pattern.
- **Test pollution on first GREEN run (Task 2.2, Session 3).** First test pass returned 7/8 — the test that overrides `supabase.from.mockImplementation` to return empty data (zero-results case) leaked into the next test (irrelevant-non-zero case), which expects the default Apple Crisp + Pumpkin Pie data. Root cause: `vi.clearAllMocks()` in `beforeEach` clears call history but NOT implementations. Fix: explicitly re-set the default `from` implementation in `beforeEach` (re-import the mocked module + `mockImplementation(...)`). Pattern worth remembering for future component tests that mutate the supabase mock per-test.
- **`eslint-disable-next-line no-unused-vars` on prop callbacks not needed in this repo.** CLAUDE.md and `src/components/CLAUDE.md` show the disable directive on callback props (e.g., `onChange`). On Task 2.2's `onSelect` prop, `npm run lint:fix` flagged the directive as unused (`no-unused-vars` doesn't actually fire on the param) and removed it. Lint+type-check stay clean without it. Not changing the documented pattern (consistent style across legacy code), but new components don't need the directive.
- **Lazy-import alias preserved in App.tsx** (Task 2.3, Session 4). Plan's snippet uses `import('./pages/NewSubmissionForm')` (relative path); the existing `SubmissionPage` lazy import in App.tsx uses `'@/pages/SubmissionPage'`. Followed the existing convention — consistency win, no semantic change.
- **Plan-conformance accept on raw Tailwind layout** (Tasks 2.4–2.6, Session 4). The plan specifies raw Tailwind layout (`max-w-3xl mx-auto px-4 py-8`, hand-styled card buttons with hover/focus rings) instead of the existing `int-shell-root` / `adm-page--narrow` / `adm-card` chrome. The internal-design-system components (`IntPageHeader`, `IntButton`, `IntFormField`, `IntStatusBadge`) are still used for all interactive primitives. Followed the plan as-written — this is a deliberate design choice (simpler, customer-facing flow distinct from the admin/reviewer chrome), not a repo-conformance gap. Worth flagging to the user only if the visual mismatch becomes a complaint after deploy preview.
- **`React` default import not needed in `SubmissionPage.tsx`.** Original file imported `React` for unused symbol; rewrite drops the default import and only pulls in `useState`, `useEffect` named imports. Matches modern React-17+ JSX transform; tsc + eslint both clean.
- **Test path adapted again to repo convention** (Task 2.7, Session 5). Plan said `supabase/functions/process-submission/__tests__/normalizeSubmissionInputs.test.ts`; existing edge function tests are co-located (`supabase/functions/detect-duplicates/similarity.test.ts`, `supabase/functions/_shared/google-docs-parser.test.ts` — no `__tests__/` subdirs). Same pattern as Sessions 2 + 3. Final path: `supabase/functions/process-submission/normalizeSubmissionInputs.test.ts`.
- **Vitest import vs Deno import drift handled cleanly** (Task 2.7). Plan's snippet imports the helper without `.ts` extension in the test (`import ... from '../normalizeSubmissionInputs'`) and WITH `.ts` extension in `index.ts` (`import ... from './normalizeSubmissionInputs.ts'`). Both correct: Vitest's bundler resolves the same TS file without the extension, while Deno requires it. Confirmed against `_shared/google-docs-parser.test.ts` which imports `from './google-docs-parser'` (no `.ts`) and `_shared/google-docs-parser.ts` is a sibling Deno file. No issue.
- **Round 1 fix-up introduced 2 regressions, caught by round 2 bots** (Session 7). My round 1 a11y improvement added `role="combobox"` + `aria-expanded` + `aria-controls` + `aria-autocomplete` + `aria-haspopup` on the picker input, plus `role="listbox"` on `<ul>` and `role="option"` on `<li>`. Bot 2 round 2 correctly flagged this as WCAG 2.1 failure: declaring combobox role tells AT users to expect ArrowUp/ArrowDown/Enter/Escape, but those weren't implemented (deliberately scoped out as ~50 LOC + tests). Reverted in round 2; kept only `aria-label`. Lesson: when adding ARIA roles, complete the contract or skip the role entirely. Plain `<input>` + `<button>` Tab nav was already accessible. Separately, my round 1 auth-re-trigger fix (`pendingSubmitRef` + `formRef.requestSubmit()`) bypassed `canSubmit`'s URL-regex check, so a user who cleared the URL during the auth modal could fire submit with empty URL. Round 2 added the missing client-side URL check to `RevisingSubmissionForm.handleSubmit` (was already in `NewSubmissionForm.handleSubmit`).
- **PR 2 merge strategy = rebase, not squash** (Session 7, same as PR 1). 14 commits including 5 doc commits + 6 task commits + 3 fix-up commits. Rebase preserved each commit's individual message rather than collapsing the iteration history into a single squash. User confirmed before merging.
- **`lessons_with_metadata` column verification before applying plan select-list** (Task 3.1, Session 8). Plan added `summary` and `file_link` to the off-list lookup's `.select(...)`. Before applying, verified both columns exist in `src/types/database.types.ts:1051,1080` (file_link `string | null`, summary `string | null`) — they do. Pattern: when impl plan adds new columns to a Supabase `.select()`, check `database.types.ts` first; cheaper than a runtime test failure.
- **`SubmitterTargetLesson` typed with `lesson_id` not `lessonId`** (Task 3.1, Session 8). Convention per `src/types/CLAUDE.md` is camelCase frontend / snake_case DB. The interface is a passthrough of DB row shape (parallel to `SimilarityWithLesson` which also uses snake_case `lesson_id`), so snake_case is the right call. Confirms the impl plan's spelling.
- **TEST DB smoke via chrome-devtools-mcp + mcp__supabase-test__execute_sql** (Session 7). Deploy preview UI submitted via 3 paths against TEST (verified hitting `rxgajgmphciuaqzvwmox` before any writes). Even though edge function 400s on fake Google Doc URLs, the INSERT happens BEFORE extraction (per `process-submission/index.ts` source) so the row's `submission_type` + `original_lesson_id` shape can still be verified. Signed in as `admin@test.com` per `reference_test_credentials.md`. Pattern worth re-using for any future submission-pipeline PR.
- **Radio chrome cleanup beyond the literal plan instruction** (Tasks 3.3 + 3.4, Session 10). Impl plan §3.4 step 2 says "remove `disabled={!selectedDuplicate}` on the radio." But the surrounding label had two more pieces of "the radio is gated" chrome: `style={!selectedDuplicate ? { opacity: 0.5 } : undefined}` dimming the label, and `{!selectedDuplicate && ' (select a duplicate first)'}` appending a parenthetical hint to the label text. Keeping these under the new "radio always selectable" design would be visually misleading. Per design doc §6.3 the inline guidance text below the submit button replaces the old hint; the dimming was the visual cue for the gated state which is no longer accurate. Treated as a single conceptual change ("the radio is no longer gated, so all three pieces of gated-state chrome go together"), not three separate scope creeps. Documented in the commit message.
- **Submit-button literal disabled condition NOT added** (Tasks 3.3 + 3.4, Session 10). Impl plan §3.4 step 3 suggests adding `|| (decision === 'approve_update' && !selectedDuplicate)` to the merge button's existing `disabled={...}`. Verified in current code: the merge button only renders inside `{decision === 'approve_update' && (...)}`, and its existing `disabled={saving || !selectedDuplicate}` already gates correctly. The plan's suggested change would be redundant in this rendering structure. Kept the existing simpler form. If the rendering structure ever flattens (e.g., button becomes always-rendered with disabled-by-decision), the gate would need updating — but that's a future refactor concern, not 8b scope.
- **Local DB seed evolved across smoke runs** (Task 3.5, Session 11). Picked up Session 10's seed rows still in local DB (`aaaaaaaa-...` new, `bbbbbbbb-...` update→LESSON-001, `cccccccc-...` update→null). Added similarities to `bbbbbbbb-...` for testing the in-list hoist case (originally just LESSON-001 @ 0.82 + LESSON-002 @ 0.42 to verify hoist+badge), then UPDATE'd to flip scores (LESSON-001 → 0.20 / match_type='low', LESSON-002 → 0.80 / match_type='medium') so the hoist behavior is verified by re-ordering rather than preservation of natural order. Also added one similarity to `aaaaaaaa-...` (LESSON-003 @ 0.52) so the (new) section actually renders the card list (otherwise hidden). **Carry-forward state for next session**: bbbbbbbb-... has 2 similarities (LESSON-001 @ 0.20, LESSON-002 @ 0.80); aaaaaaaa-... has 1 (LESSON-003 @ 0.52); cccccccc-... still has zero similarities. Useful for Task 3.6 verification: cccccccc-... (update, null) is the auto-expand-on-needsSearch case; aaaaaaaa-... is the auto-expand-on-noDups case (only 1 sim, but plan says auto-expand on `noDups = candidateCards.length === 0` so won't auto-expand here unless we delete that one similarity). Will likely need to seed a 4th submission with zero similarities + zero submitter target for noDups verification, or temporarily delete aaaaaaaa-...'s similarity.
- **Card-shape unification details** (Task 3.5, Session 11). Plan's snippet doesn't explicitly type the synthetic Case-2 card's `matchType: null` — but TS infers it as literal `null`, which fails to unify with `IntDuplicateMatchType | null` from the `fromDups` branch. Added explicit `as IntDuplicateMatchType | null` and `as string | undefined` annotations on the Case-2 synthetic card object to widen the inferred types. Without these, type-check would have failed on the array element type. The `fromDups` branch already uses `matchLabel: undefined as string | undefined` so the unification works automatically there.
- **Local DB seed strategy for manual smoke** (Tasks 3.3 + 3.4, Session 10). Local Supabase had only one `(new)`-type submission seeded, no `update`-type rows. Inserted two via `mcp__supabase__execute_sql`: `bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb` for `(update, LESSON-001)` and `cccccccc-cccc-cccc-cccc-cccccccccccc` for `(update, NULL)`. Both used the existing seeded teacher `11111111-1111-1111-1111-111111111111` and required `google_doc_id` (NOT NULL constraint discovered on first attempt — the column is required even though the table also has `google_doc_url`). Left in local DB; will be wiped on next `supabase db reset` and are isolated to local. Useful pattern for future PR 3 manual smokes (the same rows can be reused for Tasks 3.5+). Did NOT seed `submission_similarities` rows, so `topDuplicates` is empty for both — fine for Tasks 3.3 + 3.4 because the verification doesn't need duplicate cards rendering. Task 3.5+ may want similarity rows too.
- **Test path follows repo convention, not impl plan path** (Task 3.9, Session 15). Plan §3.9 specifies `src/pages/__tests__/reviewPreselect.test.ts` and `src/pages/__tests__/reviewMismatch.test.ts`, but `find src -name '*.test.*'` confirms zero existing co-located tests use a `__tests__/` subdir — every single test file in `src/` (titleSimilarity, LessonSearchPicker, normalizeSubmissionInputs, IntFormField, IntDataTable, useDebounce, useEnhancedAuth, etc.) is co-located alongside source. `src/__tests__/` exists at repo root but holds only `setup.ts`, `helpers/`, `integration/` — not unit tests. Continued the Sessions 2/3/5 precedent. Documented in commit message.
- **Pre-push reviewer agent gave 6 findings, 0 accepted** (Task 3.11, Session 17). Investigated each per `feedback_bot_review_investigation.md`'s rebuttal-pass-on-every-finding rule. Highest-severity finding (HIGH: "showSearch auto-expand fights manual toggle") was a factual misread of `useEffect` semantics — the agent claimed the effect "fires again because `noDups` is still true" but `useEffect` only fires when its dep array values CHANGE between renders, not on every render where they happen to be truthy. Verified by tracing: user closes hatch → `setShowSearch(false)` → re-render → `needsSearch`/`noDups` unchanged → effect doesn't fire → hatch stays closed. The other 5 findings were defensive hardening (one already documented as pre-existing, one admitted "not urgent given current code path", one redundant with TS contract, one redundant with `??` semantics) per the codebase's "default-reject hardening / hypothetical findings without a concrete bug" rule. Zero fix-up commits. Documented the analysis in the PR body's "Reviewer notes" section so external bots see the rejection rationale. Pattern worth keeping: even when an agent flags HIGH severity, verify against the actual code semantics before applying — a confidently-stated false positive can still be a false positive.
- **PR 3 `gh pr create` body included the rejected-finding rationale** (Task 3.11, Session 17) — chose to surface the pre-push agent's findings + my rejections in the PR description rather than as a separate comment, on the theory that external bots reading the PR may re-flag the same defensive-hardening concerns and the rationale being co-located with the PR shortens triage. If external bots come back with brand-new findings the rationale-section gets ignored; if they re-flag the same hypotheticals I can point to the section. Worst case: zero cost. Best case: saves a round of repeat-rebuttal.
- **Helper input typed as `string \| null \| undefined`, not strict union** (Task 3.9, Session 15). Plan example types `submission_type` as `'new' | 'update' | undefined`; first attempt followed that literally. Type-check then flagged TS2322 at the call site because `submissionData.submission_type` is `string` (from the DB row's loose typing — `lesson_submissions.submission_type` is a string column). Widened the helper input to `string | null | undefined` with an inline comment explaining: the helper only branches on the literal `'update'` value, anything else falls back to `approve_new` (including legacy values like a stored `'reject'`). Test now exercises both `undefined` and `'reject'` for the legacy fallback case, removing the need for `as unknown as` casts in tests.

## Out-of-scope follow-ups captured here

These came up during PR 2 bot triage but were rejected as not-PR-2-scope. Many will be naturally addressed in PR 3 since it touches the same file tree.

- **`ReviewDashboard.tsx` `Promise.all` results don't surface query errors** (Session 17, pre-push reviewer agent finding 4) — the `data ?? []` pattern silently coerces error responses to empty arrays for `profilesResult`, `similaritiesResult`, AND the new (this PR) `lessonsResult`. Reviewers see "0 matches" or missing target titles with no indication of the underlying RLS / network failure. Pre-existing pattern; not a regression introduced by PR 3. Worth a small follow-up logging pass: check each result's `.error` field and `logger.error` it before falling back to empty.
- **DRY auth setup** — `useEffect` for `getUser + onAuthStateChange + subscription cleanup` is now copied verbatim in `SubmissionPage`, `NewSubmissionForm`, and `RevisingSubmissionForm`. A `useCurrentUser()` hook would eliminate it. Three different bots flagged across all three rounds. PR 3 will touch all three files; refactor candidate.
- **DRY `SUBMISSION_STATUS_TO_BADGE`** — duplicated in `NewSubmissionForm.tsx` + `RevisingSubmissionForm.tsx`. Move to `src/utils/` shared constant.
- **DRY Google Doc URL regex** — `/\/document\/d\/([a-zA-Z0-9-_]+)/` appears in `NewSubmissionForm.tsx`, `RevisingSubmissionForm.tsx`, and `supabase/functions/process-submission/index.ts`. Extract to a shared constant + helper. Note: the bot's "ambiguous `-` position" claim is technically wrong for ECMAScript (the dash after a completed range is treated as literal), but the rewrite to `[a-zA-Z0-9_-]` is more universally readable.
- **Keyboard navigation in `LessonSearchPicker`** — Tab works through results; arrow-key + Enter + Escape keyboard nav not implemented. Bot 2 wanted full ARIA combobox pattern with `aria-activedescendant`. ~50 LOC + tests. Defer to PR 3 reviewer flow which also consumes the picker, OR a dedicated a11y follow-up.
- **`fieldset disabled` for Step 2 in `RevisingSubmissionForm`** — current `opacity-50 pointer-events-none` wrapper hides the section visually but doesn't propagate disabled state to AT (only the `<input>` itself is `disabled`). Replace with `<fieldset disabled>` to announce the whole step as disabled.
- **`cantFindOption` discriminated-union prop typing** — currently `cantFindOption?: boolean` + `onCantFind?: () => void` allows the caller to pass `cantFindOption=true` without `onCantFind` and the affordance silently disappears. Discriminated union would prevent this at compile time. Hardening, not a real bug.
- **"Can't find it" disappears when query is cleared** — `runSearch("")` resets `hasQueried=false`, so user loses the affordance after clearing input. Bot 2 round 3 suggested an `everQueried` ref that doesn't reset. UX papercut, not data-correctness.
- **Reset `results` + `hasQueried` on chip clear** — when user picks a lesson then clicks ×, the picker re-renders with previous query/results/`hasQueried=true`, so "can't find it" appears instantly. Could be feature OR bug depending on UX intent. Not critical.
- **Banner enrichment in `ReviewDetail`** — already acknowledged in PR description as PR-3 work: convert inline `style={{}}` to `adm-card border-amber-400 bg-amber-50` Tailwind/admin classes; replace raw UUID with title lookup; full Section 6.1 banner.
- **Edge function 400 error UX** — when extraction fails, frontend shows "Edge Function returned a non-2xx status code" instead of the helpful error message from the response body. Pre-existing in `parseDbError`. Not introduced by this PR. Worth a follow-up.
- **Test rows in TEST DB** — Session 7 left 3 rows in `lesson_submissions` (`phase8b_smoke_p1_new_469`, `phase8b_smoke_p2_revising_target`, `phase8b_smoke_p3_cantfind`). Clutter only; safe to clean up via `DELETE FROM lesson_submissions WHERE google_doc_url LIKE '%phase8b_smoke%'` if desired.
- **Fragile test `cantFindOption={false}` in `LessonSearchPicker.test.tsx`** — empty `waitFor(() => {}, { timeout: 500 })` pattern doesn't actually wait. Bot 2 round 3 noted. Test passes trivially but the assertion is technically before the query completes. Low priority test-reliability nit.

## Session log

### Session 1 — 2026-04-28 03:09 UTC start, 03:50 UTC end — PR 1 shipped end-to-end

Major events:
- Read kickoff prompt, design doc end-to-end, implementation plan through Task 2.2.
- Branched `feat/phase-8b-fk-on-delete-set-null` off local `main` (which had 9 unpushed Phase 8b doc commits from prior sessions).
- Wrote single FK migration; verified locally via MCP; ran `npm run test:rls` (2 pre-existing failures, no new ones); committed as `13565e0`.
- Pre-push code-reviewer agent dispatch returned 0 critical, 1 doc nit (rejected).
- Pushed branch; opened PR #468 with PR body disclosing the 9 doc commits riding along.
- User correction: pre-push review should be agent dispatch (not me); fixed kickoff + impl plan.
- User correction: second post-PR-open agent dispatch is redundant — external bots ARE the second pass; investigation/triage is my job. Re-fixed kickoff + impl plan.
- User correction: missed `claude-review` issue-comment by querying wrong API endpoint. Created `feedback_pr_comment_surfaces.md`; updated kickoff + impl plan with four-surface checklist.
- Triaged 3 `claude-review` findings (BEGIN/COMMIT wrapper, NOT VALID pattern, docs-drift advice); rejected all with documented rationale.
- Verified TEST DB: `confdeltype = 'n'`, `ON DELETE SET NULL` correct.
- User instruction: merge PR + idle while user approves PROD. Merged via rebase (preserves doc-commit history) as `9a6b09e`.
- User FYI: re-verify TEST DB each round that produces DB-affecting fix-ups, not just once. Created `feedback_per_round_test_db_verification.md`; updated kickoff + impl plan rituals.
- PROD apply attempt 1 failed with SASL flake (run `25032406625`); diagnosed as the Apply-step variant of the documented pattern; recommended + queued `gh run rerun --failed`; updated MEMORY.md SASL entry to cover Apply-step variant.
- PROD apply attempt 2 succeeded after re-approval. Verified via `mcp__supabase-remote__execute_sql`: `confdeltype = 'n'`, def `ON DELETE SET NULL`. PR 1 fully shipped.

### Session 2 — 2026-04-28 09:35 UTC start, 09:50 UTC end — Task 2.1 shipped

Major events:
- Read kickoff, design doc, status file, implementation plan from Task 2.1 through Task 2.2.
- Confirmed baseline clean (`type-check` + `lint`).
- Branched `feat/phase-8b-intent-first-submitter-flow` off local `main` (which had 2 unpushed session-1 doc commits — they ride along in PR 2 per user OK).
- Invoked `superpowers:test-driven-development` skill.
- TDD cycle for `titlesAreSimilar`: wrote test (10 cases via `it.each`), confirmed RED (module-not-found), implemented in `src/utils/titleSimilarity.ts` (~20 LOC), confirmed GREEN (10/10 pass).
- Adapted test path to repo convention: co-located alongside source rather than under `__tests__/` (see decisions above).
- Type-check + lint both clean. Committed as `edbc48a`.
- User had pre-authorized one task; ended here rather than continue into Task 2.2 (substantive ~150 LOC component) per kickoff session-scope rules.

### Session 3 — 2026-04-28 09:55 UTC start, 10:05 UTC end — Task 2.2 shipped

Major events:
- Read kickoff, design doc, status file, implementation plan from Task 2.2 through Task 2.3.
- Confirmed baseline clean (`type-check` + `lint`); worktree noise (`M .beads/.gitignore`, `?? .claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone.
- Verified vitest config + supabase export shape + lack of existing `src/components/*.test.tsx` precedent (decision held: co-located per Session 2 precedent rather than under a new `__tests__/` subdir).
- Invoked `superpowers:test-driven-development` skill.
- Wrote test (8 `it` blocks; plan said "6/6" but had 8 — plan typo). Confirmed RED: "Failed to resolve import" — module not found, exactly as expected.
- Implemented `LessonSearchPicker.tsx` (~150 LOC) following plan: debounced 300ms `ilike` query, results list, chip+clear when bound, `cantFindOption` affordance gated on `hasQueried`.
- First GREEN run: 7/8 (test pollution issue — see decisions).
- Fixed `beforeEach` to re-set default `supabase.from` implementation. Re-ran: 8/8 passing.
- `npm run lint` flagged 7 prettier formatting errors + 1 unused-eslint-disable warning. `lint:fix` cleaned all. Re-verify: lint clean, type-check clean, 8/8 still passing.
- Committed as `887449b`. Single-task session per kickoff scope rules — Task 2.3 (App.tsx routes) deferred to next session.

### Session 4 — 2026-04-28 10:08 UTC start, 10:18 UTC end — Tasks 2.3–2.6 shipped (grouped)

Major events:
- Read kickoff, design doc, status file, implementation plan from Task 2.3 through Task 2.6 (and quickly the start of Task 2.7).
- Confirmed baseline clean (`type-check` + `lint`); worktree noise (`M .beads/.gitignore`, `?? .claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone.
- User confirmed Option A: do Tasks 2.3–2.6 as a grouped session (single commit; required to avoid broken intermediate state from cross-references between App.tsx → not-yet-existing form modules).
- Verified `LessonSearchResult` is exported from LessonSearchPicker and that no `pages/__tests__/` convention exists.
- Task 2.3: added two lazy imports (using `@/pages/...` to match existing pattern) + two `<Route>` lines to `src/App.tsx`.
- Task 2.4: full rewrite of `src/pages/SubmissionPage.tsx` (413 lines → 91 lines) as the two-button intent picker. Removed `React` default import (modern JSX transform), `submissionType` / `originalLessonId` / `success` state, the radio fieldset, the conditional original-lesson-ID input, the post-submit duplicates panel, and the AuthModal `handleSubmit` re-call pattern. Replaced with `pendingIntent` state + navigate-on-modal-success.
- Task 2.5: created `src/pages/NewSubmissionForm.tsx` (~150 LOC). Hardcodes `submissionType='new'` + `originalLessonId=null`; uses raw Tailwind layout per plan; `IntPageHeader` + `IntButton` + `IntFormField` + `IntStatusBadge` for primitives.
- Task 2.6: created `src/pages/RevisingSubmissionForm.tsx` (~210 LOC). Step-1 search picker (consuming `LessonSearchPicker` with `cantFindOption=true`) + step-2 URL input gated on `targetReady`; `(update, X)` and `(update, null)` paths both shipped; success copy branches by target presence.
- Type-check clean throughout. First lint run flagged 3 prettier wrapping errors in NewSubmissionForm + RevisingSubmissionForm — all auto-fixable. `lint:fix` cleared all; second lint run clean. 18/18 unit tests still pass.
- Single grouped commit `06ce2de` per plan's "first compilable state" rule.

### Session 5 — 2026-04-28 10:35 UTC start, 10:42 UTC end — Task 2.7 shipped

Major events:
- Read kickoff, design doc, status file, implementation plan from Task 2.7 through start of Task 2.7.5.
- Confirmed baseline clean (`type-check` + `lint`); worktree noise (`M .beads/.gitignore`, `?? .claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone.
- Confirmed edge function test convention by listing files: `detect-duplicates/similarity.test.ts` and `_shared/google-docs-parser.test.ts` are co-located alongside source; no `__tests__/` subdirs in `supabase/functions/`. Adapted plan's `__tests__/` path to co-located (Sessions 2 + 3 precedent).
- Verified `_shared/google-docs-parser.test.ts` imports `from './google-docs-parser'` (no `.ts`) — confirms Vitest bundler resolves Deno-suffixed siblings without the extension, so test imports without `.ts` and `index.ts` imports with `.ts` is the correct split.
- Invoked `superpowers:test-driven-development` skill.
- TDD cycle for `normalizeSubmissionInputs`:
  - Wrote test (9 `it.each` cases per plan).
  - Confirmed RED: "Failed to resolve import './normalizeSubmissionInputs'" — module-not-found, exactly as expected.
  - Implemented helper (~22 LOC of pure TS).
  - Confirmed GREEN: 9/9 pass on first try.
- Wired into `index.ts`: added import on line 5 (`./normalizeSubmissionInputs.ts` with Deno extension), inserted ~25 LOC of coupled-normalization + pre-INSERT FK-target existence check between line 171 (after `googleDocId = docIdMatch[1]`) and line 173 (`// Step 1: Create submission record`), replaced both INSERT fields (`submission_type` and `original_lesson_id`) with normalized values.
- Verified: `type-check` ✓, `lint` ✓ (no prettier flags this round), full test suite 457/457 pass.
- Committed as `1232d1a`. Beads `bd doctor` warning printed during commit (`branch not found: bed61a46…`) — expected per `project_beads_broken.md`; left alone per kickoff "do NOT use bd".

### Session 6 — 2026-04-28 14:30 UTC start, 14:46 UTC end — Tasks 2.7.5 + 2.8 shipped (grouped)

Major events:
- Read kickoff, design doc, status file, implementation plan from Task 2.7.5 through Task 2.9 (and start of PR 3 Task 3.1).
- Confirmed baseline clean (`type-check` + `lint`); worktree noise (`M .beads/.gitignore`, `?? .claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone (5th session running).
- User confirmed grouped Tasks 2.7.5 + 2.8 in one session.
- **Task 2.7.5:** verified `AlertTriangle` was NOT in the existing `lucide-react` import (only `ExternalLink` per line 4); verified `submission_type` ('new'|'update') and `original_lesson_id` (string|undefined) on `SubmissionDetail` interface (lines 51–52); verified target line 913 (`<div className="adm-card">` wrapping decision panel — first of three `adm-card` instances at 527, 879, 913, 964; the one at 913 is the decision-panel wrapper per plan). Two edits: added `AlertTriangle` to import, inserted 33-line banner block. Type-check + lint clean. Committed as `a2287b2`.
- **Task 2.8 prep:** verified the three target text strings against actual rendered output before writing the spec (per kickoff "verify every snippet against current code" rule). All matched. Verified routes are NOT wrapped in `ProtectedRoute` (App.tsx lines 98–100 — bare `<Route>` declarations; auth handled via in-page `AuthModal`), so unauthenticated structural tests work without credentials. Verified `targetReady = Boolean(selectedLesson) || cantFind` (RevisingSubmissionForm line 49) starts false → URL field disabled on first render → `toBeDisabled()` assertion will pass.
- **Task 2.8 implement:** created `e2e/submission-flow.spec.ts` with 3 tests per plan. Adapted test 2's "Add a new lesson" assertion from plan's `getByText` to `getByRole('heading', ...)` for specificity (the text "Add a new lesson" also appears as card text on `/submit`, but only the H1 renders on `/submit/new` so either would work — `getByRole` is more precise and matches the test 1 pattern). Ran `npm run test:e2e -- submission-flow`: 3/3 pass in 4.1s.
- Type-check + lint clean post-Task-2.8. Committed as `f10be9d`.
- Both commits ride on the (still-local-only) feature branch ahead of Task 2.9 push.

### Session 7 — 2026-04-28 14:55 UTC start, 15:30 UTC end — PR 2 shipped end-to-end (Task 2.9)

Major events:

- Read kickoff, status file, implementation plan §Task 2.9 (lines 1466–1520).
- Confirmed baseline clean (`type-check` + `lint`); worktree noise (`M .beads/.gitignore`, `?? .claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone.
- **Pre-push code-reviewer agent dispatch.** Returned 0 critical, 2 high (LessonSearchPicker stale-async race + ilike wildcard escape), 2 medium. Triaged: ACCEPT stale-async (real race, small fix, likely re-flag from external bots) → applied request-id counter pattern + regression test as commit `168f52f`. REJECT ilike escape (zero real-world probability for corpus, hardening below the bar).
- Ran `type-check + lint`, pushed branch, opened **PR #469** with the test-plan body from impl plan lines 1482–1507.
- **Round 1 bots** (~3 min after push): claude-review broad report (8 findings) + claude-component-review CHANGES_REQUESTED with 7 line-comments + claude-database-review pass (no findings) + performance-review pass (no findings). All 4 surfaces queried per `feedback_pr_comment_surfaces.md`.
- Round 1 triage: 4 ACCEPT bundled into commit `c7b4417` — (1) ARIA combobox attrs on picker input + listbox/option roles on ul/li; (2) `!!` prefix on the `||`-of-two-`.length` 0-render condition; (3) `role="alert"` on dynamic error divs in both forms; (4) auth re-trigger via `pendingSubmitRef + useEffect-on-user-change + formRef.requestSubmit()` in both forms. Plus added `HTMLFormElement: 'readonly'` to `eslint.config.js` globals (alongside existing HTMLInputElement/HTMLDivElement/HTMLButtonElement). Round 1 REJECTS (11): DRY (auth, badge, URL regex, status-to-badge), hardening (ilike escape, URL host check), intentional UX (stale-on-clear, ARIA self-rate fail), type-clarity-only (`'new'|'update'|string` widening), already-acknowledged (banner inline styles, raw UUID).
- **Round 2 bots** caught 2 regressions I introduced in round 1: (a) `RevisingSubmissionForm.handleSubmit` lacks the URL-regex client-side check that `NewSubmissionForm.handleSubmit` has — round 1's `requestSubmit()` post-auth bypasses `canSubmit`'s URL gate; (b) ARIA `role="combobox"` without keyboard nav (ArrowUp/ArrowDown/Enter/Escape) is a WCAG 2.1 failure — declaring combobox role tells AT users to expect operable combobox. Both fixed in commit `3a18277`: added URL check to RevisingSubmissionForm + reverted the combobox/listbox/option roles entirely (kept just `aria-label`). All other round 2 findings were re-flags (rejected) or pre-existing minor items (out of round-2 scope per round-cap).
- **Round 3 bots** (after `3a18277` push): 0 critical findings. All flagged items were re-flags from rounds 1-2 (DRY refactors, hardening, UX polish, already-acknowledged PR-3 items) or new minor UX observations. Per round-cap rule (`feedback_pr_bot_review_workflow.md`) no fix-up commit in round 3; documented all in "Out-of-scope follow-ups" section above.
- **TEST DB MCP smoke via chrome-devtools-mcp + mcp__supabase-test__execute_sql.**
  - Confirmed deploy preview hits TEST (`rxgajgmphciuaqzvwmox.supabase.co`) before any writes.
  - Verified `process-submission` v7 deployed with new normalize-aware source (line-by-line via `mcp__supabase-test__get_edge_function`); `normalizeSubmissionInputs.ts` deployed alongside.
  - Verified FK constraint on TEST (`confdeltype = 'n'`, `ON DELETE SET NULL`).
  - Signed in as `admin@test.com` per `reference_test_credentials.md`. Exercised all 3 paths via chrome-devtools-mcp UI submission against deploy preview:
    - **Path 1 — `/submit/new`** → row `submission_type='new', original_lesson_id=NULL` ✓
    - **Path 2 — `/submit/revising` + Apple Pie picked** → row `submission_type='update', original_lesson_id='1r325MQlxLcN1l9TUzIx2qHNH7IXrecHeU6AZL0O0IP0'` resolves to "Apple Pie" via FK ✓
    - **Path 3 — `/submit/revising` + "can't find it"** → row `submission_type='update', original_lesson_id=NULL` ✓
  - Edge function 400s on fake URLs (extraction fails, expected) but INSERT happens BEFORE extraction so row shape is verifiable. Live DOM `role="alert"` attribute confirmed on error div from snapshot (round 1 fix working in production).
- **Merge** — user confirmed rebase strategy (PR 2 has 14 commits including 5 docs that benefit from preservation). Merged via `gh pr merge 469 --rebase` as `898545e`. Local main reset to `origin/main` (after verifying `git diff` showed only the pre-PR-2 baseline) so the 2 local-only session-1 doc commits (whose rebased equivalents now live on origin) get discarded as duplicates. Feat branch deleted locally.
- **PROD edge function deploy** — initially overlooked the manual approval gate (deploy-edge-functions workflow on `main` push `898545e` waited for ~10 min before user noticed). User approved run `25061950611`; deploy completed success in ~30s. Verified live via `mcp__supabase-remote__get_edge_function process-submission`: v29 deployed, ezbr_sha256 `9e6968bcfdab044dfd89d9f203ccb7178b60dde3508789854a5d6a303e2531b3` MATCHES TEST exactly (Phase 7c silent-no-op pattern would have produced different shas; identical shas mean both environments running same compiled artifact). Source includes `normalizeSubmissionInputs` import on line 5, the Phase 8b normalize call, the pre-INSERT FK existence check returning 400 with "Original lesson not found", and INSERT using `normalizedSubmissionType`/`normalizedOriginalLessonId`. Sibling helper `normalizeSubmissionInputs.ts` also deployed.

### Session 8 — 2026-04-28 16:00 UTC start, 16:15 UTC end — PR 3 Task 3.1 shipped (branch + first commit)

Major events:

- Read kickoff, status file, design doc end-to-end, implementation plan §PR 3 (lines 1522–2501).
- Confirmed baseline `npm run type-check && npm run lint` clean; worktree noise (`.beads/dolt*`, `.claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone.
- Branched `feat/phase-8b-reviewer-flow` off local `main` (3 unpushed Session-7 doc commits ride along — same precedent as PRs 1 + 2 carried prior-session docs).
- **Task 3.1 verification before edit:** grepped `ReviewDetail.tsx` for the impl plan's anchor symbols — `interface SimilarityWithLesson` at line 33, `interface SubmissionDetail` at line 46, `loadSubmission` at line 219, `similaritiesWithLessons` initialization at line 240, `setSubmission(fullSubmission)` at line 300. All matched the plan's expectations. Verified `lessons_with_metadata` view exposes `summary` (line 1080) + `file_link` (line 1051) in `database.types.ts` before adopting plan's `.select(...)` extension.
- Applied 3 edits to `src/pages/ReviewDetail.tsx`: (1) `SubmitterTargetLesson` interface added between `SimilarityWithLesson` and `SubmissionDetail` with the documented nullability rationale; (2) `submitterTargetLesson?: SubmitterTargetLesson | null` added to `SubmissionDetail`; (3) ~35-line fetch block inserted between similarities-with-lessons assembly (line ~261) and the `submission_reviews` query (line ~263), gated on `submitterTargetId && !targetInRenderedTopFive` against the SLICED top-5 (per impl plan's CRITICAL note); (4) `submitterTargetLesson` attached to `fullSubmission` payload at line ~342.
- `npm run type-check && npm run lint` clean. Reviewed diff (~56 inserted lines, no deletions). Committed as `6e5c11c`.
- Single-task session per kickoff session-scope rules — Task 3.2 (banner replacement, ~50 LOC of new JSX with three intent states + a degraded fallback) is substantively different from Task 3.1 (data fetch) and warrants its own session. Stopped at natural commit boundary.

### Session 9 — 2026-04-28 16:25 UTC start, 16:35 UTC end — Task 3.2 shipped

Major events:

- Read kickoff, status file, design doc end-to-end, implementation plan §Task 3.2 (lines 1654-1741) plus the Task 3.3+3.4 coupled section (lines 1743-1834) for context on the next session.
- Confirmed baseline `npm run type-check && npm run lint` clean; worktree noise (`.beads/dolt*`, `.claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone (6th session running).
- Located the PR 2 throwaway banner via `grep -n "submission?.submission_type === 'update'" src/pages/ReviewDetail.tsx` → line 974. Read lines 965-1019 to confirm the anchor block (lines 969-999, an inline-styled `adm-card` with `borderColor: '#fbbf24', background: '#fffbeb'` and AlertTriangle).
- Single Edit tool call: replaced the 31-line PR 2 block with the 70-line IIFE from impl plan §3.2. Four explicit states (green new, blue update+target+title, yellow update+target+no-title, yellow update+null) with the `targetTitle` resolution chain documented inline (`submitterTargetLesson?.title` first, then `topDuplicates.find(...)?.lesson?.title` fallback, then null). Used `&apos;` for apostrophes in user-visible copy.
- (Note: an "API error" was reported mid-call; the Edit had actually landed. Verified via `grep -n "Phase 8b: binding-intent banner"` showing the new block at line 969 and the old comment string absent — single edit, not duplicated.)
- First lint run flagged 2 prettier nits (`<AlertTriangle ... />` props should be single-line, not 3-line). `npm run lint:fix` cleared both. Re-verify: type-check ✓, lint ✓.
- Reviewed `git diff` — clean +61 / -28 line shape; nothing else changed in the file. Committed as `8c4c354`. `bd doctor` warning during commit (`branch not found: bed61a46…`) — expected per `project_beads_broken.md`; left alone.
- Single-task session per kickoff session-scope rules — Tasks 3.3 + 3.4 are coupled and substantive (~30+ LOC across pre-selection block + radio enable + submit-button gate + inline guidance text); they go together in a single session rather than splitting.

### Session 10 — 2026-04-28 16:50 UTC start, 17:00 UTC end — Tasks 3.3 + 3.4 shipped (coupled)

Major events:

- Read kickoff, status file, design doc, implementation plan §3.3-3.4 (lines 1743-1834).
- Confirmed baseline `npm run type-check && npm run lint` clean; worktree noise (`.beads/dolt*`, `.claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone (7th session running).
- Re-grepped `ReviewDetail.tsx` for current line numbers (impl plan numbers were pre-Task-3.2; +17 net shift after Task 3.1 + 3.2). Anchors found: `if (reviews && reviews.length > 0)` at line 358, approve_update label/radio block at lines 1051-1068, merge button inside IntDecisionBar at lines 1116-1125. `submissionData` is the raw row variable name in `loadSubmission` scope.
- Read `src/components/Internal/IntDecisionBar.tsx` to confirm the actions slot is flex-styled for buttons → guidance text goes OUTSIDE IntDecisionBar (not inside). Checked existing pattern: `saveError` block at line 1101-1105 sits outside IntDecisionBar above it.
- Three coupled `Edit` calls to `src/pages/ReviewDetail.tsx`:
  1. Pre-selection block (17 lines) inserted between line 381 (closing `}` of prior-review restoration) and line 382 (`} catch`). Comment block on the guard reasoning included.
  2. Approve_update radio cleanup: replaced lines 1051-1068 with simplified label (no opacity-0.5 dimming, no `disabled` prop, no parenthetical hint).
  3. Inline guidance `<p>` block inserted right after `</IntDecisionBar>` and before the closing `</div>`.
- `npm run type-check && npm run lint` clean on first try (no prettier nits this round). Reviewed diff: +24 / -6 lines, no other file changes.
- **Manual smoke** per impl plan §3.4 Step 4. User reminded to use Supabase MCP not psql. Used `mcp__supabase__execute_sql` for everything DB-related.
  - Listed local users: `admin@test.com` (role=admin) and `reviewer@test.com` (role=reviewer) both seeded.
  - Picked a sample lesson_id (`LESSON-001` = "Garden to Table: Growing Tomatoes") and teacher_id (`11111111-1111-1111-1111-111111111111`).
  - Inserted two seed rows: `bbbbbbbb-...` for `(update, LESSON-001)` and `cccccccc-...` for `(update, NULL)`. First attempt failed on `google_doc_id` NOT NULL — added the field on retry, both rows inserted clean.
  - Started dev server (`npm run dev` in background, ID `bxhb16w0u`); polled until `:5173` returned 200.
  - Loaded chrome-devtools-mcp tools via ToolSearch (foreground page). Signed in as `reviewer@test.com` / `password123` via the User-account → Sign In modal.
  - Visited `/review/cccccccc-...` (update, null): a11y snapshot confirmed yellow banner copy "Submitter says: Updating, but couldn't find target — please search to identify.", `Merge into existing` radio `checked`, `MERGE & ARCHIVE` button `disabled`, inline guidance "Pick a target lesson to merge into, or change to Approve as new." Screenshot at `/tmp/phase8b-update-null.png` confirms the amber color + button greyed out.
  - Visited `/review/bbbbbbbb-...` (update, X): a11y snapshot confirmed blue banner copy "Submitter says: Updating Garden to Table: Growing Tomatoes" (Task 3.1's off-list fetch correctly resolved the title since topDuplicates is empty), `Merge into existing` radio `checked`, `MERGE & ARCHIVE` button enabled (no disabled attribute), no inline guidance. Screenshot at `/tmp/phase8b-update-x.png` confirms the blue color + button active.
- All four behaviors per impl plan §3.4 step 4 verified end-to-end through real UI smoke.
- Committed as `64a9feb` with detailed multi-paragraph commit message documenting the three coupled edits, the radio chrome cleanup decision, the not-applied submit-button literal-disabled condition, and the manual smoke evidence.
- Single grouped session per kickoff session-scope rules — Tasks 3.3 + 3.4 are the same conceptual change ("decision/target pre-selected from submitter intent + submit gated on `approve_update + !target`"), and the impl plan groups them together. Stopped at natural commit boundary.

### Session 11 — 2026-04-28 17:15 UTC start, 17:30 UTC end — Task 3.5 shipped

Major events:

- Read kickoff, status file, design doc end-to-end, implementation plan §3.5 (lines 1835-1974) plus the §3.6 boundary (lines 1976-2113) for context on the next session's modification of the Task 3.5 useMemo.
- Confirmed baseline `npm run type-check && npm run lint` clean; worktree noise (`.beads/dolt*`, `.claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone (8th session running).
- **Pre-flight verifications all passed:** (1) `IntDuplicateCard` badge prop = `matchLabel` confirmed at `src/components/Internal/IntDuplicateCard.tsx:15` (optional string, falls back to default match-type label if absent); (2) `normalizeMatchType` helper exists at `ReviewDetail.tsx:111` returning `IntDuplicateMatchType | null`; (3) existing `topDuplicates.map` wrapper at `ReviewDetail.tsx:951-984` (lines shifted post-3.2/3.3+3.4 from plan's stated 885-908) — re-grepped to confirm. Also confirmed the line-581 `topDuplicates.length` usage is the top-of-page "X possible dup(es)" pill, separate concern, do not touch.
- **Two coupled `Edit` calls** to `src/pages/ReviewDetail.tsx`: (1) inserted `candidateCards` useMemo between `topDuplicates` (line 488) and `parsedContent` (was line 491) — three-case logic per impl plan with explicit type annotations on the synthetic Case-2 card object (see decisions); (2) replaced the existing `topDuplicates.map` block 951-984 with `candidateCards.map`, flipped wrapper `topDuplicates.length > 0` → `candidateCards.length > 0`, and added the conditional section header.
- `npm run type-check && npm run lint` clean on first try (no prettier nits this round).
- **Manual smoke** per impl plan §3.5 step 4 with 4 visual checks via chrome-devtools-mcp (foreground page already signed in as `reviewer@test.com` from Session 10, persisted browser state):
  1. **Off-list (Case 2)** on `bbbbbbbb-...` (no similarities, original_lesson_id=LESSON-001, Task 3.1's off-list fetch loads Garden to Table title). Snapshot confirmed: header "CANDIDATE MATCHES", single Garden to Table card with SUBMITTER'S CHOICE badge + 0% similarity. Screenshot at `/tmp/phase8b-task35-case2-offlist.png`.
  2. **In-list hoist (Case 1)** on same row after seeding 2 similarities (LESSON-001 @ 0.82 + LESSON-002 @ 0.42). Snapshot confirmed hoist visible. Then UPDATE'd to flip scores (LESSON-001 → 0.20 with match_type='low', LESSON-002 → 0.80 with match_type='medium') to verify the hoist actually re-orders rather than preserving natural DESC order. Re-loaded: header "CANDIDATE MATCHES", LESSON-001 (20%, badge) at position 0 *despite* lower score, LESSON-002 (80%, no badge) at position 1. **Hoist confirmed re-ordering**. Screenshot at `/tmp/phase8b-task35-case1-hoist.png`.
  3. **Badge anchoring** — clicked LESSON-002 to select it. Confirmed: LESSON-001 keeps its SUBMITTER'S CHOICE badge (no longer pressed), LESSON-002 becomes pressed+focused but shows no badge. Selection ≠ submitter's-choice-badge anchoring. Verified.
  4. **(new) header reverts (Case 3)** on `aaaaaaaa-...` after seeding one similarity (LESSON-003 @ 0.52). Header reverts to "POSSIBLE DUPLICATES" because no card has the badge. Banner is green "Submitter says: New lesson" (from Task 3.2). Decision pre-selected to "Approve & publish" (Tasks 3.3+3.4). Button is "PUBLISH LESSON". Screenshot at `/tmp/phase8b-task35-case3-new.png`.
- Committed as `f4a1d71` with detailed multi-paragraph message documenting the +81/-26 shape, the three useMemo cases, the wrapper-condition flip, and the manual smoke evidence.
- Stopped dev server (PIDs 13525, 94359, 95540 — `lsof -ti :5173` returned empty after kill). Final baseline check: type-check + lint both clean.
- Single-task session per kickoff session-scope rules — Task 3.6 (search escape hatch) is substantive (~30+ LOC across 2 new useState hooks, 2 useEffects, useMemo extension to add a 4th case for "Reviewer searched" badge, disclosure JSX rendering `LessonSearchPicker`) and warrants its own session.

### Session 13 — 2026-04-28 19:15 UTC start, 19:30 UTC end — Task 3.7 shipped

Major events:

- Read kickoff, status file, design doc end-to-end (loaded), implementation plan §3.7 (lines 2113-2178) plus §3.8 + §3.9 boundaries for context on the next session and the helper-extraction strategy.
- Confirmed baseline `npm run type-check && npm run lint` clean; worktree noise (`.beads/dolt*`, `.claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone (10th session running).
- **Decision flagged before coding (option b on the helper).** Plan §3.7 step 2 imports `shouldShowMismatchWarning` from `@/pages/reviewMismatch` but that file doesn't exist yet (grep + ls confirmed). Plan §3.9 step 2 reads "Then in Task 3.7's render code, gate the actual mismatch banner on `shouldShowMismatchWarning(...)`" — i.e., §3.9 only adds the test file, the helper is created in §3.7. Went with option b (create the helper in §3.7) rather than option a (inline now, refactor later) to avoid §3.9 reinventing what's already there. User confirmed "proceed".
- **Helper file created.** `src/pages/reviewMismatch.ts` (26 lines): exports `shouldShowMismatchWarning({ selectedTarget, submitterTargetId, topDuplicateIds, searchPickedId })`. Returns false if no selected target OR if selected target is the reviewer's search pick (manual = deliberate confirmation). Returns true only when selected target matches submitterTargetId OR appears in topDuplicateIds (auto-pick).
- **`ReviewDetail.tsx` wired up.** Three Edit calls: (1) added two imports immediately after the existing `LessonSearchPicker` import; (2) added `showMismatch` derived value (call to helper, no hook) right after the Task 3.6 derived block (`needsSearch / noDups / searchHelpText`); (3) inserted yellow IIFE warning block between the candidate-matches list (line 1096 closing `)}`) and the Task 3.6 search escape hatch comment (line 1098). The IIFE: returns null if `!showMismatch`, resolves `targetTitle` from `candidateCards.find((c) => c.id === selectedDuplicate)?.title`, resolves `submissionTitle` from `submission?.extracted_title`, returns null if either is empty, returns null if `titlesAreSimilar(targetTitle, submissionTitle)`, otherwise renders an `mt-2 p-2 bg-amber-50 border border-amber-200` div with the "Heads up: submitter linked to ... but submission's extracted title is ..." copy.
- `npm run type-check && npm run lint` clean on first try (no prettier nits).
- **Manual smoke** via chrome-devtools-mcp against local dev server (started in background, polled until `:5173` returned 200). Browser state persisted from Session 12 — already signed in as `reviewer@test.com`.
  1. **Fire path:** navigated to `/review/bbbbbbbb-...` (update, LESSON-001 = "Garden to Table: Growing Tomatoes", extracted_title = "Test Update With Target"). `evaluate_script + body.innerText` substring search confirmed `"Heads up: submitter linked to "Garden to Table: Growing Tomatoes" but submission's extracted title is "Test Update With Target""` rendered. Jaccard between {"test","update","with","target"} and {"garden","to","table","growing","tomatoes"} is 0/9 = 0 → < 0.3 → titlesAreSimilar = false → warning fires. ✓
  2. **Suppress path:** updated extracted_title via `mcp__supabase__execute_sql` to "Garden to Table: Growing Tomatoes (2026 Refresh)". Reloaded, took a11y snapshot + ran probe — `body.innerText` does NOT contain "Heads up: submitter linked to". Jaccard = 5/7 ≈ 0.71 → > 0.3 → titlesAreSimilar = true → suppress fires correctly. ✓
  3. **Manual-pick suppression** (search escape hatch active) covered by helper unit tests in Task 3.9 — `shouldShowMismatchWarning({ selectedTarget: 'X', searchPickedId: 'X', ... })` should return false. The state machine for selectedSearchLesson is already verified by Task 3.6's smoke; gating logic is purely the helper's responsibility.
- Restored extracted_title to "Test Update With Target" before commit so future sessions reading the status file see what's documented in DB (matches Session 12 carry-forward).
- Stopped dev server (background task `bs07n4srg` SIGTERM'd; exit 143 expected).
- Committed as `ac75e95` with multi-paragraph message documenting the helper extraction (option b), the IIFE wiring, the manual smoke evidence (fire + suppress), and the temporary extracted_title manipulation.
- Single-task session per kickoff session-scope rules — Task 3.8 is the queue-badge work in two separate files (`ReviewDashboard.tsx` + `IntQueueRow.tsx`) with a probable batched-fetch addition for `lessonTitleMap`, substantively different from Task 3.7 and warrants its own session.

### Session 12 — 2026-04-28 17:45 UTC start, 18:00 UTC end — Task 3.6 shipped

Major events:

- Read kickoff, status file, design doc end-to-end, implementation plan §3.6 (lines 1976-2113).
- Confirmed baseline `npm run type-check && npm run lint` clean; worktree noise (`.beads/dolt*`, `.claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone (9th session running).
- **Pre-flight verifications all passed:** (1) `LessonSearchResult` exported from `src/components/LessonSearchPicker.tsx:6`; (2) existing useState block at lines 136-145 (main state group) — added new hooks immediately after `saveError`; (3) Task-3.5 candidateCards useMemo at lines 497-545; deps `[submission, topDuplicates]` — refactored to add `selectedSearchLesson` and Case-4 append; (4) `LessonSearchPicker` props confirmed: `selected, onSelect, onClear, cantFindOption?, onCantFind?`.
- **Five `Edit` calls** to `src/pages/ReviewDetail.tsx`: import + 2 useState + candidateCards refactor + derived values & useEffects + disclosure JSX. +118 / -24 net shape.
- First lint run flagged 2 prettier nits (`||` line-break + extra blank line); `lint:fix` cleared both. Type-check + lint both clean.
- **Manual smoke** via chrome-devtools-mcp against local dev server, signed in as `reviewer@test.com`. Six behaviors verified: default-collapsed (`bbbbbbbb-...`, `aaaaaaaa-...` pre-cleanup), auto-expand on `needsSearch` (`cccccccc-...`), auto-expand on `noDups` only (`aaaaaaaa-...` after temp DELETE of LESSON-003 sim), help text variants (3 branches: needsSearch / has target / generic), override path (search "compost" → pick Composting 101 → REVIEWER SEARCHED badge card appended, picker chip mode, selectedDuplicate flips), onClear path (chip cleared → search-mode resumes, REVIEWER SEARCHED card removed, selectedDuplicate clears, MERGE disabled with inline guidance).
- **chrome-devtools-mcp `click()` tool oddity discovered.** Reproduced twice on fresh reloads: clicking the search-result button via the `click` tool triggered the MERGE & ARCHIVE handler (validation alert appeared) instead of the picker's `onSelect`. Same button via `evaluate_script + dispatchEvent(new MouseEvent('click', { bubbles: true }))` worked correctly. Code path is correct (verified via `__reactProps`); the `click` tool may have a coordinate-or-simulation bug when interacting with vertically-stacked buttons over a form-control region. Workaround documented for future use: dispatchEvent via evaluate_script for any list-item-style click in the picker. NOT a Phase 8b bug.
- **Restored** `aaaaaaaa-...`'s LESSON-003 similarity row (0.52, medium) before stopping dev server, so the next session's seed state matches Session 11's documented carry-forward.
- Stopped dev server (background task `b0bdkx11b` SIGTERM'd; exit 143 expected). Final baseline check: type-check + lint both clean.
- Committed as `26cfb0f` with detailed multi-paragraph message documenting the 5 coupled changes, the meta-string plan deviation rationale, the manual smoke evidence (6 behaviors), and the chrome-devtools-mcp tool oddity.
- Single-task session per kickoff session-scope rules — Task 3.6 was the substantive piece (5 coupled edits + 6-case manual smoke). Task 3.7 (title mismatch helper) is a separate concern and warrants its own session.

### Session 14 — 2026-04-28 20:15 UTC start, 20:30 UTC end — Task 3.8 shipped

Major events:

- Read kickoff, status file head (lines 1–200), implementation plan §3.8 (lines 2160–2266); confirmed baseline `npm run type-check && npm run lint` clean. Worktree noise (`.beads/dolt*`, `.claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone (10th session running).
- **Verified plan against current code:** `IntQueueRow.tsx:65` has the existing `<span className="adm-pill">{type === 'new' ? 'New lesson' : 'Update'}</span>` (replaced). `ReviewDashboard.tsx:254-269` has the `<IntQueueRow>` instantiation; no `lessonTitleMap` exists, so the third Promise.all entry is a fresh add. `original_lesson_id` is on the existing `Submission` interface as `string | undefined`, plan's new prop typed `string | null` — added explicit `?? null` coercion at the call site.
- **Two file edits, four `Edit` calls total.**
  1. `IntQueueRow.tsx`: added `originalLessonId?: string | null` + `originalLessonTitle?: string | null` to `IntQueueRowSubmission`.
  2. `IntQueueRow.tsx`: replaced the `<span className="adm-pill">…</span>` chip with the three-branch IIFE per §3.8 (green NEW chip / blue UPDATE chip + truncated title span / amber UPDATE? chip + "needs reviewer search"). Truncation logic: `fullTitle.length > 40 ? \`${fullTitle.slice(0,40).trim()}…\` : fullTitle`. The CSS `truncate` class on the title span is belt-and-braces against extra-long un-truncated content.
  3. `ReviewDashboard.tsx`: added `originalLessonTitle?: string | null` to `Submission` interface; built `targetLessonIds` distinct collection from `data.map((s) => s.original_lesson_id).filter(Boolean)`; added third entry to existing `Promise.all` (`supabase.from('lessons').select('lesson_id, title').in('lesson_id', targetLessonIds)`); built `lessonTitleMap: Record<string, string>` via `.reduce()`.
  4. `ReviewDashboard.tsx`: merged `originalLessonTitle` into each Submission row in the `setSubmissions` mapping (`submission.original_lesson_id ? lessonTitleMap[…] ?? null : null`); extended `<IntQueueRow>` `submission={…}` with `originalLessonId: submission.original_lesson_id ?? null` + `originalLessonTitle: submission.originalLessonTitle ?? null`.
- `npm run type-check && npm run lint` clean on first try (no prettier nits).
- **Manual smoke** via chrome-devtools-mcp against local dev server. Browser session was already signed in as a reviewer from prior sessions (verified by navigating to `/review` and the queue rendered without redirect). All three seeded rows visible:
  - `aaaaaaaa-...` (new) → green NEW chip; DOM probe `getComputedStyle(...).backgroundColor` returned `oklch(0.962 0.044 156.743)` (Tailwind `bg-green-100`); color `oklch(0.448 0.119 151.328)` (`text-green-800`). ✓
  - `bbbbbbbb-...` (update, LESSON-001) → blue UPDATE chip + title span text "Garden to Table: Growing Tomatoes"; computed `bg-blue-100` `oklch(0.932 0.032 255.585)` + `text-blue-800` `oklch(0.424 0.199 265.638)`; tooltip `title=` and `aria-label="Updating lesson: Garden to Table: Growing Tomatoes"` both populated. ✓
  - `cccccccc-...` (update, null) → amber UPDATE? chip + "needs reviewer search" text; computed `bg-amber-100` `oklch(0.962 0.059 95.617)` + `text-amber-800` `oklch(0.473 0.137 46.201)`; chip carries `aria-label="Submitter is updating but couldn't find target — needs reviewer search"`. ✓
- Truncation case (>40 chars + ellipsis) NOT exercised — local seed's LESSON-001 title is only 33 chars. Skipped: branch is exercised by impl-plan's literal `fullTitle.slice(0, 40)` plus the visual `truncate` class which the plan instructs as belt-and-braces. Will surface naturally on PROD data.
- **Plan adaptation worth noting:** plan's aria-label was `\`Updating lesson: ${fullTitle}\`` even when `fullTitle` is empty (would emit "Updating lesson: " with trailing colon). Implemented as `fullTitle ? \`Updating lesson: ${fullTitle}\` : 'Updating lesson'` — minor cosmetic improvement to the AT label when title fetch fails. Not a behavior change for the happy path.
- Stopped dev server (background task `b7kg9idih` SIGTERM'd; exit 144 from `kill %1`, expected).
- Committed as `a8c4da7` with the multi-paragraph commit message from the plan §3.8 step 4. Beads `bd doctor` warning printed (`branch not found: bed61a46…`) — expected per `project_beads_broken.md`; left alone per kickoff "do NOT use bd".
- Screenshot at `/tmp/phase8b-task38-three-state-badges.png`.
- Single-task session per kickoff session-scope rules — Task 3.9 (extract `computePreselection` + add unit tests for both helpers; touches three files including modifying `ReviewDetail.tsx` to import from the new helper file) is the next substantive task and warrants its own session.

### Session 15 — 2026-04-28 20:25 UTC start, 20:30 UTC end — Task 3.9 shipped

Major events:

- Read kickoff, status file, implementation plan §3.9 (lines 2268-2381). Confirmed baseline `npm run type-check && npm run lint` clean. Worktree noise (`.beads/dolt*`, `.claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone (11th session running).
- **Decision at session start: test path = co-located, not `__tests__/`.** Ran `find src -name '*.test.*'` — every existing test file is co-located alongside source (titleSimilarity, LessonSearchPicker, normalizeSubmissionInputs, IntFormField, etc.). `src/__tests__/` exists at root level but holds only `setup.ts`/`helpers/`/`integration/`. Continued the Sessions 2/3/5 precedent. Documented in commit message and decisions log.
- Invoked `superpowers:test-driven-development` skill.
- **TDD cycle for `computePreselection`:**
  - Wrote `src/pages/reviewPreselect.test.ts` (4 cases per impl plan: new, update+target, update-no-target, legacy unknown).
  - Confirmed RED: `npx vitest run src/pages/reviewPreselect.test.ts` → "Failed to resolve import @/pages/reviewPreselect" — module not found, exactly as expected.
  - Implemented `src/pages/reviewPreselect.ts` with `PreselectionInput` + `PreselectionResult` interfaces. **First attempt** typed `submission_type: 'new' | 'update' | null | undefined` per the plan's narrow-union example.
  - Confirmed GREEN: 4/4 pass on first try.
- **`reviewMismatch.test.ts` written post-hoc against existing helper.** Helper was extracted to `src/pages/reviewMismatch.ts` in Session 13 (Task 3.7) explicitly to enable §3.9 testing — not retro-fitting an existing untested behavior, but a deliberate two-step extract-then-test. Wrote 5 tests per impl plan §3.9 step 2 (no selection, submitter-bound, dup-detector, manual-pick, manual-pick-coincident-with-dup). Both files pass: 9/9.
- **Caller refactor in `src/pages/ReviewDetail.tsx`.** Added `computePreselection` import; replaced inline 8-line pre-selection block at lines 397-407 with 7-line helper call (same `setDecision` shape, same `setSelectedDuplicate` only-when-target-non-null guard).
- **First post-refactor type-check failed.** TS2322 at line 401: `submissionData.submission_type` is `string` (DB row's loose typing — `lesson_submissions.submission_type` is a string column), but my helper input was the narrow `'new' | 'update' | null | undefined`. **Fix:** widened `PreselectionInput.submission_type` to `string | null | undefined` with an inline comment explaining the helper still only branches on the literal `'update'` value. Test exercise expanded to cover both `undefined` and arbitrary string `'reject'` for the legacy fallback case (removed the `as unknown as` cast since the wider type makes it unnecessary).
- Two prettier nits surfaced after the test edit; `lint:fix` cleared both (no manual rewrite needed).
- **Final verification:** 9/9 new tests pass; full unit suite 467/467 pass (was 458, +9 from new tests); type-check + lint both clean.
- Reviewed `git diff src/pages/ReviewDetail.tsx` (+8/-7 line shape, behaviorally identical refactor — only difference is helper-call instead of inline conditional).
- Committed as `733e14e` with multi-paragraph message documenting the helper extraction, the input-type widening rationale, the test path decision, and the suite count.
- `bd doctor` warning during commit (`branch not found: bed61a46…`) — expected per `project_beads_broken.md`; left alone per kickoff "do NOT use bd".
- Single-task session per kickoff session-scope rules — Task 3.10 (E2E scaffolding, mostly `.skip` markers per plan) is the next task and warrants its own session.

### Session 16 — 2026-04-28 20:45 UTC start, 20:50 UTC end — Task 3.10 shipped

Major events:

- Read kickoff, status file head + last 3 sessions (lines 1-100 + 230-373), implementation plan §3.10 (lines 2383-2427) plus §3.11 boundary for next-session orientation. Confirmed baseline `npm run type-check && npm run lint` clean. Worktree noise (`.beads/dolt*`, `.claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone (12th session running).
- **Pre-flight per status-file watch-outs.** `ls e2e/` + `grep -rn 'admin@test\|password123\|signIn\|login' e2e/` + `ls e2e/*.setup.*` confirmed: no `auth.setup.ts` exists, no fixture imports auth credentials, every existing spec defers login per `submission-flow.spec.ts:27` comment ("The login/submit happy paths require seeded credentials and TEST DB — run those in a dedicated authenticated suite separately"). Plan-literal `.skip` is the right call. User confirmed before coding.
- **One file written.** `e2e/review-flow.spec.ts` (88 lines) with three `test.skip(...)` markers per impl plan §3.10. Each body has assertion logic spelled out in commented-but-realistic Playwright calls — specific text patterns matched to actual rendered banner copy from Sessions 9-14 (`"Submitter says: New lesson"`, `"Submitter says: Updating Garden to Table"`, `"Submitter says: Updating, but couldn't find target"`, `"Pick a target lesson to merge into, or change to Approve as new"`, `"Use this to find the lesson the submitter couldn't"`). Top-of-file comment block explains the skip rationale (no auth fixture; no seed mechanism) and points to the unit test files + Sessions 10-14 manual smoke screenshots as the current regression guard.
- **First lint run flagged unused `page` arg** on each `.skip`'d test (rule: `@typescript-eslint/no-unused-vars` allowed-args `/^_/u`). Two clean options: (a) `_page` underscore-prefix in destructure, (b) drop `page` arg entirely. Chose (b) — matches plan snippet's literal `async () => {...}` signature exactly. Comments still illustrate `page.getByText(...)` calls so the param gets added back naturally when an implementer uncomments.
- **`expect` import retained with `void expect;`** since it's only referenced in comments today. Dropping the import would cost the implementer two extra characters when uncommenting; the void-cast pattern is documented inline.
- **Verification:** `npx playwright test e2e/review-flow.spec.ts --list` reports `Total: 3 tests in 1 file` with all three signatures parsed correctly. `npm run type-check && npm run lint` clean. Did NOT run the spec — `.skip` would be a no-op anyway and starting the dev server adds noise.
- Committed as `50fa1c9` with multi-paragraph message documenting the skip rationale (auth fixture absence), the regression-guard fallbacks (unit tests + Sessions 10-14 smoke screenshots), the void-cast pattern, and the plan-literal verification (`--list` output).
- `bd doctor` warning during commit (`branch not found: bed61a46…`) — expected per `project_beads_broken.md`; left alone per kickoff "do NOT use bd".
- Single-task session per kickoff session-scope rules — Task 3.11 (pre-PR check + push + `gh pr create` + per-PR ritual: own-reviewer agent dispatch + 4-surface bot triage + investigation + round-cap-after-2) is the substantive PR-opening work and warrants its own session. Task 3.10 was the small scaffolding stub.
- Branch state at session end: 20 commits ahead of `origin/main` (1 task commit + the upcoming session-doc commit will make 21 after this status file lands). Not pushed.

### Session 17 — 2026-04-28 21:05 UTC start, 21:35 UTC end — Task 3.11 step 1-4 (pre-PR ritual + PR open)

Major events:

- Read kickoff prompt + status file head (lines 1-100) + status file tail starting at Session 14 (lines 314-401) + impl plan §3.11 (lines 2429-2475). Confirmed baseline `npm run type-check && npm run lint` clean. Worktree noise (`.beads/dolt*`, `.claude/scheduled_tasks.lock`) confirmed unrelated to Phase 8b — left alone (13th consecutive session ignoring them).
- **Per-PR ritual step 1 — pre-push reviewer agent dispatch.** Briefed `feature-dev:code-reviewer` with the PR scope (PR 3 of 3, 20 commits, 9 source files), the locked decisions, the rejection biases (default-reject hardening / hypothetical / no concrete bug), the per-finding format requirement (severity + file:line + concrete impact + evidence + fix + confidence), and a 1500-word cap. Agent returned 6 findings: 0 critical, 2 high, 2 medium, 2 low.
- **Investigated each finding per `feedback_bot_review_investigation.md`'s rebuttal-pass-on-every-finding rule.** Wrote rebuttal for all 6 (including the "low" ones — easy-fix is not an excuse to skip the rebuttal step):
  1. **HIGH: `showSearch` auto-expand fights manual toggle** — REJECTED. Agent claimed user-close gets re-expanded "because `noDups` is still true and the effect fires again." Factually wrong about `useEffect` semantics: deps fire on CHANGE, not on every render where the value happens to be truthy. Traced the sequence (close → setShowSearch(false) → re-render → deps unchanged → no fire → stays closed). The toggle works correctly.
  2. **HIGH: Banner IIFE falls through to green** — REJECTED. Agent's own evidence (line 348 coercion + interface narrowing at line 74) demolishes the claim. Defensive hardening with no current bug; redundant with the upstream contract.
  3. **MEDIUM: `onClear` stale closure** — REJECTED. Synchronous event handler reads closure value once; agent's "fast double-fires" scenario is hypothetical and doesn't apply to a single user click on a Clear button. Code is correct as written.
  4. **MEDIUM: `similaritiesResult.error` swallowed in `ReviewDashboard.tsx`** — REJECTED as out-of-scope. The `data ?? []` pattern is pre-existing for `profilesResult` AND `similaritiesResult`; the new (this PR) `lessonsResult` follows the same convention. Captured as a follow-up rather than a fix-up.
  5. **LOW: `candidateCards` memo deps include whole `submission`** — REJECTED. Agent admitted "not urgent given the current code path." `setSubmission` only fires once per load; no current bug.
  6. **LOW: missing test for `original_lesson_id = undefined`** — REJECTED as redundant. The `??` operator coalesces null/undefined identically; testing both is testing the language, not the helper. Existing 4 cases adequately cover the meaningful branches.
- **Surfaced rebuttals to user, asked for confirm.** User confirmed all 6 rejections. Zero fix-up commits.
- **Per-PR ritual steps 2-4:** Re-ran `npm run type-check && npm run lint` (clean). `git push -u origin feat/phase-8b-reviewer-flow` succeeded with branch tracking set. `gh pr create` opened **PR #470** (https://github.com/danfeder/esnyc-lesson-search-react/pull/470, 901 additions / 85 deletions / 21 commits) with the captured impl-plan §3.11 title + body, augmented with a "Reviewer notes" section documenting the 6-rejected-findings pre-push analysis (so external bots reading the PR see the analysis already done; if they re-flag the same hypotheticals I can point to the section).
- **Status file updates.** Header updated for Session 17. Task 3.11 step 1-4 added to "Done" section. "In flight" section updated to point at PR #470 awaiting bot review. Two new "Decisions made during execution" entries (the 6-rejected-findings pattern + the choice to embed rationale in PR body). One new "Out-of-scope follow-ups" entry (`Promise.all` error swallowing in ReviewDashboard.tsx). Session 17 log entry appended (this entry).
- **Single-task-ish session per kickoff session-scope rules.** Step 1-4 is the natural commit boundary (PR open, pushed, no fix-ups outstanding). Steps 5-6 (wait-for-bots, four-surface triage, manual TEST DB smoke, possible fix-up rounds, merge) start in Session 18 once external bots have landed (typically 5-15 min after PR open). Ending here keeps cache warm without idle-tick burn.
- **Branch state at session end:** PR #470 OPEN, branch pushed and tracking origin. After this status-file commit lands, branch will be 21 commits ahead of origin/main on remote (20 pushed earlier in session + 1 status-doc commit for this session — push-able as a normal docs commit per the established pattern, the PR will pick it up automatically). No outstanding local-only state.

### Next session picks up at

**PR #470 Task 3.11 step 5-6** — external bot triage + manual TEST DB smoke + possible fix-up round + merge. Per implementation plan §3.11 (steps 4-6) and per-PR ritual.

Pre-flight verifications:

1. **Confirm baseline** at session start: `npm run type-check && npm run lint` (must pass), `git status` (clean except for the same persistent `.beads/`/`.claude/` lockfiles), `git log --oneline -5 origin/main..HEAD` (should match the head documented above).
2. **Check if external bots have landed** via the four PR comment surfaces per `feedback_pr_comment_surfaces.md`:
   - `gh pr view 470 --comments` (issue-comments — where bots typically post their full report)
   - `gh api repos/danfeder/esnyc-lesson-search-react/pulls/470/reviews --jq '.[] | {user: .user.login, state, body}'` (review summaries)
   - `gh api repos/danfeder/esnyc-lesson-search-react/pulls/470/comments --jq '.[] | {user: .user.login, path, line, body}'` (line-attached review comments)
   - `gh pr checks 470` + `gh run view <id> --log-failed` for any failing CI check
3. **Bot triage.** Investigate every finding per `feedback_bot_review_investigation.md` (rebuttal pass on EVERY finding, including "minor"). Default-reject hardening per `feedback_pr_bot_review_workflow.md`. Surface accept/reject recommendations to user BEFORE applying fix-ups. PR 2 saw 4 of ~15 findings accepted across 3 rounds — similar ratio likely for PR 3. **Round-cap is 2.** If a 3rd round lands, fix only critical bugs.
4. **Watch for re-flags of the 6 pre-push findings.** External bots may surface the same hypotheticals (especially the banner-IIFE-falls-through and the Promise.all-error-swallowing). The PR body's "Reviewer notes" section captures the rejection rationale; point to it if needed rather than re-rebutting from scratch.
5. **Manual TEST DB smoke against Netlify deploy preview** (mandatory per CLAUDE.md "MANDATORY: Before merging any PR with database changes" — though PR 3 has NO schema changes, the same rule applies for any PR with substantive frontend changes). Wait for deploy preview to be live (CI builds typically take 2-5 min). Sign in as `admin@test.com` / `password123` per `reference_test_credentials.md`. Walk through the three intent states:
   - **(new)** — open a `(new)`-type submission, verify green banner ("Submitter says: New lesson"), `approve_new` decision pre-selected.
   - **(update, X)** — open an `(update, X)`-type submission, verify blue banner ("Submitter says: Updating <title>"), `approve_update` decision pre-selected, target lesson card hoisted to position 0 with SUBMITTER'S CHOICE badge, MERGE & ARCHIVE button enabled (no inline guidance).
   - **(update, null)** — open an `(update, null)`-type submission, verify yellow banner ("Submitter says: Updating, but couldn't find target — please search to identify."), `approve_update` decision pre-selected, search escape hatch auto-expanded with "Use this to find the lesson the submitter couldn't" help text, MERGE & ARCHIVE button DISABLED with inline guidance.
   - **Override flow** — pick a different lesson via the search escape hatch, verify selectedDuplicate updates, MERGE & ARCHIVE becomes enabled, save succeeds.
   - **Mismatch warning** — contrived case if seedable: target lesson with very different extracted_title (Jaccard 0) → yellow "Heads up:" warning fires. Reverse case (similar titles) → no warning.
   - **Three-state queue badge** on `/review` dashboard — verify green NEW / blue UPDATE w/ truncated title / amber UPDATE? badges across the row list.
6. **TEST DB seed.** Reuse PR 2's leftover 3 rows in TEST `lesson_submissions` (per Session 7 doc: `phase8b_smoke_p1_new_469`, `phase8b_smoke_p2_revising_target`, `phase8b_smoke_p3_cantfind`) OR seed fresh via `mcp__supabase-test__execute_sql`. The local seed pattern from Sessions 10-14 (UUIDs `aaaaaaaa-...`, `bbbbbbbb-...`, `cccccccc-...`) is documented and reusable on TEST too.
7. **Per-round TEST DB re-verification.** If a fix-up commit lands that touches DB-applied state (e.g., RLS, edge function source, migration — none of which is currently expected for PR 3 since it has no schema changes), re-run the manual smoke after Netlify rebuilds. Per `feedback_per_round_test_db_verification.md`.
8. **Merge.** Once bot rounds + manual smoke pass, merge via **rebase** (not squash — preserves the 11-doc-commit + 10-feature-commit history per the established Phase 8b pattern). No PROD migration approval gate (no schema changes). Netlify auto-deploys to PROD. Update `project_lesson_submission_tier1.md` with PR 3 merge commit + ship status.

**Watch-outs for Session 18:**

- **20+ commits = potentially 15+ findings across 4 bots.** Budget 30-60 min for round 1 triage. Use the accept-bundle-fix-push-bundle pattern from PR 2.
- **Pre-existing limitation in design doc §6.2** (prior merge target not restored on review reload) is NOT in PR 3 scope — push back with design doc reference if a bot flags.
- **Round-cap is 2.** If round 3 lands, fix only critical bugs.
- **PR 2's round 1 introduced 2 regressions caught by round 2** (a11y combobox roles without keyboard nav, missing URL regex check after `requestSubmit()` bypassed `canSubmit`). Be conservative on a11y additions — complete the contract or skip the role. Be careful that fix-ups don't bypass existing client-side validation gates.
- **Status doc commit pattern** continues — each round of fix-ups gets a status update commit alongside the feature fix-up commit (matching the established session-doc rhythm). Final session doc on the merge round.
