# PR 6 — Stage 2 Corpus Re-tag — Execution Status ARCHIVE

Reference-only (grep, don't read end-to-end). Session log entries move here
when each PR cycle closes. Active status doc:
`2026-06-11-metadata-rebuild-pr6-stage2-retag-execution-status.md`.

## PR A cycle (#507, merged 2026-06-12 as `f982d1a`) — Sessions 1-5

### Session 1 — 2026-06-11 — evidence gathering (design-lock mode) — COMPLETE

- **Execution shape:** workflow `wf_a3162cd5-7a3` ran E1/E2/E4/E5/E6 as 5 parallel gather agents each followed by an adversarial verifier (10 agents, ~21 min); E3 ran as a single background executor (~30 min, $3.58 API spend); E7 run supervisor-side (one CLI probe). Supervisor independently reproduced load-bearing PROD numbers (767/21/788, 122, 230, 0, 3) before accepting.
- **Headline findings:** live corpus 767 not ~751 (~751 is today's TEST count); OQ3 decisively favors monolithic ($23.76 vs $220.60 Opus sync over 767 lessons — per-field prefixes sit under Opus's 4096-token cache minimum and cannot cache); v3-flagged sampling bucket is EMPTY (0 flags/831, confidence anti-correlated with quality); content_text ready (OQ5 locked); batch-vs-sync retention delta is shape-not-duration (both ≤~30 days; batches deletable on demand); `--max-turns` gone from claude CLI.
- **Doc state:** design doc gains "Session 1 evidence" section; OQ5 locked; Status stays Draft pending the Session 2 walkthrough (OQ1-OQ4, OQ6-OQ13).
- **Commits:** `30d2afa` (5/7 evidence + status checkpoint), session-end commit (design-doc evidence section + OQ5 lock + this entry).
- **Process learnings:** (1) fan-out + per-item adversarial verify caught real errors (27-vs-28 option count, an attribution overstatement in E4, E2's wrong 772-origin story) — pattern worth keeping; (2) exploration doc has non-UTF8 bytes → `grep -a` required, plain grep silently empty; (3) `.env.local` `ANTHROPIC_API_KEY` is the proxy-side key (401s on direct API) — direct scripts use `ANTHROPIC_CONSOLE_API_KEY`; (4) verifiers flagged that V3's tool_choice-doesn't-invalidate-cache finding EXCEEDS documented behavior — rerun before relying on it.
- **User-side follow-ups:** Anthropic Console credits exhausted mid-V3 — top-up needed before the ~$5.50 V3 rerun (optional; OQ3 answer already clear) and before any future dry-runs. Main remains 3 commits ahead of origin (docs-only, push not yet authorized).

### Session 2 — 2026-06-11 — mechanism re-decision walkthrough — COMPLETE (same session as Session 1)

- **Format:** 4 AskUserQuestion rounds (mechanism cluster → field scope + vocab judgment calls → garden-skills list → eval/process cluster → mechanics/PR-shape), plain-language briefings before each; user decided every `[user-verdict]` question explicitly. Sessions 1+2 compressed into one calendar day as the kickoff anticipated.
- **Outcome:** all 13 OQs locked (see "Decisions made during execution"); design doc Status flipped to LOCKED; §3/§5/§6 rewritten as decided; foundation impl plan line 11 + PR 6+ section amended; decision journal gained the D-S2 addendum card; implementation plan rewritten with concrete tasks (A full, B medium, C/D/E outline + cycle-start refinement, F rider).
- **Notable user divergences from recommendations:** no-cook folded into basic-prep (rec was keep); grades = ALWAYS source-doc claim (rec was claim-else-judge); user gates everything (rec was team-gated spot-check); garden-skills folded user-side rather than via team. All recorded with rationale in design §4.
- **Process learnings:** AskUserQuestion rounds with recommendation-first options moved 13 locks through in one sitting; the evidence-section-in-design-doc pattern meant zero re-derivation during the walkthrough.
- **Commits:** session-end commit (design lock + foundation amendments + decision journal + impl plan + this entry + memory updates).

### Session 3 — 2026-06-12 (overnight autonomous, ~00:15–03:15 ET) — PR A code-complete (A1-A7 + pre-push review), held for A8 — COMPLETE

- **Authorization:** user authorized autonomous work until 6:20am ET; added fallback tasks (docs cleanup audit, simplification groundwork, frontend/UX review, working-efficiency review) — all delivered to `~/cCode/pr6-overnight-2026-06-12/` (copies also in `/tmp/overnight-review/`).
- **Execution shape:** supervisor + 4 sequential-executor workflows (chunk1 A1+A2; chunk2b A3+A4 after an A3 blocker round-trip; chunk3 A5-A7; 3-lens pre-push review) + 2 background agents (mini-worksheet draft; 13-item review-fix bundle) + a 13-agent read-only review workflow — every executor adversarially verified, every chunk supervisor-re-verified in main loop. ~4M subagent tokens total.
- **A3 blocker → supervisor resolution (pending ratification):** executor STOPPED on the OQ2 lock contradiction (observances "17 after End-of-year merge" unsatisfiable — the 17 configured options INCLUDE both End-of-year variants; merge ⇒ 16). Resolved 16, surviving spelling `End of year celebrations` (stored 15 vs 1); design doc deliberately NOT edited overnight. **Also corrected: concepts post-drop = 119** (canonical[] is already post-drop; the "112" arithmetic was wrong).
- **Pre-push review (3 lenses):** 13 consolidated findings, ALL accepted after rebuttal pass (2 in lean form). 1 major: repair-phase records carrying a transient per-field API error were permanently stranded (collides with B4's 100%-Zod-pass gate). Notables: repair model-match; `_root`-failure dead-end; **Opus 4,096-token cache-floor preflight guard** (static estimate 4,268 sits 4% above the floor — silent no-cache would add ~$16 unnoticed); billed-usage capture on truncation; runtime body-hash resume guard; `--strict` provenance; within-side case-twin dedup in the diff; fixtures encoded impossible states (mutation-tested suite caught all 3 deliberate breaks). Fixes: `cb886e8` (+54 tests).
- **Verifier-grade findings to carry:** (1) the live API has never seen the $defs/$ref tool schema — A8's first funded call validates it (fallback documented in Current State); (2) Who's-Who Google Doc is NOT link-public — A4 export fetched via authenticated `mcp__google-workspace__get_doc_content` (df@esynyc.org) into untracked sidecar `artifacts/whos-who-body.txt`; **PR C's content_text repair migration must source the body from the run artifacts at emit time** (both corpus.jsonl + sidecar are gitignored — regeneration requires the authenticated fetch); (3) corpus is a point-in-time snapshot with `EXPECTED_LIVE_ROWS=767` hardcoded fail-safe.
- **Process learnings:** (1) executor STOP-on-contradiction worked exactly as designed — the kickoff's "do not improvise" rule caught a real spec error; (2) grep-binary gotcha recurred (literal NUL in run-retag.ts; then AGAIN writing the memory note — JSON tool-call strings turn backslash-u escapes into literal bytes) → new memory `project_grep_binary_bytes_gotcha.md`; add `file` scan over agent-written sources to verification; (3) ESLint flat-config: `!scripts/stage2-retag/` negation works only against `scripts/*` (single-star) — negation after `scripts/**` does NOT re-include; (4) the supervisor+workflow pattern carried 9 tasks + review in one session with light supervisor context — the chunked-workflow (2-3 tasks per workflow) variant beats one-mega-workflow for checkpoint granularity.
- **Blockers for morning:** Console credits (A8 + PR-open); observances=16 ratification; mini-worksheet review/send (draft ready); decide overnight-review artifact dispositions.

### Session 4 — 2026-06-12 (interactive, ~10:30–13:00 ET) — worksheet decided · proxy path · A8 complete · four-model verdict · PR #507 OPEN — COMPLETE

- **Worksheet:** cooking_skills + main_ingredients vocab DECIDED in an in-session user walkthrough (team SKIPPED, user call; two-rounds confirmed). Record: `2026-06-12-metadata-rebuild-stage1-cooking-skills-ingredients-worksheet-decided.md`. Headline reversals from the draft: ONE general Knife skills entry (absorbs the 5 cuts + knife safety); ingredients two-level (C), 24 groups, NO "Other" catch-all (distributed: Squash-cucumbers-melons rename, Mushrooms own group); Potatoes→Root vegetables; guyanese=Both (hierarchy parent stays + dual-tag rule).
- **Funding:** user committed to the CLIProxyAPI proxy path. v6.10.8→v7.1.68 (user-installed after checksum verification; backup kept); **May cache-defeat CONFIRMED FIXED** (probe 2 read 1,359 cloak tokens); `--base-url` plumbing `55df535`.
- **A8 (all via proxy):** preflight 9,518→10,890 tokens (budget 12K after cloak allowance `55549bd`); `$defs/$ref` ACCEPTED live; `submit_tags` cleared detection; `strict:true` REJECTED (uniqueItems); fable rejects forced tool_choice → `--tool-choice-auto` (`cee59bc`). Prompt r2 (`31310a6`) FAILED inspection; de-confound proved academic co-occurrence prompt-immune across models → **code-enforcement pivot** (`47139e1` normalize.ts). **r3 four-model comparison verdict: fable 8/15 clean (won 6/8 dims) vs o47 2 / o48 0 / sonnet 1 → USER: fable primary + o47 challenger, 4-8 + sonnet dropped, DEFAULT_MODEL→4-7 (`9d068bc`).** Measured: fable $2.54/15→~$129 · o47 ~$79 w-repairs · o48 ~$48 · sonnet ~$22.
- **A9:** round-2 review → 3 fixes (`07cc5b8`, 285 tests) + 4 PR-B deferrals; pushed; **PR #507 open**.
- **Process learnings:** (1) de-confound runs are cheap and decisive — when prompt+model change together, $0.90 separates the variables; (2) prompt-immune rules go to code with provenance, not to a third prompt iteration; (3) the model-comparison inspector explicitly verifying provenance (promptSchemaHash) before judging prevented a confound-blind verdict; (4) subagent model tiering updated for Fable era (memory + kickoff amended: Fable/inherit for judgment, Opus executors, Sonnet floor, never Haiku); (5) the harness permission classifier correctly stopped a supervisor binary install — user-run `!` command was the right channel.
- **Memory updates:** `feedback_opus_subagents.md` rewritten (tiering); cliproxy reference updated (v7.1.68 live, cache fixed, fable needs auto).

### Session 5 — 2026-06-12 (interactive, ~13:00–13:30 ET) — PR #507 bot round 1: zero accepted findings — COMPLETE

- **Surfaces collected (all four):** issue-comments (netlify + claude[bot] review, 6.2K chars), review summaries (none), line-comments (none), checks (Security Audit the sole failure).
- **claude[bot] (5 findings + 3 nitpicks, verdict "ready to merge"):** ALL no-action after rebuttal pass — (1) API-key warn REJECTED (both keys permanently in `.env.local` → the warn is steady-state noise; missing-key errors already direction-specific at run-retag.ts:951-965); (2) fable cache-floor DUPLICATE of existing PR-B deferral (and not silent — explicit "check manually" message); (3) promptMemo comment REJECTED (memoization self-evident in 4 lines); (4) mapWithConcurrency informational, no change requested; (5) `scripts/lib/` non-existence REFUTED (evalMetrics.ts + test live there; B3 reuses). Nitpicks: already-commented (2×) / covered by B4's user-gated cost step.
- **Security Audit failure: pre-existing main breakage** — 5 consecutive main failures pre-dating the PR; PR's only dep delta (tsx pin + esbuild subdeps) absent from audit output. New main-side hygiene item surfaced: **critical advisory on vitest 4.0-beta range** (`@vitest/ui` arbitrary-file-read when UI server listening; dev-only exposure) + moderates on `react-router-dom`/`ws` — belongs on the main hygiene list with the known `@lhci/cli` chain.
- **Outcome:** no fix-up commits → no round 2; docs push (`bd0f10f` + `96590e6`) bundled as the final push; **user merged #507 as `f982d1a` (2026-06-12 16:55 UTC)**, branch deleted. This status edit is a LOCAL commit on main (push to main not authorized) — carry it onto the PR-B branch.
- **Process learnings:** the bot reviewer independently re-surfaced a finding already triaged in the pre-push round (cache-floor labeling) — the PR-B deferral list in the status header made the duplicate-detection instant; keep listing deferrals there.

## PR A cycle — archived Current-State snapshot paragraphs (moved 2026-06-12)

**SESSION-4 AGENDA (user-set 2026-06-12 ~10:10 ET):** (A) ✅ **DONE 2026-06-12 — mini-worksheet decided in-session** (user chose talk-through over team send-out; team SKIPPED entirely; two-rounds confirmed — PR F stays a separate second pass, unblocked). Decision record: `docs/plans/2026-06-12-metadata-rebuild-stage1-cooking-skills-ingredients-worksheet-decided.md`; design doc OQ2/OQ8/§5/§7 + impl plan PR-F entries amended. (B) ✅ **DECIDED 2026-06-12: PROXY PATH for A8 onward** (user diverged from the brief's Console-first recommendation; user installed v7.1.68 binary themselves after supervisor download+checksum). State: CLIProxyAPI **v7.1.68 RUNNING** on 127.0.0.1:8317 (v6.10.8 backed up as `~/.local/bin/cli-proxy-api.v6.10.8.bak`); OAuth healthy (`claude-mail@danfeder.org`); 2-probe smoke PASSED and **the May cache defeat is FIXED** (probe 2 `cache_read_input_tokens=1359` — cloak prefix cached+read; expect near-API-rate costs, ~$24-30 Opus full run in Max credits). `--base-url` plumbing landed as `55df535` (+17 tests → 247; no-flag path byte-for-byte unchanged; proxy path switches key source to `ANTHROPIC_API_KEY`; trailing-/v1 strip guard; `effectiveBaseUrl` provenance in every record). Operating model accepted: pull-latest before runs; mid-run detection 400 → pull latest → `--resume`. Proxy base URL: `http://127.0.0.1:8317/api/provider/anthropic`. Still unverified until the `--limit 3` probe: `submit_tags` custom-tool detection, OUR prefix caching through the cloak, $defs/$ref acceptance. (C) then A8 proper — ALL RUNS THROUGH THE PROXY (results not path-interchangeable). Observances=16 RATIFIED + doc lock lines amended (`12d99fd`).

**A8 IN PROGRESS (2026-06-12, via proxy):** (1) ✅ preflight 9,518 tokens (under 10K budget, clears Opus floor; count includes ~1.4K cloak); (2) ✅ `--limit 3` probe — **$defs/$ref ACCEPTED by live API** (contingency closed), `submit_tags` cleared detection, caching active; (3) ✅ Opus 15-lesson stratified dry-run (corpus reordered: 15 even-length-quantile records first, backup `corpus-pre-strat-backup.jsonl`) — 14/15 Zod-pass + 1 repaired = 100%; $0.87 incl. repair; **projected full run ~$42 Max credits** (cloak partially defeats caching: stable 3,934-token prefix reads but ~3.6-5.3K/call cache-writes — within 2× bar); (4) ✅ inspection (artifact `artifacts/dryrun-notes-opus-r1.md`): 2 clean / 7 minor / 6 serious — dominated by ONE mechanical bug (`academic` co-occurrence 11/15); grade fabrication ZERO (v3's worst failure gone); tasting≠cooking violated 2/5 chances; perfect fields: tags/observances/season_timing. (5) ✅ user verdicts: grade bands EXPAND (locked table: MS→6,7,8 · Elem→K-5 · UpperElem→3-5 · AllGrades→full list) + activity-over-topic (food-workers OUT of garden arm). (6) ✅ prompt r2 applied (`31310a6`) + token budget 10K→12K for cloak overhead (`55549bd`). (7) ❌ **r2 inspection FAILED (0/5/10 vs r1's 2/7/6)** — but model+prompt changed together; **de-confound run (r2 prompt on 4-7) shows academic co-occurrence is BOTH-MODELS, BOTH-PROMPTS (11/15→10/15→9/15)** → prompt-only enforcement abandoned; supervisor decision: **code-enforce the mechanical rules** (academic strip + concepts⇄integration reconcile + synonym-pair lint, provenance-recorded normalizations; executor dispatched). `--strict` probe verdict: REJECTED by API (`uniqueItems` unsupported in strict schema subset) — deferred to PR B as optional hardening. Zod-reliability: 4-8 = 0 malformed, 4-7 = 1 (r1) + 3 (r2) → 4-8 more schema-reliable. (8) ✅ 4-7-vs-4-8 same-prompt comparison: **4-7 wins 4 quality dimensions (heritage decisively), 4-8 wins schema-reliability + tags/obs perfection; 4-8's fabrications are SILENT vs 4-7's caught-by-Zod malformations** — full table in dryrun-notes.md. (9) ✅ code-enforcement landed `47139e1` (normalize.ts: academic-strip + concepts⇄integration reconcile + synonym-pair lint, provenance-recorded, +21 tests → 271) after academic co-occurrence proved prompt-immune across both models (11/15, 10/15, 9/15). (10) ✅ **FABLE EXPERIMENT (user-requested):** forced tool_choice is API-REJECTED on claude-fable-5 ("not compatible with this model") → `--tool-choice-auto` escape hatch built (`cee59bc`, +12 tests → 283; system addendum inside the cached block; resume-identity aware) + fable pricing entry (`e2384b2`). (11) ✅ **r3 FOUR-CONTESTANT RUNS complete** (same 15 stratified lessons, same r3 state = r2 prompt + fixed tool description + normalization): **fable $2.54 → ~$129 full-run, 15/15 clean 0 repairs, 15/15 tool-call compliance under auto · opus-4-7 $0.92 → ~$47, 2 repairs · opus-4-8 $0.88 → ~$45, 1 repair · sonnet $0.40 → ~$21, 1 repair** (all proxy-billed Max credits). (12) ✅ **four-model comparison + USER DECISIONS (2026-06-12): FABLE PRIMARY + OPUS-4-7 CHALLENGER for the PR-B answer key; 4-8 and Sonnet DROPPED; DEFAULT_MODEL reverted to claude-opus-4-7.** Fable: 8/15 clean (everyone else ≤2), won 6/8 dimensions, 0 repairs, 0 normalizations, perfect tasting≠cooking (first ever) + both locked band expansions; ~$129 full run accepted as one-time-pass premium. Escalations carried to PR B: L14 heritage ban fails on ALL FOUR models (answer-key adversarial item, possible code-side rule); academic-strip can amplify errors when `academic` was correct (consider recheck routing); even Fable fabricated one season (L10) — answer-key check on [Table] bodies; o47's garden_skills fabrications live in REPAIR records 3 rounds running (review repair-pass prompting). Prompt FROZEN at r2+fixed-description state (no r3 wording pass — eval gates anyway; Fable honors the bands natively). (13) ✅ **A9 COMPLETE — PR #507 OPEN.** Pre-push review round 2 (fresh reviewer on the cb886e8..HEAD delta): verdict safe-to-push, 2 major + 5 minor; rebuttal pass accepted 3 (proxy joins resume identity; `MAX_TOKENS_TOOL_CHOICE_AUTO=8192` headroom for fable's always-on thinking; budget-test semantics retitle) → `07cc5b8` (+2 tests → **285**); 4 deferred to PR B (listed in the Active-PR header above). PR body refreshed (full A8 story + measured cost table). Pushed + `gh pr create` → **#507**. Artifacts: dryrun-opus.jsonl (r1) · dryrun-{opus47,opus48}-r2.jsonl · dryrun-{fable,opus47,opus48,sonnet}-r3.jsonl · probe-strict.jsonl (strict REJECTED: uniqueItems) · probe-fable.jsonl (forced-tool REJECTED) · dryrun-notes*.md.

**Session 3 progress (2026-06-12, overnight autonomous — user-authorized until 6:20am ET):** A1 ✅ `2503b01` (tsx ^4.22.4 pinned). A2 ✅ `091fb60` (check surface; ESLint ignore narrowed to `scripts/*` + `!scripts/stage2-retag/` — negation after `scripts/**` does NOT re-include; gates proven to bite). A3 ✅ `82ff374` (vocab module, 25 tests; observances=16 per supervisor resolution below; concepts post-drop = **119** — canonical[] is already post-drop, drops are disjoint worksheet literals; heritage 71; academic_concepts mapping `column: null` + `subject-keyed-object` shape + subjectKeys). A4 ✅ `79132c9` (corpus export: 765 records verified — 767 live − 2 ghosts; PROD via publishable anon key env-injected as STAGE2_SUPABASE_URL/STAGE2_SUPABASE_ANON_KEY; Who's-Who body 3,235 chars via authenticated Google-Doc fetch — doc NOT link-public, sidecar `artifacts/whos-who-body.txt` untracked; record `id` = `lessons.lesson_id`; EXPECTED_LIVE_ROWS=767 hardcoded fail-safe). A5 ✅ `911c45d` (system prompt + submit_tags tool built FROM vocab module, 13 top-level properties = 12 vocab fields + grade_levels, synonym pairs nested in academic_concepts per OQ10; grade enum from FILTER_CONFIGS.gradeLevels — lessonMetadata.zod has NO grade enum; static token mass ~4.3K; 69 tests). A6 ✅ `61d63d4` (+ NUL-byte fix `cb6a31f`): sync monolithic runner, two cache breakpoints verified at run-retag.ts:647-650, resume keyed on prompt+schema hash + model, repair pass appends provenance-marked records; per-field repair tool mirrors PROD shape (academic_concepts uses its own object property — PROD selected_values can't express it). A7 ✅ `bbe675b` (validate-output + plain-language diff report, casing-only separated, grade_levels excluded from diff — corpus doesn't carry current grades; 176 tests total across the runner dir). All adversarially verified (v5 confirmed-with-caveats, v6/v7 confirmed) + supervisor-verified. **⚠️ A8-contingency: schema dedupes the 119-value concepts enum via $defs/$ref (12 inline copies would blow the token budget) — live-API acceptance of $ref in tool input_schema is UNVERIFIED (even count_tokens is credit-gated); A8's first funded call validates it; fallback = restructure concepts property.** Console credits CONFIRMED exhausted (1-token probe, 00:30 ET) → A8 + PR-open blocked on morning top-up. **Next: pre-push review (multi-lens) → fix-ups → hold for A8.** Overnight fallback review artifacts at `/tmp/overnight-review/`; mini-worksheet draft at `/tmp/pr6f-mini-worksheet-draft.md`.

**Sessions 1 + 2 (2026-06-11) — DESIGN LOCKED.** All 7 evidence items gathered + adversarially verified; all 13 OQs decided by the user in the Session-2 walkthrough (4 AskUserQuestion rounds). Design doc Status = **LOCKED**; per-OQ lock lines + Session-1 evidence section written in; foundation impl plan (line 11 + PR 6+ section) amended; decision journal D-S2 card appended; **implementation plan rewritten with concrete tasks** (PR A full detail; B medium; C/D/E outlines refined at cycle start; F rider). **Next session: execute PR A** — branch `feat/pr6a-stage2-retag-pipeline`, tasks A1-A9 (tsx pin → check surface → vocab module TDD → corpus export (765 records) → schema TDD → runner → validate/diff TDD → ≤20-lesson dry-run → ritual + PR). The headline locks: TS+Zod runner, monolithic, sync API, 12 fields now (cooking_skills + main_ingredients via team mini-worksheet → rider PR F), Protocol A+B eval with user gating everything, grades = source-doc claim only, 5-PR breakdown. Mini-worksheet for the team (cooking_skills + main_ingredients + guyanese-parent) should go OUT during PR A/B. Optional leftover: V3 cache-confirmation rerun (~$5.50) blocked on Console credit top-up — no decision depends on it.

**Headline Session-1 findings** (full detail in `docs/plans/pr6-stage2-retag-evidence/`):
- **Live corpus = 767 lessons, not ~751.** 788 total − 21 `import:*` retirements; ~751 reproduces only on TEST (772/751 today). Definitional locked decision unaffected; resize plan text + cost projections to 767.
- **OQ2 (E1):** smaller fields split in tiers — 5 nearly-clean fields (academic subjects, SEL, core competencies, cooking methods, observances; ~90%+ canonical, only kebab twins + 3 single-value judgment calls) lockable in the walkthrough without worksheets; garden_skills needs a ~9-entry fold list; **cooking_skills (122 distinct, 93% of tagged rows off-vocab) + main_ingredients (230 distinct, 81% off-vocab, abstraction-level mix) genuinely need worksheets.** All 8 fields: uniform camelCase JSONB key + text[] column dual representation (apply must dual-write); kebab drift is submission-era (88/105 rows) not corpus-wide (7/662).
- **OQ5 (E2):** content_text READY — 100% coverage, no truncation/corruption, 5/5 random Google-Doc comparison fresh. 3 bad rows: exclude ghost stubs `1l9KH63QBe2xhyH0zp6VIavtj0uKSRZtd` + `1nFbpkwlujk8fIO8RkeIcDoO2fuO83Iil2Srf8t5iqm8`; re-extract or hand-tag `1n8wS0X-dXAw9sfQuLFgsMg_kNvACph3cT4yd9p2i1eg` (Who's Who in the Food System).
- **OQ6 (E4):** v3 flagged **zero** of 831 lessons for review → "v3-flagged" sampling bucket is EMPTY, not just biased; v3 confidence highest exactly where wrong. Citable confidently-wrong gallery built (soap lesson with cooking tags, 64 grade-smeared lessons at ≥90 conf, 82 incoherent Stovetop+"Basic prep only" pairs). Protocol rec: pre-run ~60-lesson answer key (~40 random + ~20 adversarial, curriculum-team labels with agent pre-fill) + post-run 3-bucket diff audit; "beats v3" = per-field F1 ≥ v3 everywhere + macroF1 ≥ 0.7 + per-value recall ≥ 0.5. Prerequisite user decision: grade-level ground-truth policy (source-doc claim vs age-appropriateness).
- **OQ4 (E5):** Batch retention ≤29 days confirmed, not ZDR-eligible, BUT batches deletable on demand post-download (effective window → hours); sync is ALSO retained up to 30 days under standard commercial terms, so the retention delta is artifact-shape, not duration. 50% discount confirmed, stacks with caching (best-effort 30-98% in batch). 767 lessons fit one batch. Acceptability = user verdict at walkthrough.
- **OQ1/OQ3/OQ12 (E6):** "mirror, don't extend" HOLDS; both call sites unchanged since exploration; per-field AND monolithic both expressible in the canonical shape → OQ3 is purely token economics. Gotchas: edge enums come from `_shared/metadataSchemas.ts` (not enums.json); tsx unpinned; scripts/ has no tsc/eslint gate (OQ12 lift confirmed needed); Opus 4.x 4096-token cache minimum can silently nullify caching of small per-field prompts.
- Verifier corrections to carry: cookingSkills configured list = 27 options (not 28), mainIngredients = 44 (not 45) — no downstream effect on drift percentages; E4's "validator fabricated tags at scale" softened to "fabrication designed in MetadataO4Mini variant; generation-level non-enforcement proven"; E2's "772 unexplained" → it's today's TEST count.
- Operational: the exploration doc has non-UTF8 bytes — `grep` sees it as binary and silently returns nothing; use `grep -a`.

**Branch:** `feat/pr6a-stage2-retag-pipeline` (off main at `d693e19`; main itself is 5 docs-only commits ahead of origin — user aware, push not yet authorized)

**Branch commits (task → hash):** A1 `2503b01` · A2 `091fb60` · A3 `82ff374` · A4 `79132c9` · A5 `911c45d` · A6 `61d63d4` · NUL fix `cb6a31f` · A7 `bbe675b` · review fixes `cb886e8` · checkpoints `d245105`/`32e3995`/`e771918`/`16ee131`. **230 tests** in `scripts/stage2-retag/` (6 files), full suite + `type-check` (both surfaces) + `lint` green at session end.

**Pre-next-PR verification (if any):** none yet. Inherited substrate state: PROD concepts census 675 / 119 / 1923 zero non-canonical; `pr5a_heritage_rollback` (37) + `pr5b_concepts_rollback` (676) live until this track's cleanup PR.

---

## PR B cycle (#508, merged 2026-06-13 as `44da8f6`) — Sessions 6-11

> Archived 2026-06-16 at the C2 (PR C2 apply) cycle start. Condensed; full verbatim per-session detail is recoverable from git history of the live status doc at `51bc312`. **No new promotable process learnings** — the durable ones (AskUserQuestion full-explanation-inside-the-field; supervisor independent spot-checks catching real misses) were already in `feedback_plain_language.md` / `feedback_workflow_orchestration.md`.

Branch `feat/pr6b-stage2-retag-run` off main at `f982d1a` (PR A merge). **Zero DB writes the entire cycle** — PR B produced the answer key, the full LLM re-tag run, and the apply artifacts (no migrations).

**Pipeline tasks (TDD throughout):** impl-plan B-refinement `f3451d8` (B0-B7) · **B0** `7efcf50` (cache-floor `level:'unknown'` + preflight `--help`; two deferrals dissolved on investigation — toolChoice asymmetry already closed in PR A, garden fabrications prompt-immune) · **B1** `fb6ab84` (deterministic sampler seed 20260612, 40 stratified + 20 adversarial, worksheet emitter + parser CONFIRMED-falls-back-to-DRAFT) · **B2 pre-fill** (6 parallel Fable agents, bodies-only to keep v3 an unbiased contestant; 60/60 validated, zero vocab violations).

**B2 verification walkthrough (Session 7, interactive):** 8 cluster rounds + 16 lesson-by-lesson rounds, every lesson user-verified vs the live Google Doc. **25 policy rulings A1-A25** at `docs/plans/2026-06-12-pr6b-answer-key-verification-rulings.md` (source of truth for the key AND the full-run prompt-rule seeds). Headlines: CRF AND SEL verbatim stamps KEPT (cell-primary; user overrode the discount rec); **3 lessons removed as incomplete/non-lesson (L10 Celebrating Eid, L21 Kitchen Appendices, L27 Three Sister Arepas)** → corpus-deletion follow-ups; **systemic: grade/season claims live in Drive FILENAMES + page HEADERS that `content_text` lacks** → both become pipeline inputs (A20), all-seasons headers → all four seasons (A21), cross-surface conflicts → union (A22). Commits `8ac6b24`/`9d4b626` (exclusions + §F apply, 323 tests)/`0591008`/`ff3c7ee` (B2.5 doc-surfaces sidecar, 342)/`c908144` (--ids)/`41a4f61` (score-answer-key.ts, 379). Final key = **57 records**.

**B3 contestant scoring (Session 8):** fable macroF1 **0.858** · o47 0.777 · v3 0.532; fable ≥ v3 on all 13 fields. Fable deterministic refusal on Seed Dispersal `1X7HadA1…` ("build seeds that explode" false-positive) → scored 56/57. **User rulings:** Gate 3 amended to support≥2 floor (`cfffdee`) → **fable passes all 3 gates, confirmed B4 winner**; garden_skills code rule R6 adopted (`90208d0`, fires when predicted activity_type lacks `garden`); heritage mention-vs-about prompt rule (`088fba0`) — retest proved it INEFFECTIVE (claims-vs-delivery, not mention-vs-about; over-tagging ACCEPTED, Protocol-B/OQ9 are the catch surfaces); `--fallback-model` refusal-only o47 fallback (`3374a27`).

**B3.5 prep (Session 9):** B3.5c corpus-exclusions mechanism `7750586`; B3.5b completeness screen → user excluded 7 + re-extracted 6 tag-card bodies via a generalized `body-overrides.json` manifest `8953bb3` (exclusions 3→12); B3.5a corpus-wide doc-surfaces sweep `8a57623` (sidecar 57→758, 9 background agents). **B4 run corpus = 753** (765 − 12 exclusions).

**B4 full run (Session 9, $121.28):** fable `--tool-choice-auto` + `--fallback-model claude-opus-4-7`, proxy v7.1.68, 6 window-paced batches via `--resume --limit`. **753/753 Zod-passed, 0 unusable.** 5 fable refusals all o47-fallback-recovered; one lesson `1_WlZmDJv8Ql…` hit a deterministic o47-fallback payload-stuffing failure (quarantined to `full-run.fable.rejected.jsonl`, resolved via direct o47 main-pass + per-field repair). Sole-source artifact `artifacts/full-run.fable.jsonl` (gitignored; **Fable 5 suspended 2026-06-12 by export-control directive → the run is no longer reproducible as-Fable; do not lose this file**).

**B5 apply artifacts (Session 10):** CI test-guards `b2a2101` (skip-if-missing on gitignored-artifact assertions); B5a full-corpus diff report `18763c7` (753/753 compared; academic_concepts 98% change by-design; Activity Type +craft 146 / −garden 105); B5b prepare-apply `59f0d5c` (staging SQL/CSV + draft migration mirroring PR-5a snapshot-then-dual-write + 90-lesson spot-check; verified academic_concepts JSONB-only, dual-write present).

**B6 register adjudication + gate (Sessions 10-11):** `29da364` classified all 74 signals (R 24 · V 18 · U 19rows/13q · P 5 · D 4 · X 3 · B 1). **Headline: re-tag emits leaf heritage tags only; AA/Indigenous root under `indigenous-and-diaspora` → no `North American` even post-expansion (de-facto identity-anchor = U-1).** 13-question user walkthrough (U-1..U-13) → **10 accept-as-is + 3 targeted PR-C2 apply-time fixes:** U-5 Fattoush `1Dz-Jv4cV0N0ntxZ8z0VcRgenI0hpZuOIDJuPbEvr7bI` drop Israeli+Jordanian · U-8 Alternative Proteins (EUR-07) drop Italian/European · U-12 Intro to Salad Project `1V2Xt4c…` drop Middle Eastern. U-1 = accept identity-anchor (no `North American` re-added to AA/Indigenous). User declined every "log future follow-up" → no new future-track items. 74-signal bundled register apply `2767f83` (zero Open rows remain). **B7 ritual → PR B #508 opened, bot round 1 = zero accepted findings (F2 "12 missing" verified == the 12 exclusions), merged `44da8f6`.**

