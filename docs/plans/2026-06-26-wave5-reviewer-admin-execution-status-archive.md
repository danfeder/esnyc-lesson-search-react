# Wave 5 — Reviewer/Admin Features — Execution Status ARCHIVE

**Reference-only.** This file holds session-log entries moved out of the active execution-status doc
(`2026-06-26-wave5-reviewer-admin-execution-status.md`) as each PR cycle closes, to keep the active
doc lean. Don't read end-to-end at session start — `grep -n` for what you need. The active doc's
"Current State" header + "Recent decisions worth carrying forward" + "Out-of-scope follow-ups" remain
the load-bearing orientation; this is the forensic trail.

**Archive log:**
- 2026-06-27 — PR-1a cycle start: moved Sessions 0–2 (scaffold → design-lock → PR-0 built + merged) here.

---

## PR-0 cycle (Sessions 0–2) — scaffold · design-lock · PR-0 safety net SHIPPED (#552, `3258365`)

### Session 0 — 2026-06-26 — scaffold + GATE 1A

Major events:
- Oriented via a parallel-read workflow (roadmap Wave 5 detail · campaign status · ReviewDetail anatomy/seams ·
  personalization-table backend reality) + independent baseline-snapshot cross-check + table shapes.
- Confirmed PR breakdown shape with the user (reversible-first; PR 0 tests → decompose → C107 → C111/112/113 →
  admin tail).
- Scaffolded the four-file pattern (design Draft + impl SKELETON + kickoff + status) on `chore/wave5-scaffold`.
- Ran GATE 1A (Codex gpt-5.5 inline + Claude reviewer); folded 19 findings into the design doc.
- STOP before design-lock and before any implementation code (per session scope). Next: Session 1 = design lock.

### Session 1 — 2026-06-26 — design lock + impl authoring + GATE 1B + RE-SCOPE

Major events:
- **Discovery:** 5-agent read-only workflow (`wf_a536efc9-c53`) re-verified all 11 §5 seam anchors + the
  §5.bis C107 graph + test infra + personalization shapes + render surfaces. Supervisor-verified the 4
  load-bearing corrections against the real files (search-page mock is rpc+functions not `.from()`;
  loadSubmission catch only logs / never hits ErrorBoundary; `useEnhancedAuth` has no `isAuthenticated`;
  bookmark surface = 3 leaves w/ role=button stopPropagation).
- **Locked Q2/Q3/Q4/Q8** from evidence + corrections C-a…C-f into design §4.
- **RE-SCOPE (user-confirmed):** Wave 5 narrowed to **PR 0–2 only** (ReviewDetail test net → decompose →
  C107). Personalization (PR 3–5) + admin tail (PR 6+) **deferred to a future wave** — only ~3 internal
  accounts (general-user login is a later rollout → personalization audience ≈0) + reviewers never collide
  (→ C22/C78 moot). Saved memory `project_user_base_accounts`; updated MEMORY.md + re-scope banners in
  design + impl + this status doc; §§6/7 + Q3/Q4/Q5/Q9/Q6/Q7 retained as future-wave reference.
- **Q1 user-verdict = split** PR-1 into PR-1a (easy seams) + PR-1b (risky core); data hook returns an
  initial-form-state object. Design **Status → Locked**.
- **Authored impl plan PR 0–2 concrete tasks** (0.1–0.3, 1a.1–1a.5, 1b.1–1b.4, 2.1) with file paths,
  anchors, verify cmds, commit msgs.
- **GATE 1B:** Codex (`gpt-5.5`, inline) + Claude reviewer, parallel — both **GO-WITH-CHANGES**, both
  confirmed every PR 0–2 anchor EXACT. Folded all findings into PR-0 (dual-shape mock; `tagged_metadata`
  fixture columns; 3rd `noReviewUpdateFixture` + behaviors 6–9 + 4-state banner coverage to pin
  preselect/auto-expand/view-toggle/closed-enum before their seams move; 1a.2 type co-location; PR-2
  fetch-dependency caveat). No BLOCKER.

Process learnings:
- The re-scope collapsed 5 of the 9 design questions (Q5/Q9/Q6/Q7 → deferred) — surfacing the *audience*
  reality before grinding through mechanism questions saved a lot of motion. Worth asking "who uses this"
  early on any feature wave.
- GATE 1B caught a real coverage gap (PR-0 pinned only the restore branch, not preselect/effect-ordering)
  that would have let PR-1b silently regress — the gate earned its keep on a plan-only review.

Decisions roll-up: see "Recent decisions worth carrying forward" + the design §4 locked answers. Commits:
docs-only on `chore/wave5-scaffold` (see `git log`). Branch not pushed.

### Session 2 — 2026-06-26/27 — PR-0 built end-to-end + GATE 3 + 3 bot rounds + MERGED (#552)

Major events:
- Reconciled a **stale kickoff RIGHT-NOW banner** (still said "design-lock / Draft") against git — design-lock was already done (`61ae519`). Trusted git, repointed the banner at PR-0 (`43ac2f6`).
- **Housekeeping verdict (user): "carry docs forward"** → cut `test/wave5-reviewdetail-safety-net` from `chore/wave5-scaffold`; the 2 scaffold doc commits ride inside PR-0's PR (no separate docs PR).
- **Built PR-0 via 3 fresh-context executor dispatches, each supervisor-verified:** 0.1 table-aware supabase mock (dual-shape terminals) + 3 fixtures (`9d237ae`); 0.2 page-level RTL gate test, 13 behaviors + a 4th `degradedUpdateFixture` for the yellow banner (`3b530ef`, `92213d5`); 0.3 export-in-place helper unit suites + 6 export-only ReviewDetail additions, diff confirmed export-only (`cc1c96d`). Verified `tagged_metadata` (not `metadata`) + the decision enum against real code before accepting fixtures.
- **GATE 3 (pre-push):** code-reviewer agent + Codex (`gpt-5.5`, inline) in parallel on `git diff main...HEAD -- src/`. Rebuttal-passed all 7 findings. **Accepted** C-2 (non-null preselect-target seed unpinned), C-3 (save `submissionId`/`selectedLessonId` + merge-save unpinned), F1 (mock missing `ilike`/`is`/etc.); **accept-lite** F3 (pin doc-embed flag), F2/H-2 (kept honest comment — investigated, **no value≠label case exists** in the frozen closed-enum vocab). **Rejected** C-1 (mock arg-blindness = the locked dispatch-by-table tradeoff; PR-2 manual smoke is the mitigation) + H-1 (candidate-card coverage = PR-1a Task 1a.2's mandatory TDD 4-case unit test). Fix-ups in `ba97c1b`. Grounded the C-2/C-3 accepts by reading the real seed block (L472–486) + save body (L571–579) myself first.
- **Final net:** 15 page-level behaviors + 23 helper units; **full suite 2002 green**, `npm run check` clean, zero production-logic change.
- **Opened PR-0 = #552** (user go-ahead). Round-1 CI all green (E2E/Test&Build/CodeQL/semgrep/Lighthouse/coverage + Security Audit + deploy preview). Four-surface triage: only `claude[bot]` posted (6 findings); no line comments, no review-state reviews, `claude-database-review` silent (no DB). Rebuttal-passed all 6 + GATE-4 2nd opinion: accepted F3 (reviews-error→silent-preselect pin) + F4/F5/F6 (test hygiene), deferred F1 (name collision → PR-1a note), rejected F2 (self-resolves PR-1a). Fix-up `9304e34`; pushed with the docs/status bundle → round 2.
- **Round 2:** all CI green; `claude[bot]` 3 findings → R2-1 (High, error-drop/silent-overwrite) DEFERRED to PR-1b (real bug, out of pure-additive scope; test 12 pins it; recorded as a prominent Out-of-scope follow-up), R2-2 (`@/` alias) + R2-3 (corrected my own inaccurate F5 comment) accepted. Verified both code claims (L323/L390/L397 drop `error`; off-list lookup keys on `targetInRenderedTopFive`) myself before triaging. User verdict: **"quick cleanup, then merge."** Cleanup `61a917d` pushed (round 3).
- **Round 3:** all green + mergeable CLEAN; `claude[bot]` 5 findings = re-raises of already-triaged/deferred items (R3-2=R2-1, R3-3=mock two-terminal, R3-4=PR-1a relocation, R3-5=beforeEach) + one NEW dormant LOW nit (R3-1, mutation-method mock returns read data). Per the **round-cap (3rd = critical-only)** + user authorization → **squash-merged PR-0 (`3258365`, #552)** + deleted the branch. Decomposition gate satisfied.

Process learnings:
- **GATE 3 earned its keep on a test-only PR:** Codex C-2/C-3 found real holes (the non-null-preselect-target seed + the merge-save `selectedLessonId` path were unpinned) that PR-1b's extraction could have silently regressed — the spec'd 9 behaviors missed them. Closing holes for the exact seams the next PR moves is faithful to "covered before any move," not scope creep.
- **Reject discipline mattered too:** two "Critical/High" findings (C-1, H-1) were already mitigated by locked design decisions — accepting them would have re-introduced order-sensitivity (C-1) or duplicated PR-1a's planned coverage (H-1). The rebuttal pass paid off both directions.
- Supervisor-grounding (reading L472–486 / L571–579 myself before triaging the Criticals) made the accept/reject calls evidence-based rather than trusting the reviewers' assumptions.
- **Codex was rate-limited (until 2026-06-29)** — the `codex:codex-rescue` agent auto-fell-back to a direct Claude-family file read for the GATE-4 2nd opinion (`feedback_make_failing_tools_work`). Usable for a low-stakes test-only PR, but NOT a true independent-model-family signal; for a higher-stakes (DB/behavior) round, wait for Codex or escalate. Recorded the weaker-signal caveat in the triage.
- **Calibrated-skipped the pre-push GATE-3 re-dispatch** on the round-1 fix-up (test-only +139, GATE-4-vetted + supervisor-verified, round-2 external bots are the second pass, Codex unavailable). Proportionality over rote ritual — noted explicitly.
- **The bot re-raised the same High finding (R2-1) across rounds 2 and 3**, framing it as "a characterization test that guards the bug instead of a fix." Correct response held: PR-0's job is to PIN current behavior (pure-additive); the FIX + the test-12 update belong in PR-1b where the load logic moves into the hook. Pinning-now-fixing-later is the intended sequence, not an anti-pattern — but the bug is real and is now a tracked, prominent follow-up (data-integrity).
- **Round-cap discipline paid off:** without it, rounds 4/5 would have chased cosmetic re-raises indefinitely. Calling the cap (with each round-3 item explicitly dispositioned, not hand-waved) is the right close.

Decisions roll-up: see "Recent decisions worth carrying forward" + the Out-of-scope R2-1 follow-up. **PR-0 MERGED (`3258365`). Next: PR-1a.**

### Session 3 — 2026-06-27/28 — PR-1a built end-to-end + GATE 3 + 3 bot rounds + MERGED (#553)

Major events:
- **Session-start ritual:** read kickoff + status + design doc end-to-end + impl plan PR-1a section; `npm run check` clean baseline; verified git matches (PR-0 `3258365` merged on main, worktree dirty only with the 2 carry-forward docs + unrelated untracked `docs/plans/*.md`). Confirmed PR-0's 6 `export` helpers + 4 test files present, `ReviewDetail.tsx` = 1,483 lines. User confirmed orientation.
- **Cut `refactor/wave5-reviewdetail-easy-seams` from main** (`3258365`). First commit bundled the carry-forward docs (status + kickoff, from PR-0's merge onto main) + the PR-0-cycle session-log archival (ritual step 5: Sessions 0–2 → new archive file). Untracked unrelated docs NOT staged.
- **Ritual-step-5 learnings audit (Sessions 0–2):** all process learnings already covered by existing feedback memories (GATE 3/round-cap/reject-discipline → `feedback_pr_bot_review_workflow` + `feedback_bot_review_investigation`; Codex-fallback → `feedback_make_failing_tools_work`). The "ask who uses this early on a feature wave" learning overlaps `project_user_base_accounts`; not promoting a new memory mid-initiative — revisit at initiative-close retrospective (ritual step 6).
- **PR-1a build complete (all 5 extractions, each supervisor-verified):** 1a.1 `240663a` · 1a.2 `cf1e469` · 1a.3 `36fcd1c` · 1a.4 `7e5550a` · 1a.5 `8ddd36f`. `ReviewDetail.tsx` 1483→1120 lines (−363/−24.5%). Full suite 2031 green; +28 new unit tests (buildCandidateCards 7, reviewValidation 21). Zero behavior change.
- **GATE 3 (pre-push) — both families CLEAN:** Claude code-reviewer + **Codex `gpt-5.5`** (re-ran after the user reset the usage limit → genuine cross-family signal, not a fallback) both confirmed byte-parity across all 5 extractions + the dep-array superset reasoning + banner invariant + dedup/ordering + no circular imports + prop completeness. Zero behavior drift. No fix-ups.
- **PR #553 opened (2026-06-28); 3 bot rounds, all `claude[bot]`, no `claude-database-review` (no DB), all CI green every round.** Round 1: 7 findings; accepted comment-cleanup (non-semantic) + misleading-`(new)`-comment fix; rejected the `return null` guard (behavior change); deferred F1 logger (GATE-4 Codex AGREE-DEFER) / F3 / F5 / F6. Round 2 (fix-up `f708d44`): "shippable as-is," 5 non-blocking; R2-A corrected MY F1 note (verified `GoogleDocEmbed` myself: error → "Unable to Load Document" screen + MANUAL "Show Text View" button, not silent fallback); R2-B/R2-C = real branch-coverage holes → added 2 cases (additive); R2-D/R2-E/F6 → deferred to PR-1b cosmetic bucket. User: "add 2 tests, then merge." Round 3 (final, `383dbfa`): "No correctness bugs found"; GATE-4 Codex on the one "needs-fix-before-merge" flag (`ReviewDocPanel:31` unvalidated localStorage cast) → AGREE-DEFER-AND-MERGE (verified byte-identical + sole-writer + no reachable invalid-value path; bonus: found an unused `ReviewContent.tsx` referencing the key). **Squash-merged PR-1a (`e4c248d`, #553)** + branch auto-deleted. main: `ReviewDetail.tsx` 1,120 lines, all 6 modules present.

Process learnings:
- **Supervisor-verify caught the one risky change every time:** 1a.5's `[metadata]` dep-array narrowing was the sole non-byte-identical edit; the executor flagged it, I reasoned the superset property, and BOTH GATE-3 families + BOTH later bot rounds independently confirmed it — exactly the verify-chain working as designed.
- **`feedback_bot_review_investigation` cuts both ways:** investigating R2-A myself (reading `GoogleDocEmbed.tsx`) revealed MY OWN tracked F1 note was wrong ("auto-recovery" → actually a manual button). Investigate every finding, including the ones that flatter the bot — sometimes they correct you.
- **GATE-4 earns its keep at the merge boundary:** the bot escalated a pre-existing localStorage cast to "needs fix before merge"; a 1-min Codex 2nd opinion (AGREE-DEFER) let me merge over it with confidence + a documented rebuttal, rather than either blocking or hand-waving.
- **Round-cap + "deliberate final round" framing:** round 3 added 2 genuinely-valuable safety-net coverage tests (not bot-chasing) and then HARD-capped — the cap is against thrash, not against valuable additive work.

Decisions roll-up: PR-1a SHIPPED (`e4c248d`, #553). Next: PR-1b (core/risky seams + the R2-1 fix). All cosmetic-cleanup nits folded into the PR-1b cleanup bucket (carried in the live status doc's Out-of-scope section).

## PR-1b cycle (Sessions 4–5) — core seams + R2-1 fix · built · GATE 3 · 2 bot rounds · MERGED (#554, `262eeaa`)

> Moved from the live status doc at PR-2-cycle start (2026-06-28). `ReviewDetail.tsx` 1,120→425
> across PR-1b; R2-1 RESOLVED. Grep here for forensic detail; the durable carry-forward (the
> deferred PR-1b cleanup cluster, etc.) lives in the live status doc's Out-of-scope section.

### Session 4 — 2026-06-28 — PR-1b start (branch cut + carry-forward + archival)

Major events:
- **Session-start ritual:** read kickoff + status + design doc end-to-end + impl plan PR-1b section; `npm run check` clean baseline; verified git matches (PR-1a `e4c248d` merged on main; `ReviewDetail.tsx` = 1,120 lines; all 6 PR-1a modules present; worktree dirty only with the 2 carry-forward docs + unrelated untracked `docs/plans/*.md`). User confirmed orientation.
- **Cut `refactor/wave5-reviewdetail-core-seams` from main** (`e4c248d`). First commit bundles the carry-forward docs (status + kickoff, PR-1a's post-merge edits) + the PR-1a-cycle session-log archival (ritual step 5: Session 3 → archive file). Untracked unrelated docs NOT staged.
- **NEXT:** dispatch PR-1b Task 1b.1 (`useReviewSubmission(id)` hook returning initial-form-state **+ the R2-1 data-integrity fix** + page-test-12 update) to a fresh-context executor; supervisor-verify + checkpoint after each of 1b.1–1b.4.

- **Task 1b.1 DONE (`6a55297`) — `useReviewSubmission(id)` hook + R2-1 fix, supervisor-verified.** Dispatched a fresh-context executor (one task); it extracted `loadSubmission`+effect into `src/pages/useReviewSubmission.ts` returning a computed `initialFormState` seed (NOT the 13 setters); the page applies it via one seeding effect + keeps its form `useState`. **R2-1 fix:** user picked the "block with load-error screen" shape (AskUserQuestion) → on a `submission_reviews` fetch error the hook sets `loadError` + returns (form never renders → no overwrite); `similarities`/`profile` errors `logger.warn`+degrade; fetch ORDER unchanged (PR-2 owns parallelization). Page test 12 flipped to the safe behavior. **Supervisor-verify:** 16/16 page tests green + deterministic across 4 runs; `npm run check` clean; test 12 the only test changed; seed logic + restore-vs-preselect mutual-exclusion + `selectedDuplicate: null` byte-faithful; `loadError` branch ordered loading→loadError→not-found.
  - **Accepted deviation (flag for GATE-3):** executor used **`useLayoutEffect`** (not the spec's literal `useEffect`) for the seeding effect — passive `useEffect` flushed the seed async, leaving a default-state first frame that made tests 6/10/11 flaky (measured 3/0/1 fails across runs); `useLayoutEffect` seeds synchronously in the same commit the data arrives, matching the original batched-setter behavior (no visible flash) and going 4/4 deterministic. More faithful than `useEffect`, honors the locked "one seeding effect" design. Like 1a.5's dep-array change — the kind of deviation the verify chain is for; GATE-3 (reviewer + Codex) to re-confirm at pre-push.
  - **Cleanup-bucket items surfaced (not done — fold opportunistically):** `useReviewSubmission.ts` carries the pre-existing `teacher.email: 'teacher@example.com'` placeholder; `SubmissionDetail.review` is built but appears never read by the page (seed comes from `initialFormState`, not `submission.review`) → candidate dead-field drop; `id!` non-null assertions remain (safe via the load effect's `if (id)` guard).

- **Task 1b.2 DONE (`44ef44b`) — `useSearchEscapeHatch` + `<ReviewSearchPanel>`, supervisor-verified.** Fresh-context executor; ZERO behavior change. The two search-hatch effects moved into `src/pages/useSearchEscapeHatch.ts` (owns `showSearch`); panel JSX → `src/components/Review/ReviewSearchPanel.tsx` (+index). **Risk-4 preserved:** reset effect declared FIRST (deps `[submissionId, setSelectedSearchLesson]` — the stable page setter added for exhaustive-deps, behavior-neutral), auto-expand declared SECOND (deps `[needsSearch, noDups]` VERBATIM, one-directional body intact). **Entanglement honored:** `selectedSearchLesson` stays page-owned (`buildCandidateCards` reads it upstream; `noDups` derives from `candidateCards`); the hook resets it via the passed setter. **Supervisor-verify:** commit = exactly 4 files (no test/doc edits); page test 16/16 deterministic (6 runs total across executor+supervisor); `npm run check` exit 0; full suite 2033/2033; panel markup byte-identical (only `onClick`→`onToggle` + a prettier whitespace collapse). No deviations.

- **Task 1b.3 DONE (`6219786`) — `<ReviewMetadataForm>` (LEFT col), supervisor-verified.** Fresh-context executor; ZERO behavior change. Lifted the LEFT-column `<div>` (~313 lines) → `src/components/Review/ReviewMetadataForm.tsx` (+index). **Risk-7 preserved:** the 5 closed-enum `<Select>`s keep distinct label resolution — 3 config-lookup (`mainIngredients`/`cookingSkills`/`gardenSkills` via `ALL_FIELD_CONFIGS.<f>.options.find`) + 2 raw `label:v` (`observancesHolidays`/`culturalResponsivenessFeatures`); **NO generic shared helper introduced** (supervisor-grepped). Prop surface (9): executor sensibly KEPT `fieldProgress` (also read by RIGHT `IntDecisionBar`), `validationErrors`/`errorBannerRef` (page validation-focus effect), `legacyDecisionWarning` (page state) in the page and passed down (risk-6 correct); moved `fieldError`/`singleProps`/`heritageOptions` IN (grep-confirmed LEFT-only). **Supervisor-verify:** commit = exactly 3 files (no test/doc edits); page test 16/16 deterministic (test 9 = closed-enum chips); `npm run check` exit 0; no dangling page imports. Only prettier whitespace re-wraps; all value-maps logically identical.

- **Task 1b.4 DONE (`c5188e9`) — `<ReviewDecisionPanel>` (RIGHT col), supervisor-verified. PR-1b build COMPLETE.** Fresh-context executor; ZERO behavior change. Lifted the RIGHT-column `<div>` → `src/components/Review/ReviewDecisionPanel.tsx` (+index), composing the already-extracted `<SubmitterIntentBanner>`/`<TitleMismatchWarning>`/`<ReviewSearchPanel>` + candidate cards + decision radios + notes + saveError banner + `<IntDecisionBar>`. **`handleSaveReview` STAYS in the page**, passed as `onSave` (the only substitution: 3× `onClick={handleSaveReview}`→`onClick={onSave}`); the `complete-review` invoke payload (`submissionId`/`decision`/`metadata:payload`/`notes`/`selectedLessonId` conditional) is **byte-identical** (supervisor-grepped the committed page + the diff — no payload line changed). 20-prop surface (whole `submission` passed to keep banner/mismatch/notes JSX byte-identical); no hooks moved (risk 6); saveError behavior unchanged (risk 8). **Supervisor-verify:** commit = exactly 3 files; `ReviewDetail.tsx` 581→**425 lines** (now a thin 3-child shell + early returns); page test 16/16 deterministic; full suite 2033/2033; `npm run check` exit 0.
- **PR-1b RESULT:** 4 cohesive seams extracted, ZERO behavior change except the deliberate R2-1 data-integrity fix (1b.1). `ReviewDetail.tsx` 1,120→425 (−695/−62% this PR; 1,483→425/−71% across PR-1a+1b). New modules: `useReviewSubmission.ts`, `useSearchEscapeHatch.ts`, `Review/{ReviewSearchPanel,ReviewMetadataForm,ReviewDecisionPanel}.tsx`. R2-1 RESOLVED. Standing risks 2/3/4/6/7/8 all preserved + verified. **NEXT: GATE 3 (reviewer agent + Codex on `git diff main...HEAD`) → rebuttal-pass → fix-ups → bundle status-doc commit → push + PR.**

- **GATE 3 (pre-push) — both families ran; ONE cross-family-converged finding, fixed (`112d04f`).** Dispatched a Claude `feature-dev:code-reviewer` + a Codex `gpt-5.5` adversarial review in parallel on `git diff main...HEAD -- src/`. **Codex was NOT rate-limited** (genuine cross-family signal). Both independently rated CLEAN: seed restore-vs-preselect logic byte-faithful, R2-1 scoping correct (only reviews error blocks; loadError ordered before not-found), search-effect order/deps `[needsSearch, noDups]`, the 5 closed-enum selects' 3-config/2-raw split (no shared mapper), `complete-review` payload unchanged, hooks-order, and the `useLayoutEffect` seeding choice (safe + faithful in this SPA). **The one real finding (both families):** the new Retry button (`reload`) didn't reset `loading` → a "Submission not found" flash on retry (and a stale-form flash in the navigate→error→Retry case) — undercutting R2-1's intent on a path R2-1 introduced. **Rebuttal-pass:** real + new. **Scoped fix** (`112d04f`): a dedicated `reload()` that sets `loading=true` before re-loading (spinner until retry resolves); id-effect path untouched → preserves the pre-existing navigation behavior (no global behavior change). Also corrected the stale `reviewsErrorPreselectFixture` comment (low nit, both flagged). Page test 16/16 + `npm run check` exit 0 after the fix. **GATE 4 not separately dispatched** — Codex IS the cross-family reviewer here and concurred on the fix; re-dispatching a full GATE-3 on a 10-line vetted fix-up = disproportionate (calibrated skip). **Test-coverage note:** a Retry-success test (error→success) isn't feasible with the dispatch-by-table mock (no per-call sequencing); test 12 still pins the primary R2-1 block. Surfaced as a future mock-infra item, not a blocker.

- **PR #554 OPENED (2026-06-28), Session 4 ends here.** Branch pushed (`112d04f` code tip). Session 4 carried the entire PR-1b BUILD: branch cut + carry-forward/archival (`b5de5e3`), all 4 extractions (`6a55297`/`44ef44b`/`6219786`/`c5188e9`), the GATE-3 fix-up (`112d04f`), each supervisor-verified (page test 16/16 deterministic + `npm run check` + diff spot-check after every task). **Handoff:** next session does the four-surface bot triage of #554. The status-doc edits adding the PR# + this entry are committed LOCALLY but held from push (bundle with the first fix-up per `feedback_no_docs_push_during_pr`; PR #554 is open). If bots find nothing across 2 rounds, push the held docs commit standalone before requesting merge.
  - **Session-4 process learnings (promote at initiative-close):** (1) the supervisor+fresh-executor model held up across 4 sequential extractions — one task per dispatch, supervisor-verify (re-run page test + check + diff spot-check) caught nothing wrong because each spec was precise, but the verify chain stayed load-bearing (e.g. confirming the `complete-review` payload byte-identity, the risk-7 split, the effect dep arrays myself). (2) Two executor deviations were correctly surfaced + accepted: 1b.1's `useLayoutEffect` (more faithful than the spec's `useEffect`; eliminates a flaky default-state flash) and 1b.2's `[submissionId, setSelectedSearchLesson]` reset deps (behavior-neutral stable-setter addition). (3) The R2-1 fix's UX shape was a genuine `[user-verdict]` — AskUserQuestion (block-with-error-screen chosen) before dispatching, not assumed. (4) GATE-3 cross-family convergence on the Retry-loading gap (a NEW path R2-1 introduced) validated running BOTH families even on a "clean refactor."

### Session 5 — 2026-06-28 — PR-1b bot round 1 triage + fix-ups

Major events:
- **Session-start ritual:** kickoff "RIGHT NOW" was STALE (it describes *starting* PR-1b; Session 4 already built it end-to-end + opened #554). Status doc = source of truth; git matched (tip `0da6aef` held docs atop `112d04f`; branch ahead 1 of origin). `npm run check` clean baseline. Judgment call (flagged to user): did NOT re-read the design doc end-to-end — triage session, design LOCKED, status headlines sufficient. User confirmed orientation.
- **Four-surface triage of #554** — both Claude bot jobs PASSED CI (advisory, non-blocking). 10 findings across 3 surfaces (1 review summary `claude-component-review`, 1 issue comment `claude-review`, 5 line comments). Deduped to: C1 (correctness), F1/F2/F3, N1–N6.
- **Rebuttal-pass every finding + investigation** (supervisor-owned): verified C1 unreachable (only entry to `/review/:id` is `ReviewDashboard:293`; error screen's sole nav → `/review` dashboard which UNMOUNTS → resets `loadError`; bot's `useEffect` fix is post-commit ∴ ineffective anyway; zero data risk — error screen renders no form). Verified `ReviewActions` is dead (barrel-export only, not in diff). Verified the 5× closed-enum comments are CARRIED BYTE-IDENTICAL from main (lines 743–872) and the cited "CLAUDE.md one-short-line rule" does NOT exist. Verified no actual circular dep today (N4).
- **GATE 4 (Codex `gpt-5.5`, inline):** dispatched a single codex-rescue cross-exam on all 10 findings + my rebuttals. **Codex AGREED on all 10 calls** (clean cross-family convergence, no MUST-FIX). One refinement both families independently reached: the N2 logger cleanup should ALSO align the off-list submitter-target fetch error (`useReviewSubmission.ts:171`) — folded in.
- **Accepted + applied (3, behavior-neutral, `fa02550`):** N1 hoist `heritageOptions`→module `HERITAGE_OPTIONS`; N2 `logger.error`→`warn` at L137 **+ L171**; N3 stale comment fix in `buildCandidateCards.ts`. Supervisor-verify: `npm run check` exit 0; `review-detail-page` gate + `buildCandidateCards` + `reviewValidation` = 46/46 green; diff spot-checked (15 lines, exactly the intended changes).
- **Deferred (7, captured in the live status doc):** C1, F1, F2, F3, N4, N5, N6.
- **GATE-3 re-dispatch = calibrated SKIP** (same as `112d04f`): the fix-ups ARE the GATE-4-Codex-vetted accept set (15 lines, behavior-neutral), so a fresh reviewer+Codex pass on them is disproportionate.
- **Pushed** `fa02550` + the held `0da6aef` docs + this Session-5 status commit in one CI cycle (per `feedback_no_docs_push_during_pr`). **NEXT:** wait for bot round 2; if clean → request merge (round-cap reached).
- **Process learning (promote at close):** the kickoff's "RIGHT NOW" footer can lag the status doc across sessions — always reconcile against git + status header first, and trust the status doc. (Worked exactly as the ritual intends here.)

**PR-1b bot round 2 (Session 5 cont., round-cap reached):**
- **Four-surface re-triage of #554 by timestamp** (post-`118b8ca`): both Claude jobs PASSED; `mergeStateStatus: CLEAN`/mergeable. New round-2 review summary (`claude-component-review`) + issue comment (`claude-review`) + 3 line comments.
- **Both bots' round-2 set is mostly RE-RAISES of round-1 deferrals** (F2 `catch`→no `loadError`; N4 `ReviewMetadataForm` runtime import from `@/pages/`; `SubmissionDetail.review` dead field; N5 a11y textarea) — each EXPLICITLY acknowledged by the bots as deferred. `claude-review`'s "non-findings" list independently VERIFIED-OK the R2-1 mechanics (loading reset, reload batching, navigation-no-reset = zero data risk, `useLayoutEffect` seeding, search-effect order, `selectedDuplicate: null`).
- **3 NEW round-2 findings, all DEFERRED (GATE-4 Codex concurred on all 3):**
  - **R2-NEW-1 (Med) — seed `useLayoutEffect` omits `setValidationErrors([])`/`setSaveError(null)`** → stale validation/save banners could survive an A→B nav. SAME root cause + SAME unreachability as C1 (only entry to `/review/:id` = `ReviewDashboard:278/293`; ReviewDetail only navigates to `/review` → every A→B unmounts → fresh state; only a manual URL-bar param edit triggers it). Pre-existing (original page didn't reset them either), zero data risk. **Codex caught:** the bot's 2-line fix is INCOMPLETE — id-loads don't set `loading` (`useReviewSubmission.ts:327`), so the stale frame survives B's load window even with the patch. → **PR-2** gets the deliberate fix (`key={id}` or `useLayoutEffect([id])` reset) where the load/seed path is redesigned, with test coverage.
  - **R2-NEW-2 (Low) — redundant `setLoading(false)` on the `!submissionData` path** (`finally` already covers it; harmless double-write). → cosmetic, fold into PR-2 if `useReviewSubmission` is touched.
  - **R2-NEW-3 (Low) — `inputIds`/`handleSaveReview` not memoized** → speculative perf; NO `React.memo` exists under `src/components/Review` (Codex live-confirmed) so stable identity buys nothing today. → defer.
- **GATE 4 (Codex round 2):** resumed the round-1 codex-rescue agent (route-graph context intact) via SendMessage; pinned `gpt-5.5`, inline. **AGREED on all 3 calls + the overall MERGE-with-all-deferred recommendation; no MUST-FIX.**
- **Decision: NO round-2 fix-up** (all deferred) → respects the 2-round cap; recommend MERGE. Per `feedback_no_docs_push_during_pr`, this round-2 status edit is committed LOCALLY and HELD (no code to bundle); it carries forward to PR-2.
- **✅ MERGED — user authorized "merge" → `gh pr merge 554 --squash` → squash `262eeaa` on main (2026-06-28T22:46Z).** Post-merge verified: local main synced to `262eeaa`; `ReviewDetail.tsx` = 425 lines; 4 new modules present (`useReviewSubmission`/`useSearchEscapeHatch`/`ReviewMetadataForm`/`ReviewSearchPanel` + `ReviewDecisionPanel`); `npm run check` exit 0 on main. Session 5 WRAPPED here. The local round-2 status commit (`13b2da7`, then amended at wrap to record the merge) stays on the old branch as the PR-2 carry-forward source. **PR-1b track COMPLETE; only PR-2 remains in re-scoped Wave 5.**
  - **Session-5 process learnings (promote at initiative-close):** (1) the kickoff "RIGHT NOW" footer lagged reality by a full PR — always reconcile against git + status header first (the ritual caught it cleanly). (2) Two-round bot triage converged fast because most round-2 findings were re-raises of round-1 deferrals the bots themselves acknowledged — a sign the deferral rationale was legible. (3) GATE-4 Codex earned its keep on REASONING, not just code: it confirmed C1/R2-NEW-1 unreachability via the route graph AND caught that the bot's R2-NEW-1 fix was incomplete (post-commit timing + the no-loading-on-id-load gap) — a miss a code-only check would pass over. (4) The C1↔R2-NEW-1 pair shows a single root cause (route reuses the component instance; no `key={id}`) surfacing as two separate bot findings across two rounds — PR-2 should fix the root, not patch each symptom.
