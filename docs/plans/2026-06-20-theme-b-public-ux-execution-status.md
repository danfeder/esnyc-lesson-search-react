# Theme B — Public "Broken-Windows" UX Execution Status

**Last updated:** 2026-06-20 by Session 9 (**W1c IMPLEMENTATION COMPLETE + pre-push gate done + PR #526 OPEN.** Tasks 4.4/4.2/4.3 shipped+verified; pre-push review (code-reviewer + Codex GATE 3) cleared the core, found 2 `urlParams.ts` items → user chose "fix both" → fixed (`890afa6` pre-push commit) + 1431 tests green; branch PUSHED, **PR #526 opened**.) **Next = wait for CI/external bots → four-surface bot triage + GATE 4 → user-gated merge.** Branch `feat/theme-b-w1c-url-state` (PUSHED, tracks origin); §4 Q6–Q8 locked; GATE-1B done. **Pure-frontend: deploy-preview + manual smoke only — NO TEST-DB gate, NO PROD migration.**

## Current State

**W1a + W1b SHIPPED + PROD-verified** (Sessions 1–7). PR1 #522 → `19d99b7`, PR2 #523 → `530b2536`, PR3a #524 (migration) → `3c592b1`, PR3b #525 (sort wiring) → `5197069`. Full PR1/PR2/W1b detail + session logs live in `…-execution-status-archive.md` (grep, don't read whole).

> **🟢 W1c / PR4 (URL state) cycle is OPEN — Tasks 4.1 + 4.4 + 4.2 SHIPPED.** Pure-frontend, no migration, no RLS change. Design §4 Q6–Q8 are **all LOCKED** (below); GATE-1B done. Branch `feat/theme-b-w1c-url-state` (off `main` @ `5197069`) key code commits: `3681d22` (4.1 `urlParams.ts`) + `e2ff644` (4.4 — retire `tags` facet + the 4.1 tags revision) + **`26a2ff5` (4.2 — `useUrlSync` hook + atomic `hydrateUrlState`)** (plus docs commits `8b4b13f`/`346e882`/`0fcb797`/`f5a8902`/`24e011e`). **4.2 specifics:** two-way URL↔store sync, 300ms debounce, `replace` mode; one-use `lastWrittenRef` token + single `canonicalSearchString` (URL side merged onto exported `initialFilters`) on both equality sides → browser Back/forward correctly re-hydrates (the WIP's sticky source-flag bug fixed); `hydrateUrlState` FULL-replaces filters + sort + `currentPage:1` in one write (the GATE-1B BLOCKER). 11 hook tests (`createMemoryRouter`, all assert the URL) + 3 store tests. **Tasks 4.3 + pre-push gate now also DONE; W1c is fully built, pushed, and open as PR #526** (https://github.com/danfeder/esnyc-lesson-search-react/pull/526). **NEXT = four-surface bot triage on #526 + GATE 4 → user-gated merge** (see Next-actions §8).
>
> **⚠️ Task-4.2 dispatch note (found during 4.1 verify):** `canonicalSearchString(filters, sortBy)` takes a COMPLETE `SearchFilters`. The hook's loop-guard echo check needs to compare the *incoming raw URL* against the last-written string — to canonicalize an incoming `URLSearchParams` you must merge `parseSearchParams(...).filters` onto a full default. Two clean options: (a) **export `initialFilters` from `searchStore.ts`** so the hook can build a complete filter object, or (b) use **raw `searchParams.toString()` vs `buildSearchParams(...).toString()`** for the echo/no-op checks (order round-trips through parse→hydrate→build, so raw equality holds) and keep `canonicalSearchString` as the robustness net. Tell the 4.2 executor to pick one explicitly + test it.

**What W1c ships:** URL persistence for the public search — query + all filters + `sortBy` serialized into the URL so a search is **shareable / bookmarkable / refresh-surviving**. Two net-new files (`src/utils/urlParams.ts` + `src/hooks/useUrlSync.ts`) + their tests + a mount point in `SearchPage`. The `feat/url-persistence` WIP (`e6610678`, 346 commits behind) is a **read-only pattern reference only** — NOT a base to copy.

**§4 Q6–Q8 LOCKED 2026-06-20** (rationale inline in design §4; W1c flipped to Locked):
- **Q6 reuse** [user verdict, Codex-consulted] = **WRITE FRESH; WIP as reference only.** User was unsure → asked for a Codex 2nd opinion. Codex (different family) read the WIP and found (a) its loop guard has a **latent back/forward bug** (`lastSyncSourceRef` not reset after a store-origin URL write → a subsequent browser **Back** can be silently swallowed) and (b) its store→URL tests **never assert the URL** (false confidence). So lift-and-adapt's only advantage evaporates; field mapping must be rewritten anyway. Rebase (346 behind, won't type-check) rejected. **User locked WRITE FRESH.**
- **Q7 scope** [user verdict] = **query + filters + `sortBy`**; **lesson deep-linking DEFERRED.** sortBy is real now (W1b shipped) so a shared link should preserve "Title A-Z" / "Updated"; whitelist the sort param to `relevance`/`title`/`modified` (else fall back to `relevance`, mirroring the RPC `CASE…ELSE`). `?lesson=<id>` deferred (lessons open in a client-only modal; separate concern).
- **Q8 history mode** [evidence — WIP-confirmed] = **`replace`** (`setSearchParams(params, { replace: true })`). Filter/sort toggles update the URL in place; Back leaves the search rather than undoing one filter.

**Guards to bake into the PR4 tasks (Codex-named, from the Q6 consult):**
1. **Zero `lessonFormat`** references in new code/tests (field was deleted in the 2026-05 metadata rebuild).
2. **⚠️ SUPERSEDED — `tags` is being RETIRED, not added** (user verdict 2026-06-20: the "Lesson Type" facet is vestigial). Task 4.1 was built WITH `tags` → **revise it to DROP `tags` from the URL schema**, and **Task 4.4** removes the facet. (This guard originally said "add tags"; the retirement reverses it.)
3. **Sort hydration calls `setViewState({ sortBy, currentPage: 1 })`** — `setViewState` (unlike `setFilters`) does NOT reset the page; URL doesn't carry a page anyway.
4. **Test the loop guard for real:** actual debounced URL writes, URL→store hydration, invalid-URL cleanup, AND back/forward after a store-origin write (the WIP's latent bug). Tests must **assert the URL** (router/location probe), not just store state.
5. **Validate against current `FILTER_CONFIGS`** incl. recursive hierarchical-heritage children.
6. **Whitelist the sort param** (only `relevance`/`title`/`modified`; else relevance).
7. React Router is **^7.8.2 on both** WIP and `main` (Codex-confirmed — no `useSearchParams` API drift).

**Pre-flight facts confirmed for task authoring (Session 8 evidence agent + Codex):** current `SearchFilters` (`src/types/index.ts`) carries `tags: string[]`, no `lessonFormat`; `ViewState.sortBy` union = `'title'|'confidence'|'grade'|'modified'|'relevance'`; `searchStore` `partialize` persists ONLY `view`+`density` (not filters/query/sortBy/page) → **URL becomes the filter-persistence mechanism**; `setFilters` resets `currentPage:1`; `useLessonSearch` queryKey + RPC params already include `filters` + `order_by:sortBy`; lessons open via client-only `selectedLesson` state in `SearchPage` (no `/lesson/:id` route). **Executors must re-verify exact line numbers before editing — anchors drift.**

**Next actions (in order):**
1. ✅ **PR4 tasks authored** (impl plan): **4.1** `urlParams.ts` (serialize/parse/validate + `canonicalSearchString` + sort whitelist) · **4.2** `useUrlSync.ts` + atomic `hydrateUrlState` store action (two-way sync, canonical loop guard w/ one-use token, correct back/forward) · **4.3** mount in `SearchPage` + `enabled`-gate the first query on `useLessonSearch`.
2. ✅ **GATE 1B DONE** (Codex, inline, 2026-06-20) — caught **1 BLOCKER** (partial `setFilters` merge can't clear URL-absent filters → Back-to-unfiltered keeps stale + the hook re-writes them) + **5 HIGH** (pre-hydration default RPC flash; double-write; stale `lastWrittenRef`; missing scope `searchStore`/`useLessonSearch`; first-RPC test assertion) + MEDIUMs. **All accepted** and folded: atomic `hydrateUrlState`, `hydrated`+`enabled` query gate, `canonicalSearchString`, one-use token, StrictMode/createMemoryRouter/full-replacement tests. (Calibrated down: query-debounce compounding = documented; comma-in-value = invariant test.)
3. ✅ **Task 4.1 DONE + supervisor-verified** (Session 8) — `urlParams.ts` + 32 tests, commit `3681d22`. All 6 locked exports match the contract; schema correct (tags in / lessonFormat out of source); 1412 suite green; `npm run check` clean. (grep "hits" are intentional test-file negative assertions.)
4. ✅ **Task 4.4 DONE + supervisor-verified** (Session 9, reordered first) — `tags` facet retired + the Task-4.1 tags revision, commit `e2ff644`. 14 files, +34/−81; zero out-of-scope files touched; `npm run check` clean; 1413 tests green; scoping greps confirm no filter-facet `tags` refs remain (only the inert `database.types.ts filter_tags?` + new negative-assertion tests). `urlParams.ts` `FILTER_TO_PARAM` is now `Record<keyof SearchFilters, string>` → URL schema compiler-bound to the filter shape.
5. ✅ **Task 4.2 DONE + supervisor-verified** (Session 9) — `useUrlSync` hook + `hydrateUrlState`, commit `26a2ff5`. Chose dispatch-note option (a) (export `initialFilters`). **Supervisor read the hook line-by-line and caught a real bug** (toggle-and-revert within the debounce window left a stale pending write → URL/store desync); sent back for a TDD fix (clear the timer in the no-op branch) + regression test. 1427 tests green; `npm run check` clean. The 11 hook tests all assert the URL (`router.state.location.search`); back/forward (#3) red-green proven by the executor.
6. ✅ **Task 4.3 DONE + supervisor-verified** (Session 9) — mount `useUrlSync` in SearchPage + `enabled?: boolean` (default `true`, back-compat) on `useLessonSearch` gated on `hydrated`, commit `5c49002`. Production diff small + correct. Test-harness ripple handled: 3 integration files converted `BrowserRouter`→`MemoryRouter` + URL-seeding via the production `buildSearchParams` (the empty-URL mount pass full-replaces, so pre-render `store.setFilters` no longer survives); `'Nutrition'`→`'Food Systems'` (the former was never a valid `thematicCategories` option — URL validation correctly drops it); pill assertion tightened to the unique remove-button. New W1c test asserts the first/only RPC fires exactly once with the URL-hydrated filters (proves the gate). Supervisor read all test diffs — sound, no coverage erosion. 1429 green; check clean.
7. ✅ **Pre-push gate DONE + branch pushed + PR #526 OPEN** (Session 9). Parallel code-reviewer (Opus, verdict *ship*) + Codex GATE 3 (different family, verdict *go-with-changes*): core CLEAN (loop guard, hydrate, enabled gate, tags retirement). 2 `urlParams.ts` findings, both rebuttal-passed; **user chose "fix both"** → (a) dropped grade group-id URL acceptance (silent-zero bug both flagged; safe — no UI produces a group id), (b) outbound `MAX_PARAM_LENGTH`/`MAX_ARRAY_LENGTH` caps in `buildSearchParams` for parse-symmetry. Commit `890afa6`; 1431 tests green; pushed → **PR #526**.
8. **Four-surface bot triage on PR #526 (← NEXT SESSION).** Wait for CI/external bots (claude-review/component/feature, performance-review, semgrep, CodeQL, E2E — all `pending` at handoff; 1 auto-comment, 0 reviews). Then collect findings from ALL FOUR surfaces (`/pr-triage 526`), rebuttal-pass each, **GATE 4 (Codex 2nd opinion) on every real suggested change**, consolidated fix-ups (bundle the held docs commit with the first fix-up push — see below), round-cap after 2 rounds. **Pure-frontend = deploy-preview + manual smoke only; NO TEST-DB gate, NO PROD migration.** Then **user-gated merge** + initiative-close retrospective (final session: campaign roadmap update, MEMORY.md hygiene).
9. **⚠️ Held local commits (NOT pushed):** the docs commit(s) updating this status file are committed LOCALLY but deliberately not pushed (PR is open → a docs-only push wastes a CI cycle, per `feedback_no_docs_push_during_pr.md`). Bundle them with the first bot-triage fix-up push.

## Recent decisions worth carrying forward

- **W1c §4 locks** (2026-06-20, Session 8): see Current State. Q6 write-fresh (Codex-consulted — the WIP's "battle-tested" loop guard has a latent Back bug + URL-less tests, so lift-and-adapt's premise fails); Q7 include sortBy + defer deep-linking; Q8 `replace`.
- **GATE 4 / Codex-2nd-opinion discipline extended to design decisions** (Session 8): when the user is unsure on a `[user-verdict]`, a Codex consult (different family, return-inline) is a first-class input — it flipped my Q6 lean (A→C) on a real code finding. Triage Codex's take with the same rebuttal discipline; surface the reconciliation to the user; never lock a `[user-verdict]` unilaterally.
- **"Lesson Type" (`tags`) facet is VESTIGIAL → retirement FOLDED INTO W1c** (user verdict 2026-06-20, after a me+Codex identity verification). Confirmed end-to-end that the public "Lesson Type" facet IS `lessons.tags` = {orientation(69), bilingual_handouts(9)}, LLM/migration-assigned, never reviewer-curated. **W1c now also retires it:** remove `tags` from `FILTER_CONFIGS` (+ the dead `IntSidebar` suppression, + `tags` from `SearchFilters`/store/`useLessonSearch`/`facetCounts`) AND **drop `tags` from the URL schema** — so **Task 4.1 (`urlParams.ts`, built WITH tags) must be revised to exclude tags**, and add a retire task (see impl plan). Leave the `lessons.tags` column + RPC `filter_tags` param alone (inert). Naming-collision note: `process-submission`'s `submit_tags` tool / "tags" prose is a GENERIC name for the activity-type + CR-feature extractors — NOT the Lesson Type field. Full detail in the `project_tags_field_audit_pending` memory.
- **GATE 1B (pre-code plan review) caught a BLOCKER again** (Session 8): the partial-`setFilters`-merge hydration bug (Back-to-unfiltered keeps stale filters) was invisible until Codex traced it against `searchStore.ts`. Mechanism fix = atomic `hydrateUrlState` action + a `hydrated`/`enabled` query gate (the latter prevents a C59-class empty-result flash on shared-link load). These are implementation corrections INSIDE the locked Q6–Q8 design (no user gate, like W1b's strip-before-split), but they expand the file scope to `searchStore.ts` + `useLessonSearch.ts` — noted in the tasks.

## Out-of-scope follow-ups captured here

- **`tags` public badge (C84 path-a)** — deferred to its own audit session (`project_tags_field_audit_pending` memory). The `LessonMetadata.tags` type gap (`database.types.ts` has `tags`; `src/types/index.ts` `LessonMetadata` does not) is part of THAT future work. *(W1c URL state syncs the `tags` filter regardless — the badge suppression is independent.)*
- **`filter_lesson_format` dead RPC param** — harmless leftover in the W1b signature; dropping it = a separate deferred task (also needs the frontend to stop sending it). Not W1c.
- **Lesson deep-linking (`?lesson=<id>`/`/lesson/:id`)** — DEFERRED out of W1c (user verdict 2026-06-20). Separate concern (~200–500 lines, own back-button model). Tracked in design §9.
- **C14 IntPillGroup ARIA-forwarding** (from PR2) — pill-group controls only get wrapper-level `id`/`aria-label`; flagged follow-up.
- **LessonSearchPicker stale state after select→clear (R2-4, from PR2)** — REAL but pre-existing; fix later with a reset on `selected→null` + a test.
- **ArrowUp APG return-to-input (R2-2, from PR2)** — current clamp-at-0 is LOCKED design §5; only change with user sign-off.

## Pointers to durable context

- Kickoff prompt: `2026-06-20-theme-b-public-ux-kickoff.md`
- Design doc: `2026-06-20-theme-b-public-ux-design.md` (W1a + W1b SHIPPED; **W1c §4 Q6–Q8 LOCKED**)
- Implementation plan: `2026-06-20-theme-b-public-ux-implementation.md` (PR1/PR2/PR3 concrete; **PR4 = skeleton, author next**)
- Campaign roadmap: `2026-06-20-deferred-work-roadmap.md` (Theme B = Wave 1)
- Archive (PR1 + PR2 + W1b cycles): `2026-06-20-theme-b-public-ux-execution-status-archive.md` — grep, don't read whole.

## Session log

### Session 9 — 2026-06-20 — Task 4.4 (retire the vestigial `tags` facet) shipped + verified

Supervisor session. Oriented (git matched the status file; baseline `npm run check` clean), then **reordered the remaining PR4 tasks** — did Task 4.4 (the `tags`-facet retirement, which subsumes the "Task 4.1 revision") **before** 4.2/4.3, user-confirmed. Rationale: 4.4 removes `tags` from `SearchFilters`; building the sync hook (4.2) against a soon-to-change shape would force re-revision of freshly-written 4.2 tests. Doing the retirement first establishes the final tags-free shape once.

- **Supervisor grep-before-dispatch** caught two things the impl-plan Task-4.4 file list missed: (1) `useLessonSuggestions.ts` references `filters.tags` (queryKey + `smart-search` edge body) — compiler-caught once `SearchFilters.tags` is removed; (2) `filterUtils.ts` had a SECOND `tags` ref (icon map :74, not just the label map :54) + `ScreenReaderAnnouncer.tsx:36`. Also surfaced the **dangerous scoping trap**: ~25 unrelated `tags` refs (`lessons.tags` DB column type, `lessonMetadata.zod` TagEnum, edge-schema tests, Sentry, HTML-sanitize, ReviewDetail) that a naive sweep could destroy. Baked an explicit IN/OUT-of-scope file list into the dispatch.
- **Task 4.4 executor** (general-purpose/Opus, fresh context, TDD): commit **`e2ff644`** (local). 14 files, **+34/−81**. Removed the facet from `types/index.ts`, `filterDefinitions.ts`, `IntSidebar.tsx`, `searchStore.ts`, `facetCounts.ts`, `filterUtils.ts`, `useLessonSearch.ts`, `useLessonSuggestions.ts`, `ScreenReaderAnnouncer.tsx`, `urlParams.ts` (the 4.1 revision) + 4 test files; added 2 negative-assertion URL tests. Left `lessons.tags` column + RPC `filter_tags` + smart-search `tags` inert (frontend stops sending).
- **Supervisor-verify (load-bearing):** re-ran `npm run check` (clean), `npm run test:run` (**69 files / 1413 tests green**), `git show --stat` (zero out-of-scope files touched), and the scoping greps myself — only remaining `tags` hits are the inert `database.types.ts filter_tags?`, the metadata `enums.json` test, and the executor's own retirement-proving negative tests. Accepted.
- **Structural win noted:** `urlParams.ts` `FILTER_TO_PARAM` is `Record<keyof SearchFilters, string>`, so with `tags` gone the URL param schema is now compiler-bound to the filter shape — it can't silently drift.
- **Task 4.2 executor** (general-purpose/Opus, fresh, TDD): `useUrlSync.ts` + `useUrlSync.test.tsx` + `searchStore.ts` (`hydrateUrlState` + exported `initialFilters`) + `searchStore.test.ts` → commit `b6be5b2`. Chose dispatch-note option (a) (export `initialFilters`, merge incoming partial onto it before canonicalizing) so `canonicalSearchString` is the single serializer on both equality sides. One-use `lastWrittenRef` token + post-hydration no-op guard fix the WIP's swallowed-Back bug; executor proved #3 red-green by re-introducing the WIP bug. `hydrateUrlState` = FULL filter replace + sort + `currentPage:1` in one `set()` (the BLOCKER fix). 10 hook tests (all assert the URL via `createMemoryRouter`) + 3 store tests.
- **Supervisor-verify caught a real loop-guard bug (load-bearing).** Read `useUrlSync.ts` line-by-line: the store→URL no-op branch (`if (next === current) return;`) returned WITHOUT cancelling a pending debounce timer → a toggle-and-revert within 300ms (type `x`, clear it) let the stale timer fire and write `?q=x` while the store was empty → URL/store desync a shared link would reproduce. None of the 10 tests covered it. Sent back to the same executor (SendMessage, context intact) → TDD fix (clear+null the timer in the no-op branch) + new regression test (`cancels a pending write when filters revert…`), red-green confirmed. Amended → commit `26a2ff5`. Re-verified: 1427 tests green, `npm run check` clean, the patched branch + test present.
- **Task 4.3 executor** (general-purpose/Opus, fresh, TDD): `enabled?: boolean` (default `true`) on `useLessonSearch` → `useInfiniteQuery`; `useUrlSync()` mounted in `SearchPage` (stable hook order) with `enabled: hydrated`; integration test → commit `5c49002` (5 files, +183/−82). Test-harness ripple (flagged in the dispatch): 3 integration files (`search-page`, `lesson-search.params`, `lesson-search.suggestions`) converted `BrowserRouter`→`MemoryRouter` + URL-seeding via the production `buildSearchParams` (the empty-URL mount pass full-replaces filters, so pre-render `store.setFilters` no longer survives — seed via URL instead). `'Nutrition'`→`'Food Systems'` across pill tests (the former was never a valid `thematicCategories` option; the store accepted it free-form but `parseSearchParams` correctly drops it). Pill assertion tightened `getByText`→ the unique `remove …` button. New `URL state (W1c)` describe: first/only RPC fires exactly once with URL-hydrated filters (proves the `enabled` gate).
- **Supervisor-verify (load-bearing on test changes):** read the production diff (clean) + ALL test diffs line-by-line — the harness conversions are faithful (more realistic: they exercise the real URL→store hydration path), the filter-value + assertion changes are correctness improvements not coverage erosion, and the new test's exactly-one-call assertion is the right gate proof. Re-ran `npm run check` (clean) + `npm run test:run` (**1429 green**) myself. Accepted. **W1c implementation COMPLETE on the branch.**
- **Pre-push gate (PER-PR RITUAL step 1):** dispatched a code-reviewer (Opus, `superpowers:code-reviewer`) + Codex GATE 3 (`codex:codex-rescue`, return-inline — no async flake) IN PARALLEL on `git diff main...HEAD -- src/`. Both cleared the core (loop guard, `hydrateUrlState`, `enabled` gate, tags retirement). Reviewer verdict *ship*; Codex *go-with-changes*. Two `urlParams.ts` findings, both rebuttal-passed: **(1) grade group-id acceptance** (flagged by BOTH) — the validator accepted `?grades=lower-elementary` but the sidebar only emits individual grades (`IntSidebar.tsx:45`) and the RPC matches by those → a crafted URL silently returned zero results; verified safe to drop (no UI producer). **(2) outbound cap asymmetry** (Codex MEDIUM, pathological-only — caps are 1000/50, `canonicalSearchString` is symmetric so no loop bug).
- **User chose "fix both"** (AskUserQuestion). Applied both in `urlParams.ts` (+ tests): dropped the `groups[].id` block from `validValuesForFilter` (flipped the test to assert group ids are dropped) and added `MAX_PARAM_LENGTH`/`MAX_ARRAY_LENGTH` slicing in `buildSearchParams` (+2 outbound-cap tests). Supervisor-verified: targeted urlParams+useUrlSync run green (loop guard unaffected by the build caps), full suite **1431 green**, `npm run check` clean. Commit `890afa6`.
- **Pushed + opened PR #526** (https://github.com/danfeder/esnyc-lesson-search-react/pull/526). At handoff: all CI/bot checks `pending` (claude-review/component/feature, performance-review, semgrep, CodeQL, E2E, Test&Build, coverage, Lighthouse, Netlify preview), 1 auto-comment, 0 reviews. **Session boundary here** — bots are the async second pass; next session does the four-surface triage + GATE 4. Status-doc commits held LOCAL (not pushed — open PR; bundle with the first fix-up push).

Learnings:
- **Pre-push convergence is a strong signal.** The grade-group-id silent-zero bug was independently surfaced by BOTH the Opus reviewer (LOW) and Codex (MEDIUM, different family) — when two independent reviewers land on the same finding, it's worth the deviation-from-spec to fix even though the impl plan had explicitly specified accepting group ids. Verified the fix was safe (no UI producer) before deviating, and surfaced the locked-plan deviation to the user rather than applying silently.
- **Supervisor line-by-line read of the highest-judgment file paid off again** — the toggle-and-revert desync was invisible to a strong 10-test suite (every test asserted the URL, the back/forward regression was red-green-proven) because no test exercised "revert to the URL state mid-debounce." Loop-guard code needs the supervisor to trace the timer lifecycle by hand, not just trust green tests.
- **SendMessage-to-the-same-executor for a supervisor-caught fix** keeps the test-harness context (the `act()`-splitting fake-timer nuance) and enforces TDD red-green on the fix — cleaner than a fresh dispatch for a surgical correction.

### Session 8 — 2026-06-20 — W1c / PR4 cycle opened: §4 Q6–Q8 locked; W1b archived

Supervisor session. Opened the W1c (URL-state) PR cycle: cut the branch, carried the status doc forward, design-locked §4 Q6–Q8 (one via a Codex consult the user requested), and archived the W1b cycle. No code on the branch yet — task authoring + GATE 1B are next.

- Oriented; git matched the status file (W1a+W1b shipped; `origin/main`=`5197069`; the Session-7 status-doc carry-forward was a local-only commit `327aae7` on `feat/theme-b-w1b-sort-frontend`). Baseline `npm run check` clean. Untracked `docs/plans/*` are unrelated other-initiative kickoffs (left alone).
- **Branch cut:** `feat/theme-b-w1c-url-state` off `main` @ `5197069`; `git checkout feat/theme-b-w1b-sort-frontend -- …execution-status.md` carried this doc forward (staged, +32/−3 vs main's stale PR3b-merge copy).
- **WIP-evidence agent** (Explore, read-only): digested the WIP `urlParams.ts`/`useUrlSync.ts`/tests + current `SearchFilters`/store/`useLessonSearch`/`filterDefinitions`/`App.tsx`. Confirmed the field-mapping delta (drop `lessonFormat`, add `tags`) + the integration points.
- **User decisions (AskUserQuestion):** Q7 = **include sortBy in URL** + **defer deep-linking**; Q6 = "ask Codex, I'm not sure."
- **Codex consult on Q6** (`codex:codex-rescue`, return-inline — no async flake): recommended **C (write fresh)** over my initial A (lift-and-adapt). Decisive findings: the WIP loop guard has a **latent back/forward bug** (`lastSyncSourceRef` not reset after a store-origin write → Back can be swallowed) and its store→URL tests **never assert the URL**. Also confirmed React Router ^7.8.2 on both (no drift) + that sort hydration needs `setViewState({ sortBy, currentPage: 1 })`. I reconciled (agreed with C) and brought it back to the user → **user locked WRITE FRESH.**
- **Q8 locked from evidence** = `replace` (WIP-confirmed; standard filter-UI back-button model).
- **Design-doc locks:** wrote 🔒 LOCKED blocks for §4 Q6/Q7/Q8 + the Codex-named guards; updated the top status line (W1b→SHIPPED, W1c→Locked), §6 W1c paragraph, and §9 (added the deep-linking deferral).
- **PR-cycle archival:** appended a full `## PR 3 — W1b-search-rpc` section to the archive (Done detail + §4 Q1–Q5 locks + TEST/PROD verification numbers + bot rounds + Sessions 5/6/7 verbatim); rewrote this live doc to be W1c-focused (W1b detail → 1-line archive pointer).
- **Grounded the tasks in current code** (Read `SearchFilters`/`ViewState`, full `searchStore`, `SearchPage` mount, `FILTER_CONFIGS`, + the WIP `urlParams.ts`/`useUrlSync.ts` via `git show`) then **authored PR4 Tasks 4.1/4.2/4.3** in the impl plan + W1c test-plan entries.
- **GATE 1B (Codex plan review, inline):** 1 BLOCKER + 5 HIGH + MEDIUMs/LOWs. Rebuttal-passed each → **all substantive accepted, two calibrated to notes**. Folded: atomic `hydrateUrlState` (full filter replace + sort+page in one write — the BLOCKER), `hydrated`+`enabled` gate on `useLessonSearch` (no pre-hydration empty-RPC flash), one-use `lastWrittenRef` + single `canonicalSearchString`, and StrictMode/`createMemoryRouter`/full-replacement/first-RPC tests. Scope grew to `searchStore.ts` + `useLessonSearch.ts`.
- **Task 4.1 executor** (general-purpose/Opus, fresh context, TDD): built `src/utils/urlParams.ts` + `urlParams.test.ts` (32 tests) → commit **`3681d22`** (local, not pushed). **Supervisor-verify:** read the full source (all 6 locked exports present + matching the contract; schema = tags-in/lessonFormat-out; sort whitelist + recursive `FILTER_CONFIGS` validation + the stable `canonicalSearchString` all correct); re-ran the targeted test (32 green), `npm run test:run` (**1412** green), `npm run check` (clean), grep (source clean — the only hits are intentional negative assertions in the test). **Surfaced a 4.2 nuance** (canonicalizing an incoming raw URL needs a full-default merge) → captured as the ⚠️ dispatch note in Current State. Accepted.
- **Handoff at the Task-4.1 commit boundary:** the planning phase (design-lock + GATE 1B + task authoring) was the session's bulk; Task 4.2 (the hook + loop guard — exactly where the WIP had its latent bug) is the highest-judgment remaining task and warrants a fresh supervisor budget for careful verify.

Learnings:
- **A Codex consult is a first-class tool for an unsure `[user-verdict]`** — not just for bot-finding 2nd opinions. It flipped a real decision (A→C) on a concrete code finding the supervisor's evidence agent had glossed (the agent called the loop guard "battle-tested"; Codex found the actual Back-button bug). Reconcile + surface to the user; don't auto-lock.
- **GATE 1B paid off on a pure-frontend PR too** (not just migrations): the partial-merge hydration BLOCKER + the pre-hydration-fetch flash were design-level traps invisible from the task prose alone — they only surfaced when Codex traced the plan against `searchStore.ts`/`useLessonSearch.ts`. Run the plan-review gate before dispatching executors regardless of DB involvement.
- **"Battle-tested" from a read-only digest ≠ correct.** The evidence agent's reusability assessment was optimistic; a second, adversarial read (different family) on the same files is what found the latent bug. Worth the extra dispatch for a load-bearing design call.
