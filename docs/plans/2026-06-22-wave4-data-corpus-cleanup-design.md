# Wave 4 — Data / Corpus Cleanup — Design Document

**Date:** 2026-06-22
**Status:** **LOCKED 2026-06-22 (Session 1)** — all §4 open questions resolved against TEST/PROD evidence (read-only discovery workflow + adversarial verify + supervisor re-verify) and the user's verdicts. Scope reshaped from 4 PRs → **3 PRs** (C08 closed as a no-op; C02 relocated to its own re-tag session). The impl plan's concrete tasks are authored. **GATE 1B Codex review done — 7 findings, all accepted + folded** (2 C11 HIGH: snapshot-completeness guard before delete + rollback re-GRANT/NOTIFY; C83 fail-loud-on-unresolved; C88 importer round-trip contract; C12 `updated_at` snapshot; + 2 LOW). Build may begin.
**Related:** `docs/plans/2026-06-20-deferred-work-roadmap.md` (Wave 4 §, source of scope) · `docs/plans/2026-06-21-deferred-campaign-status.md` (master tracker) · memory `reference_data_mutation_gotchas`, `project_imported_non_esynyc_drops`, `project_metadata_cleanup_candidates`, `project_metadata_rebuild_initiative`

---

## 1. Why this exists

Wave 4 is the data/corpus-cleanup wave of the deferred-work campaign. Waves 1–3 shipped public-UX fixes, email/security P1s, and repo/docs hygiene. Wave 4 is **the first wave that mutates real lesson-corpus data** — so data safety, not velocity, is the defining constraint (`feedback_data_safety_top_priority`). Everything here goes through the 3-tier pipeline (local → TEST → PROD-with-manual-approval), snapshot-before-mutate, with reversibility ordered first.

The corpus carries a handful of small, distinct cleanup debts left behind by the metadata-rebuild and submission-pipeline work. Grounded against **TEST DB** 2026-06-22 (read-only):

- **766 lesson rows** — 745 live (`retired_at IS NULL`), 21 retired. The 21 retired rows are exactly the non-ESYNYC curriculum imports soft-deleted by PR #478 (`supabase/migrations/20260520000000_corpus_cleanup_retire_imports.sql`, `retired_at = 2026-05-08`).
- **3 ghost "Unknown" rows** still live (`retired_at IS NULL`): `1l9KH63QBe2xhyH0zp6VIavtj0uKSRZtd`, `1lDjv2GUFzOC9pSWTpCVQW2ctWvvNmTPP4Jc1iAzrsaU`, `1nFbpkwlujk8fIO8RkeIcDoO2fuO83Iil2Srf8t5iqm8` — `title='Unknown'`, sharing one fabricated `content_hash` (`238f211f…`). Hidden from public search only by a **hardcoded 3-ID exclusion list inside the `search_lessons` RPC** (the W1b band-aid, PR3a `3c592b1`), not retired.
- **17 stuck submissions** in `lesson_submissions` (`status='submitted'`): 15 `new` (oldest 2025-07-23 ≈ 11 months) + 2 `update` (both 2026-04-28, the orphan-recovery artifacts).
- **17 zod-failing season values** in `submission_reviews.tagged_metadata->'season'` (re-verified TEST 2026-06-22, supervisor): 113 review rows carry the `season` key; 96 are arrays (pass), **17 are string-typed and fail** the Zod `seasonTiming: z.array(SeasonTimingEnum)` schema (`SeasonTimingEnum = ['Fall','Winter','Spring','Summer']`): `year-round`×13, `end-of-year`×2, `winter`×2. The published `lessons.metadata.seasonTiming` is **766/766 clean arrays** — so this is **reviews-only validation hygiene, no live-lesson impact**. **Lock-time discovery (changes the mechanism):** all 17 failing reviews are completed approvals (`approve_new`/`approved`) and each resolves to exactly one published lesson — **14 of 17 backfill cleanly** from that lesson's clean `seasonTiming` array (zero semantic invention), and **3 link to a lesson with an empty `[]` seasonTiming** (year-round×2 + end-of-year×1) → user verdict: set those 3 to `[]` to mirror their own published lesson. The resolving join is the **`lessons.original_submission_id = submission_reviews.submission_id` OUT-ref** (NULL `canonical_lesson_id` on 16/17; `canonical_lesson_id` is the path for only 1 row, "Applesauce") — same OUT-ref gotcha family as C11 (source: roadmap C83 line — NOT `project_metadata_cleanup_candidates`, which doesn't cover seasonTiming).
- **C08 straggler sweep = 0 (closed).** A re-sweep of the 745 live rows for the non-ESYNYC import drop-signature (external-org template structure + third-party copyright + org-name search) found **zero** stragglers on TEST and is consistent on PROD; all 21 known wholesale imports are already soft-retired (PR #478, `20260520000000_corpus_cleanup_retire_imports.sql`). The roadmap/memory "~2 expected" was a stale early estimate (the authoritative drop list was always exactly 21 IDs; the migration header documents the 23→21 reconciliation). **C08 is a no-op — dropped from PR1, recorded closed.**

None of these are blocking; all are small. The wave's job is to clear them carefully without breaking user state, search, or FK integrity.

## 2. Constraints (the bar every item must clear)

1. **Data safety supersedes velocity.** No PROD mutation without explicit user go. TEST-DB MCP verify before merge; PROD MCP verify after the manual approval gate (CI's own verify step flakes — MCP is source of truth, `reference_ci_flakes`).
2. **Snapshot before mutate.** Every destructive or bulk change writes a rollback artifact first (forward-rollback migration and/or a snapshot/archive table), idempotent, ready before merge.
3. **Reversibility-first ordering.** Reversible status/normalization changes ship first; the single irreversible hard-delete is isolated in its own PR; the bulk metadata rewrite is isolated in its own PR.
4. **Blast-radius isolation.** One concern per PR. The irreversible delete and the corpus-wide metadata update never share a PR with anything else.
5. **No silent search/filter regressions.** Any `search_lessons` RPC edit re-verified end-to-end on TEST (and the additive/removed-param deploy-ordering rule, `reference_ci_flakes`, respected).

## 3. The chosen shape: isolated-blast-radius PRs, lowest-risk-first

**Three PRs** (reshaped at lock from the original four — see §3.1), ordered by ascending risk. Reversible bundle → irreversible delete (isolated) → a no-DB repo-file refresh.

| # | PR | Items | Risk | Reversibility |
|---|-----|-------|------|---------------|
| 1 | Reversible data cleanups | C12, C83 | low | fully reversible (status flip / value normalize) |
| 2 | Ghost-row hard-delete + search-RPC cleanup | C11 (+ C49 folded) | **highest** | irreversible delete (snapshot-archived first); RPC change git-revertable |
| 3 | Local dev-seed refresh | C88 | none (no DB write, no PROD mutation) | git revert |

### 3.1 Scope reshape at lock (Session 1, user verdicts 2026-06-22)
- **C08 dropped → closed (no-op).** The straggler sweep found 0 live non-ESYNYC imports (§1). PR1 is now C12 + C83 only.
- **C02 relocated → its own re-tag session (out of Wave 4).** Lock-time discovery showed C02 is the metadata-rebuild's deferred "PR F" content re-tag, *not* a value-rename: the worksheet (`2026-06-12-...worksheet-decided.md`) is a content-re-tag design (vague cooking tags are "replaced by real skills at re-read"; the two-level main-ingredients specific-food tier is filled by content judgment, "open to adding further as the re-tag reveals true counts"). A hand-written SQL pass could only do the group/canonical level and would leave the content-judgment work for a second pass over the same ~700 rows — against this wave's "touch the corpus once" data-safety ethos. The tested `scripts/stage2-retag/` harness (export→normalize→run-retag→validate→diff→apply, with Opus-4.8/Fable dry-run artifacts + answer-key scoring) already exists for exactly this. **User verdict: do C02 properly in its own `stage2-retag` session.** The reviewer-dropdown rewrite + any Zod-enum/DB-CHECK enforcement (old Q14a) travel *with* that session (the dropdown vocab must match the re-tag's output). Surface-sync — the old "430 vs 709" worry — is a non-issue (the typed column and JSONB mirror differ on only 2 rows; the column is canonical and feeds `search_vector`). See §8 for the carry-forward findings.

### Why this shape over the alternatives

| Option | Why rejected |
|---|---|
| **One big "corpus cleanup" migration** (all items in one PR) | Concentrates an irreversible hard-delete + a corpus-wide metadata rewrite + status flips in one blast radius. A single bad section forces a full rollback of unrelated good work. Violates Constraint 4. |
| **Data-only via MCP `execute_sql`** (CLAUDE.md allows it for "data" changes) | Loses the rollback artifact, the TEST→PROD-approval gate, the migration-file audit trail, and idempotent re-runnability. PR #478 set the precedent that *bulk* corpus data changes go through **migration files** (with `.rollback`), even though they're "data". We follow that. |
| **Defer C11 hard-delete to soft-delete** | Considered and rejected by user verdict 2026-06-22: the 3 ghosts are fabricated error-skeletons (shared fake hash), not real lessons — hard-delete is correct, and it lets us *remove* the hardcoded RPC exclusion rather than convert it. |

**Scope provenance (so a future reader doesn't read a silent scope add):** C11/C12/C08/C02/C01/C09 come from the roadmap's Wave-4 § + master tracker. C49/C83/C88 are pulled from the roadmap's per-item detail table (lines 156/158/160) **plus the user's locked-scope decision 2026-06-22** ("Core 5 + tiny extras"). The user's locked scope (as reshaped at Session-1 lock) is the binding authority: **PR1 = C12 + C83, PR2 = C11 (hard-delete) + C49, PR3 = C88**; **C08 closed as a no-op** (0 stragglers); **deferred = C02 (own re-tag session), C01, C09/C07/C03.**

## 4. Design questions — LOCKED (Session 1, 2026-06-22)

All questions below are **resolved** (read-only discovery workflow → adversarial verify → supervisor re-verify on TEST/PROD → user verdicts). Each carries the locked answer + one-line rationale + evidence pointer. Original tags shown for provenance. Settled — do not re-debate.

> **Evidence basis:** every count was re-confirmed by the supervisor's own MCP probes on 2026-06-22 (C11 scan run on **both** TEST and PROD). File-path corrections folded: the Zod schema is `src/types/lessonMetadata.zod.ts` (not `src/utils/`); the authoritative `search_lessons` DROP+CREATE baseline is `supabase/migrations/20260620000000_search_lessons_w1b.sql`.

**PR 1 — C12 (stuck submissions):**
1. **End-state + note. LOCKED:** `status='rejected'` (the only schema-valid terminal value; the `status` CHECK = `['submitted','in_review','needs_revision','approved','rejected']`, and adding a new `stale` value is scope creep) + a `reviewer_notes` note (the note column is **`reviewer_notes`** — there is no `review_notes`/`notes` column) + `reviewed_at = NOW()`. **User verdict: distinct notes** — `new` rows get a "stale, never reviewed (submitted `<created_date>`)" note; `update` rows get an "orphan-recovery artifact (2026-04-28 reconciliation)" note. Both reference "Wave 4 corpus cleanup (2026-06-22)". `[user-verdict]` ✅.
   - **⚠️ TEST≠PROD — DO NOT hardcode "17". Scope environment-independently.** Supervisor re-verified both DBs 2026-06-22: **TEST = 17 stuck** (15 `new` + 2 `update`, newest 2026-04-28); **PROD = 14 stuck** (14 `new`, **0 `update`**, newest 2025-09-10). Both have **0 submissions after 2026-05-01** (pipeline dormant → safe to close the backlog). The migration scopes by `WHERE status='submitted' AND created_at < '2026-05-01'` (a stable threshold above every stale row and below any possible future submission — self-protecting), runs two `UPDATE`s split by `submission_type` (the `update` branch flips 0 rows on PROD, harmless), and **asserts the post-condition `count(status='submitted' AND created_at < '2026-05-01') = 0`** (env-independent invariant) + `RAISE NOTICE` the per-env count — **not** a hardcoded `=17`. **Snapshot-before-mutate (Constraint 2):** capture each affected row's `(id, status, reviewer_notes, reviewed_at, updated_at)` into a dedicated `wave4_c12_submissions_rollback` table BEFORE the UPDATE; the `.rollback` restores exact prior values (incl. `updated_at`, restored with the `updated_at` trigger temporarily disabled so it isn't re-stamped — Codex C12-MED).
2. **Side effects. LOCKED: none.** A bare SQL `UPDATE` cannot fire any email. The only trigger on `lesson_submissions` is the no-op `updated_at` setter; rejection email is an application-layer `fetch()` POST inside `complete-review/index.ts` (not a DB path); and **no HTTP-capable extension is installed** (`pg_net`/`http`/`pg_cron` absent — only `supabase_vault`), so no DB trigger/RPC can make an outbound call. The 17 teachers receive **no closure email** — correct/desired for stale/test/orphan rows. `[evidence-lockable]` ✅. Evidence: `pg_trigger` + `pg_extension` + `complete-review/index.ts:292-310`.
3. **The 2 `update` rows. LOCKED:** same `status='rejected'` flip with the distinct orphan-recovery note (see Q1) — **but these exist only on TEST** (PROD has 0 `update`-type stuck rows). On TEST, row `d9b1965d…` targets a live lesson ("Apple Pie"); row `f0520f33…` has `original_lesson_id IS NULL` (malformed update). Because the migration's `update`-branch is scoped by `submission_type='update'`, it self-adapts (2 rows TEST / 0 PROD). `[evidence-lockable]` ✅. Note: there is **no `original_submission_id` column on `lesson_submissions`** (that OUT-ref lives on `lessons`); the back-ref here is `original_lesson_id` (text, SET NULL).

**PR 1 — C83 (seasonTiming):**
4. **Normalization mechanism + the residual mapping. LOCKED: backfill-from-published-lesson** — set each failing review's `season` to its resolved published lesson's clean `metadata->'seasonTiming'` array. This resolves **14 of 17** with zero semantic invention (the published lesson is the authority; the review's string is simply discarded — including both `winter` rows, which widen to their lessons' real arrays). The resolving join is **`COALESCE(canonical_lesson_id-join, lessons.original_submission_id = submission_reviews.submission_id OUT-ref-join)`** — `canonical_lesson_id` is NULL on 16/17, so the OUT-ref is the working key for 16 rows and `canonical_lesson_id` for the 1 "Applesauce" row (per-row resolution = exactly 1 lesson, 0 conflicts). The **3 reviews whose published lesson has an empty `[]` seasonTiming** (`df7beb5f…` end-of-year → "Worms K-1"; `33166fac…` year-round → "Anthotype"; `586336b1…` year-round → "Mushroom Cultivation") → **user verdict: set to `[]`** (mirror their own published lesson; Zod-valid `z.array(...)` accepts `[]`; zero curriculum-semantics call). So `year-round`/`end-of-year` are **never** mapped to seasons. `[user-verdict]` ✅ (mechanism `[evidence-lockable]` ✅). Evidence: linkage aggregate `{total:17, linked:17, backfillable:14, empty-source:3}` re-verified by supervisor. **Migration must (a) snapshot the original 17 string values BEFORE the UPDATE (unrecoverable after — they become arrays); (b) PRE-assert every failing row resolves to exactly ONE lesson whose `seasonTiming` is a JSON array (length 0 OK) and RAISE on null/non-array/unresolved/multi — NEVER let an unresolved row silently coerce to `[]` (Codex C83-MED); then write the resolved array directly (so `[]` only ever comes from a genuinely empty-source lesson); (c) post-assert `jsonb_typeof(...->'season')='array'` for all 17 and 0 remaining string-typed.** Reviews-only → no live-lesson/search/FK/email impact; the `reviewToLessonMapper` rename (`season`→`seasonTiming`) is NOT in this path (these are already-completed approvals). **Write it CTE-driven (no hardcoded review IDs) so it self-adapts per DB** — PROD census is identical to TEST (17 string = year-round×13/end-of-year×2/winter×2, 96 array, 113 approved), so the same logic resolves correctly on both; the "3 → `[]`" fallback is defined as "any failing review whose resolved lesson has an empty `seasonTiming`", not a hardcoded ID list. **Post-apply PROD re-verify** the season census before the approval gate (`feedback_per_round_test_db_verification`).
5. **Companion `lessons.metadata.seasonTiming`. LOCKED: out of scope (already clean).** `766/766` clean arrays, 0 non-canonical on TEST → C83 is confirmed reviews-only; prior drift-repair migrations already cleaned the lessons side. `[evidence-lockable]` ✅. Re-verified by supervisor.

**~~PR 1~~ — C08 (last imports) — CLOSED, no-op:**
6. **Straggler sweep. LOCKED: 0 stragglers.** Re-ran the drop-signature sweep (external-org template structure + third-party copyright + org-name search) against all 745 live rows on TEST → empty; consistent on PROD. All 21 known wholesale imports already soft-retired (PR #478). The "~2 expected" in memory/roadmap was a stale early estimate (authoritative drop list = exactly 21 IDs; the cited "near-empty stub" `Teas around the World` is already retired). Note: the template-structure regex reliably catches the multi-lesson import *families* but not arbitrary one-offs — the "0" rests on the org-name + copyright + short-row reads all being clean, not the template sweep alone. `[evidence-lockable]` ✅. Evidence: supervisor + adversarial-verifier independent sweeps.
7. **Retire-or-keep. LOCKED: nothing to retire → C08 dropped from PR1, recorded closed.** `[user-verdict]` ✅ (user 2026-06-22: "Drop C08, record it closed"). If a future candidate ever surfaces, the PR #478 soft-delete pattern (`retired_at` + `retired_reason` + `AND retired_at IS NULL` idempotency guard) remains the template.

**PR 2 — C11 (ghost hard-delete) + C49:**
8. **Pre-delete reference scan — SPLIT BY ENFORCEMENT. LOCKED: all-clear on TEST *and* PROD; proceed with a GUARDED delete.** Supervisor re-ran the full scan on **both** DBs with verbatim IDs: identity = exactly 3 rows (`title='Unknown'`, shared `content_hash='238f211fc9915924a2c8c1a8772039846ea736f0d3af04fa403d875d668e83ef'`, `retired_at IS NULL`, `original_submission_id IS NULL`); guarded-predicate count = 3 on both; **all reference probes = 0 on both** — 6 enforced FKs `[bookmarks.lesson_id(CASCADE), canonical_lessons.canonical_id(NO ACTION), canonical_lessons.duplicate_id(CASCADE), duplicate_resolutions.canonical_lesson_id(NO ACTION), lesson_archive.canonical_id(SET NULL), lesson_submissions.original_lesson_id(SET NULL)]` + 6 unenforced text/text[] refs `[lesson_versions.lesson_id, lesson_archive.lesson_id, submission_similarities.lesson_id, submission_reviews(+_archive).canonical_lesson_id, lesson_collections.lesson_ids[] (&&), duplicate_group_dismissals.lesson_ids[] (&&)]` + the `lessons.original_submission_id` OUT-ref. CASCADE-child precondition satisfied (0 children on both CASCADE FKs) → parent-row `.rollback` is whole. **GUARDED DELETE:** match the 3 IDs **AND** all four identity predicates, then `GET DIAGNOSTICS` and `RAISE EXCEPTION` unless exactly 3 rows deleted. `[evidence-lockable]` ✅. **Carry-forward provenance (verifier completeness footnote):** ghost #2 (`1lDjv2GU…`) was historically snapshotted+retagged into a `pr6_retag_rollback` table by `20260617000000_pr6c2_retag_apply.sql` — but that table was DROPPED by PR E #516, so **zero current dangling ref**; it confirms why the Q9 dedicated snapshot is mandatory (migration history is not a recovery path). **PROD re-verify is still mandatory immediately before the approval-gated apply** (point-in-time read; `feedback_per_round_test_db_verification`).
9. **Snapshot mechanism. LOCKED: dedicated single-purpose rollback table**, full `SELECT *` of the 3 rows, `INSERT` BEFORE the `DELETE`. (Chosen over `lesson_versions`, which has no FK back but needs a computed NOT-NULL `version_number` and would pollute the forensic-version stream.) **Snapshot-completeness guard (Codex C11-HIGH):** because the table is `CREATE TABLE IF NOT EXISTS` + `INSERT … ON CONFLICT DO NOTHING` (idempotent), assert it holds **exactly the 3 identity-matching rows BEFORE the DELETE** (a pre-existing/partial snapshot must not let the delete proceed against an incomplete artifact). Deleted rows are recoverable **only** from this snapshot. `[evidence-lockable]` ✅.
10. **C49 — drop the dead `filter_lesson_format` param. LOCKED: expand/contract, but it's a hygiene formality (the param is already inert at the wire).** `useLessonSearch.ts:150` sends `filter_lesson_format: undefined` and `JSON.stringify` strips `undefined`-valued keys, so PostgREST **never receives the arg** — no live request can carry a real value (the lesson_format UI was removed in D3; the W1b RPC already deleted its WHERE clause). So no caller can PGRST202. **Ordering (locked):** within PR2, delete the dead frontend lines + regen types as the first commit, then drop the param from the RPC's DROP+CREATE in the migration commit (consistency with `reference_ci_flakes`; the split is belt-and-braces, not a functional requirement here). **C49 = exactly 2 files:** delete `useLessonSearch.ts:147-150` (the inert `filter_lesson_format: undefined` line **+ its stale 3-line `Task 1.3a` TODO**) and regenerate `database.types.ts` (drops `:1444`). Leave alone: `database.types.ts` `lesson_archive.lesson_format` column (schema-real), the `_alias_lesson_format` *different* RPC, and `urlParams.test.ts` guard assertions. `[evidence-lockable]` ✅. **RPC recreate baseline = `20260620000000_search_lessons_w1b.sql`** — remove BOTH whole ghost-exclusion blocks (lines `283-287` COUNT path + `376-380` RESULT path, not just the ID literals), drop the param from both the DROP arg-list (`:184`) and CREATE signature (`:203`), preserve everything else, then **re-`GRANT EXECUTE` to `anon, authenticated, service_role`** (now a 15-arg type list) **+ `NOTIFY pgrst, 'reload schema'`** (mirror `w1b:397-405`).

**~~PR 3~~ — C02 (vocab canonicalization) — RELOCATED to its own `stage2-retag` session (user verdict 2026-06-22):**
11–14a. **LOCKED: C02 leaves Wave 4.** It is the metadata-rebuild's deferred "PR F" content re-tag (not a value-rename) and belongs in the tested `scripts/stage2-retag/` harness — rationale in §3.1. The reviewer-dropdown rewrite + Zod-enum/DB-CHECK enforcement (old Q14a) go with that session. **Carry-forward findings for the C02 session (so the discovery isn't re-done):**
   - **Canonical surface = the typed `text[]` columns** (`lessons.cooking_skills` / `main_ingredients`) — they feed `search_vector` (SEL trigger `20260618000000_search_vector_add_sel.sql:62 B-weight` / `:67 C-weight`) and are the more-populated surface. **Surface sync is a non-issue:** column-nonempty vs JSONB-nonempty diverge on only **2 rows each** (430 vs 428 / 425 vs 423; the "430 vs 709" framing compared non-empty-column to key-present-incl-empty). The 2 divergent rows ("The Seasons: Fall/Spring") carry the truth in the column. A canonicalization must still write BOTH surfaces (column + `jsonb_set` mirror) to keep lockstep; **an UPDATE to either column or `metadata` auto-fires the BEFORE-UPDATE trigger** (`:84-92`) so no manual `search_vector` poke is needed.
   - **The real work is the vocab mapping:** 123 distinct `cooking_skills` → 23 canonical; 227 distinct `main_ingredients` → 24 groups + ~34 specifics (two-level). The group level + named cooking-skill folds are deterministic; the vague-tag replacement (`Basic Skills`/`Cooking Techniques` → real skills "at re-read") and the specific-food tier ("when 1–3 ingredients star, add specifics") are **content judgment** → harness, not SQL.
   - **Re-pollution is unguarded on two surfaces:** reviewer dropdowns (`filterDefinitions.ts` `mainIngredients:174` / `cookingSkills:271`) still hold OLD kebab vocab, AND Zod leaves both fields OPEN `z.array(z.string())` ("deferred to PR F", `lessonMetadata.zod.ts:215/217`) with **no DB CHECK** (confirmed 0 constraints). So the re-tag session must also rewrite the dropdowns (mirror the shipped `gardenSkills` Title-Case value===label pattern) and may close the enum + add a CHECK (after the data is clean — a CHECK can only follow canonicalization).
   - Worksheet: `docs/plans/2026-06-12-metadata-rebuild-stage1-cooking-skills-ingredients-worksheet-decided.md`. Harness: `scripts/stage2-retag/` (has Opus-4.8/Fable dry-run artifacts + answer-key scoring — user believes the dry-run was shippable). PR2/PR3-overlap insurance (exclude the 3 ghost IDs) is moot now that C02 is sequenced after PR2 in its own session.

**PR 3 — C88 (dev seed):**
14. **Source + shape for `data/consolidated_lessons.json`. LOCKED: re-export from PROD live rows** (`retired_at IS NULL`, ~745), current post-rebuild metadata (no `lessonFormat`; `academicConcepts`/typed-vocab). `[user-verdict]` ✅ (user: "PROD live rows"). **Scope caveat (locked):** there is **no existing export script** — the current seed is an upstream artifact (last touched PR #133, Nov 2025). PR3 must **author a small read-only generator** that emits the **legacy camelCase envelope** `import-data.js` consumes (`lessonId`/`lessonTitle`/`lessonSummary`/`fileLink`/`lastModified`/`metadata`/`confidence`/`flaggedForReview`/`reviewNotes`/`processingNotes`) — else the import silently drops every row on the `lesson_id`/`file_link` filter. The PROD query is **read-only** (`mcp__supabase-remote__execute_sql` SELECT) and writes only the local file — no PROD mutation, no migration, no approval gate. Seed shrinks 831 → ~745 (intended de-staling). `[evidence-lockable]` ✅.

## 5. Per-PR data changes (scope + hazards)

### PR 1 — Reversible data cleanups (C12 + C83) — *C08 dropped (0 stragglers)*
- **What changes:** 17 `lesson_submissions.status` → `'rejected'` + distinct `reviewer_notes` + `reviewed_at` (C12); 17 `submission_reviews.tagged_metadata->'season'` string values → arrays via backfill-from-published-lesson (14) + `[]` (3) (C83).
- **Hazards:** all low. C12 — no email side-effect (Q2 proven). C83 — reviews-only on already-approved rows; the OUT-ref join (`lessons.original_submission_id`) is the load-bearing detail; snapshot the 17 original strings before the UPDATE (unrecoverable after).
- **Mechanism:** one migration file + `.rollback`, sectioned C12 then C83, each individually reversible, each with its own snapshot table (`wave4_c12_submissions_rollback`, `wave4_c83_season_rollback`) populated BEFORE its UPDATE. `.rollback` restores both from their snapshots. Snapshot tables persist post-merge as recovery artifacts (drop in a future cleanup once PROD-verified stable, per the `pr6_retag_rollback` precedent).

### PR 2 — Ghost-row hard-delete + search-RPC cleanup (C11 + C49)
- **What changes:** snapshot 3 ghost rows → `DELETE FROM lessons WHERE lesson_id IN (…3…)`; `DROP+CREATE search_lessons` to (a) remove the hardcoded 3-ID exclusion and (b) drop the dead `filter_lesson_format` param (C49, gated on Q10).
- **Hazards:** **highest in the wave.** Irreversible delete → split-by-enforcement reference scan IN+OUT (Q8) on TEST *and* PROD with verbatim IDs, snapshot first (Q9). RPC signature change → reverse-PGRST202 ordering (Q10); after `DROP+CREATE` re-grant `EXECUTE` to `anon, authenticated, service_role` **and emit `NOTIFY pgrst, 'reload schema'`** (the W1b migration `20260620000000_search_lessons_w1b.sql:397-405` did both — mirror it, including the cache reload). Re-verify search end-to-end on TEST (live count, location-Both, sort) so the RPC recreate didn't regress W1b behavior. **The `DELETE` is guarded** (Q8: identity predicates + exactly-3 assertion, fail otherwise).
- **Mechanism:** one migration (snapshot + delete + RPC recreate) + `.rollback`. The `.rollback` recreates the prior RPC and re-INSERTs the snapshot — but it restores the **parent `lessons` row only**; this relies on the Q8-verified precondition that the 3 ghosts have zero CASCADE-child rows (re-confirm on PROD). Deleted rows are recoverable **only** from the snapshot — call that out.

### ~~PR 3 — Vocabulary canonicalization (C02)~~ — RELOCATED to its own `stage2-retag` session
C02 left Wave 4 at lock (user verdict). It is the deferred "PR F" content re-tag, done properly via `scripts/stage2-retag/`. Carry-forward findings (canonical surface = typed columns; 2-row surface divergence; 123→23 / 227→58 vocab map; reviewer-dropdown + Zod/CHECK enforcement) are recorded under §4 Q11–14a and §8. **Nothing C02-shaped ships in Wave 4.**

### PR 3 — Local dev-seed refresh (C88)
- **What changes:** author a read-only generator + regenerate `data/consolidated_lessons.json` from PROD live rows (~745), legacy camelCase envelope shape, current post-rebuild metadata.
- **Hazards:** none to PROD/DB — the generator only SELECTs PROD and writes a local file; the seed is local-dev only, never read by PROD or the deployed app. No migration, no PROD approval. **Only real trap:** the emitted shape must match what `import-data.js` reads (camelCase keys), or the seed import silently drops every row.

## 6. Migration / shipping strategy

**PR breakdown:** see §3 table (3 PRs). Order: PR1 → PR2; PR3 (C88) anytime (independent, no DB write).

**Migration naming (every Wave-4 migration):** use the full `YYYYMMDDHHMMSS_` prefix with a timestamp strictly greater than the latest existing (`20260621000000`); if two Wave-4 migrations land the same calendar day, give them distinct `HHMMSS` so file-sort == apply-order (the ASCII gotcha: digit < underscore, so `YYYYMMDDHHMMSS_x` sorts before a bare `YYYYMMDD_x`). Use the `/new-migration` skill (it enforces this) + the `database-migrations` skill before editing any migration file.

### Gap risk between PRs
**Fully independent now that C02 is gone.** PR1 is reversible; PR2 is isolated + snapshot-protected; PR3 (C88) touches no DB. The old PR2/PR3-shared-ghost-rows coupling no longer exists. No dangerous interleaving in any order (PR1 → PR2 is the recommended sequence to build TEST→PROD-verify rhythm before the irreversible delete).

### TEST DB rehearsal (per `feedback_per_round_test_db_verification`)
- **PR 1:** apply to TEST; verify 17 submissions now `status='rejected'` + distinct notes + `reviewed_at` set; 17 `season` values now arrays (0 string-typed, all pass `z.array(SeasonTimingEnum)`, the 3 fallback rows are `[]`). Re-verify after every fix-up round.
- **PR 2:** apply to TEST; verify the 3 ghost rows gone (live-count **delta = −3**, NOT an absolute compare — TEST 745→742 while PROD 767→764), snapshot table populated, `search_lessons` returns expected live results with no ghost IDs, location-Both + sort still correct, FK integrity intact. **Then PROD MCP verify after approval.**
- **PR 3 (C88):** no DB write — author/run the read-only generator; local `npm run dev` smoke + `npm run build`; confirm `import-data.js` round-trips the regenerated seed.

### Rollback paths
- **PR 1:** `.rollback` restores each touched submission's exact prior `(status, reviewer_notes, reviewed_at)` from `wave4_c12_submissions_rollback`, and the original season strings from `wave4_c83_season_rollback`.
- **PR 2:** `.rollback` recreates the prior `search_lessons` (with exclusion + 16-arg param) — **and must itself re-`GRANT EXECUTE` + `NOTIFY pgrst, 'reload schema'`** since a rollback is also a DROP+CREATE (Codex C11-HIGH) — then re-INSERTs from the snapshot, restoring the **parent `lessons` rows only** (whole, given the Q8-verified zero-CASCADE-child precondition). Deleted rows are recoverable **only** from the snapshot — this is why the snapshot is mandatory before delete.
- **PR 3 (C88):** `git revert`.

### Per-PR ritual
Per `feedback_pr_bot_review_workflow` + the kickoff PER-PR RITUAL: pre-push code-reviewer agent **+ GATE 3 Codex** (parallel, different family) → baseline checks → push + `gh pr create` → wait for external bots → four-surface triage (`feedback_pr_comment_surfaces`) → rebuttal-pass every finding + **GATE 4 Codex** on real suggested changes → consolidated fix-ups → per-round TEST re-verify → round-cap after 2. **GATE 2 Codex** on every migration SQL before TEST apply.

### Known issues / pre-existing flakes
`migrate-production.yml` SASL apply/verify flake → PROD MCP verification mandatory + rerun. esm.sh 522 flake. See `reference_ci_flakes` before any PROD migration.

## 7. Testing strategy

### Unit / Integration
- C83: add a Zod fixture asserting the post-backfill `season` values (the 14 backfilled arrays **and** the 3 `[]` fallbacks) parse against `z.array(SeasonTimingEnum)` and that no string-typed value remains (TDD: write the failing fixture first).
- C49: delete `useLessonSearch.ts:147-150` (the inert `filter_lesson_format: undefined` line + its stale `Task 1.3a` TODO) + regen `database.types.ts`; `npm run check` clean. Leave the unrelated `lesson_archive.lesson_format` column type + `_alias_lesson_format` RPC + `urlParams.test.ts` guard untouched.

### E2E
- No new E2E. PR2's `search_lessons` recreate is covered by re-running the existing search E2E against the TEST deploy preview (must stay green) + manual search smoke.

### RLS
- No RLS changes expected. `npm run test:rls` must pass unchanged after each migration.

### Manual smoke (per `superpowers:verification-before-completion`)
- PR2: public search returns results, no "Unknown" rows surface, location-Both + sort behave, count drops by exactly 3.
- PR1: reviewer queue no longer shows the 17 stuck submissions as actionable.

## 8. Out of scope (captured for future work)

- **C02 — `cooking_skills`/`main_ingredients` canonicalization → its own `stage2-retag` session** (the metadata-rebuild's deferred "PR F"; user verdict 2026-06-22). It is a content re-tag, not a SQL value-rename. Carry-forward findings (canonical surface, vocab map sizes, the reviewer-dropdown + Zod/CHECK re-pollution gap, the harness location) are in §3.1 + §4 Q11–14a so the discovery isn't re-done. **Next-up after Wave 4.**
- **C08 — non-ESYNYC import stragglers → CLOSED (no-op).** 0 live stragglers found (TEST + PROD sweep, 2026-06-22); the 21 known imports already soft-retired. If a future candidate ever surfaces, use the PR #478 soft-delete pattern. Neutralizes the stale "~2 expected" note in memory/roadmap.
- **C01 — full-corpus embeddings regeneration** (L). Blocked on fixing the embedding-pipeline bugs (C89/C87) + the `lesson_submissions`-vs-`lessons` vector-space mismatch (C07). Own session.
- **C09 / C07 / C03 — dedup-pipeline rework** (L, feature-scale: sibling-variant "third state", series handling, parser title-case). ~Wave 5.
- **The 21 already-retired imports stay soft-deleted** — no hard-delete of them this wave (only the 3 ghost rows are hard-deleted, by user verdict). Revisit only if a future archival policy calls for it.
- **C65** (Seed Bursts near-dup pair) — observation feeding the dedup track; stays with C09.
- **C67** (guyanese/cuban heritage dual-membership) — a curriculum/UX decision, not buildable cleanup; stays parked.
- **C117** (schools soft-delete) — a hardening feature, not corpus cleanup; defer.
- **C36** (clearing concepts via reviewer UI) — design-flag only; surfaces if a concepts-editor UI is ever built (`reference_data_mutation_gotchas` "clearing concepts").
- **Wave-3 edge-CI follow-ups** (C33 composite-action extraction; poll-budget bump) — route to a future edge-CI track, NOT Wave 4.

## 9. References

- `docs/plans/2026-06-20-deferred-work-roadmap.md` — Wave 4 §, item table (C11/C12/C08/C02/C49/C88 + deferred C01/C09/C07/C03)
- `docs/plans/2026-06-21-deferred-campaign-status.md` — master cross-wave tracker
- memory `reference_data_mutation_gotchas` — pre-delete FK checklist (IN + OUT), facetCounts, concepts-clearing
- memory `project_imported_non_esynyc_drops` — drop signature + the 23-import list (21 retired)
- memory `project_metadata_cleanup_candidates` — the 3 ghost rows + 14 (now 17) stuck submissions + seasonTiming
- memory `project_metadata_rebuild_initiative` — the canonicalization pipeline C02 reuses; PR #478 corpus_cleanup precedent
- memory `reference_ci_flakes` — SASL flake, additive/removed-RPC-param deploy ordering, 3-signal edge verify
- `feedback_data_safety_top_priority`, `feedback_per_round_test_db_verification`, `feedback_pr_bot_review_workflow`, `feedback_bot_review_investigation`, `feedback_codex_over_crossexamine`
